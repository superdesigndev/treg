# dataforseo — EXTENDED tier: the provider's full endpoint surface, machine-generated by
# scripts/catalog_ingest.py. Do not hand-edit; re-run the script instead. Entries carry no
# capability, no verification and no example response — that is what the core
# dataforseo.yaml is for. Routes already curated there are excluded here.
# 
# Selection rule: one canonical call per operation. DataForSEO exposes most jobs three ways
# (sync `/live/...`, async `/task_post` + `/task_get`, plus a `/live/html` raw-HTML twin);
# the sync `live` form is kept when it exists, `task_post` only when it does not. Task-
# bookkeeping routes (tasks_ready, tasks_fixed, id_list, errors, task_get) and reference
# lookups (locations, languages, categories, available_filters) are omitted.
# `platform` is the system the data DESCRIBES (google, amazon, app-store, chatgpt…), not the
# API family; anything not tied to one engine is `web`. No per-endpoint price: DataForSEO's
# pricing is published per API family on a separate page, not in the spec.

provider: dataforseo
source:
  method: openapi
  ingested: '2026-07-28'
  spec_urls:
  - https://github.com/dataforseo/OpenApiDocumentation/blob/master/openapi_specification.yaml
endpoints:
- id: dataforseo.x.ai-optimization-ai-keyword-data-keywords-search-volume-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/ai_keyword_data/keywords_search_volume/live
  name: "AI search volume for keywords across AI tools"
  summary: This endpoint provides search volume data for your target keywords, reflecting their estimated usage in AI tools.
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: '<em>keywords</em><br><strong>required field</strong><br>UTF-8 encoding<br>The maximum number of keywords you can specify: 1000;<br>The maximum number of characters in a single keyword: 250;<br>The keywords will be converted to lowercase format;<p>learn more about rules and limitations of <code>keywo'
        example:
        - iphone
        - seo
      location_name:
        type: string
        required: false
        note: <em>full name of the location</em><br><strong>required field if you don't specify</strong> <code>location_code</code><br><strong>Note:</strong> it is required to specify either <code>location_name</code> or <code>location_code</code><br>you can receive the list of available locations with their <cod
      location_code:
        type: integer
        required: false
        note: <em>unique location identifier</em><br><strong>required field if you don't specify</strong> <code>location_name</code><br><strong>Note:</strong> it is required to specify either <code>location_name</code> or <code>location_code</code><br>you can receive the list of available locations with their <co
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of the language</em><br><strong>required field if you don't specify</strong> <code>language_code</code><br><strong>if you use this field, you don't need to specify</strong> <code>language_code</code><br>you can receive the list of available languages with their <code>language_name</cod
        example: English
      language_code:
        type: string
        required: false
        note: <em>language code</em><br><strong>required field if you don't specify</strong> <code>language_name</code><br><strong>if you use this field, you don't need to specify</strong> <code>language_name</code><br>you can receive the list of available languages with their <code>language_code</code> by making
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_name: English
      location_code: 2840
      keywords:
      - iphone
      - seo
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-ai-keyword-data-keywords-search-volume-live.json
  observed_cost: 0.0102
  capability: ai-search.keywords.volume
- id: dataforseo.x.ai-optimization-llm-mentions-historical-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/historical/live
  name: "Monthly LLM mention history for a keyword or domain"
  summary: Live LLM Mentions Historical endpoint provides month-by-month historical metrics for mentions of the keywords or domains specified in the target array of the request.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - domain: en.wikipedia.org
          search_filter: exclude
        - keyword: bmw
          search_scope:
          - answer
      date_from:
        type: string
        required: false
        note: <em>start date of the time range</em><br>optional field<br>minimal value <code>2025-08-01</code><br>date format <code>"yyyy-mm-dd"</code>
      date_to:
        type: string
        required: false
        note: <em>end date of the time range</em><br>optional field<br><strong>Note</strong> value specified in <code>date_from</code> cannot exceed the value in <code>date_to</code><br>date format <code>"yyyy-mm-dd"</code>
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: es
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: google
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: es
      location_code: 2840
      platform: google
      target:
      - domain: en.wikipedia.org
        search_filter: exclude
      - keyword: bmw
        search_scope:
        - answer
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-historical-live.json
  observed_cost: 0.101
  capability: ai-search.mentions.historical
- id: dataforseo.x.ai-optimization-llm-mentions-multi-target-metrics-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/multi_target_metrics/live
  name: "LLM mention metrics for many keywords or domains, grouped"
  summary: Live LLM Mentions Multi-Target Metrics endpoint provides aggregated metrics grouped by custom keys for mentions of the keywords or domains specified in the target array of the request.
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        example:
        - key: chat_gpt
          target:
          - keyword: chat gpt
        - key: claude
          target:
          - keyword: claude
        - key: gemini
          target:
          - keyword: gemini
        - key: perplexity
          target:
          - keyword: perplexity
            search_filter: include
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br>default value: <code class="long-string">google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:<'
        example: google
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      limit:
        type: integer
        required: false
        note: '<em>the maximum number of returned objects</em><br>optional field<p>default value: <code>100</code><br>maximum value: <code>1000</code>'
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      internal_list_limit:
        type: integer
        required: false
        note: '<em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code><br><code>search_results_domain</code><br>minimum value: <code>1</code><br>maximum value: <code>10</code><'
        example: 5
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: google
      targets:
      - key: chat_gpt
        target:
        - keyword: chat gpt
      - key: claude
        target:
        - keyword: claude
      - key: gemini
        target:
        - keyword: gemini
      - key: perplexity
        target:
        - keyword: perplexity
          search_filter: include
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      internal_list_limit: 5
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-multi-target-metrics-live.json
  observed_cost: 0.104
  capability: ai-search.mentions.multi_target
- id: dataforseo.x.ai-optimization-llm-mentions-search-mentions-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/search_mentions/live
  name: "Search LLM mentions of a keyword or domain in AI answers"
  summary: Live LLM Mentions Search endpoint provides mention data and related metrics from AI searches.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - domain: dataforseo.com
          search_filter: exclude
        - keyword: bmw
          search_scope:
          - answer
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
        example: English
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: google
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
        example:
        - - ai_search_volume
          - '>'
          - 1000
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
        example:
        - ai_search_volume,desc
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
        example: 0
      search_after_token:
        type: string
        required: false
        note: '<em>token for subsequent requests</em><br>optional field<br>provided in the identical filed of the response to each request;<br>use this parameter to avoid timeouts while trying to obtain over <code>1000</code> results in a single request;<br>by specifying the unique <code>search_after_token</code> '
      limit:
        type: integer
        required: false
        note: '<em>the maximum number of returned objects</em><br>optional field<p>default value: <code>100</code><br>maximum value: <code>1000</code>'
        example: 3
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_name: English
      location_code: 2840
      target:
      - domain: dataforseo.com
        search_filter: exclude
      - keyword: bmw
        search_scope:
        - answer
      platform: google
      filters:
      - - ai_search_volume
        - '>'
        - 1000
      order_by:
      - ai_search_volume,desc
      offset: 0
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-search-mentions-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.search
- id: dataforseo.x.ai-optimization-llm-mentions-target-metrics-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/target_metrics/live
  name: "LLM mention metrics for a keyword or domain"
  summary: Live LLM Mentions Target Metrics endpoint provides aggregated metrics for mentions of the keywords or domains specified in the target array of the request.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - domain: en.wikipedia.org
          search_filter: exclude
        - keyword: bmw
          search_scope:
          - answer
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: chat_gpt
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      internal_list_limit:
        type: integer
        required: false
        note: '<em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code><br><code>search_results_domain</code><br>minimum value: <code>1</code><br>maximum value: <code>10</code><'
        example: 10
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - domain: en.wikipedia.org
        search_filter: exclude
      - keyword: bmw
        search_scope:
        - answer
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      internal_list_limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-target-metrics-live.json
  observed_cost: 0.101
  capability: ai-search.mentions.summary
- id: dataforseo.x.ai-optimization-llm-mentions-target-metrics-lite-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/target_metrics_lite/live
  name: "LLM mention metrics for a keyword or domain, lite"
  summary: Live LLM Mentions Target Metrics Lite endpoint is the simplified version of the Target Metrics endpoint and provides a simplified view of aggregated LLM mentions for keywords and domains specified in the target array of the request.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - domain: en.wikipedia.org
          search_filter: exclude
        - keyword: bmw
          search_scope:
          - answer
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: es
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: google
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 6
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: es
      location_code: 2840
      platform: google
      target:
      - domain: en.wikipedia.org
        search_filter: exclude
      - keyword: bmw
        search_scope:
        - answer
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 6
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-target-metrics-lite-live.json
  observed_cost: 0.101
  capability: ai-search.mentions.summary
- id: dataforseo.x.ai-optimization-llm-mentions-timeseries-delta-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/timeseries_delta/live
  name: "Change in LLM mentions between two dates for a target"
  summary: The Live LLM Mentions Timeseries Delta endpoint provides the difference in historical mentions and AI search volume data between two specified dates.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - domain: dataforseo.com
          search_filter: exclude
        - keyword: bmw
          search_scope:
          - answer
      date_from:
        type: string
        required: false
        note: '<em>start date of the time range</em><br><strong>required field</strong><br>minimal value: <code>2025-08-01</code><br>date format: <code>"yyyy-mm-dd"</code>'
        example: '2025-08-01'
      date_to:
        type: string
        required: false
        note: '<em>end date of the time range</em><br><strong>required field</strong><br><strong>Note:</strong>the value specified in <code>date_from</code> cannot exceed the value in <code>date_to</code><br>date format: <code>"yyyy-mm-dd"</code>'
        example: '2025-12-01'
      group_range:
        type: string
        required: false
        note: <em>timeseries delta range</em><br><strong>required field</strong><br>possible values:<br><code>day</code>, <code>week</code>, <code>month</code>, <code>year</code>
        example: month
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
        example: English
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br>default value: <code class="long-string">google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:<'
        example: google
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_name: English
      location_code: 2840
      target:
      - domain: dataforseo.com
        search_filter: exclude
      - keyword: bmw
        search_scope:
        - answer
      platform: google
      date_from: '2025-08-01'
      date_to: '2025-12-01'
      group_range: month
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-timeseries-delta-live.json
  observed_cost: 0.105
  capability: ai-search.mentions.timeseries_delta
- id: dataforseo.x.ai-optimization-llm-mentions-timeseries-new-lost-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/timeseries_new_lost/live
  name: "New and lost LLM mentions for a keyword or domain"
  summary: This endpoint will provide you with the number of new and lost LLM mentions, as well as ai_search_volume for the domain or keyword specified in the target field.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - domain: dataforseo.com
          search_filter: exclude
        - keyword: serp
          search_scope:
          - answer
      date_from:
        type: string
        required: false
        note: '<em>start date of the time range</em><br><strong>required field</strong><br>minimal value: <code>2025-08-01</code><br>date format: <code>"yyyy-mm-dd"</code>'
        example: '2025-08-01'
      date_to:
        type: string
        required: false
        note: '<em>end date of the time range</em><br><strong>required field</strong><br><strong>Note:</strong>the value specified in <code>date_from</code> cannot exceed the value in <code>date_to</code><br>date format: <code>"yyyy-mm-dd"</code>'
        example: '2025-12-01'
      group_range:
        type: string
        required: false
        note: <em>timeseries range</em><br><strong>required field</strong><br>possible values:<br><code>day</code>, <code>week</code>, <code>month</code>, <code>year</code>
        example: month
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
        example: English
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br>default value: <code class="long-string">google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:<'
        example: google
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_name: English
      location_code: 2840
      target:
      - domain: dataforseo.com
        search_filter: exclude
      - keyword: serp
        search_scope:
        - answer
      platform: google
      date_from: '2025-08-01'
      date_to: '2025-12-01'
      group_range: month
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-timeseries-new-lost-live.json
  observed_cost: 0.105
  capability: ai-search.mentions.timeseries_new_lost
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brand-categories-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_brand_categories/live
  name: "Top brand categories in LLM mentions of a target"
  summary: Live LLM Mentions Top Mentioned Brand Categories endpoint provides aggregated LLM mentions metrics grouped by the most frequently mentioned brand categories for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: chat_gpt
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_brand_categories:
        type: array
        required: false
        note: '<em>array of brand_categories to include in the response</em><br>optional field<br>if specified, only the listed brand categories will be returned in the <code>items</code> array<br>example:<br><code>"include_brand_categories": ["business"]</code>'
      exclude_brand_categories:
        type: array
        required: false
        note: '<em>array of brand categories to exclude from the response</em><br>optional field<br>if specified, the listed brand categories will be omitted from the <code>items</code> array<br>example:<br><code>"exclude_brand_categories": ["business"]</code>'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brand-categories-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_brand_categories
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brand-categories-lite-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_brand_categories_lite/live
  name: "Top brand categories in LLM mentions of a target, lite"
  summary: Live LLM Mentions Top Mentioned Brand Categories Lite endpoint is the simplified version of the Top Mentioned Brand Categories endpoint and provides a simplified view of aggregated LLM mentions metrics grouped by the most frequently mentioned brand categories for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br>default value: <code class="long-string">google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:<'
        example: chat_gpt
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_brand_categories:
        type: array
        required: false
        note: <em>array of brand categories to include in the response</em><br>optional field<br>if specified, only the listed brand categories will be returned in the <code>items</code> array<br>example:<br><code>["company","insurance"]</code>
      exclude_brand_categories:
        type: array
        required: false
        note: <em>array of brand categories to exclude from the response</em><br>optional field<br>if specified, the listed brand categories will be omitted from the <code>items</code> array<br>example:<br><code>["company","insurance"]</code>
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brand-categories-lite-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_brand_categories
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brands-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_brands/live
  name: "Top brands in LLM mentions of a keyword or domain"
  summary: Live LLM Mentions Top Mentioned Brands endpoint provides aggregated LLM mentions metrics grouped by the most frequently mentioned brands for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: chat_gpt
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_brands:
        type: array
        required: false
        note: '<em>array of brands to include in the response</em><br>optional field<br>if specified, only the listed brands will be returned in the <code>items</code> array<br>example:<br><code>"include_brands": ["Audi"]</code>'
      exclude_brands:
        type: array
        required: false
        note: '<em>array of brands to exclude from the response</em><br>optional field<br>if specified, the listed brands will be omitted from the <code>items</code> array<br>example:<br><code>"exclude_brands": ["Audi"]</code>'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brands-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_brands
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brands-lite-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_brands_lite/live
  name: "Top brands in LLM mentions of a keyword or domain, lite"
  summary: Live LLM Mentions Top Mentioned Brands Lite endpoint is the simplified version of the Top Mentioned Brands endpoint and provides a simplified view of aggregated LLM mentions metrics grouped by the most frequently mentioned brands for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br>default value: <code class="long-string">google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:<'
        example: chat_gpt
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_brands:
        type: array
        required: false
        note: <em>array of brands to include in the response</em><br>optional field<br>if specified, only the listed brands will be returned in the <code>items</code> array<br>example:<br><code>["BMW","Audi"]</code>
      exclude_brands:
        type: array
        required: false
        note: <em>array of brands to exclude from the response</em><br>optional field<br>if specified, the listed brands will be omitted from the <code>items</code> array<br>example:<br><code>["BMW","Audi"]</code>
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-brands-lite-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_brands
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-domains-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_domains/live
  name: "Top domains in LLM mentions of a keyword or domain"
  summary: Live LLM Mentions Top Mentioned Domains endpoint provides aggregated LLM mentions metrics grouped by the most frequently mentioned domains for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: chat_gpt
      links_scope:
        type: string
        required: false
        note: '<em>links source scope</em><br>optional field<br>this parameter specifies which links will be used to extract domains and aggregation data<br>possible values: <code>sources</code>, <code>search_results</code><br>default value: <code>sources</code>'
        example: sources
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_domains:
        type: array
        required: false
        note: '<em>array of domains to include in the response</em><br>optional field<br>if specified, only the listed domains will be returned in the <code>items</code> array<br>example:<br><code>"include_domains": ["en.wikipedia.org"]</code>'
      exclude_domains:
        type: array
        required: false
        note: '<em>array of domains to exclude from the response</em><br>optional field<br>if specified, the listed domains will be omitted from the <code>items</code> array<br>example:<br><code>"exclude_domains": ["en.wikipedia.org"]</code>'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      links_scope: sources
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-domains-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_domains
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-domains-lite-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_domains_lite/live
  name: "Top domains in LLM mentions of a keyword or domain, lite"
  summary: Live LLM Mentions Top Mentioned Domains Lite endpoint is the simplified version of the Top Mentioned Domains endpoint and provides a simplified view of aggregated LLM mentions metrics grouped by the most frequently mentioned domains for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code>_by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br>default value: <code class="long-string">google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:<'
        example: chat_gpt
      links_scope:
        type: string
        required: false
        note: '<em>links source scope</em><br>optional field<br>this parameter specifies which links will be used to extract domains and aggregation data<br>possible values: <code>sources</code>, <code>search_results</code><br>default value: <code>sources</code>'
        example: sources
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_domains:
        type: array
        required: false
        note: <em>array of domains to include in the response</em><br>optional field<br>if specified, only the listed domains will be returned in the <code>items</code> array<br>example:<br><code>["dataforseo.com","seoinsider.com"]</code>
      exclude_domains:
        type: array
        required: false
        note: <em>array of domains to exclude from the response</em><br>optional field<br>if specified, the listed domains will be omitted from the <code>items</code> array<br>example:<br><code>["google.com","bing.com"]</code>
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      links_scope: sources
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-domains-lite-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_domains
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-pages-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_pages/live
  name: "Top pages in LLM mentions of a keyword or domain"
  summary: Live LLM Mentions Top Mentioned Pages endpoint provides aggregated LLM mentions metrics grouped by the most frequently mentioned pages for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:</strong><code>chat_gpt</code> data is available for the <c'
        example: chat_gpt
      links_scope:
        type: string
        required: false
        note: '<em>links source scope</em><br>optional field<br>this parameter specifies which links will be used to extract domains and aggregation data<br>possible values: <code>sources</code>, <code>search_results</code><br>default value: <code>sources</code>'
        example: sources
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_pages:
        type: array
        required: false
        note: '<em>array of pages to include in the response</em><br>optional field<br>if specified, only the listed pages will be returned in the <code>items</code> array<br>example:<br><code>"include_pages": ["https://example.page/"]</code>'
      exclude_pages:
        type: array
        required: false
        note: '<em>array of pages to exclude from the response</em><br>optional field<br>if specified, the listed pages will be omitted from the <code>items</code> array<br>example:<br><code>"exclude_pages": ["https://example.page/"]</code>'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      links_scope: sources
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-pages-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_pages
- id: dataforseo.x.ai-optimization-llm-mentions-top-mentioned-pages-lite-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/llm_mentions/top_mentioned_pages_lite/live
  name: "Top pages in LLM mentions of a keyword or domain, lite"
  summary: Live LLM Mentions Top Mentioned Pages Lite endpoint is a simplified version of the Top Mentioned Pages that provides a simplified view of aggregated LLM mentions metrics grouped by the most frequently mentioned pages for the specified target.
  input:
    bodyType: json
    body:
      target:
        type: array
        required: false
        note: 'array of objects containing target entities required field you can specify up to 10 entities (objects) in the target field one target entity can contain either one domain or one keyword and related parameters examples: target array with a domain entity'
        example:
        - keyword: bmw
          search_scope:
          - answer
        - keyword: auto
          search_scope:
          - question
          match_type: partial_match
      location_name:
        type: string
        required: false
        note: <em>full name of search location</em><br>optional field<br>if you use this field, you don't need to specify <code>location_code</code><br>if you don't specify this field, the <code>location_code</code> with <code class="long-string">2840</code> value will be used by default;<br>you can receive the l
      location_code:
        type: integer
        required: false
        note: <em>search location code</em><br>optional field<br>if you use this field, you don't need to specify <code>location_name</code><br>you can receive the list of available locations of the search engine with their <code>location_code</code> by making a separate request to the <code>https://api.dataforse
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search language</em><br>optional field<br>if you use this field, you don't need to specify <code>language_code</code>;<br>if you don't specify this field, the <code>language_code</code> with <code class="long-string">en</code> value will be used by default;<br>you can receive the li
      language_code:
        type: string
        required: false
        note: <em>search language code</em><br>optional field<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to the <code>https://api.datafors
        example: en
      platform:
        type: string
        required: false
        note: '<em>target platform</em><br>optional field<br>possible values:<br><code>chat_gpt</code>, <code>google</code><br>default value: <code class="long-string">google</code><br><strong>Note:</strong> if the <code>platform</code> is not specified, the data is returned for both platforms<br><strong>Note #2:<'
        example: chat_gpt
      links_scope:
        type: string
        required: false
        note: '<em>links source scope</em><br>optional field<br>this parameter specifies which links will be used to extract domains and aggregation data<br>possible values: <code>sources</code>, <code>search_results</code><br>default value: <code>sources</code>'
        example: sources
      filters:
        type: array
        required: false
        note: <em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>=</code>, <cod
      initial_dataset_filters:
        type: array
        required: false
        note: <em>array of filter expressions applied before aggregation</em><br>optional field<br>you can use this array to filter expressions applied to the raw mentions database before aggregation to limit the rows contributing to the result;<p><strong>you can add several filters at once (8 filters maximum)</s
        example:
        - - ai_search_volume
          - '>'
          - 10
      limit:
        type: integer
        required: false
        note: '<em>maximum number of results in the items array</em><br>optional field<br>you can use this parameter to limit the number of data objects you receive in the <code>items</code> array<br>minimum value: <code>1</code><br>maximum value: <code>1000</code><br>default value: <code>100</code>'
        example: 3
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>sources_domain</code>, <code>search_results_domain</code>, <code>brand_entities_title</code>, <code>brand_entities_category</co
        example: 2
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of the returned mentions data</em><br>optional field<p>default value: <code>0</code><br>example: if you specify the <code>10</code> value, the first ten mentions objects in the results array will be omitted and the data will be provided for the successive objects;<br>'
      include_pages:
        type: array
        required: false
        note: <em>array of page URLs to include in the response</em><br>optional field<br>if specified, only the listed pages will be returned in the <code>items</code> array<br>example:<br>`["https://dataforseo.com/apis/ai-optimization-api/llm-mentions-api", "https://dataforseo.com/apis/ai-optimization-api"]`
      exclude_pages:
        type: array
        required: false
        note: <em>array of page URLs to exclude from the response</em><br>optional field<br>if specified, the listed pages will be omitted from the <code>items</code> array<br>example:<br>`["https://dataforseo.com/apis/ai-optimization-api/llm-mentions-api", "https://dataforseo.com/apis/ai-optimization-api"]`
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      platform: chat_gpt
      target:
      - keyword: bmw
        search_scope:
        - answer
      - keyword: auto
        search_scope:
        - question
        match_type: partial_match
      links_scope: sources
      initial_dataset_filters:
      - - ai_search_volume
        - '>'
        - 10
      limit: 3
      internal_list_limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-llm-mentions-top-mentioned-pages-lite-live.json
  observed_cost: 0.103
  capability: ai-search.mentions.top_pages
- id: dataforseo.x.dataforseo-labs-amazon-bulk-search-volume-live
  tier: extended
  platform: amazon
  method: POST
  path: /dataforseo_labs/amazon/bulk_search_volume/live
  name: "Amazon search volume for up to 1000 keywords"
  summary: This endpoint will provide you with search volume values for a maximum of 1,000 keywords in one API request.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/amazon/bulk_search_volume/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article'
        example:
        - buy laptop
        - cheap laptops for sale
        - purchase laptop
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if don’t specify location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the following'
      location_code:
        type: integer
        required: false
        note: 'location code required field if don’t specify location_name you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports these locations and langu'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages Note: this endpoint currently supports these location'
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages Note: this endpoint currently supports these locations and langua'
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keywords:
      - buy laptop
      - cheap laptops for sale
      - purchase laptop
      location_code: 2840
      language_code: en
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-amazon-bulk-search-volume-live.json
  observed_cost: 0.01236
  observed_time: 0.51
- id: dataforseo.x.dataforseo-labs-amazon-product-competitors-live
  tier: extended
  platform: amazon
  method: POST
  path: /dataforseo_labs/amazon/product_competitors/live
  name: "Amazon products competing with an ASIN in search"
  summary: This endpoint will provide you with a list of products that intersect with a target asin in Amazon SERPs.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/amazon/product_competitors/live/
  input:
    bodyType: json
    body:
      asin:
        type: string
        required: false
        note: product ID required field unique product identifier (ASIN) on Amazon; you can receive the asin parameter by making a separate request to the Amazon Products endpoint
        example: 019005476X
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if don’t specify location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt'
      location_code:
        type: integer
        required: false
        note: 'location code required field if don’t specify location_name you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt, Saudi Arab'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English'
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en'
        example: en
      limit:
        type: integer
        required: false
        note: 'the maximum number of products in the results array optional field default value: 100; maximum value: 1000'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, ilike, not_ilike, like, not_like, ma'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter example:'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned product competitors optional field default value: 0 if you specify the 10 value, the first ten product competitors in the results array will be omitted and the data will be provided for the successive product competitors'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - asin: 019005476X
      location_code: 2840
      language_code: en
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-amazon-product-competitors-live.json
  observed_cost: 0.01968
- id: dataforseo.x.dataforseo-labs-amazon-product-keyword-intersections-live
  tier: extended
  platform: amazon
  method: POST
  path: /dataforseo_labs/amazon/product_keyword_intersections/live
  name: "Amazon keywords where several ASINs rank together"
  summary: This endpoint will provide you with a list of keywords for which the target products intersect in Amazon SERP.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/amazon/product_keyword_intersections/live/
  input:
    bodyType: json
    body:
      asins:
        type: object
        required: false
        note: 'asins of target products required field product IDs of the products for which you need to find keyword intersections; specify the ASINs as in the following example: "asins": { "1": "019005476X", "2": "0190074442" } the maximum number of ASINs you can specify in this object is 20; learn more about th'
        example:
          '1': B09172433Z
          '2': B07GBZ4Q68
          '3': B07GCKQD77
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if don’t specify location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt'
      location_code:
        type: integer
        required: false
        note: 'location code required field if don’t specify location_name you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt, Saudi Arab'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en'
      limit:
        type: integer
        required: false
        note: 'the maximum number of products in the results array optional field default value: 100; maximum value: 1000'
      intersection_mode:
        type: string
        required: false
        note: 'mode for finding asin intersections optional field possible values: union, intersect; default value: intersect; learn more about the parameter in this help center guide'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, ilike, not_ilike, like, not_like, ma'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter example:'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - asins:
        '1': B09172433Z
        '2': B07GBZ4Q68
        '3': B07GCKQD77
      language_name: English
      location_code: 2840
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-amazon-product-keyword-intersections-live.json
  observed_cost: 0.012
  observed_time: 1.22
- id: dataforseo.x.dataforseo-labs-amazon-product-rank-overview-live
  tier: extended
  platform: amazon
  method: POST
  path: /dataforseo_labs/amazon/product_rank_overview/live
  name: "Amazon organic and paid rank for products by ASIN"
  summary: This endpoint will provide you with ranking data from organic and paid Amazon SERPs for the target products.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/amazon/product_rank_overview/live/
  input:
    bodyType: json
    body:
      asins:
        type: array
        required: false
        note: 'product IDs to compare required field product IDs to receive ranking data for; the maximum number of ASINs you can specify in this array is 1000; you can receive the asin parameter by making a separate request to the Amazon Products endpoint Note: all letters in ASIN code must be specified in upperc'
        example:
        - B001TJ3HUG
        - B01LW2SL7R
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if don’t specify location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt'
      location_code:
        type: integer
        required: false
        note: 'location code required field if don’t specify location_name you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt, Saudi Arab'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - asins:
      - B001TJ3HUG
      - B01LW2SL7R
      language_name: English
      location_code: 2840
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-amazon-product-rank-overview-live.json
  observed_cost: 0.01224
  observed_time: 0.42
- id: dataforseo.x.dataforseo-labs-amazon-ranked-keywords-live
  tier: extended
  platform: amazon
  method: POST
  path: /dataforseo_labs/amazon/ranked_keywords/live
  name: "Amazon keywords a product ranks for, by ASIN"
  summary: This endpoint will provide you with a list of keywords the target product ranks for on Amazon.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/amazon/ranked_keywords/live/
  input:
    bodyType: json
    body:
      asin:
        type: string
        required: false
        note: product ID required field unique product identifier (ASIN) on Amazon; you can receive the asin parameter by making a separate request to the Amazon Products endpoint
        example: B00R92CL5E
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if don’t specify location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt'
      location_code:
        type: integer
        required: false
        note: 'location code required field if don’t specify location_name you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages; Note: this endpoint currently supports the US, Egypt, Saudi Arab'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English'
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en'
        example: en
      limit:
        type: integer
        required: false
        note: 'the maximum number of products in the results array optional field default value: 100; maximum value: 1000'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like, not_like, match, not_match you'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter example:'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - asin: B00R92CL5E
      location_code: 2840
      language_code: en
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-amazon-ranked-keywords-live.json
  observed_cost: 0.01548
- id: dataforseo.x.dataforseo-labs-amazon-related-keywords-live
  tier: extended
  platform: amazon
  method: POST
  path: /dataforseo_labs/amazon/related_keywords/live
  name: "Amazon related-search keywords for a seed keyword"
  summary: The Related Keywords endpoint provides keywords appearing in the "Related Searches" section on Amazon.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/amazon/related_keywords/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field UTF-8 encoding the keywords should be specified in the lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article
        example: computer mouse
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
      depth:
        type: integer
        required: false
        note: 'keyword search depth optional field default value: 1; number of the returned results depends on the value you set in this field; you can specify a level from 0 to 4; estimated number of keywords for each level (maximum): 0 – the keyword set in the keyword field 1 – 6 keywords 2 – 42 keywords 3 – 258'
      include_seed_keyword:
        type: boolean
        required: false
        note: 'include data for the seed keyword optional field if set to true, data for the seed keyword specified in the keyword field will be provided in the seed_keyword_data array of the response default value: false'
        example: true
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 5
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: computer mouse
      language_name: English
      location_code: 2840
      limit: 5
      include_seed_keyword: true
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-amazon-related-keywords-live.json
  observed_cost: 0.0126
- id: dataforseo.x.merchant-amazon-asin-live-advanced
  capability: amazon.product.variants
  platform: amazon
  tier: extended
  method: POST
  path: /merchant/amazon/asin/live/advanced
  name: "Amazon ASINs for all variations of a product, live"
  summary: This endpoint will provide you with a full list of ASINs assigned to different modifications of a product.
  input:
    bodyType: json
    body:
      asin:
        type: string
        required: false
        note: <em>product ID</em><br><strong>required field</strong><br><a href="https://dataforseo.com/help-center/asin-in-amazon-api" target="_blank" rel="noopener noreferrer">unique product identifier (ASIN)</a> in Amazon<br>you can receive the <code>asin</code> parameter by making a separate request to the <a
        example: B0756FCPPN
      location_name:
        type: string
        required: false
        note: <em>full name of search engine location</em><br><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><br>y
      location_code:
        type: integer
        required: false
        note: <em>search engine location code</em><br><strong>required field if you don't specify</strong> <code>location_name</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><br>you can r
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: <em>GPS coordinates of a location</em><br><strong>required field if you don't specify</strong> <code>location_name</code> or <code>location_code</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><br><code>location_coor
      language_name:
        type: string
        required: false
        note: <em>full name of search engine language</em><br><strong>required field if you don't specify</strong> <code>language_code</code><br><strong>if you use this field, you don't need to specify <code>language_code</code></strong><br>you can receive the list of available languages with their <code>language
      language_code:
        type: string
        required: false
        note: <em>search engine language code</em><br><strong>required field if you don't specify</strong> <code>language_name</code><br><strong>if you use this field, you don't need to specify <code>language_name</code></strong><br>you can receive the list of available languages with their <code>language_code</c
        example: en_US
      se_domain:
        type: string
        required: false
        note: <em>search engine domain</em><br>optional field<br>we choose the relevant search engine domain automatically according to the location and language you specify<br>however, you can set a custom search engine domain in this field<br>example:<br><em><code>amazon.com</code></em>, <em><code>amazon.co.uk<
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en_US
      location_code: 2840
      asin: B0756FCPPN
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.merchant-amazon-asin-live-advanced.json
  observed_cost: 0.005
  observed_time: 22.57
- id: dataforseo.x.merchant-amazon-locations-country
  kind: utility
  tier: extended
  platform: amazon
  method: GET
  path: /merchant/amazon/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Amazon location codes by country"
  summary: You will receive the list of supported Amazon locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/merchant/amazon/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.merchant-amazon-products-live-advanced
  tier: extended
  platform: amazon
  method: POST
  path: /merchant/amazon/products/live/advanced
  name: "Amazon product search results for a keyword, live"
  summary: This endpoint provides results from Amazon product listings according to the specified keyword (product name), location, and language parameters.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <em>keyword</em><br><strong>required field</strong><br>you can specify <strong>up to 700 characters</strong> in this field<br><strong>all %## will be decoded (plus character ‘+’ will be decoded to a space character)</strong><br>if you need to use the “%” character for your <code>keyword</code>, plea
        example: shoes
      url:
        type: string
        required: false
        note: <em>direct URL of the search query</em><br>optional field<br>you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, we
      location_name:
        type: string
        required: false
        note: <em>full name of search engine location</em><br><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><br>y
      location_code:
        type: integer
        required: false
        note: <em>search engine location code</em><br><strong>required field if you don't specify</strong> <code>location_name</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><br>you can r
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: <em>GPS coordinates of a location</em><br><strong>required field if you don't specify</strong> <code>location_name</code> or <code>location_code</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><br><code>location_coor
      language_name:
        type: string
        required: false
        note: <em>full name of search engine language</em><br><strong>required field if you don't specify</strong> <code>language_code</code><br><strong>if you use this field, you don't need to specify <code>language_code</code></strong><br>you can receive the list of available languages with their <code>language
      language_code:
        type: string
        required: false
        note: <em>search engine language code</em><br><strong>required field if you don't specify</strong> <code>language_name</code><br><strong>if you use this field, you don't need to specify <code>language_name</code></strong><br>you can receive the list of available languages with their <code>language_code</c
        example: en_US
      se_domain:
        type: string
        required: false
        note: <em>search engine domain</em><br>optional field<br>we choose the relevant search engine domain automatically according to the location and language you specify<br>however, you can set a custom search engine domain in this field<br>example:<br><code><em>amazon.com</em></code>, <code><em>amazon.co.uk<
      depth:
        type: integer
        required: false
        note: '<em>parsing depth</em><br>optional field<br>number of results to be retrieved from the Amazon results page<br>default value: <code>100</code><br>max value: <code>700</code><br><strong>Your account will be billed per each SERP containing up to 100 results;</strong> <br>Setting depth above 100 may res'
      max_crawl_pages:
        type: integer
        required: false
        note: '<em>page crawl limit</em><br>optional field<br>number of search results pages to crawl<br>max value: <code>7</code><br><strong>Note:</strong> the <code>max_crawl_pages</code> and <code>depth</code> parameters complement each other;<br>learn more at <a href="https://dataforseo.com/help-center/what-is'
      department:
        type: string
        required: false
        note: <em>amazon product department</em><br>optional field<br>specify one of the following amazon departments for extracting product listings:<br><code>"Arts & Crafts"</code>, <code>"Automotive"</code>, <code>"Baby"</code>, <code>"Beauty & Personal Care"</code>, <code>"Books"</code>, <code>"Computers"</co
      search_param:
        type: string
        required: false
        note: <em>additional parameters of the search query</em><br>optional field<br>you can use the following Amazon search URL parameters for customizing the search<br>example:<br><code>&low-price=52</code> - search for products that cost <em>more than 52 USD</em>;<br><code>&high-price=45</code> - search for p
      price_min:
        type: integer
        required: false
        note: <em>minimum product price</em><br>optional field<br>minimum price of the returned products listed on Amazon for the specified query<br>example:<br><code>5</code><br><strong>Note:</strong> if you specify <code>price_min</code>, the <code>search_param</code> parameter will be ignored
      price_max:
        type: integer
        required: false
        note: <em>maximum product price</em><br>optional field<br>maximum price of the returned products listed on Amazon for the specified query<br>example:<br><code>100</code><br><strong>Note:</strong> if you specify <code>price_max</code>, the <code>search_param</code> parameter will be ignored
      sort_by:
        type: string
        required: false
        note: '<em>results sorting rules</em><br>optional field<br>the following sorting rules are supported:<br><code>relevance</code>, <code>price_low_to_high</code>, <code>price_high_to_low</code>, <code>featured</code>, <code>avg_customer_review</code>, <code>newest_arrival</code><br>example:<br><code>sort_by:'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en_US
      location_code: 2840
      keyword: shoes
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.merchant-amazon-products-live-advanced.json
  observed_cost: 0.0033
  observed_time: 23.64
- id: dataforseo.x.merchant-amazon-sellers-live-advanced
  capability: amazon.product.sellers
  platform: amazon
  tier: extended
  method: POST
  path: /merchant/amazon/sellers/live/advanced
  name: "Amazon sellers of a product by ASIN, live"
  summary: This endpoint provides a list of sellers of the specified product on Amazon.
  input:
    bodyType: json
    body:
      asin:
        type: string
        required: false
        note: <em>unique product identifier on Amazon</em><br><strong>required field</strong><br>you can get this value making a separate request to <a href="/v3/merchant/amazon/products/task_post">the Amazon Products endpoint</a><br>note that there is no full list of possible values as the <code>asin</code> valu
        example: B07D528W98
      location_name:
        type: string
        required: false
        note: <em>full name of the location</em><br><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><br>you can rec
      location_code:
        type: integer
        required: false
        note: <em>location code</em><br><strong>required field if you don't specify</strong> <code>location_name</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><br>you can receive the lis
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: <em>GPS coordinates of a location</em><br><strong>required field if you don't specify</strong> <code>location_name</code> or <code>location_code</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><br><code>location_coor
      language_name:
        type: string
        required: false
        note: <em>full name of the language</em><br><strong>required field if you don't specify</strong> <code>language_code</code><br><strong>if you use this field, you don't need to specify <code>language_code</code></strong><br>you can receive the list of available Amazon languages with their <code>language_na
      language_code:
        type: string
        required: false
        note: <em>language code</em><br><strong>required field if you don't specify</strong> <code>language_name</code><br><strong>if you use this field, you don't need to specify <code>language_name</code></strong><br>you can receive the list of available Amazon languages with their <code>language_code</code> by
        example: en_US
      se_domain:
        type: string
        required: false
        note: <em>search engine domain</em><br>optional field<br>we choose the relevant search engine domain automatically according to the location and language you specify<br>however, you can set a custom search engine domain in this field<br>example:<br><em><code>amazon.co.uk</code></em>, <em><code>amazon.com.
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en_US
      location_code: 2840
      asin: B07D528W98
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.merchant-amazon-sellers-live-advanced.json
  observed_cost: 0.0033
  observed_time: 105.51
- id: dataforseo.x.app-data-apple-app-info-task-post
  tier: extended
  platform: app-store
  method: POST
  path: /app_data/apple/app_info/task_post
  name: "App Store app details by app id (async task)"
  summary: This endpoint will provide you with information about the App Store application specified in the app_id field of the POST request.
  docs_url: https://docs.dataforseo.com/v3/app_data/apple/app_info/task_post/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the app required field ID of the mobile application on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL https://apps.apple.com/us/app/id835599320 the id is 835599320'
        example: '835599320'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/apple/languag
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/apple/languages
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: '835599320'
      location_code: 2840
      language_code: en
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-apple-app-info-task-post.json
  observed_cost: 0.0006
- id: dataforseo.x.app-data-apple-app-list-task-post
  tier: extended
  platform: app-store
  method: POST
  path: /app_data/apple/app_list/task_post
  name: "App Store top chart apps by collection (async task)"
  summary: This endpoint will provide you with a list of mobile applications published in the top app charts on the App Store platform.
  docs_url: https://docs.dataforseo.com/v3/app_data/apple/app_list/task_post/
  input:
    bodyType: json
    body:
      app_collection:
        type: string
        required: false
        note: 'app collection required field app collection on App Store from which apps will be collected; you can specify the following values: top_free_ios, top_paid_ios, top_grossing_ios, new_ios, new_free_ios, new_paid_ios'
        example: top_free_ios
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/apple/languag
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/apple/languages
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of apps to be returned from the App Store SERP; we strongly recommend setting the parsing depth in the multiples of 100, because our system processes 100 results in a row; default value: 100 maximum value: 1000 Your account will be billed per each SERP containing '
        example: 200
      app_category:
        type: string
        required: false
        note: 'application category on the App Store optional field you can filter the results by app category; example: lifestyle; you can review the full list of available categories here or by making a separate request to https://api.dataforseo.com/v3/app_data/apple/categories'
        example: games
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_collection: top_free_ios
      location_code: 2840
      language_code: en
      depth: 200
      app_category: games
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-apple-app-list-task-post.json
  observed_cost: 0.0024
- id: dataforseo.x.app-data-apple-app-listings-search-live
  tier: extended
  platform: app-store
  method: POST
  path: /app_data/apple/app_listings/search/live
  name: "Search App Store apps — rating, reviews count, price"
  summary: 'This endpoint will provide you with a list of apps published on App Store along with additional information: its ID, icon, reviews count, rating, price, and other data.'
  docs_url: https://docs.dataforseo.com/v3/app_data/apple/app_listings/search/live/
  input:
    bodyType: json
    body:
      categories:
        type: array
        required: false
        note: app categories optional field the categories you specify are used to search for app listings; you can get the full list of available app listing categories by this link you can specify up to 10 categories
        example:
        - Tools
      description:
        type: string
        required: false
        note: keyword in the app’s description optional field keywords that occur in the description of the app; can contain up to 200 characters
        example: vpn
      title:
        type: string
        required: false
        note: keyword in the app’s title optional field keywords that occur in the title of the app; can contain up to 200 characters
        example: vpn
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % ope'
        example:
        - - item.rating.value
          - '>'
          - 4.5
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter example:'
        example:
        - item.rating.value,desc
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned apps optional field default value: 100 maximum value: 1000'
        example: 2
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned apps optional field default value: 0 if you specify the 10 value, the first ten entities in the results array will be omitted and the data will be provided for the successive entities Note: we recommend using this parameter only when retrieving up to 10,000 re'
      offset_token:
        type: string
        required: false
        note: token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 100,000 results in a single request; by specifying the unique offset_token value from the response array, you will get the su
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - title: vpn
      description: vpn
      categories:
      - Tools
      order_by:
      - item.rating.value,desc
      filters:
      - - item.rating.value
        - '>'
        - 4.5
      limit: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-apple-app-listings-search-live.json
  observed_cost: 0
- id: dataforseo.x.app-data-apple-app-reviews-task-post
  tier: extended
  platform: app-store
  method: POST
  path: /app_data/apple/app_reviews/task_post
  name: "App Store reviews for an app by app id (async task)"
  summary: This endpoint will provide you with reviews published on the App Store platform for the app specified in the app_id field.
  docs_url: https://docs.dataforseo.com/v3/app_data/apple/app_reviews/task_post/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the app required field ID of the mobile application on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL https://apps.apple.com/us/app/id835599320 the id is 835599320'
        example: '835599320'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/apple/languag
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/apple/languages
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of reviews to be returned in the API response; we strongly recommend setting the parsing depth in the multiples of 25, because our system processes 25 reviews in a row; default value: 25; maximum value: 500; Your account will be billed per each SERP containing up '
        example: 200
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field you can use this field to sort the results; possible types of sorting: most_recent — sort by the most recent reviews; most_helpful — sort by the most relevant reviews; default rule: most_helpful'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: '835599320'
      location_code: 2840
      language_code: en
      depth: 200
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-apple-app-reviews-task-post.json
  observed_cost: 0.006
- id: dataforseo.x.app-data-apple-app-searches-task-post
  tier: extended
  platform: app-store
  method: POST
  path: /app_data/apple/app_searches/task_post
  name: "App Store apps ranking for a keyword (async task)"
  summary: This endpoint will provide you with a list of apps ranking on the App Store for the specified keyword.
  docs_url: https://docs.dataforseo.com/v3/app_data/apple/app_searches/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field; all %## will be decoded (plus character ‘+’ will be decoded to a space character); if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keywor
        example: vpn
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if language_code is not specified if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/apple/language
      language_code:
        type: string
        required: false
        note: 'search engine language code required field if language_name is not specified if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/apple/languages '
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results to be returned from the App Store SERP; we strongly recommend setting the parsing depth in the multiples of 100, because our system processes 100 results in a row; default value: 100 maximum value: 700 Your account will be billed per each SERP containin'
        example: 200
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: vpn
      location_code: 2840
      language_code: en
      depth: 200
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-apple-app-searches-task-post.json
  observed_cost: 0.0024
- id: dataforseo.x.dataforseo-labs-apple-app-competitors-live
  tier: extended
  platform: app-store
  method: POST
  path: /dataforseo_labs/apple/app_competitors/live
  name: "App Store apps competing with an app, by app id"
  summary: This endpoint will provide you with a list of mobile applications that intersect with the target app for its ranking keywords on App Store.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/apple/app_competitors/live/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the app required field ID of the mobile application on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL https://apps.apple.com/us/app/id835599320 the id is 835599320'
        example: '686449807'
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in example: ["intersections",">",500] [["competitor_metric'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results; possible sorting types: asc – results will be sorted in the ascending order; desc – results will be sorted in the descending order; you should use a comma to specify a sorting type; example:'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned apps optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned apps optional field default value: 0 if you specify the 10 value, the first ten apps in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: '686449807'
      language_name: English
      location_code: 2840
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-apple-app-competitors-live.json
  observed_cost: 0.0132
- id: dataforseo.x.dataforseo-labs-apple-app-intersection-live
  tier: extended
  platform: app-store
  method: POST
  path: /dataforseo_labs/apple/app_intersection/live
  name: "Keywords several App Store apps rank for together"
  summary: This endpoint will provide you with a list of keywords for which the mobile applications specified in the app_ids object rank within the same App Store SERP.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/apple/app_intersection/live/
  input:
    bodyType: json
    body:
      app_ids:
        type: object
        required: false
        note: 'ids of the target apps required field IDs of the target mobile applications on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL https://apps.apple.com/us/app/id835599320 the id is 835599320; the ids should be specified the following way: "app_ids": { "1'
        example:
          '1': '686449807'
          '2': '382617920'
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in example: ["keyword_data.keyword_info.search_volume",">"'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results; possible sorting types: asc – results will be sorted in the ascending order; desc – results will be sorted in the descending order; you should use a comma to specify a sorting type; example:'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_ids:
        '1': '686449807'
        '2': '382617920'
      language_name: English
      location_code: 2840
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-apple-app-intersection-live.json
  observed_cost: 0.0132
- id: dataforseo.x.dataforseo-labs-apple-bulk-app-metrics-live
  tier: extended
  platform: app-store
  method: POST
  path: /dataforseo_labs/apple/bulk_app_metrics/live
  name: "Ranking metrics for up to 1000 App Store apps"
  summary: This endpoint will provide you with ranking metrics for up to 1000 App Store applications.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/apple/bulk_app_metrics/live/
  input:
    bodyType: json
    body:
      app_ids:
        type: array
        required: false
        note: 'ids of the apps required field IDs of mobile applications on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL https://apps.apple.com/us/app/id835599320 the id is 835599320; the maximum number of IDs you can specify in this field is 1000'
        example:
        - '686449807'
        - '382617920'
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_ids:
      - '686449807'
      - '382617920'
      language_name: English
      location_code: 2840
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-apple-bulk-app-metrics-live.json
  observed_cost: 0.01224
- id: dataforseo.x.dataforseo-labs-apple-keywords-for-app-live
  tier: extended
  platform: app-store
  method: POST
  path: /dataforseo_labs/apple/keywords_for_app/live
  name: "Keywords an App Store app ranks for, by app id"
  summary: This endpoint will provide you with a list of keywords for which the target app ranks on App Store.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/apple/keywords_for_app/live/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the app required field ID of the mobile application on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL https://apps.apple.com/us/app/id835599320 the id is 835599320'
        example: '686449807'
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in example: ["keyword_data.keyword_info.search_volume",">"'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results; possible sorting types: asc – results will be sorted in the ascending order; desc – results will be sorted in the descending order; you should use a comma to specify a sorting type; example:'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: '686449807'
      language_name: English
      location_code: 2840
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-apple-keywords-for-app-live.json
  observed_cost: 0.0132
- id: dataforseo.x.serp-baidu-locations-country
  kind: utility
  tier: extended
  platform: baidu
  method: GET
  path: /serp/baidu/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Baidu location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/serp/baidu/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.serp-baidu-organic-task-post
  tier: extended
  platform: baidu
  method: POST
  path: /serp/baidu/organic/task_post
  name: "Baidu organic search results for a keyword (async task)"
  summary: Baidu SERP API provides top 10 search engine results.
  docs_url: https://docs.dataforseo.com/v3/serp/baidu/organic/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keyword,
        example: best iphone ever
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
        example: 2
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 10 max value: 700 Your account will be billed per each SERP containing up to 10 results; Setting depth above 10 may result in additional charges if the search engine returns more than 10 results; The cost can be calculated on the '
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl default value: 1 max value: 100 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforseo.com/v3/serp/baidu/languagesnote that the only language suppor
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3/serp/baidu/languagesnote that the only language supported in B
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
        example: 2156
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      device:
        type: string
        required: false
        note: 'device type optional field return results for a specific device type can take the values: desktop, mobile, tablet default value: desktop'
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android if you specify tablet in the dev'
      get_website_url:
        type: boolean
        required: false
        note: 'include direct URL for each ranked result optional field if set to true, the returned results will contain direct URLs of the ranked websites by default, the URLs in Baidu results are encoded by the search engine, for example: http://www.baidu.com/link?url=KQt6LSwU5OHnPtB8210R8flBP40grY6lTPxH_0UO7S2'
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_value:
        type: string
        required: false
        note: 'target domain, subdomain, or wildcard value required field if stop_crawl_on_match is specified specify a target domain, subdomain, or wildcard value; Note: domain or subdomain must be specified without a request protocol; example: "match_value": "dataforseo.com", "match_value": "/blog/post-*"'
      match_type:
        type: string
        required: false
        note: 'target match type required field if stop_crawl_on_match is specified type of match for the match_value possible values: domain – specific domain or subdomain with_subdomains – main domain and subdomains wildcard – wildcard pattern'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: some_string_123
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: regular, html'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2156
      keyword: best iphone ever
      tag: some_string_123
      priority: 2
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-baidu-organic-task-post.json
  observed_cost: 0.0012
  capability: baidu.serp.organic
- id: dataforseo.x.keywords-data-bing-audience-estimation-industries
  kind: utility
  tier: extended
  platform: bing
  method: GET
  path: /keywords_data/bing/audience_estimation/industries
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Bing Ads industries for audience targeting"
  summary: By calling this API you will receive the list of industries with industry_id supported by Bing Ads Audience Estimation endpoint.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/audience_estimation/industries/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.keywords-data-bing-audience-estimation-job-functions
  kind: utility
  tier: extended
  platform: bing
  method: GET
  path: /keywords_data/bing/audience_estimation/job_functions
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Bing Ads job functions for audience targeting"
  summary: By calling this API you will receive the list of job functions with job_function_id supported by Bing Ads Audience Estimation endpoint.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/audience_estimation/job_functions/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.keywords-data-bing-audience-estimation-live
  tier: extended
  platform: bing
  method: POST
  path: /keywords_data/bing/audience_estimation/live
  name: "Bing Ads audience size estimate for campaign targeting"
  summary: This endpoint provides estimated audience size for an ad campaign based on specified targeting criteria.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/audience_estimation/live/
  input:
    bodyType: json
    body:
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius (in km)” format the data will be provid
        example: 29.6821525,-82.4098881,100
      age:
        type: array
        required: false
        note: 'selection of age ranges for targeting possible values: eighteen_to_twenty_four, fifty_to_sixty_four, sixty_five_and_above, thirteen_to_seventeen, thirty_five_to_forty_nine, twenty_five_to_thirty_four, unknown, zero_to_twelve'
        example:
        - twenty_five_to_thirty_four
        - eighteen_to_twenty_four
        - unknown
      bid:
        type: number
        required: false
        note: 'desired bid setting value in USD maximum value: 1000'
        example: 1
      daily_budget:
        type: number
        required: false
        note: 'daily campaign budget value in USD maximum value: 10000'
        example: 24
      gender:
        type: array
        required: false
        note: 'gender to target possible values: male, female, unknown'
        example:
        - male
      industry:
        type: array
        required: false
        note: 'industry of LinkedIn profile targeting if you use this field, you can receive the list of available industry names with industry_id by making a separate request to the https://api.dataforseo.com/v3/keywords_data/bing/audience_estimation/industries example: 806301758'
        example:
        - '806303407'
        - '806301758'
      job_function:
        type: array
        required: false
        note: 'job function of LinkedIn profile targeting if you use this field, you can receive the list of available job function names with job_function_id by making a separate request to the https://api.dataforseo.com/v3/keywords_data/bing/audience_estimation/job_functions example: 806300451'
        example:
        - '806298607'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_coordinate: 29.6821525,-82.4098881,100
      age:
      - twenty_five_to_thirty_four
      - eighteen_to_twenty_four
      - unknown
      bid: 1
      daily_budget: 24
      gender:
      - male
      industry:
      - '806303407'
      - '806301758'
      job_function:
      - '806298607'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-bing-audience-estimation-live.json
  observed_cost: 0.09
  capability: bing.audience.estimate
- id: dataforseo.x.keywords-data-bing-keyword-performance-live
  tier: extended
  platform: bing
  method: POST
  path: /keywords_data/bing/keyword_performance/live
  name: "Bing keyword performance stats for a keyword list"
  summary: You can receive a set of keyword performance stats for a group of keywords depending on the specified match type, location and language parameters.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/keyword_performance/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field The maximum number of keywords you can specify: 1000 The maximum number of characters for each keyword: 80 The maximum number of words for each keyword phrase: 10 the specified keywords will be converted to lowercase, data will be provided in a separate array learn more about'
        example:
        - dataforseo
        - seo
        - ranking
      device:
        type: string
        required: false
        note: 'device type optional field specify this field if you want to get the data for a particular device typepossible values: desktop, mobile, tablet, all default value: all'
      match:
        type: string
        required: false
        note: 'keywords match type optional field can take the following values: aggregate returns data across all match types; broad returns data for all user queries containing the specified keyword with varying word order; phrase returns data for all user queries containing the specified keyword with identical '
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations and languages by making a separate request to https://api
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations and languages by making a separate request to https://api.datafor
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude” format the data will be provided for the coun
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available locations and languages by making a separate request to https://api.dataforseo.com/v3/keywords_data/bing/keyword_
      language_code:
        type: string
        required: false
        note: 'search engine language code required field if you don’t specify language_name you can receive the list of available locations and languages by making a separate request to https://api.dataforseo.com/v3/keywords_data/bing/keyword_performance/locations_and_languages example: "en"'
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_code: en
      keywords:
      - dataforseo
      - seo
      - ranking
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-bing-keyword-performance-live.json
  observed_cost: 0.09
  capability: bing.keywords.performance
- id: dataforseo.x.keywords-data-bing-keywords-for-keywords-live
  tier: extended
  platform: bing
  method: POST
  path: /keywords_data/bing/keywords_for_keywords/live
  name: "Bing keyword ideas from seed keywords"
  summary: This endpoint will select the relevant keywords for the specified ones.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/keywords_for_keywords/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: keywords required field you can specify the maximum of 200 keywords with each keyword containing no more than 100 characters; the specified keywords will be converted to lowercase, data will be provided in a separate array learn more about rules and limitations of keyword and keywords fields in Data
        example:
        - average page rpm adsense
        - adsense blank ads how long
        - leads and prospects
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
        example: United States
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude” format the data will be provided for the coun
      language_name:
        type: string
        required: false
        note: 'full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code supported languages: English, French, German'
        example: English
      language_code:
        type: string
        required: false
        note: 'search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name supported languages: en, fr, de'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field Use these parameters to sort the results by search_volume, cpc, competition or relevance in the descending order default value: relevance'
      keywords_negative:
        type: array
        required: false
        note: keywords negative array optional field These keywords will be ignored in the results array; You can specify a maximum of 200 terms that you want to exclude from the results; the specified keywords will be converted to lowercase format
      device:
        type: string
        required: false
        note: 'device type optional field specify this field if you want to get the data for a particular device type; possible values: all, mobile, desktop, tablet default value: all'
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field minimal value: 24 months from today’s date; if you don’t specify this field, data will be provided for the last 12 months date format: "yyyy-mm-dd" example: "2020-01-01" Note: we do not recommend using a custom time range for the past year’s dates'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, data will be provided for the last 12 months; minimum value: two years back from today’s date; maximum value: one month from today’s date; date format: "yyyy-mm-dd" example: "2020-03-15" Note: we do not recommend using a c'
      search_partners:
        type: boolean
        required: false
        note: 'Bing search partners type optional field if you specify true, the results will be delivered for owned, operated, and syndicated networks across Bing, Yahoo, AOL and partner sites that host Bing, AOL, and Yahoo search. default value: false – results are returned for Bing, AOL, and Yahoo search networ'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_name: United States
      language_name: English
      keywords:
      - average page rpm adsense
      - adsense blank ads how long
      - leads and prospects
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-bing-keywords-for-keywords-live.json
  observed_cost: 0.09
  capability: bing.keywords.ideas
- id: dataforseo.x.keywords-data-bing-keywords-for-site-live
  tier: extended
  platform: bing
  method: POST
  path: /keywords_data/bing/keywords_for_site/live
  name: "Bing keywords for a site — volume, CPC, competition"
  summary: This endpoint will provide you with a list of keywords relevant to the specified URL along with their search volume for the last month, search volume trend for up to 24 past months (for estimating search volume dynamics), current cost-per-click and competition values for paid search.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/keywords_for_site/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain or URL required field the domain name or URL of the target website
        example: dataforseo.com
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude” format the data will be provided for the coun
      language_name:
        type: string
        required: false
        note: 'full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code supported languages: English, French, German'
      language_code:
        type: string
        required: false
        note: 'search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name supported languages: en, fr, de'
        example: en
      keywords_negative:
        type: array
        required: false
        note: keywords negative array optional field These keywords will be ignored in the results array; You can specify a maximum of 200 terms that you want to exclude from the results; the specified keywords will be converted to lowercase format
      device:
        type: string
        required: false
        note: 'device type optional field specify this field if you want to get the data for a particular device typepossible values: all, mobile, desktop, tablet default value: all'
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field minimal value: 24 months from today’s date; if you don’t specify this field, data will be provided for the last 12 months date format: "yyyy-mm-dd" example: "2020-01-01" Note: we do not recommend using a custom time range for the past year’s dates'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, data will be provided for the last 12 months; minimum value: two years back from today’s date; maximum value: one month from today’s date; note: we do not recommend using a custom time range for the past year’s dates; date'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field Use these parameters to sort the results by search_volume, cpc, competition or relevance in the descending order default value: relevance'
      search_partners:
        type: boolean
        required: false
        note: 'Bing search partners type optional field if you specify true, the results will be delivered for owned, operated, and syndicated networks across Bing, Yahoo, AOL and partner sites that host Bing, AOL, and Yahoo search. default value: false – results are returned for Bing, AOL, and Yahoo search networ'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      target: dataforseo.com
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-bing-keywords-for-site-live.json
  observed_cost: 0.09
  capability: bing.keywords.for_site
- id: dataforseo.x.keywords-data-bing-search-volume-live
  tier: extended
  platform: bing
  method: POST
  path: /keywords_data/bing/search_volume/live
  name: "Bing search volume, CPC and competition for keywords"
  summary: This endpoint will provide you with search volume data for the last month, search volume trend for up to 24 past months (that will let you estimate search volume dynamics), current cost-per-click and competition values for paid search.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/search_volume/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field The maximum number of keywords you can specify: 1000 The maximum number of characters for each keyword: 100 the specified keywords will be converted to lowercase, data will be provided in a separate arraylearn more about rules and limitations of keyword and keywords fields in'
        example:
        - tom and jerry
        - silicon valley
        - spider man
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
        example: United States
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude” format the data will be provided for the coun
      language_name:
        type: string
        required: false
        note: 'full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code supported languages: English, French, German'
      language_code:
        type: string
        required: false
        note: 'search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name supported languages: en, fr, de'
        example: en
      device:
        type: string
        required: false
        note: 'device type optional field specify this field if you want to get the data for a particular device type; possible values: all, mobile, desktop, tablet default value: all'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field Use these parameters to sort the results by search_volume, cpc, competition or relevance in the descending order default value: relevance'
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field minimal value: 24 months from today’s date if you don’t specify this field, data will be provided for the last 12 months minimum value: two years back from today’s date date format: "yyyy-mm-dd" example: "2020-01-01"Note: we do not recommend using a cus'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, data will be provided for the last 12 months; minimum value: two years back from today’s date; maximum value: one month from today’s date; note: we do not recommend using a custom time range for the past year’s dates; date'
      search_partners:
        type: boolean
        required: false
        note: 'Bing search partners type optional field if you specify true, the results will be delivered for owned, operated, and syndicated networks across Bing, Yahoo, AOL and partner sites that host Bing, AOL, and Yahoo search. default value: false – results are returned for Bing, AOL, and Yahoo search networ'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_name: United States
      language_code: en
      keywords:
      - tom and jerry
      - silicon valley
      - spider man
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-bing-search-volume-live.json
  observed_cost: 0.09
  capability: bing.keywords.volume
- id: dataforseo.x.keywords-data-bing-search-volume-history-live
  tier: extended
  platform: bing
  method: POST
  path: /keywords_data/bing/search_volume_history/live
  name: "Bing historical search volume for up to 1000 keywords"
  summary: This endpoint will provide you with historical search volume data for up to 1000 keywords in one request.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/bing/search_volume_history/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field The maximum number of keywords you can specify: 1000 The maximum number of characters for each keyword: 100 the specified keywords will be converted to lowercase, data will be provided in a separate arraylearn more about rules and limitations of keyword and keywords fields in'
        example:
        - 10 minute timer
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude” format the data will be provided for the coun
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages of the search engines with their language_code by making a separate request to https://api.dataforseo.c
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages of the search engines with their language_code by making a separate request to https://api.dataforseo.com/v3/ke
        example: en
      device:
        type: array
        required: false
        note: 'device types optional field specify this field if you want to get the data for a particular device types possible values: mobile, desktop, tablet, non_smartphones default value: ["mobile", "desktop", "tablet", "non_smartphones"]'
      period:
        type: string
        required: false
        note: 'aggregates the returned data to a certain time period optional field specify this field if you want to get the data in monthly, weekly or daily formatpossible values: monthly, weekly, daily monthly – returns data up to past 24 months weekly – returns data up to past 15 weeks daily – returns data up '
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field minimum value: 24 months back from today’s date maximum value: one day from today’s date date format: "yyyy-mm-dd" example: "2020-01-01"Note: we do not recommend using a custom time range Note 2: if date_from and date_to parameters are not specified, th'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional fieldminimum value: two years back from today’s date; maximum value: one day from today’s date; date format: "yyyy-mm-dd" example: "2020-03-15" Note: we do not recommend using a custom time range Note 2: if date_from and date_to parameters are not specified, th'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_code: en
      keywords:
      - 10 minute timer
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-bing-search-volume-history-live.json
  observed_cost: 0.09
  capability: bing.keywords.volume_history
- id: dataforseo.x.serp-bing-locations-country
  kind: utility
  tier: extended
  platform: bing
  method: GET
  path: /serp/bing/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Bing location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/serp/bing/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.serp-bing-organic-live-advanced
  tier: extended
  platform: bing
  method: POST
  path: /serp/bing/organic/live/advanced
  name: "Bing organic results for a keyword — full SERP features"
  summary: Live SERP provides real-time data on top 100 search engine results for the specified keyword, search engine, and location.
  docs_url: https://docs.dataforseo.com/v3/serp/bing/organic/live/advanced/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character '+' will be decoded to a space character) if you need to use the "%" character for your keyword, please specify it as "%25"; if you need to use the “+” character for your keyword,
        example: flight ticket new york san francisco
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don't specify location_name or location_coordinate if you use this field, you don't need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
        example: 2840
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don't specify language_name if you use this field, you don't need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 10 max value: 200 Your account will be billed per each SERP containing up to 10 results; Setting depth above 10 may result in additional charges if the search engine returns more than 10 results; The cost can be calculated on the '
      device:
        type: string
        required: false
        note: 'device type optional field return results for a specific device type can take the values:desktop, mobile default value: desktop'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don't specify location_code or location_coordinate if you use this field, you don't need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don't specify language_code if you use this field, you don't need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      target:
        type: string
        required: false
        note: target domain, subdomain, or webpage to get results for optional field a domain or a subdomain should be specified without https:// and www. note that the results of target-specific tasks will only include SERP elements that contain a url string; you can also use a wildcard (‘*’) character to specif
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_type:
        type: string
        required: false
        note: 'target match type required field if stop_crawl_on_match is specified type of match for the match_value possible values: domain – specific domain or subdomain with_subdomains – main domain and subdomains wildcard – wildcard pattern'
      match_value:
        type: string
        required: false
        note: 'target domain, subdomain, or wildcard value required field if stop_crawl_on_match is specified specify a target domain, subdomain, or wildcard value; Note: domain or subdomain must be specified without a request protocol; example: "match_value": "dataforseo.com", "match_value": "/blog/post-*"'
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl default value: 1 max value: 100 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      search_param:
        type: string
        required: false
        note: additional parameters of the search query optional field get the list of available parameters and additional details here
      calculate_rectangles:
        type: boolean
        required: false
        note: 'calculate pixel rankings for SERP elements in advanced results optional field pixel ranking refers to the distance between the result snippet and top left corner of the screen; Visit Help Center to learn more>> by default, the parameter is set to false Note: you will be charged extra $0.002 for usin'
      browser_screen_width:
        type: integer
        required: false
        note: 'browser screen width optional field you can set a custom browser screen width to calculate pixel rankings for a particular device; can be specified within the following range: 240-9999; by default, the parameter is set to: 1920 for desktop; 360 for mobile on android; 375 for mobile on iOS; Note: to '
      browser_screen_height:
        type: integer
        required: false
        note: 'browser screen height optional field you can set a custom browser screen height to calculate pixel rankings for a particular device; can be specified within the following range: 240-9999; by default, the parameter is set to: 1080 for desktop; 640 for mobile on android; 812 for mobile on iOS; Note: t'
      browser_screen_resolution_ratio:
        type: integer
        required: false
        note: 'browser screen resolution ratio optional field you can set a custom browser screen resolution ratio to calculate pixel rankings for a particular device; can be specified within the following range: 0.5-3; by default, the parameter is set to: 1 for desktop; 3 for mobile on android; 3 for mobile on iO'
      url:
        type: string
        required: false
        note: direct URL of the search query optional field you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, we wouldn’t recom
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don't specify location_name or location_code if you use this field, you don't need to specify location_name or location_code location_coordinate parameter should be specified in the "latitude,longitude" format the maximum number of decimal digits f
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: flight ticket new york san francisco
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-bing-organic-live-advanced.json
  observed_cost: 0.002
  capability: bing.serp.organic
- id: dataforseo.x.serp-bing-organic-live-regular
  tier: extended
  platform: bing
  method: POST
  path: /serp/bing/organic/live/regular
  name: "Bing organic search results for a keyword, live"
  summary: Live SERP provides real-time data on search engine results for the specified keyword, search engine, and location.
  docs_url: https://docs.dataforseo.com/v3/serp/bing/organic/live/regular/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character '+' will be decoded to a space character) if you need to use the "%" character for your keyword, please specify it as "%25"; if you need to use the “+” character for your keyword,
        example: albert einstein
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don't specify location_name or location_coordinate if you use this field, you don't need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
        example: 2840
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don't specify language_name if you use this field, you don't need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 10 max value: 200 Your account will be billed per each SERP containing up to 10 results; Setting depth above 10 may result in additional charges if the search engine returns more than 10 results; The cost can be calculated on the '
      device:
        type: string
        required: false
        note: 'device type optional field return results for a specific device type can take the values:desktop, mobile default value: desktop'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don't specify location_code or location_coordinate if you use this field, you don't need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don't specify language_code if you use this field, you don't need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      target:
        type: string
        required: false
        note: target domain, subdomain, or webpage to get results for optional field a domain or a subdomain should be specified without https:// and www. note that the results of target-specific tasks will only include SERP elements that contain a url string; you can also use a wildcard (‘*’) character to specif
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_type:
        type: string
        required: false
        note: 'target match type required field if stop_crawl_on_match is specified type of match for the match_value possible values: domain – specific domain or subdomain with_subdomains – main domain and subdomains wildcard – wildcard pattern'
      match_value:
        type: string
        required: false
        note: 'target domain, subdomain, or wildcard value required field if stop_crawl_on_match is specified specify a target domain, subdomain, or wildcard value; Note: domain or subdomain must be specified without a request protocol; example: "match_value": "dataforseo.com", "match_value": "/blog/post-*"'
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl default value: 1 max value: 100 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      search_param:
        type: string
        required: false
        note: additional parameters of the search query optional field get the list of available parameters and additional details here
      url:
        type: string
        required: false
        note: direct URL of the search query optional field you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, we wouldn’t recom
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don't specify location_name or location_code if you use this field, you don't need to specify location_name or location_code location_coordinate parameter should be specified in the "latitude,longitude" format the maximum number of decimal digits f
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-bing-organic-live-regular.json
  observed_cost: 0.002
  capability: bing.serp.organic
- id: dataforseo.x.ai-optimization-chat-gpt-llm-responses-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/chat_gpt/llm_responses/live
  name: "ChatGPT answer to a prompt, live"
  summary: Live ChatGPT LLM Responses endpoint allows you to retrieve structured responses from a specific ChatGPT AI model, based on the input parameters.
  input:
    bodyType: json
    body:
      user_prompt:
        type: string
        required: false
        note: <em>prompt for the AI model</em><br><strong>required field</strong><br>the question or task you want to send to the AI model;<br>you can specify <strong>up to 500 characters</strong> in the <code>user_prompt</code> field
        example: provide information on how relevant the amusement park business is in France now
      model_name:
        type: string
        required: false
        note: <em>name of the AI model</em><br><strong>required field</strong><br><code>model_name</code >consists of the actual model name and version name;<br>if the basic model name is specified, its latest version will be set by default;<br>for example, if <code>gpt-4.1</code> is specified, the <code>gpt-4.1-
        example: gpt-4.1-mini
      max_output_tokens:
        type: integer
        required: false
        note: '<em>maximum number of tokens in the AI response</em><br>optional field<br>minimum value for reasoning models (e.g., <code>reasoning</code> is <code>true</code> in the <a href="/v3/ai_optimization/chat_gpt/llm_responses/models/" target="_blank">Models endpoint</a>): <code>1024</code>;<br>minimum valu'
        example: 200
      temperature:
        type: number
        required: false
        note: '<em>randomness of the AI response</em><br>optional field<br>higher values make output more diverse; <br>lower values make output more focused;<br>minimum value: <code>0</code><br>maximum value: <code>2</code><br>default value: <code>0.94</code><br><strong>Note:</strong> not supported in reasoning mo'
        example: 0.3
      top_p:
        type: number
        required: false
        note: '<em>diversity of the AI response</em><br>optional field <br>controls diversity of the response by limiting token selection;<br>minimum value: <code>0</code><br>maximum value: <code>1</code> <br>default value: <code>0.92</code><p><strong>Note:</strong> <code>top_p</code> cannot be used together with '
        example: 0.5
      web_search:
        type: boolean
        required: false
        note: '<em>enable web search</em><br>optional field<br>when enabled, the AI model can access and cite current web information;<br>default value: <code>false</code>;<br><strong>Note:</strong> refer to the <a href="https://docs.dataforseo.com/v3/ai_optimization/chat_gpt/llm_responses/models/">Models endpoint'
        example: true
      force_web_search:
        type: boolean
        required: false
        note: '<em>force AI agent to use web search</em><br>optional field<br>to enable this parameter, <code>web_search</code> must also be enabled;<br>when enabled, the AI model is forced to access and cite current web information;<br>default value: <code>false</code>;<br><strong>Note:</strong> even if the param'
      web_search_country_iso_code:
        type: string
        required: false
        note: <em>ISO country code of the location</em><br>optional field<br>to enable this parameter, <code>web_search</code> must also be enabled;<br>when enabled, the AI model will search the web from the country you specify;<br><strong>Note:</strong> not supported in <code>o3-mini</code>, <code>o1-pro</code>,
        example: FR
      web_search_city:
        type: string
        required: false
        note: <em>city name of the location</em><br>optional field<br><strong>Note:</strong> not supported in <code>o3-mini</code>, <code>o1-pro</code>, <code>o1</code> models
        example: Paris
      system_message:
        type: string
        required: false
        note: <em>instructions for the AI behaviour</em><br>optional field<br>defines the AI's role, tone, or specific behavior <br>you can specify <strong>up to 500 characters</strong> in the <code>system_message</code> field
        example: communicate as if we are in a business meeting
      message_chain:
        type: array
        required: false
        note: 'conversation history optional field array of message objects representing previous conversation turns; each object must contain: role string with either user or ai role; message string with message content (max 500 characters); you can specify maximum of 10 message objects in the array; Note: for Pe'
        example:
        - role: user
          message: Hello, what’s up?
        - role: ai
          message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - system_message: communicate as if we are in a business meeting
      message_chain:
      - role: user
        message: Hello, what’s up?
      - role: ai
        message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      max_output_tokens: 200
      temperature: 0.3
      top_p: 0.5
      model_name: gpt-4.1-mini
      web_search: true
      web_search_country_iso_code: FR
      web_search_city: Paris
      user_prompt: provide information on how relevant the amusement park business is in France now
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-chat-gpt-llm-responses-live.json
  observed_cost: 0.0292488
  capability: ai-search.chatgpt.answer
- id: dataforseo.x.ai-optimization-chat-gpt-llm-scraper-live-advanced
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/chat_gpt/llm_scraper/live/advanced
  name: "ChatGPT search results for a keyword, live"
  summary: Live ChatGPT LLM Scraper endpoint provides results from ChatGPT searches.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: '<em>keyword</em><br><strong>required field</strong><br>you can specify <strong>up to 2000 characters</strong> in the <code>keyword</code> field<br>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<br>if you need to use the “%” character for your <code>keyword</code>, '
        example: albert einstein
      location_name:
        type: string
        required: false
        note: <em>full name of search engine location</em><br><strong>required field if you don't specify</strong> <code>location_code</code><br><strong>if you use this field, you don't need to specify <code>location_code</code></strong><br>you can receive the list of available locations of the search engine with
      location_code:
        type: integer
        required: false
        note: '<em>search engine location code</em><br><strong>required field if you don''t specify</strong> <code>location_name</code><br><strong>if you use this field, you don''t need to specify <code>location_name</code></strong><br>you can receive the list of available locations of the search engines with their '
        example: 2840
      language_name:
        type: string
        required: false
        note: <em>full name of search engine language</em><br><strong>required field if you don't specify <code>language_code</code>;</strong><br>if you use this field, you don't need to specify <code>language_code</code>;<br>you can receive the list of available languages of the search engine with their <code>la
      language_code:
        type: string
        required: false
        note: <em>search engine language code</em><br><strong>required field if you don't specify <code>language_name</code>;</strong><br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_c
        example: en
      force_web_search:
        type: boolean
        required: false
        note: '<em>force AI agent to use web search</em><br>optional field<br>when enabled, the AI model is forced to access and cite current web information;<br>default value: <code>false</code>;<br><strong>Note:</strong> even if the parameter is set to <code>true</code>, there is no guarantee web sources will be'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-chat-gpt-llm-scraper-live-advanced.json
  observed_cost: 0.004
  capability: ai-search.chatgpt.scrape
- id: dataforseo.x.ai-optimization-chat-gpt-llm-scraper-locations-country
  kind: utility
  tier: extended
  platform: ai-search
  method: GET
  path: /ai_optimization/chat_gpt/llm_scraper/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "ChatGPT search location codes by country"
  summary: You will receive the list of locations by this API call.
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.ai-optimization-claude-llm-responses-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/claude/llm_responses/live
  name: "Claude answer to a prompt, live"
  summary: Live Claude LLM Responses endpoint allows you to retrieve structured responses from a specific Claude model, based on the input parameters.
  input:
    bodyType: json
    body:
      user_prompt:
        type: string
        required: false
        note: <em>prompt for the AI model</em><br><strong>required field</strong><br>the question or task you want to send to the AI model;<br>you can specify <strong>up to 500 characters</strong> in the <code>user_prompt</code> field
        example: provide information on how relevant the amusement park business is in France now
      model_name:
        type: string
        required: false
        note: <em>name of the AI model</em><br><strong>required field</strong><br><code>model_name</code >consists of the actual model name and version name;<br>if the basic model name is specified, its latest version will be set by default;<br>for example, if <code>claude-opus-4-0</code> is specified, the <code>
        example: claude-opus-4-0
      max_output_tokens:
        type: integer
        required: false
        note: '<em>maximum number of tokens in the AI response</em><br>optional field<br>minimum value: <code>1</code>;<br>maximum value: <code>4096</code>;<br>default value: <code>2048</code>;<br><strong>Note:</strong> if <code>web_search</code> is set to <code>true</code> or the reasoning model is specified in t'
        example: 200
      temperature:
        type: number
        required: false
        note: '<em>randomness of the AI response</em><br>optional field<br>higher values make output more diverse; <br>lower values make output more focused;<br>minimum value: <code>0</code><br>maximum value: <code>1</code><br>default value: <code>0.7</code><p><strong>Note:</strong> <code>temperature</code> cannot'
        example: 0.3
      top_p:
        type: number
        required: false
        note: '<em>diversity of the AI response</em><br>optional field <br>controls diversity of the response by limiting token selection;<br>minimum value: <code>0</code><br>maximum value: <code>1</code> <br>default value: <code>null</code><p><strong>Note:</strong> <code>top_p</code> cannot be used together with '
      web_search:
        type: boolean
        required: false
        note: '<em>enable web search for current information</em><br>optional field<br>when enabled, the AI model can access and cite current web information;<br><strong>Note:</strong> refer to the <a href="https://docs.dataforseo.com/v3/ai_optimization/claude/llm_responses/models/">Models endpoint</a> for a list '
        example: true
      force_web_search:
        type: boolean
        required: false
        note: '<em>force AI agent to use web search</em><br>optional field<br>to enable this parameter, <code>web_search</code> must also be enabled;<br>when enabled, the AI model is forced to access and cite current web information;<br>default value: <code>false</code>;<br><strong>Note:</strong> even if the param'
      web_search_country_iso_code:
        type: string
        required: false
        note: '<em>ISO country code of the location used for searching the web</em><br>optional field<br>possible values: <code>''AR'',''AT'',''AU'',''BE'',''BR'',''CA'',''CH'',''CL'',''CN'',''DE'',''DK'',''ES'',''FI'',''FR'',''GB'',''HK'',''ID'',''IN'',''IT'',''JP'',''KR'',''MX'',''MY'',''NL'',''NO'',''NZ'',''PH'',''PL'',''PT'',''RU'',''SA'',''SE'',''TR'',''TW'',''US'',''ZA''</code>'
        example: FR
      web_search_city:
        type: string
        required: false
        note: <em>city name of the location used for searching the web</em><br>optional field<br>
      system_message:
        type: string
        required: false
        note: <em>instructions for the AI behaviour</em><br>optional field<br>defines the AI's role, tone, or specific behavior;<br>you can specify <strong>up to 500 characters</strong> in the <code>system_message</code> field
        example: communicate as if we are in a business meeting
      message_chain:
        type: array
        required: false
        note: 'conversation history optional field array of message objects representing previous conversation turns; each object must contain: role string with either user or ai role; message string with message content (max 500 characters); you can specify maximum of 10 message objects in the array; Note: for Pe'
        example:
        - role: user
          message: Hello, what’s up?
        - role: ai
          message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      use_reasoning:
        type: boolean
        required: false
        note: <em>enable reasoning for the AI model</em><br>optional field<br>when enabled, the model will perform reasoning before generating a response<br>refer to the <a href="https://docs.dataforseo.com/v3/ai_optimization/claude/llm_responses/models/" target="_blank">Models endpoint</a> for a list of models t
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - system_message: communicate as if we are in a business meeting
      message_chain:
      - role: user
        message: Hello, what’s up?
      - role: ai
        message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      max_output_tokens: 200
      model_name: claude-opus-4-0
      temperature: 0.3
      web_search: true
      web_search_country_iso_code: FR
      user_prompt: provide information on how relevant the amusement park business is in France now
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-claude-llm-responses-live.json
  observed_cost: 0
  capability: ai-search.claude.answer
- id: dataforseo.x.ai-optimization-gemini-llm-responses-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/gemini/llm_responses/live
  name: "Gemini answer to a prompt, live"
  summary: Live Gemini LLM Responses endpoint allows you to retrieve structured responses from a specific Gemini AI model, based on the input parameters.
  input:
    bodyType: json
    body:
      user_prompt:
        type: string
        required: false
        note: <em>prompt for the AI model</em><br><strong>required field</strong><br>the question or task you want to send to the AI model;<br>you can specify <strong>up to 500 characters</strong> in the <code>user_prompt</code> field
        example: provide information on how relevant the amusement park business is in France now
      model_name:
        type: string
        required: false
        note: <em>name of the AI model</em><br><strong>required field</strong><br><code>model_name</code >consists of the actual model name and version name;<br>if the basic model name is specified, its latest version will be set by default;<br>for example, if <code>gemini-1.5-pro</code> is specified, the <code>g
        example: gemini-2.5-flash
      max_output_tokens:
        type: integer
        required: false
        note: '<em>maximum number of tokens in the AI response</em><br>optional field<br>minimum value: <code>1</code><br>maximum value: <code>4096</code>;<br>default value: <code>2048</code>;<br><strong>Note:</strong> if <code>web_search</code> is set to <code>true</code> or the reasoning model is specified in th'
        example: 200
      temperature:
        type: number
        required: false
        note: '<em>randomness of the AI response</em><br>optional field<br>higher values make output more diverse <br>lower values make output more focused<br>minimum value: <code>0</code><br>maximum value: <code>2</code><br>default value: <code>1.3</code>'
        example: 0.3
      top_p:
        type: number
        required: false
        note: '<em>diversity of the AI response</em><br>optional field <br>controls diversity of the response by limiting token selection<br>minimum value: <code>0</code><br>maximum value: <code>1</code> <br>default value: <code>0.9</code>'
        example: 0.5
      web_search:
        type: boolean
        required: false
        note: '<em>enable web search for current information</em><br>optional field<br>when enabled, the AI model can access and cite current web information;<br><strong>Note:</strong> refer to the <a href="https://docs.dataforseo.com/v3/ai_optimization/gemini/llm_responses/models/">Models endpoint</a> for a list '
        example: true
      system_message:
        type: string
        required: false
        note: <em>instructions for the AI behavior</em><br>optional field<br>defines the AI's role, tone, or specific behavior <br>you can specify <strong>up to 500 characters</strong> in the <code>system_message</code> field
        example: communicate as if we are in a business meeting
      message_chain:
        type: array
        required: false
        note: 'conversation history optional field array of message objects representing previous conversation turns; each object must contain: role string with either user or ai role; message string with message content (max 500 characters); you can specify maximum of 10 message objects in the array; Note: for Pe'
        example:
        - role: user
          message: Hello, what’s up?
        - role: ai
          message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      use_reasoning:
        type: boolean
        required: false
        note: <em>enable reasoning for the AI model</em><br>optional field<br>when enabled, the model will perform reasoning before generating a response<br>refer to the <a href="https://docs.dataforseo.com/v3/ai_optimization/gemini/llm_responses/models/" target="_blank">Models endpoint</a> for a list of models t
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - system_message: communicate as if we are in a business meeting
      message_chain:
      - role: user
        message: Hello, what’s up?
      - role: ai
        message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      max_output_tokens: 200
      temperature: 0.3
      top_p: 0.5
      model_name: gemini-2.5-flash
      web_search: true
      user_prompt: provide information on how relevant the amusement park business is in France now
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-gemini-llm-responses-live.json
  observed_cost: 0.036256
  capability: ai-search.gemini.answer
- id: dataforseo.x.ai-optimization-gemini-llm-scraper-live-advanced
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/gemini/llm_scraper/live/advanced
  name: "Gemini search results for a keyword, live"
  summary: Live Gemini LLM Scraper endpoint provides structured results from Gemini.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: '<em>keyword</em><br><strong>required field</strong><br>you can specify <strong>up to 2000 characters</strong> in the <code>keyword</code> field<br>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<br>if you need to use the “%” character for your <code>keyword</code>, '
        example: albert einstein
      location_name:
        type: string
        required: false
        note: <em>full name of search engine location</em><br><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><br>y
      location_code:
        type: integer
        required: false
        note: <em>search engine location code</em><br><strong>required field if you don't specify</strong> <code>location_name</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><br>you can r
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
      language_name:
        type: string
        required: false
        note: <em>full name of search engine language</em><br>required field if you don't specify <code>language_code</code>;<br>if you use this field, you don't need to specify <code>language_code</code>;<br>you can receive the list of available languages of the search engine with their <code>language_name</code
      language_code:
        type: string
        required: false
        note: <em>search engine language code</em><br>required field if you don't specify <code>language_name</code>;<br>if you use this field, you don't need to specify <code>language_name</code>;<br>you can receive the list of available languages of the search engine with their <code>language_code</code>_by mak
        example: en
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-gemini-llm-scraper-live-advanced.json
  observed_cost: 0.004
  capability: ai-search.gemini.scrape
- id: dataforseo.x.business-data-google-extended-reviews-task-post
  tier: extended
  platform: google
  method: POST
  path: /business_data/google/extended_reviews/task_post
  name: "Business reviews from Google and other sites (async task)"
  summary: This endpoint provides results from the “Reviews” element of Google SERPs, including not only Google user reviews but also reviews from other reputable sources (e.g., TripAdvisor, Yelp, Trustpilot).
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <em>keyword</em><br><strong>required field if you don't specify <code>cid</code> or <code>place_id</code></strong><br>the keyword you specify should indicate the name of the local establishment;<br>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> filed;<br>all %## wi
      cid:
        type: string
        required: false
        note: <i>unique, google-defined id of the business entity</i><br><strong>required field if you don't specify <code>keyword</code> or <code>place_id</code></strong><br>example:<br><code>194604053573767737</code><br>learn more about the identifier in <a href="https://dataforseo.com/help-center/what-is-cid-p
        example: '17626775537598922320'
      place_id:
        type: string
        required: false
        note: <i>identifier of the business entity in Google Maps</i><br><strong>required field if you don't specify <code>keyword</code> or <code>cid</code></strong><br>example:<br><code>GhIJQWDl0CIeQUARxks3icF8U8A</code><br>learn more about the identifier in <a href="https://dataforseo.com/help-center/what-is-c
      priority:
        type: integer
        required: false
        note: '<em>task priority</em><br>optional field<br>can take the following values:<br>1 – normal execution priority (set by default)<br>2 – high execution priorityYou will be additionally charged for the tasks with high execution priority.<br>The cost can be calculated on the <a title="Pricing" href="https:'
      location_name:
        type: string
        required: false
        note: <em>full name of search engine location</em><br><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><br>y
        example: London,England,United Kingdom
      location_code:
        type: integer
        required: false
        note: <em>search engine location code</em><br><strong>required field if you don't specify</strong> <code>location_name</code>_or <code>location_coordinate</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><br>you can r
      location_coordinate:
        type: string
        required: false
        note: <em>GPS coordinates of a location</em><br><strong>required field if you don't specify</strong> <code>location_name</code>_or <code>location_code</code><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><br><code>location_coor
      language_name:
        type: string
        required: false
        note: <em>full name of search engine language</em><br><strong>required field if you don't specify</strong> <code>language_code</code><br><strong>if you use this field, you don't need to specify <code>language_code</code></strong><br>you can receive the list of available languages with <code>language_name<
        example: english
      language_code:
        type: string
        required: false
        note: <em>search engine language code</em><br><strong>required field if you don't specify</strong> <code>language_name</code><br><strong>if you use this field, you don't need to specify <code>language_name</code></strong><br>you can receive the list of available languages with their <code>language_code</c
      depth:
        type: integer
        required: false
        note: '<em>parsing depth</em><br>optional field<br>number of reviews in SERP<br>we strongly recommend setting the parsing depth in the multiples of twenty, because our systems processes twenty reviews in a row<br>default value: <code>20</code><br>maximum value: <code>1000</code><p><strong>Your account will'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
      postback_url:
        type: string
        required: false
        note: <em>URL for sending task results</em><br>optional field<br>once the task is completed, we will send a POST request with its results compressed in the <code>gzip</code> format to the <code>postback_url</code> you specified<br>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as u
      pingback_url:
        type: string
        required: false
        note: <em>notification URL of a completed task</em><br>optional field<br>when a task is completed we will notify you by GET request sent to the URL you have specified<br>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as urlencoded <code>$tag</code> variable. We will set the necessa
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_name: London,England,United Kingdom
      language_name: english
      cid: '17626775537598922320'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-google-extended-reviews-task-post.json
  observed_cost: 0.0015
- id: dataforseo.x.business-data-google-hotel-info-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /business_data/google/hotel_info/live/advanced
  name: "Google Hotels details by hotel id — rating, prices"
  summary: 'Google Hotel Info will provide you with structured data available for a specific hotel entity on the Google Hotels platform: such as service description, location details, rating, amenities, reviews, images, prices, and more.'
  input:
    bodyType: json
    body:
      hotel_identifier:
        type: string
        required: false
        note: <em>unique hotel identifier</em><br><strong>required field</strong><br>unique identifier of a hotel entity in Google search;<br>you can obtain the value by making a request to Advanced <a href="/v3/serp/google/organic/overview/" rel="noopener noreferrer" target="_blank">Google SERP API</a> (enclosed
        example: CgoI-KWyzenM_MV3EAE
      location_name:
        type: string
        required: false
        note: <em>full name of search engine location</em><br><strong>required field if you don't specify <code>location_code</code> or <code>location_coordinate</code></strong><br><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><br>y
        example: New York,New York,United States
      location_code:
        type: integer
        required: false
        note: <em>search engine location code</em><br><strong>required field if you don't specify <code>location_name</code>_or <code>location_coordinate</code></strong><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><br>you can r
      location_coordinate:
        type: string
        required: false
        note: <em>GPS coordinates of a location</em><br><strong>required field if you don't specify <code>location_name</code>_or <code>location_code</code></strong><br><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><br><code>location_coor
      language_name:
        type: string
        required: false
        note: <em>full name of search engine language</em><br><strong>required field if you don't specify <code>language_code</code></strong><br><strong>if you use this field, you don't need to specify <code>language_code</code></strong><br>you can receive the list of available languages with <code>language_name<
      language_code:
        type: string
        required: false
        note: <em>search engine language code</em><br><strong>required field if you don't specify <code>language_name</code></strong><br><strong>if you use this field, you don't need to specify <code>language_name</code></strong><br>you can receive the list of available languages with their <code>language_code</c
        example: en
      check_in:
        type: string
        required: false
        note: '<em>check-in date</em><br>optional field<br>if you don''t specify this field, tomorrow''s date will be used by default;<br>the value must not be earlier than today''s date<br>date format: <code>"yyyy-mm-dd"</code><br>example:<br><code>"2019-01-15"</code>'
      check_out:
        type: string
        required: false
        note: <em>check-out date</em><br>optional field<br>if you don't specify this field, our system will apply the date of two days from now by default;<br><strong>Note:</strong> the value cannot be less than or equal to <code>check_in</code>;<br>the range between <code>check_in</code> and <code>check_out</cod
      currency:
        type: string
        required: false
        note: <em>currency</em><br>optional field<br>example:<br><code>"USD"</code>
      adults:
        type: integer
        required: false
        note: <em>number of adults</em><br>optional field<br>if you don't specify this field, two adults will be used by default<br>example:<br><code>1</code>
      children:
        type: array
        required: false
        note: <em>number and age of children</em><br>optional field<br>if you don't specify this field, no children will be included in the search;<p>set the following value if you want to include one 14-years-old child:<br><code>[14]</code><br>set the following value if you want to include one 13-years-old child
      load_prices_by_dates:
        type: boolean
        required: false
        note: <em>load hotel stay prices by dates</em><br>optional field<br>if you specify this parameter with <code>true</code>, the response will include the <code>prices_by_dates</code> array with hotel stay prices divided by dates<br>if you use this parameter, you will be charged <strong>double the base price
      prices_start_date:
        type: string
        required: false
        note: '<em>start date to load prices by dates</em><br>optional field<br>to use this parameter, you must specify <code>load_prices_by_dates</code> with <code>true</code><br>if this parameter is not specified, the start date is set to <code>check_in</code> date<br>date format: <code>yyyy-mm-dd</code><br>exam'
      prices_end_date:
        type: string
        required: false
        note: '<em>end date to load prices by dates</em><br>optional field<br>to use this parameter, you must specify <code>load_prices_by_dates</code> with <code>true</code><br>if this parameter is not specified, you will get prices by date for the month <br>date format: <code>yyyy-mm-dd</code><br>example:<br><co'
      prices_date_range:
        type: string
        required: false
        note: '<em>predefined period for retrieving daily price data</em><br>optional field<br>to use this parameter, you must specify <code>load_prices_by_dates</code> with <code>true</code><br>if the <code>prices_start_date </code>is not specified, the start date is set to <code>check_in</code> date<br>possible '
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_name: New York,New York,United States
      hotel_identifier: CgoI-KWyzenM_MV3EAE
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-google-hotel-info-live-advanced.json
  observed_cost: 0.004
- id: dataforseo.x.business-data-google-hotel-searches-live
  tier: extended
  platform: google
  method: POST
  path: /business_data/google/hotel_searches/live
  name: "Google Hotels search results for a keyword, live"
  summary: Hotel Searches API provides results containing information about different hotels listed on Google Hotels.
  docs_url: https://docs.dataforseo.com/v3/business_data/google/hotel_searches/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword optional field the keyword you specify is used to search for the list of hotels; if you don’t use this field, we will return the list of hotels found in a specified location; you can specify up to 700 characters in the keyword filed all %## will be decoded (plus character ‘+’ will be decoded
        example: cheap hotel
      location_name:
        type: string
        required: false
        note: 'full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations with location_name by making a separate request to https:'
        example: New York,New York,United States
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations with location_code by making a separate request to the https://ap
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude” format the maximum number of decimal digits f
      search_this_area:
        type: boolean
        required: false
        note: 'show hotels from the displayed area optional field can take the values: true, false default value: true if set to false the search_this_area mode will be turned off Note: if the search_this_area mode is turned off, the location_name won’t be appended to the keyword during search learn more about thi'
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/business_data/google/l
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/business_data/google/lan
        example: en
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in Google Hotels default value: 18 organic results max value: 140 Note: your account will be billed per each 18 organic results regardless of paid listings in the response; thus, setting a depth above 18 may result in additional charges if Google Hotels'
      check_in:
        type: string
        required: false
        note: 'check-in date optional field if you don’t specify this field, tomorrow’s date will be used by default; date format: "yyyy-mm-dd" example: "2019-01-15" Note: the value cannot precede the today’s date'
        example: '2026-08-24'
      check_out:
        type: string
        required: false
        note: 'check-out date optional field if you don’t specify this field, our system will apply the date of two days from now by default; date format: "yyyy-mm-dd" example: "2019-01-15" Note: the value cannot be less than or equal to check_in; the range between check_in and check_out values cannot exceed 30 da'
        example: '2026-08-25'
      currency:
        type: string
        required: false
        note: 'currency optional field example: "USD"'
        example: USD
      adults:
        type: integer
        required: false
        note: 'number of adults optional field if you don’t specify this field, the default value of 2 will be applied; note that you can specify up to 6 persons including both adults and children example: 1'
        example: 2
      children:
        type: array
        required: false
        note: 'number and age of children optional field if you don’t specify this field, no children will be included in the search; age of child can be from 0 to 17; note that you can specify up to 6 persons including both adults and children set the following value if you want to include one 14-year-old child: '
        example:
        - '14'
      stars:
        type: array
        required: false
        note: 'hotel stars optional field set this field to [5] if you want to get the list of 5-star hotels only example: [3,4,5]'
      min_rating:
        type: number
        required: false
        note: 'minimum rating optional field you can use this field to specify guest rating higher than a certain value example: 2.5'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field you can use this field to sort the results possible types of sorting: relevance – sort by most relevant lowest_price – sort by the lowest price highest_rating – sort by highest rating most_reviewed – sort by most reviewed default value: relevance'
        example: highest_rating
      min_price:
        type: integer
        required: false
        note: 'minimum price per night optional field the currency of this value depends on the currency field example: 100'
      max_price:
        type: integer
        required: false
        note: 'maximum price per night optional field the currency of this value depends on the currency field example: 600'
      free_cancellation:
        type: boolean
        required: false
        note: 'hotels with a free cancellation optional field set this field to true if you want to get the list of hotels with free cancellation of reservations default value: false'
      is_vacation_rentals:
        type: boolean
        required: false
        note: 'search for vacation rentals optional field set this field to true if you want to get the list of vacation rentals instead of hotels default value: false'
      amenities:
        type: array
        required: false
        note: 'hotel amenities optional field you can use this field to specify different hotel amenities example: [ "free_parking", "pets_allowed" ] possible values: "air_conditioning", "all_inclusive_available", "bar", "free_breakfast", "fitness_center", "kid_friendly", "free_parking", "pets_allowed", "pool", "r'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: example
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_name: New York,New York,United States
      keyword: cheap hotel
      check_in: '2026-08-24'
      check_out: '2026-08-25'
      currency: USD
      adults: 2
      children:
      - '14'
      sort_by: highest_rating
      tag: example
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-google-hotel-searches-live.json
  observed_cost: 0.004
- id: dataforseo.x.business-data-google-locations-country
  kind: utility
  tier: extended
  platform: google
  method: GET
  path: /business_data/google/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Google Business location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/business_data/google/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.business-data-google-my-business-info-live
  tier: extended
  platform: google
  method: POST
  path: /business_data/google/my_business_info/live
  name: "Google Business Profile details by business name, live"
  summary: Business Data API provides results containing information about specific business entity from Google.
  docs_url: https://docs.dataforseo.com/v3/business_data/google/my_business_info/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field the keyword you specify should indicate the name of the local establishment you can specify up to 700 characters in the keyword filed all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please
        example: RustyBrick, Inc.
      location_name:
        type: string
        required: false
        note: 'full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations with location_name by making a separate request to https:'
        example: New York,New York,United States
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations with location_code by making a separate request to the https://ap
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/business_data/google/l
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/business_data/google/lan
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_name: New York,New York,United States
      keyword: RustyBrick, Inc.
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-google-my-business-info-live.json
  observed_cost: 0.0054
- id: dataforseo.x.business-data-google-my-business-updates-task-post
  tier: extended
  platform: google
  method: POST
  path: /business_data/google/my_business_updates/task_post
  name: "Google Business Profile posts for a business (async task)"
  summary: This endpoints provides the latest updates of a specific business entity from Google SERP.
  docs_url: https://docs.dataforseo.com/v3/business_data/google/my_business_updates/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field the keyword you specify should indicate the name of the local establishment you can specify up to 700 characters in the keyword filed all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please
        example: RustyBrick, Inc.
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations with location_name by making a separate request to the ht
        example: New York,New York,United States
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations with location_code by making a separate request to the https://ap
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to the https://api.dataforseo.com/v3/business_data/goog
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/business_data/google
        example: en
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of updates in SERP we strongly recommend setting the parsing depth in the multiples of ten, because our systems processes ten updates in a row please note that Google returns 4490 updates maximum default value: 10'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_name: New York,New York,United States
      keyword: RustyBrick, Inc.
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-google-my-business-updates-task-post.json
  observed_cost: 0.00225
- id: dataforseo.x.business-data-google-questions-and-answers-live
  tier: extended
  platform: google
  method: POST
  path: /business_data/google/questions_and_answers/live
  name: "Questions and answers on a Google Business listing, live"
  summary: This endpoint will provide you with a detailed overview of questions and answers associated with a specific business entity listed on Google My Business.
  docs_url: https://docs.dataforseo.com/v3/business_data/google/questions_and_answers/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field the keyword you specify should indicate the name of the local establishment you can specify up to 700 characters in the keyword filed all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please
        example: The Last Bookstore
      location_name:
        type: string
        required: false
        note: 'full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations with location_name by making a separate request to https:'
        example: Los Angeles,California,United States
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations with location_code by making a separate request to the https://ap
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/business_data/google/l
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/business_data/google/lan
        example: en
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 20 max value: 100 Your account will be billed per each SERP containing up to 20 results; Setting depth above 20 may result in additional charges if the search engine returns more than 20 results; If the specified depth is higher t'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_name: Los Angeles,California,United States
      keyword: The Last Bookstore
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-google-questions-and-answers-live.json
  observed_cost: 0.0054
- id: dataforseo.x.business-data-google-reviews-task-post
  tier: extended
  platform: google
  method: POST
  path: /business_data/google/reviews/task_post
  name: "Google reviews for a business (async task)"
  summary: This endpoint provides results from the “Reviews” element of Google SERPs.
  docs_url: https://docs.dataforseo.com/v3/business_data/google/reviews/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field if you don’t specify cid or place_id the keyword you specify should indicate the name of the local establishment; you can specify up to 700 characters in the keyword filed; all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the
        example: hedonism wines
      cid:
        type: string
        required: false
        note: 'unique, google-defined id of the business entity required field if you don’t specify keyword or place_id example: 194604053573767737 learn more about the identifier in this help center article'
      place_id:
        type: string
        required: false
        note: 'identifier of the business entity in Google Maps required field if you don’t specify keyword or cid example: GhIJQWDl0CIeQUARxks3icF8U8A learn more about the identifier in this help center article'
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations with location_name by making a separate request to the ht
        example: London,England,United Kingdom
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations with location_code by making a separate request to the https://ap
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to the https://api.dataforseo.com/v3/business_data/goog
        example: English
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/business_data/google
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of reviews in SERP we strongly recommend setting the parsing depth in the multiples of ten, because our systems processes ten reviews in a row default value: 10 maximum value: 4490 Your account will be billed per each SERP containing up to 10 results; Setting dept'
        example: 50
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field you can use this field to sort the results possible types of sorting: newest – sort by newest first highest_rating – sort by highest rating lowest_rating – sort by lowest rating relevant – sort by relevance default value: relevant'
        example: highest_rating
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_name: London,England,United Kingdom
      language_name: English
      keyword: hedonism wines
      depth: 50
      sort_by: highest_rating
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-google-reviews-task-post.json
  observed_cost: 0.00375
- id: dataforseo.x.dataforseo-labs-google-app-competitors-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/app_competitors/live
  name: "Google Play apps competing with an app, by app id"
  summary: This endpoint will provide you with a list of mobile applications that intersect with the target app for its ranking keywords on Google Play.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/app_competitors/live/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the app required field ID of the mobile application on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL https://play.google.com/store/apps/details?id=org.telegram.messenger the id is org.telegram.messenger'
        example: org.telegram.messenger
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in example: ["intersections",">",500] [["competitor_metric'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results; possible sorting types: asc – results will be sorted in the ascending order; desc – results will be sorted in the descending order; you should use a comma to specify a sorting type; example:'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned apps optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned apps optional field default value: 0 if you specify the 10 value, the first ten apps in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: org.telegram.messenger
      language_name: English
      location_code: 2840
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-app-competitors-live.json
  observed_cost: 0.0132
- id: dataforseo.x.dataforseo-labs-google-app-intersection-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/app_intersection/live
  name: "Keywords several Google Play apps rank for together"
  summary: This endpoint will provide you with a list of keywords for which the mobile applications specified in the app_ids object rank within the same Google Play SERP.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/app_intersection/live/
  input:
    bodyType: json
    body:
      app_ids:
        type: object
        required: false
        note: 'ids of the target apps required field IDs of the target mobile applications on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL https://play.google.com/store/apps/details?id=org.telegram.messenger the id is org.telegram.messenger;; the ids should be'
        example:
          '1': org.telegram.messenger
          '2': com.zhiliaoapp.musically
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in example: ["keyword_data.keyword_info.search_volume",">"'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results; possible sorting types: asc – results will be sorted in the ascending order; desc – results will be sorted in the descending order; you should use a comma to specify a sorting type; example:'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_ids:
        '1': org.telegram.messenger
        '2': com.zhiliaoapp.musically
      language_name: English
      location_code: 2840
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-app-intersection-live.json
  observed_cost: 0.0132
- id: dataforseo.x.dataforseo-labs-google-available-history
  kind: utility
  tier: extended
  platform: google
  method: GET
  path: /dataforseo_labs/google/available_history
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Available dates for domain metrics history"
  summary: By calling this endpoint, you will find obtain a list of dates available for setting in the first_date and second_date fields of the Domain Metrics by Categories endpoint.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/available_history/live/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.dataforseo-labs-google-bulk-app-metrics-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/bulk_app_metrics/live
  name: "Ranking metrics for up to 1000 Google Play apps"
  summary: This endpoint will provide you with ranking metrics for up to 1000 Google Play applications.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/bulk_app_metrics/live/
  input:
    bodyType: json
    body:
      app_ids:
        type: array
        required: false
        note: 'ids of the app required field IDs of the mobile applications on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL https://play.google.com/store/apps/details?id=org.telegram.messenger the id is org.telegram.messenger; the maximum number of IDs you can'
        example:
        - org.telegram.messenger
        - com.zhiliaoapp.musically
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_ids:
      - org.telegram.messenger
      - com.zhiliaoapp.musically
      language_name: English
      location_code: 2840
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-bulk-app-metrics-live.json
  observed_cost: 0.01224
- id: dataforseo.x.dataforseo-labs-google-bulk-keyword-difficulty-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/bulk_keyword_difficulty/live
  name: "Keyword difficulty scores for up to 1000 keywords"
  summary: This endpoint will provide you with the Keyword Difficulty metric for a maximum of 1,000 keywords in one API request.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/bulk_keyword_difficulty/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000 the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article'
        example:
        - dentist new york
        - pizza brooklyn
        - car dealer los angeles
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if don’t specify location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: United Kingdom'
      location_code:
        type: integer
        required: false
        note: 'location code required field if don’t specify location_name you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: 2840'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English'
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en'
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_code: en
      keywords:
      - dentist new york
      - pizza brooklyn
      - car dealer los angeles
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-bulk-keyword-difficulty-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-bulk-traffic-estimation-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/bulk_traffic_estimation/live
  name: "Monthly traffic estimates for up to 1000 domains"
  summary: This endpoint will provide you with estimated monthly traffic volumes for up to 1,000 domains, subdomains, or webpages.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/bulk_traffic_estimation/live/
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: target domains, subdomains, and webpages required field you can specify domains, subdomains, and webpages in this field; domains and subdomains should be specified without https:// and www.; pages should be specified with absolute URL, including https:// and www.; you can set up to 1000 domains, sub
        example:
        - dataforseo.com
        - cnn.com
        - forbes.com
      location_name:
        type: string
        required: false
        note: full name of the location if you use this field, you don’t have to specify location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the res
      location_code:
        type: integer
        required: false
        note: location code if you use this field, you don’t have to specify location_name you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the results for all
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of the language if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the res
      language_code:
        type: string
        required: false
        note: language code if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the results for all
        example: en
      item_types:
        type: array
        required: false
        note: 'display results by item type optional field indicates the type of search results included in the response Note: if the item_types array contains item types that are different from organic, the results will be ordered by the first item type in the array possible values: ["organic", "paid", "featured_'
        example:
        - organic
        - paid
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true, only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - dataforseo.com
      - cnn.com
      - forbes.com
      location_code: 2840
      language_code: en
      item_types:
      - organic
      - paid
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-bulk-traffic-estimation-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-categories-for-domain-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/categories_for_domain/live
  name: "Google product categories a domain ranks in"
  summary: This endpoint will provide you with Google product or service categories that include keywords the domain ranks for in search.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/categories_for_domain/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain or subdomain required field the domain or subdomain name of the target website the domain or subdomain should be specified without https:// and www.
        example: dataforseo.com
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: United States
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: en
      include_subcategories:
        type: boolean
        required: false
        note: 'indicates if the subcategories will be included in the search optional field if set to false, the subcategories will be ignored default value: false learn more about the parameter in this help center article'
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_etv, clickstream_gender_distribution, and clickstream_age_distribution fields with clickstream data in the response default value: false with this paramet'
      historical_serp_mode:
        type: string
        required: false
        note: 'data collection mode optional field you can use this field to filter the results; possible types of filtering: live — return metrics for SERPs in which the specified target currently has ranking results; lost — return metrics for SERPs in which the specified target had previously had ranking results'
      item_types:
        type: array
        required: false
        note: 'display results by item type optional field indicates the type of search results included in the response Note: if the item_types array contains item types that are different from the organic object, the results will be ordered by the first item type in the array; you will not be able to sort and fi'
        example:
        - paid
        - organic
        - featured_snippet
        - local_pack
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in example: ["metrics.organic.pos_1,">",'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to specify a sorting type example: ["m'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned categories optional field default value: 100 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned categories optional field default value: 0 if you specify the 10 value, the first ten categories in the results array will be omitted and the data will be provided for the successive categories'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
      language_code: en
      location_name: United States
      item_types:
      - paid
      - organic
      - featured_snippet
      - local_pack
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-categories-for-domain-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-categories-for-keywords-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/categories_for_keywords/live
  name: "Google product categories for keywords"
  summary: This endpoint will provide you with Google product and service categories related for each specified keyword.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/categories_for_keywords/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000 the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article'
        example:
        - dentist new york
        - pizza brooklyn
        - car dealer los angeles
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/google/categories_for_keywords/languages example: English'
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/google/categories_for_keywords/languages example: en'
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      keywords:
      - dentist new york
      - pizza brooklyn
      - car dealer los angeles
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-categories-for-keywords-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-competitors-domain-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/competitors_domain/live
  name: "Competitor domains of a domain — rank and traffic"
  summary: This endpoint will provide you with a full overview of ranking and traffic data of the competitor domains from organic and paid search.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/competitors_domain/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain required field the domain name of the target website the domain should be specified without https:// and www. you can specify page URL, but the results will be specific to the domain in the specified URL
        example: newmouth.com
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      item_types:
        type: array
        required: false
        note: 'display results by item type optional field indicates the type of search results included in the response Note: if the item_types array contains item types that are different from organic, the results will be ordered by the first item type in the array; you will not be able to sort and filter result'
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_etv, clickstream_gender_distribution, and clickstream_age_distribution fields with clickstream data in the response default value: false with this paramet'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in example: ["metrics.organic.count",">"'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to specify a sorting type example: ["m'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned domains optional field default value: 100 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned domains optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      max_rank_group:
        type: integer
        required: false
        note: 'maximum rank up to which competitors will be considered optional field default value: 100 if you specify 10 here, we will extract competitors from the top 10 Google search results only'
      exclude_top_domains:
        type: boolean
        required: false
        note: 'indicates whether to exclude world’s largest websites optional field default value: false set to true if you want to get highly-relevant competitors excluding the websites listed below: wikipedia.org pinterest.com amazon.com google.com facebook.com wordpress.com medium.com quora.com reddit.com youtu'
      exclude_domains:
        type: array
        required: false
        note: 'exclude domains from the results optional field use this parameter to exclude specific domains from the results Note: you can specify up to 1000 domains in this array example: "exclude_domains": [ "reddit.com", "youtube.com" ]'
      intersecting_domains:
        type: array
        required: false
        note: additional domains for improving results accuracy optional field to improve the accuracy of the result, you can specify domains that are known to intersect with the target in SERPs; if you use this array, metrics in the result will be based on SERPs where both target website and intersecting_domains
        example:
        - dentaly.org
        - health.com
        - trysnow.com
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true, only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: newmouth.com
      intersecting_domains:
      - dentaly.org
      - health.com
      - trysnow.com
      language_name: English
      location_code: 2840
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-competitors-domain-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-domain-intersection-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/domain_intersection/live
  name: "Keywords two domains both rank for in Google"
  summary: This endpoint will provide you with the keywords for which both specified domains rank within the same SERP.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/domain_intersection/live/
  input:
    bodyType: json
    body:
      target1:
        type: string
        required: false
        note: domain required field the domain name of the first target website the domain should be specified without https:// and www.
        example: mom.com
      target2:
        type: string
        required: false
        note: domain required field the domain name of the second target website the domain should be specified without https:// and www.
        example: quora.com
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: en
      intersections:
        type: boolean
        required: false
        note: 'domain intersections in SERP optional field if you set intersections to true, you will get the keywords for which both target domains specified as target1 and target2 have results within the same SERP; the corresponding SERP elements for both domains will be provided in the results array Note: this '
      item_types:
        type: array
        required: false
        note: 'search results type indicates type of search results included in the response optional field possible values: ["organic", "paid", "featured_snippet", "local_pack"] default value: ["organic", "paid"]'
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
        example: true
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, clickstream_etv, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the items array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike, '
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter example:'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target1: mom.com
      target2: quora.com
      language_code: en
      location_code: 2840
      include_serp_info: true
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-domain-intersection-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-domain-metrics-by-categories-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/domain_metrics_by_categories/live
  name: "Domain metric trends for product categories"
  summary: This endpoint will provide you with dynamics of change in metrics of domains relevant to the specified product and service categories.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/domain_metrics_by_categories/live/
  input:
    bodyType: json
    body:
      category_codes:
        type: array
        required: false
        note: 'product and service categories required field The maximum number of categories you can specify: 5 you can download the full list of possible categories'
        example:
        - '13418'
        - '11494'
      first_date:
        type: string
        required: false
        note: 'first date of comparison period required field first date for which domain metrics will be provided; date format: "yyyy-mm-dd"; example: "2021-06-01"; the list available dates is available through the available history endpoint; Note: first_date cannot be greater than today’s date; Also note: the da'
        example: '2026-04-24'
      second_date:
        type: string
        required: false
        note: 'second date of comparison period required field second date for which domain metrics will be provided; date format: "yyyy-mm-dd"; example: "2021-10-01"; the list available dates is available through the available history endpoint; Note: second_date cannot be greater than today’s date; Also note: the'
        example: '2026-06-24'
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code; you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/lo'
      location_code:
        type: integer
        required: false
        note: 'unique location identifier required field if you don’t specify location_name Note: it is required to specify either location_name or location_code; you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code; you can receive the list of available languages with their language_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/lo'
      language_code:
        type: string
        required: false
        note: 'unique language identifier required field if you don’t specify language_name Note: it is required to specify either language_name or language_code; you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/l'
        example: en
      item_types:
        type: array
        required: false
        note: 'display results by item type optional field indicates the type of search results included in the response; Note: if the item_types array contains item types that are different from the organic object, the results will be ordered by the first item type in the array; you will not be able to sort and f'
      top_categories_count:
        type: integer
        required: false
        note: number of additional domain categories optional field by using this parameter, you can receive domains relevant to additional categories that are not specified in category_codes above; to learn more about the parameter, please refer to this help center article; by default, top_categories_count is eq
      include_subdomains:
        type: boolean
        required: false
        note: 'return subdomains in the API response optional field if false, the API response will contain main_domain only; if true, the API will return main_domain plus its subdomains (if available); default value: true'
      etv_min:
        type: integer
        required: false
        note: minimum current organic ETV of the domain optional field if specified, the API will return only domains with organic_etv greater than the specified value
      etv_max:
        type: integer
        required: false
        note: maximum current organic ETV of the domain optional field if specified, the API will return only domains with organic_etv lesser than the specified value
      correlate:
        type: boolean
        required: false
        note: 'correlate data with previously obtained datasets optional field default value: true; if you use this parameter, our system will correlate data you obtain now with previously obtained datasets; this parameter is intended to mitigate any inconsistencies that may result from changes to our database; No'
      limit:
        type: integer
        required: false
        note: 'the maximum number of domains in the results array optional field default value: 100; maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned domains optional field default value: 0; if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive domains'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum); you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike,'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results; default rule: ["organic_etv,desc"]; possible sorting types: asc – results will be sorted in ascending order desc – results will be sorted in descending order; you should use a comma to set u'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255; you can use this parameter to identify the task and match it with the result; you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_code: en
      category_codes:
      - '13418'
      - '11494'
      first_date: '2026-04-24'
      second_date: '2026-06-24'
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-domain-metrics-by-categories-live.json
  observed_cost: 0.1236
- id: dataforseo.x.dataforseo-labs-google-domain-rank-overview-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/domain_rank_overview/live
  name: "Google rank and traffic overview for a domain"
  summary: This endpoint will provide you with ranking and traffic data from organic and paid search for the specified domain.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/domain_rank_overview/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain required field the domain name of the target website the domain should be specified without https:// and www.
        example: dataforseo.com
      location_name:
        type: string
        required: false
        note: full name of the location optional field if you use this field, you don’t need to specify location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field
      location_code:
        type: integer
        required: false
        note: 'location code optional field if you use this field, you don’t need to specify location_name you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the '
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of the language optional field if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field
        example: English
      language_code:
        type: string
        required: false
        note: 'language code optional field if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the '
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true, all highly similar keywords will be excluded from the ranking and traffic calculations, the results will be based on data for main keywords from groups of synonyms default value: false'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned results for domain optional field default value: 100 maximum value: 1000'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned items optional field default value: 0 if you specify the 10 value, the first ten items in the results array will be omitted and the data will be provided for the successive items'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
      language_name: English
      location_code: 2840
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-domain-rank-overview-live.json
  observed_cost: 0.01212
- id: dataforseo.x.dataforseo-labs-google-historical-bulk-traffic-estimation-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/historical_bulk_traffic_estimation/live
  name: "Historical monthly traffic for up to 1000 domains"
  summary: This endpoint will provide you with historical monthly traffic volumes for up to 1,000 domains collected within the specified time range through October 2020.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/historical_bulk_traffic_estimation/live/
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: target domains and subdomains required field you can specify domains and subdomains in this field; domains and subdomains should be specified without https:// and www.; you can set up to 1000 domains or subdomains
        example:
        - dataforseo.com
        - cnn.com
        - forbes.com
      location_name:
        type: string
        required: false
        note: full name of the location if you use this field, you don’t have to specify location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the res
      location_code:
        type: integer
        required: false
        note: location code if you use this field, you don’t have to specify location_name you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the results for all
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of the language if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the res
      language_code:
        type: string
        required: false
        note: language code if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the results for all
        example: en
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the data will be provided for the previous 12 months minimal possible value: 2020-10-01 date format: "yyyy-mm-dd"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default; date format: "yyyy-mm-dd" example: "2021-04-01"'
        example: '2026-06-24'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      item_types:
        type: array
        required: false
        note: 'display results by item type optional field indicates the type of search results included in the response; Note: if the item_types array contains item types that are different from organic, the results will be ordered by the first item type in the array; possible values: ["organic", "paid", "feature'
        example:
        - organic
        - paid
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - dataforseo.com
      - cnn.com
      - forbes.com
      location_code: 2840
      language_code: en
      date_from: '2026-04-24'
      date_to: '2026-06-24'
      item_types:
      - organic
      - paid
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-historical-bulk-traffic-estimation-live.json
  observed_cost: 0.1236
- id: dataforseo.x.dataforseo-labs-google-historical-keyword-data-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/historical_keyword_data/live
  name: "Google historical search volume and CPC for keywords"
  summary: This endpoint provides Google historical keyword data for specified keywords, including search volume, cost-per-click, competition values for paid search, monthly searches, and search volume trends.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/historical_keyword_data/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field The maximum number of keywords you can specify: 700 The maximum number of characters for each keyword: 80 The maximum number of words for each keyword phrase: 10 the specified keywords will be converted to lowercase format, data will be provided in a separate array note that '
        example:
        - iphone
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keywords:
      - iphone
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-historical-keyword-data-live.json
  observed_cost: 0.01212
- id: dataforseo.x.dataforseo-labs-google-historical-rank-overview-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/historical_rank_overview/live
  name: "Historical Google rank and traffic for a domain"
  summary: This endpoint will provide you with historical data on rankings and traffic of the specified domain, such as domain ranking distribution in SERPs and estimated monthly traffic volume for both organic and paid results.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/historical_rank_overview/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain required field the domain name of the target website the domain should be specified without https:// and www.
        example: dataforseo.com
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: en
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the data will be provided for the previous 6 months minimal possible value: 2020-10-01 date format: "yyyy-mm-dd"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2021-04-01"'
        example: '2026-06-24'
      correlate:
        type: boolean
        required: false
        note: 'correlate data with previously obtained datasets optional field default value: true if you use this parameter, our system will correlate data you obtain now with previously obtained datasets this parameter is intended to mitigate any inconsistencies that may result from changes to our database we re'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true, only data based on core keywords will be returned, data for all highly similar keywords will be excluded; default value: false'
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_etv, clickstream_gender_distribution, and clickstream_age_distribution fields with clickstream data in the response; default value: false; Note: historica'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
      location_code: 2840
      language_code: en
      date_from: '2026-04-24'
      date_to: '2026-06-24'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-historical-rank-overview-live.json
  observed_cost: 0.1236
- id: dataforseo.x.dataforseo-labs-google-historical-serps-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/historical_serps/live
  name: "Past Google search results for a keyword, by date range"
  summary: This endpoint will provide you with Google SERPs collected within the specified time frame.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/historical_serps/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field; all %## will be decoded (plus character ‘+’ will be decoded to a space character); if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keywor
        example: albert einstein
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the API will return all SERPs collected for 365 days starting from the current datetime value; minimal possible value: 365 days from the current datetime value; date format: "yyyy-mm-dd"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default; date format: "yyyy-mm-dd"; example: "2021-09-01"'
        example: '2026-06-24'
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'unique location identifier required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/lo'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name parameters by making a separate request to the https://api.dataforseo.com/v3/data'
      language_code:
        type: string
        required: false
        note: 'unique language identifier required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code parameters by making a separate request to the https://api.dataforseo.com/v3/dat'
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: albert einstein
      location_code: 2840
      language_code: en
      date_from: '2026-04-24'
      date_to: '2026-06-24'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-historical-serps-live.json
  observed_cost: 0.00012
- id: dataforseo.x.dataforseo-labs-google-keyword-ideas-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/keyword_ideas/live
  name: "Google keyword ideas for seed keywords, by category"
  summary: The Keyword Ideas endpoint provides search terms that are relevant to the product or service categories of the specified keywords.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/keyword_ideas/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field UTF-8 encoding The maximum number of keywords you can specify: 200. The keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article'
        example:
        - phone
        - watch
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'unique location identifier required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_lab'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language optional field if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English '
      language_code:
        type: string
        required: false
        note: 'language code optional field if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en Note: if omitted,'
        example: en
      closely_variants:
        type: boolean
        required: false
        note: 'search mode optional field if set to true the results will be based on the phrase-match search algorithm if set to false the results will be based on the broad-match search algorithm default value: false'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
        example: true
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false with this parame'
      limit:
        type: integer
        required: false
        note: 'the maximum number of keywords in the results array optional field default value: 700 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords Note: we recommend using this parameter only when retrieving up to 10,00'
      offset_token:
        type: string
        required: false
        note: 'offset token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 10,000 results in a single request; by specifying the unique offset_token value from the response array, you will get '
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike, '
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter default '
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keywords:
      - phone
      - watch
      location_code: 2840
      language_code: en
      include_serp_info: true
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-keyword-ideas-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-keyword-overview-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/keyword_overview/live
  name: "Google keyword overview for a list of keywords"
  summary: This endpoint provides Google keyword data for specified keywords.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/keyword_overview/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field The maximum number of keywords you can specify: 700 The maximum number of characters for each keyword: 80 The maximum number of words for each keyword phrase: 10 the specified keywords will be converted to lowercase format, data will be provided in a separate array note that '
        example:
        - iphone
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: en
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
        example: true
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false with this parame'
        example: true
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      include_clickstream_data: true
      include_serp_info: true
      keywords:
      - iphone
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-keyword-overview-live.json
  observed_cost: 0.02412
- id: dataforseo.x.dataforseo-labs-google-keyword-suggestions-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/keyword_suggestions/live
  name: "Google keyword suggestions containing a seed keyword"
  summary: The Keyword Suggestions endpoint provides search queries that include the specified seed keyword.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/keyword_suggestions/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field UTF-8 encoding the keywords will be converted to lowercase format; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article
        example: phone
      location_name:
        type: string
        required: false
        note: full name of the location optional field if you use this field, you don’t need to specify location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field
      location_code:
        type: integer
        required: false
        note: 'location code optional field if you use this field, you don’t need to specify location_name you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the '
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language optional field if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English '
      language_code:
        type: string
        required: false
        note: 'language code optional field if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en Note: if omitted,'
        example: en
      include_seed_keyword:
        type: boolean
        required: false
        note: 'include data for the seed keyword optional field if set to true, data for the seed keyword specified in the keyword field will be provided in the seed_keyword_data array of the response default value: false'
        example: true
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
        example: true
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false with this parame'
      exact_match:
        type: boolean
        required: false
        note: 'search for the exact phrase optional field if set to true, the returned keywords will include the exact keyword phrase you specified, with potentially other words before or after that phrase default value: false'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike, '
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order a comma is used as a separator example: ["keyword_info.compet'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 1
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords Note: we recommend using this parameter only when retrieving up to 10,00'
      offset_token:
        type: string
        required: false
        note: 'offset token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 10,000 results in a single request; by specifying the unique offset_token value from the response array, you will get '
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: phone
      location_code: 2840
      language_code: en
      include_serp_info: true
      include_seed_keyword: true
      limit: 1
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-keyword-suggestions-live.json
  observed_cost: 0.01212
- id: dataforseo.x.dataforseo-labs-google-keywords-for-app-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/keywords_for_app/live
  name: "Keywords a Google Play app ranks for, by app id"
  summary: This endpoint will provide you with a list of keywords for which the target app ranks on Google Play.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/keywords_for_app/live/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the apps required field ID of the mobile application on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL https://play.google.com/store/apps/details?id=org.telegram.messenger the id is org.telegram.messenger'
        example: org.telegram.messenger
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/loc'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_l'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in example: ["keyword_data.keyword_info.search_volume",">"'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results; possible sorting types: asc – results will be sorted in the ascending order; desc – results will be sorted in the descending order; you should use a comma to specify a sorting type; example:'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: org.telegram.messenger
      language_name: English
      location_code: 2840
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-keywords-for-app-live.json
  observed_cost: 0.0132
- id: dataforseo.x.dataforseo-labs-google-keywords-for-categories-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/keywords_for_categories/live
  name: "Keywords for Google product categories"
  summary: This endpoint will provide you with a list of keywords relevant to the specified product categories.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/keywords_for_categories/live/
  input:
    bodyType: json
    body:
      category_codes:
        type: array
        required: false
        note: 'product and service categories required field The maximum number of categories you can specify: 20 you can download the full list of possible categories'
        example:
        - '12191'
        - '12193'
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'unique location identifier required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_lab'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'unique language identifier required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_lab'
      category_intersection:
        type: boolean
        required: false
        note: 'category intersections optional field if set to true, you will get keywords featured in all specified categories; if set to false, you will keywords that are specified in any of the specified categories; default value: true'
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
        example: true
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false with this parame'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      limit:
        type: integer
        required: false
        note: 'the maximum number of keywords in the results array optional field default value: 100 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords Note: we recommend using this parameter only when retrieving up to 10,00'
      offset_token:
        type: string
        required: false
        note: 'offset token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 10,000 results in a single request; by specifying the unique offset_token value from the response array, you will get '
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike, '
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["ke'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - category_codes:
      - '12191'
      - '12193'
      language_name: English
      location_code: 2840
      include_serp_info: true
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-keywords-for-categories-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-keywords-for-site-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/keywords_for_site/live
  name: "Keywords relevant to a domain in Google"
  summary: The Keywords For Site endpoint will provide you with a list of keywords relevant to the target domain.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/keywords_for_site/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: target domain required field the domain name of the target website the domain should be specified without https://
        example: apple.com
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'unique location identifier required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_lab'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language optional field if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: English '
      language_code:
        type: string
        required: false
        note: 'language code optional field if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages example: en Note: if omitted,'
        example: en
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
        example: true
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
        example: true
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false with this parame'
      limit:
        type: integer
        required: false
        note: 'the maximum number of keywords in the results array optional field default value: 100 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords Note: we recommend using this parameter only when retrieving up to 10,00'
      offset_token:
        type: string
        required: false
        note: 'offset token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 10,000 results in a single request; by specifying the unique offset_token value from the response array, you will get '
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike, '
        example:
        - serp_info.se_results_count
        - '>'
        - 0
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter default '
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: apple.com
      language_code: en
      location_code: 2840
      include_serp_info: true
      include_subdomains: true
      filters:
      - serp_info.se_results_count
      - '>'
      - 0
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-keywords-for-site-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-page-intersection-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/page_intersection/live
  name: "Keywords several pages rank for together, by URL"
  summary: This endpoint will provide you with the keywords for which specified pages rank within the same SERP.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/page_intersection/live/
  input:
    bodyType: json
    body:
      pages:
        type: object
        required: false
        note: 'target URLs of pages required field you can set up to 20 pages in this object the pages should be specified with absolute URLs (including http:// or https://) example: "pages": { "1":"https://www.apple.com/mac/*", "2":"https://dataforseo.com/*", "3":"https://support.microsoft.com/" }if you specify a'
        example:
          '1': https://forbes.com
          '2': https://cnn.com/*
      exclude_pages:
        type: array
        required: false
        note: URLs of pages you want to exclude optional field you can set up to 10 pages in this array if you use this array, results will contain the keywords for which URLs from the pages object rank, but URLs from exclude_pages array do not; note that if you specify this field, the results will be based on th
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      item_types:
        type: array
        required: false
        note: 'search results type indicates type of search results included in the response optional field possible values: ["organic", "paid", "featured_snippet", "local_pack"] default value: ["organic", "paid"]'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the items array of returned keywords optional field default value: 0 if you specify 10 here, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      intersection_mode:
        type: string
        required: false
        note: 'indicates whether to intersect keywords optional field use this field to intersect or merge results for the specified URLs possible values: union, intersect union – results are based on all keywords any URL from pages rank for; intersect – results are based on the keywords all URLs from pages rank f'
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
        example: true
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, clickstream_etv, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, ilike, not_ilike, like, not_like, ma'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter example:'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - pages:
        '1': https://forbes.com
        '2': https://cnn.com/*
      language_name: English
      location_code: 2840
      include_serp_info: true
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-page-intersection-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-relevant-pages-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/relevant_pages/live
  name: "Top ranking pages of a domain in Google"
  summary: GoogleRelevantPagesLive
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/relevant_pages/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain required field the domain name of the target website the domain should be specified without https:// and www.
        example: amazon.com
      location_name:
        type: string
        required: false
        note: full name of the location optional field if you use this field, you don’t need to specify location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field
      location_code:
        type: integer
        required: false
        note: 'location code optional field if you use this field, you don’t need to specify location_name you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the '
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of the language optional field if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field
        example: English
      language_code:
        type: string
        required: false
        note: 'language code optional field if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the '
      item_types:
        type: array
        required: false
        note: 'display results by item type optional field indicates the type of search results included in the response Note: if the item_types array contains item types that are different from organic, the results will be ordered by the first item type in the array; you will not be able to sort and filter result'
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_etv, clickstream_gender_distribution, and clickstream_age_distribution fields with clickstream data in the response default value: false with this paramet'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned pages optional field default value: 100 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned pages optional field default value: 0 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages'
      historical_serp_mode:
        type: string
        required: false
        note: 'data collection mode optional field you can use this field to filter the results; possible types of filtering: live — return metrics for SERPs in which the specified target currently has ranking results; lost — return metrics for SERPs in which the specified target had previously had ranking results'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true, only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in example: ["metrics.paid.count",">",0]'
        example:
        - - metrics.organic.pos_1
          - <>
          - 0
        - or
        - - metrics.organic.pos_2_3
          - <>
          - 0
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to specify a sorting type example: ["m'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: amazon.com
      language_name: English
      location_code: 2840
      filters:
      - - metrics.organic.pos_1
        - <>
        - 0
      - or
      - - metrics.organic.pos_2_3
        - <>
        - 0
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-relevant-pages-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-search-intent-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/search_intent/live
  name: "Search intent for up to 1000 keywords"
  summary: This endpoint will provide you with search intent data for up to 1,000 keywords.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/search_intent/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article'
        example:
        - login page
        - audi a7
        - elon musk
        - milk store new york
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages Note: this endpoint currently supports the following '
      language_code:
        type: string
        required: false
        note: 'language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages Note: this endpoint currently supports these languages only; exam'
        example: en
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      keywords:
      - login page
      - audi a7
      - elon musk
      - milk store new york
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-search-intent-live.json
  observed_cost: 0.01248
- id: dataforseo.x.dataforseo-labs-google-serp-competitors-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/serp_competitors/live
  name: "Domains ranking in Google for your keywords"
  summary: This endpoint will provide you with a list of domains ranking for the keywords you specify.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/serp_competitors/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: keywords array required field the results will be based on the keywords you specify in this array UTF-8 encoding; the keywords will be converted to lowercase format; you can specify the maximum of 200 keywords learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs i
        example:
        - phone
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with location_name parameters by making a separate request to the https://api.dataforseo.com/v3/dataforseo'
      location_code:
        type: integer
        required: false
        note: 'unique location identifier required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code parameters by making a separate request to the https://api.dataforseo.com/v3/dat'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_name parameters by making a separate request to the https://api.dataforseo.com/v3/data'
        example: English
      language_code:
        type: string
        required: false
        note: 'unique language identifier required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available languages with their language_code parameters by making a separate request to the https://api.dataforseo.com/v3/dat'
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      item_types:
        type: array
        required: false
        note: 'search results type indicates type of search results included in the response optional field possible values: ["organic", "paid", "featured_snippet", "local_pack"] default value: ["organic", "paid"]'
        example:
        - organic
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned domains optional field default value: 100 maximum value: 1000'
        example: 5
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned domains optional field default value: 0 if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive domains'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike, '
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order the comma is used as a separator example: ["avg_position,asc"'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keywords:
      - phone
      language_name: English
      location_code: 2840
      item_types:
      - organic
      limit: 5
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-serp-competitors-live.json
  observed_cost: 0.0126
- id: dataforseo.x.dataforseo-labs-google-subdomains-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/subdomains/live
  name: "Subdomains of a domain with Google rank distribution"
  summary: This endpoint will provide you with a list of subdomains of the specified domain, along with the ranking distribution across organic and paid search.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/subdomains/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain required field the domain name of the target website the domain should be specified without https:// and www.
        example: dataforseo.com
      location_name:
        type: string
        required: false
        note: full name of the location optional field if you use this field, you don’t need to specify location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field
      location_code:
        type: integer
        required: false
        note: 'location code optional field if you use this field, you don’t need to specify location_name you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the '
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of the language optional field if you use this field, you don’t need to specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field
        example: English
      language_code:
        type: string
        required: false
        note: 'language code optional field if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_and_languages ignore this field to get the '
      item_types:
        type: array
        required: false
        note: 'display results by item type optional field indicates the type of search results included in the response Note: if the item_types array contains item types that are different from organic, the results will be ordered by the first item type in the array; you will not be able to sort and filter result'
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_etv, clickstream_gender_distribution, and clickstream_age_distribution fields with clickstream data in the response default value: false with this paramet'
      historical_serp_mode:
        type: string
        required: false
        note: 'data collection mode optional field you can use this field to filter the results; possible types of filtering: live — return metrics for SERPs in which the specified target currently has ranking results; lost — return metrics for SERPs in which the specified target had previously had ranking results'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true, only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in example: ["metrics.paid.count",">",0]'
        example:
        - - metrics.organic.pos_1
          - <>
          - 0
        - or
        - - metrics.organic.pos_2_3
          - <>
          - 0
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to specify a sorting type example: ["m'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
      language_name: English
      location_code: 2840
      filters:
      - - metrics.organic.pos_1
        - <>
        - 0
      - or
      - - metrics.organic.pos_2_3
        - <>
        - 0
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-subdomains-live.json
  observed_cost: 0.01236
- id: dataforseo.x.dataforseo-labs-google-top-searches-live
  tier: extended
  platform: google
  method: POST
  path: /dataforseo_labs/google/top_searches/live
  name: "Top Google searches from the 7B keyword database"
  summary: The Top Searches endpoint of DataForSEO Labs API can provide you with over 7 billion keywords from the DataForSEO Keyword Database.
  docs_url: https://docs.dataforseo.com/v3/dataforseo_labs/google/top_searches/live/
  input:
    bodyType: json
    body:
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of the language required field if you don’t specify language_code Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_name by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs'
        example: English
      language_code:
        type: string
        required: false
        note: 'language code required field if you don’t specify language_name Note: it is required to specify either language_name or language_code you can receive the list of available locations with their language_code by making a separate request to the https://api.dataforseo.com/v3/dataforseo_labs/locations_a'
      include_serp_info:
        type: boolean
        required: false
        note: 'include data from SERP for each keyword optional field if set to true, we will return a serp_info array containing SERP data (number of search results, relevant URL, and SERP features) for every keyword in the response default value: false'
      include_clickstream_data:
        type: boolean
        required: false
        note: 'include or exclude data from clickstream-based metrics in the result optional field if the parameter is set to true, you will receive clickstream_keyword_info, keyword_info_normalized_with_clickstream, and keyword_info_normalized_with_bing fields in the response default value: false with this parame'
      ignore_synonyms:
        type: boolean
        required: false
        note: 'ignore highly similar keywords optional field if set to true only core keywords will be returned, all highly similar keywords will be excluded; default value: false'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, match, not_match, ilike, not_ilike, '
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["ke'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field note: you can get more than 1000 results by using the offset_token provided in the response to each subsequent request default value: 1000 maximum value: 1000'
        example: 3
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned keywords optional field default value: 0 if you specify the 10 value, the first ten keywords in the results array will be omitted and the data will be provided for the successive keywords Note: we recommend using this parameter only when retrieving up to 10,00'
      offset_token:
        type: string
        required: false
        note: 'offset token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 10,000 results in a single request; by specifying the unique offset_token value from the response array, you will get '
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_name: English
      location_code: 2840
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.dataforseo-labs-google-top-searches-live.json
  observed_cost: 0.01236
- id: dataforseo.x.keywords-data-google-ads-ad-traffic-by-keywords-live
  tier: extended
  platform: google
  method: POST
  path: /keywords_data/google_ads/ad_traffic_by_keywords/live
  name: "Google Ads traffic forecast for keywords and bid"
  summary: GoogleAdsAdTrafficByKeywordsLive
  docs_url: https://docs.dataforseo.com/v3/keywords_data/google_ads/ad_traffic_by_keywords/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field The maximum number of keywords you can specify: 1000 The maximum number of characters for each keyword: 80 The maximum number of words for each keyword phrase: 10 the keywords you specify will be converted to a lowercase format Note: Google Ads may return no data for certain '
        example:
        - seo marketing
      bid:
        type: integer
        required: false
        note: the maximum custom bid required field the collected data will be based on this value it stands for the price you are willing to pay for an ad; the higher value you specify here, the higher values you will get in the returned metrics learn more in this help center article
        example: 999
      match:
        type: string
        required: false
        note: 'keywords match-type required field can take the following values: exact, broad, phrase'
        example: exact
      location_name:
        type: string
        required: false
        note: 'full name of search engine location optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the '
      location_code:
        type: integer
        required: false
        note: 'search engine location code optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_name or location_coordinate; you can receive the list of available locations of the search '
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_name or location_code; location_coordinate parameter should be specified in the “latitude
      language_name:
        type: string
        required: false
        note: 'full name of search engine language optional field you can receive the list of available languages of the search engine with their language_name by making a separate request to https://api.dataforseo.com/v3/keywords_data/google_ads/languages example: English'
      language_code:
        type: string
        required: false
        note: 'search engine language code optional field you can receive the list of available languages of the search engine with their language_code by making a separate request to https://api.dataforseo.com/v3/keywords_data/google_ads/languages example: en'
        example: en
      date_from:
        type: string
        required: false
        note: 'starting date of the forecasting time range required field if you specify date_to if you indicate date_from and date_to, you don’t need to specify date_interval minimum value is tomorrow’s date the value you specify in date_from shouldn’t be further than date_to date format: "yyyy-mm-dd" example: "2'
      date_to:
        type: string
        required: false
        note: 'ending date of the forecasting time range required field if you specify date_from if you indicate date_from and date_to, you don’t need to specify date_interval minimum value is date_from +1 day maximum value is current day and month of the next year date format: "yyyy-mm-dd" example: "2022-10-30"'
      date_interval:
        type: string
        required: false
        note: 'forecasting date interval optional field if you specify date_interval, you don’t need to indicate date_from and date_to possible values: next_week, next_month, next_quarter default value: next_month'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field Use these parameters to sort the results by relevance, impressions, ctr, average_cpc, cost, or clicks in the descending order default value: relevance'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_code: en
      bid: 999
      match: exact
      keywords:
      - seo marketing
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-google-ads-ad-traffic-by-keywords-live.json
  observed_cost: 0.09
- id: dataforseo.x.keywords-data-google-ads-keywords-for-keywords-live
  tier: extended
  platform: google
  method: POST
  path: /keywords_data/google_ads/keywords_for_keywords/live
  name: "Google Ads keyword ideas from seed keywords"
  summary: Note that Google Ads Keywords Data API is based on the latest version of the Google Ads API that has replaced legacy Google AdWords API.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/google_ads/keywords_for_keywords/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field The maximum number of keywords you can specify: 20 The maximum number of characters for each keyword: 80 the keywords you specify will be converted to a lowercase format Note: Google Ads may return no data for certain groups of keywords visit our Help Center to learn more Als'
        example:
        - phone
        - cellphone
      location_name:
        type: string
        required: false
        note: 'full name of search engine location optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the '
      location_code:
        type: integer
        required: false
        note: 'search engine location code optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_name or location_coordinate; you can receive the list of available locations of the search '
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_name or location_code; location_coordinate parameter should be specified in the “latitude
      language_name:
        type: string
        required: false
        note: 'full name of search engine language optional field you can receive the list of available languages of the search engine with their language_name by making a separate request to https://api.dataforseo.com/v3/keywords_data/google_ads/languages example: English'
      language_code:
        type: string
        required: false
        note: 'search engine language code optional field you can receive the list of available languages of the search engine with their language_code by making a separate request to https://api.dataforseo.com/v3/keywords_data/google_ads/languages example: en'
      search_partners:
        type: boolean
        required: false
        note: 'include Google search partners optional field if you specify true, the results will be delivered for owned, operated, and syndicated networks across Google and partner sites that host Google search; default value: false – results are returned for Google search sites'
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field date format: "yyyy-mm-dd" minimal value: 4 years from the current date by default, data is returned for the past 12 months; Note: the indicated date cannot be greater than that specified in date_to and/or yesterday’s date;if Status endpoint returns fals'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field Note: the indicated date cannot be greater than yesterday’s date; if you don’t specify this field, yesterday’s date will be used by default date format: "yyyy-mm-dd" example: "2022-11-30"'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field Use these parameters to sort the results by relevance, search_volume, competition_index, low_top_of_page_bid, or high_top_of_page_bid in descending order default value: relevance'
      include_adult_keywords:
        type: boolean
        required: false
        note: 'include keywords associated with adult content optional field if set to true, adult keywords will be included in the response default value: false note that the API may return no data for such keywords due to Google Ads restrictions'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      keywords:
      - phone
      - cellphone
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-google-ads-keywords-for-keywords-live.json
  observed_cost: 0.09
- id: dataforseo.x.keywords-data-google-ads-keywords-for-site-live
  tier: extended
  platform: google
  method: POST
  path: /keywords_data/google_ads/keywords_for_site/live
  name: "Google Ads keyword ideas for a site, by domain"
  summary: Note that Google Ads Keywords Data API is based on the latest version of the Google Ads API that has replaced legacy Google AdWords API.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/google_ads/keywords_for_site/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: 'domain or page required field the domain name of the target website or the url of the target page; note: to obtain keywords for the target website, use the target_type parameter'
        example: dataforseo.com
      target_type:
        type: string
        required: false
        note: 'search keywords for site or for url optional field possible values: site, page; default value: page; if set to site, keywords will be provided for the entire site; if set to page, keywords will be provided for the specified webpage'
      location_name:
        type: string
        required: false
        note: 'full name of search engine location optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the '
      location_code:
        type: integer
        required: false
        note: 'search engine location code optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_name or location_coordinate; you can receive the list of available locations of the search '
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location optional field if you do not indicate the location, you will receive worldwide results, i.e., for all available locations; if you use this field, you don’t need to specify location_name or location_code; location_coordinate parameter should be specified in the “latitude
      language_name:
        type: string
        required: false
        note: 'full name of search engine language optional field you can receive the list of available languages of the search engine with their language_name by making a separate request to https://api.dataforseo.com/v3/keywords_data/google_ads/languages example: English'
      language_code:
        type: string
        required: false
        note: 'search engine language code optional field you can receive the list of available languages of the search engine with their language_code by making a separate request to https://api.dataforseo.com/v3/keywords_data/google_ads/languages example: en'
      search_partners:
        type: boolean
        required: false
        note: 'include Google search partners optional field if you specify true, the results will be delivered for owned, operated, and syndicated networks across Google and partner sites that host Google search; default value: false – results are returned for Google search sites'
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field date format: "yyyy-mm-dd" minimal value: 4 years from the current date by default, data is returned for the past 12 months; Note: the indicated date cannot be greater than that specified in date_to and/or yesterday’s date;if Status endpoint returns fals'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field Note: the indicated date cannot be greater than yesterday’s date; if you don’t specify this field, yesterday’s date will be used by default date format: "yyyy-mm-dd" example: "2022-11-30"'
      include_adult_keywords:
        type: boolean
        required: false
        note: 'include keywords associated with adult content optional field if set to true, adult keywords will be included in the response default value: false note that the API may return no data for such keywords due to Google Ads restrictions'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field Use these parameters to sort the results by relevance, search_volume, competition_index, low_top_of_page_bid, or high_top_of_page_bid in descending order default value: relevance'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      target: dataforseo.com
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-google-ads-keywords-for-site-live.json
  observed_cost: 0.09
- id: dataforseo.x.keywords-data-google-ads-locations-country
  kind: utility
  tier: extended
  platform: google
  method: GET
  path: /keywords_data/google_ads/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Google Ads location codes by country"
  summary: We use Google Geographical Targeting.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/google_ads/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.keywords-data-google-trends-explore-live
  tier: extended
  platform: google
  method: POST
  path: /keywords_data/google_trends/explore/live
  name: "Google Trends keyword popularity over time"
  summary: This endpoint will provide you with the keyword popularity data from the ‘Explore’ feature of Google Trends.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/google_trends/explore/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field the maximum number of keywords you can specify: 5 the maximum number of characters you can specify in a keyword: 100 the minimum number of characters must be greater than 1 comma characters (,) in the specified keywords will be unset and ignored Note: keywords cannot consist '
        example:
        - rugby
        - cricket
      location_name:
        type: string
        required: false
        note: full name of search engine location optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_code you can use this field as an array to set several locations, each corresponding to a specific keyword – learn more; you can r
        example: United States
      location_code:
        type: string
        required: false
        note: search engine location code optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_name you can use this field as an array to set several locations, each corresponding to a specific keyword – learn more; you can receive t
      language_name:
        type: string
        required: false
        note: 'full name of search engine language optional field default value: English if you use this field, you don’t need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to https://api.dataforseo.com/v3/keyword'
      language_code:
        type: string
        required: false
        note: 'search engine language code optional field default value: en if you use this field, you don’t need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to https://api.dataforseo.com/v3/keywords_data/google'
      type:
        type: string
        required: false
        note: type of element
        example: youtube
      category_code:
        type: integer
        required: false
        note: 'google trends search category optional field if you don’t specify this field, the 0 value will be applied by default and the search will be carried out across all available categories you can receive the list of available categories with their category_code by making a separate request to the https:'
        example: 3
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the current day and month of the preceding year will be used by default minimal value for the web type: 2004-01-01 minimal value for other types: 2008-01-01 date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-06-24'
      time_range:
        type: string
        required: false
        note: 'preset time ranges optional field if you specify date_from or date_to parameters, this field will be ignored when setting a task possible values for all type parameters: past_hour, past_4_hours, past_day, past_7_days, past_30_days, past_90_days, past_12_months, past_5_years possible values for web o'
      item_types:
        type: array
        required: false
        note: 'types of items returned optional field to speed up the execution of the request, specify one item at a time; possible values: "google_trends_graph", "google_trends_map", "google_trends_topics_list","google_trends_queries_list" default value: "google_trends_graph" Note: to obtain google_trends_topics'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_name: United States
      date_from: '2026-04-24'
      date_to: '2026-06-24'
      type: youtube
      category_code: 3
      keywords:
      - rugby
      - cricket
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-google-trends-explore-live.json
  observed_cost: 0.011
- id: dataforseo.x.keywords-data-google-trends-locations-country
  kind: utility
  tier: extended
  platform: google
  method: GET
  path: /keywords_data/google_trends/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Google Trends location codes by country"
  summary: GoogleTrendsLocationsCountry
  docs_url: https://docs.dataforseo.com/v3/keywords_data/google_trends/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.merchant-google-locations-country
  kind: utility
  tier: extended
  platform: google
  method: GET
  path: /merchant/google/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Google Shopping location codes by country"
  summary: MerchantGoogleLocationsCountry
  docs_url: https://docs.dataforseo.com/v3/merchant/google/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.merchant-google-product-info-task-post
  tier: extended
  platform: google
  method: POST
  path: /merchant/google/product_info/task_post
  name: "Google Shopping product details by id (async task)"
  summary: This endpoint provides data on a product listed on Google Shopping, including product description, images, rating, variations, specifications and sellers.
  docs_url: https://docs.dataforseo.com/v3/merchant/google/product_info/task_post/
  input:
    bodyType: json
    body:
      product_id:
        type: string
        required: false
        note: unique product identifier on Google Shopping required field if data_docid or gid is not specified we recommend specifying product_id together with data_docid and gid for optimal results; you can get this value for a certain product by making a separate request to the Google Shopping Products endpoin
        example: '1113158713975221117'
      data_docid:
        type: string
        required: false
        note: 'unique identifier of the SERP data element required field if product_id or gid is not specified we recommend specifying data_docid together with product_id and gid for optimal results; you can get this value for a certain element by making a separate request to the Google Shopping Products endpoint '
      gid:
        type: string
        required: false
        note: 'global product identifier on Google Shopping required field if product_id or data_docid is not specified we recommend specifying gid together with product_id and data_docid for optimal results; you can get this value for a certain product by making a separate request to the Google Shopping Products '
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      location_name:
        type: string
        required: false
        note: full name of the location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available Google Shopping locations with their location_name by making a separate reque
      location_code:
        type: integer
        required: false
        note: location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available Google Shopping locations with their location_code by making a separate request to the ht
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of the language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available Google Shopping languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/mercha
      language_code:
        type: string
        required: false
        note: language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available Google Shopping languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/merchant/google/la
        example: en
      se_domain:
        type: string
        required: false
        note: 'search engine domain optional field we choose the relevant search engine domain automatically according to the location and language you specify however, you can set a custom search engine domain in this field example: google.co.uk, google.com.au, google.de, etc.'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype optional field corresponds to the datatype that will be sent to your server possible values: advanced'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      product_id: '1113158713975221117'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.merchant-google-product-info-task-post.json
  observed_cost: 0.001
- id: dataforseo.x.merchant-google-products-task-post
  tier: extended
  platform: google
  method: POST
  path: /merchant/google/products/task_post
  name: "Google Shopping results for a keyword (async task)"
  summary: Google Shopping Products endpoint will provide you with the list of products found on Google Shopping for the specified query.
  docs_url: https://docs.dataforseo.com/v3/merchant/google/products/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: 'keyword required field you can specify up to 700 characters in the keyword filed all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please specify it as “%25”; learn more about rules and limitations of keyword and '
        example: iphone
      url:
        type: string
        required: false
        note: direct URL of the search query optional field you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, we wouldn’t recom
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      location_name:
        type: string
        required: false
        note: full name of the location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available Google Shopping locations with their location_name by making a separate reque
      location_code:
        type: integer
        required: false
        note: location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available Google Shopping locations with their location_code by making a separate request to the ht
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of the language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available Google Shopping languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/mercha
      language_code:
        type: string
        required: false
        note: language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available Google Shopping languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/merchant/google/la
        example: en
      se_domain:
        type: string
        required: false
        note: 'search engine domain optional field we choose the relevant search engine domain automatically according to the location and language you specify however, you can set a custom search engine domain in this field example: google.co.uk, google.com.au, google.de, etc.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results to be retrieved from Google Shopping SERP default value: 40 max value: 120 Your account will be billed per each SERP containing up to 40 results; Setting depth above 40 may result in additional charges if the search engine returns more than 40 results; '
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl max value: 7 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      search_param:
        type: string
        required: false
        note: 'additional parameters of the search query optional field you can use the following search URL parameters for customizing the search; example: &tbs=ppr_min:45 – search for products that cost more than 45 USD; &tbs=ppr_max:50 – search for products that cost less than 50 USD; &tbs=p_ord:p – sort by asc'
      price_min:
        type: integer
        required: false
        note: 'minimum product price optional field minimum price of the returned products listed on Google Shopping for the specified query example: 5 Note: if you specify price_min, the search_param parameter will be ignored'
        example: 5
      price_max:
        type: integer
        required: false
        note: 'maximum product price optional field maximum price of the returned products listed on Google Shopping for the specified query example: 100 Note: if you specify price_max, the search_param parameter will be ignored'
      sort_by:
        type: string
        required: false
        note: 'results sorting rules optional field the following sorting rules are supported: review_score, price_low_to_high, price_high_to_low example: sort_by:"review_score" Note: if you specify sort_by, the search_param parameter will be ignored'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced, html'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: iphone
      price_min: 5
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.merchant-google-products-task-post.json
  observed_cost: 0.001
- id: dataforseo.x.merchant-google-sellers-ad-url-shop-ad-aclk
  kind: utility
  tier: extended
  platform: google
  method: GET
  path: /merchant/google/sellers/ad_url/{shop_ad_aclk}
  cost: {type: per_call, value: 1e-06, currency: USD, per: 1, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', confidence: verified, note: 'read from our own account rate card'}
  name: "Resolve a Google Shopping ad to its full URL"
  summary: Google Shopping Sellers Ad URL is designed to provide you with a full URL of the advertisement containing all additional parameters set by the seller.
  docs_url: https://docs.dataforseo.com/v3/merchant/google/sellers/ad_url/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.merchant-google-sellers-task-post
  tier: extended
  platform: google
  method: POST
  path: /merchant/google/sellers/task_post
  name: "Google Shopping sellers for a product (async task)"
  summary: Google Shopping Sellers endpoint will provide you with the list of top 10 sellers that listed the specified product on Google Shopping.
  docs_url: https://docs.dataforseo.com/v3/merchant/google/sellers/task_post/
  input:
    bodyType: json
    body:
      product_id:
        type: string
        required: false
        note: unique product identifier on Google Shopping required field if data_docid or gid is not specified we recommend specifying product_id together with data_docid and gid for optimal results; you can get this value for a certain product by making a separate request to the Google Shopping Products endpoin
        example: '1113158713975221117'
      data_docid:
        type: string
        required: false
        note: 'unique identifier of the SERP data element required field if product_id or gid is not specified we recommend specifying data_docid together with product_id and gid for optimal results; you can get this value for a certain element by making a separate request to the Google Shopping Products endpoint '
      gid:
        type: string
        required: false
        note: 'global product identifier on Google Shopping required field if product_id or data_docid is not specified we recommend specifying gid together with product_id and data_docid for optimal results; you can get this value for a certain product by making a separate request to the Google Shopping Products '
      pvf:
        type: string
        required: false
        note: 'product variant filter on Google Shopping optional field parameter in Google Shopping URL, setting optional product variant filtration; example: Eg4iBWNvbG9yKgV3aGl0ZRISIgxwYWNrYWdlIHNpemUqAjE0EgoiBHNpemUqAnhs'
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      location_name:
        type: string
        required: false
        note: full name of the location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available Google Shopping locations with their location_name by making a separate reque
      location_code:
        type: integer
        required: false
        note: location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available Google Shopping locations with their location_code by making a separate request to the ht
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of the language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available Google Shopping languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/mercha
      language_code:
        type: string
        required: false
        note: language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available Google Shopping languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/merchant/google/la
        example: en
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results to be retrieved from Google Shopping SERP default value: 10 max value: 200 your account will be billed per each SERP containing up to 10 results; setting depth above 10 may result in additional charges if the search engine returns more than 10 results; '
      se_domain:
        type: string
        required: false
        note: 'search engine domain optional field we choose the relevant search engine domain automatically according to the location and language you specify however, you can set a custom search engine domain in this field example: google.co.uk, google.com.au, google.de, etc.'
      get_shops_on_google:
        type: boolean
        required: false
        note: 'include “buy on Google” shops optional field if set to true, the response will contain the list of sellers that allow to purchase a given product directly on Google Note: if set to true, the cost of a task will be doubled'
      additional_specifications:
        type: object
        required: false
        note: 'object containing additional url parameters you can get additional information about the product by using the "additional_specifications object, which you can get by making a separate request to the Google Shopping Products endpoint example: "additional_specifications": { "eto": "1615712105016757276'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype optional field corresponds to the datatype that will be sent to your server possible values: advanced'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      product_id: '1113158713975221117'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.merchant-google-sellers-task-post.json
  observed_cost: 0.001
- id: dataforseo.x.serp-ai-summary
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: google
  method: POST
  path: /serp/ai_summary
  name: "AI summary of a fetched SERP by task id and prompt"
  summary: The purpose of the Live SERP API AI Summary endpoint is to provide a summary of the content found on any SERP and generate a response based on the user’s specified prompt.
  docs_url: https://docs.dataforseo.com/v3/serp/ai_summary/
  input:
    bodyType: json
    body:
      task_id:
        type: string
        required: false
        note: task identifier required field unique identifier of the associated task in the UUID format you will be able to use it within 30 days to request the results of the task at any time
        example: 07031739-1535-0139-0000-9d1e639a5b7d
      prompt:
        type: string
        required: false
        note: 'AI prompt optional field additional task for AI summariser; any form of text, question or information that communicates to AI what response you’re looking for; max number of symbols or characters you can specify: 2000; note: your prompt has to be relevant to the keyword specified in the POST request'
        example: explain what DataForSEO is
      support_extra:
        type: boolean
        required: false
        note: 'support extra SERP features optional field if set to true, the AI model will consider the following extra SERP features, in addition to organic results: answer_box, knowledge_graph, featured_snippet; default value: true'
      fetch_content:
        type: boolean
        required: false
        note: 'fetch content from pages in SERPs optional field if set to true, the API will fetch the content from pages featured in SERP results, and the AI model will consider this content when generating the summary in the result; default value: false'
        example: true
      include_links:
        type: boolean
        required: false
        note: 'include source links in the summary optional field if set to true, the summary field in the API response will contain links to sources of the generated summary; default value: false'
        example: true
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - task_id: 07031739-1535-0139-0000-9d1e639a5b7d
      prompt: explain what DataForSEO is
      include_links: true
      fetch_content: true
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-ai-summary.json
  observed_cost: 0.01
- id: dataforseo.x.serp-google-ads-advertisers-task-post
  tier: extended
  platform: google
  method: POST
  path: /serp/google/ads_advertisers/task_post
  name: "Google Ads Transparency advertisers by keyword (async task)"
  summary: Google Ads Advertisers provides information on advertisers that run campaigns on Google Ads based on the Ads Transparency platform.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<p>if you need to use the “%” character for your <code>keyword</code>, pl
        example: apple
      priority:
        type: integer
        required: false
        note: <p><em>task priority</em><p>optional field<p>can take the following values:<p>1 – normal execution priority (set by default)<p>2 – high execution priority</p><br>You will be additionally charged for the tasks with high execution priority.<p>The cost can be calculated on the <a title="Pricing" href="
      location_code:
        type: integer
        required: false
        note: <p><em>search engine location code</em><p>optional field<p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you can receive the list of available locations of the search engines with their <code>location_code</code> by
        example: 2840
      pingback_url:
        type: string
        required: false
        note: <p><em>notification URL of a completed task</em><p>optional field<p>when a task is completed we will notify you by GET request sent to the URL you have specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as urlencoded <code>$tag</code> variable. We will set the necessa
      postback_url:
        type: string
        required: false
        note: <p><em>URL for sending task results</em><p>optional field<p>once the task is completed, we will send a POST request with its results compressed in the <code>gzip</code> format to the <code>postback_url</code> you specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as u
      postback_data:
        type: string
        required: false
        note: <p><em>postback_url datatype</em><p><strong>required field if you specify <code>postback_url</code></strong><p>corresponds to the function you used for setting a task<p>possible values:<p><code>advanced</code></p>
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p>optional field<p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>you can receive the list of available locations of the search engine with their <code>location_name</c
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p>optional field<p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p>example:<p><code class="long-string">52.6178549,-155.352142</code></p><br><p><strong>Note:</strong> if you don't s
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      keyword: apple
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-ads-advertisers-task-post.json
  observed_cost: 0.0006
- id: dataforseo.x.serp-google-ads-search-task-post
  tier: extended
  platform: google
  method: POST
  path: /serp/google/ads_search/task_post
  name: "Google Ads Transparency ads by advertiser (async task)"
  summary: Google Ads Search provides information on ads that are run by advertisers on Google Ads.
  input:
    bodyType: json
    body:
      advertiser_ids:
        type: array
        required: false
        note: <p><em>advertiser identifiers</em><p><strong>required field if <code>target</code> is not specified</strong></p><br><p>you can specify the maximum of 25 values in this array;<p><code>advertiser_ids</code> values for this parameter can be found in the <a href="http://docs.dataforseo.com/v3/serp/googl
        example:
        - AR13752565271262920705
        - AR02439908557932462081
      target:
        type: string
        required: false
        note: <p><em>domain name</em><p><strong>required field if <code>advertiser_ids</code> is not specified</strong><p>domain name associated with an advertiser account</p>
      location_code:
        type: integer
        required: false
        note: <p><em>search engine location code</em><p>optional field<p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you can receive the list of available locations of the search engines with their <code>location_code</code> by
        example: 2840
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p>default value: <code>40</code><p>max value: <code>700</code></p><br><strong>Your account will be billed per each SERP containing up to 40 results;</strong><p>Setting depth above 40 may result in additional charges <a href="http'
      priority:
        type: integer
        required: false
        note: <p><em>task priority</em><p>optional field<p>can take the following values:<p>1 – normal execution priority (set by default)<p>2 – high execution priority</p><br>You will be additionally charged for the tasks with high execution priority.<p>The cost can be calculated on the <a title="Pricing" href="
      pingback_url:
        type: string
        required: false
        note: <p><em>notification URL of a completed task</em><p>optional field<p>when a task is completed we will notify you by GET request sent to the URL you have specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as urlencoded <code>$tag</code> variable. We will set the necessa
      postback_url:
        type: string
        required: false
        note: <p><em>URL for sending task results</em><p>optional field<p>once the task is completed, we will send a POST request with its results compressed in the <code>gzip</code> format to the <code>postback_url</code> you specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as u
      postback_data:
        type: string
        required: false
        note: <p><em>postback_url datatype</em><p><strong>required field if you specify <code>postback_url</code></strong><p>corresponds to the function you used for setting a task<p>possible values:<p><code>advanced</code></p>
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p>optional field<p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>you can receive the list of available locations of the search engine with their <code>location_name</c
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p>optional field<p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p>example:<p><code class="long-string">52.6178549,-155.352142</code></p><br><p><strong>Note:</strong> if you don't s
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      platform:
        type: string
        required: false
        note: '<p><em>advertising platform</em><p>optional field<p>possible values: <code>all</code>, <code>google_play</code>, <code>google_maps</code>, <code>google_search</code>, <code>google_shopping</code>, <code>youtube</code><p>default value: <code>all</code></p>'
        example: google_search
      format:
        type: string
        required: false
        note: '<p><em>ad format</em><p>optional field<p>possible values: <code>all</code>, <code>text</code>, <code>image</code>, <code>video</code></p>'
      date_from:
        type: string
        required: false
        note: '<p><em>starting date of the time range</em><p>optional field<p>required field if <code>date_to</code> is specified; </p><br><p>date format: <code>"yyyy-mm-dd"</code><p>minimum value: 2018-05-31<p>maximum value: today''s date<p>example:<p><code>"2020-01-01"</code></p>'
      date_to:
        type: string
        required: false
        note: '<p><em>ending date of the time range</em><p>optional field<p>required field if <code>date_from</code> is specified; </p><br><p>date format: <code>"yyyy-mm-dd"</code><p>minimum value: 2018-05-31<p>maximum value: today''s date<p>example:<p><code>"2020-01-01"</code></p>'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      platform: google_search
      advertiser_ids:
      - AR13752565271262920705
      - AR02439908557932462081
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-ads-search-task-post.json
  observed_cost: 0.0006
- id: dataforseo.x.serp-google-ai-mode-live-advanced
  capability: google.serp.ai_mode
  platform: google
  tier: extended
  method: POST
  path: /serp/google/ai_mode/live/advanced
  name: "Google AI Mode results for a keyword, live"
  summary: Google AI Mode SERP API provides search results from the AI Mode feature of Google Search.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<p>if you need to use the “%” character for your <code>keyword</code>, pl
        example: what is google ai mode
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p>required field if you don't specify <code>language_name</code>;<p>if you use this field, you don't need to specify <code>language_name</code>;<p>you can receive the list of available languages of the search engine with their <code>language_code</code>&nbsp;b
        example: en
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>can take the values:<code>desktop</code>, <code>mobile</code><p>default value: <code>desktop</code></p>'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p>required field if you don't specify <code>language_code</code>;<p>if you use this field, you don't need to specify <code>language_code</code>;<p>you can receive the list of available languages of the search engine with their <code>language_name</code
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>if you specify <code>desktop</code> in the <code>device</code> field, choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code><p>if you specify <code>mobile</code> in the <code>device</code'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      calculate_rectangles:
        type: boolean
        required: false
        note: <p><em>calculate pixel rankings for SERP elements in advanced results</em><p>optional field<p>pixel ranking refers to the distance between the result snippet and top left corner of the screen;<p><a href="https://dataforseo.com/help-center/pixel-ranking-in-serp-api">Visit Help Center to learn more&gt
      browser_screen_width:
        type: integer
        required: false
        note: '<p><em>browser screen width</em><p>optional field<p>you can set a custom browser screen width to calculate pixel rankings for a particular device;<p>can be specified within the following range: <code>240-9999</code>;<p>by default, the parameter is set to:<p><code>1920</code> for <code>desktop</code>'
      browser_screen_height:
        type: integer
        required: false
        note: '<p><em>browser screen height</em><p>optional field<p>you can set a custom browser screen height to calculate pixel rankings for a particular device;<p>can be specified within the following range: <code>240-9999</code>;<p>by default, the parameter is set to:<p><code>1080</code> for <code>desktop</cod'
      browser_screen_resolution_ratio:
        type: integer
        required: false
        note: '<p><em>browser screen resolution ratio</em><p>optional field<p>you can set a custom browser screen resolution ratio to calculate pixel rankings for a particular device;<p>can be specified within the following range: <code>0.5-3</code>;<p>by default, the parameter is set to:<p><code>1</code> for <cod'
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: what is google ai mode
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-ai-mode-live-advanced.json
  observed_cost: 0.004
- id: dataforseo.x.serp-google-autocomplete-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/autocomplete/live/advanced
  name: "Google autocomplete suggestions for a keyword, live"
  summary: Google Autocomplete is a feature within Google Search that improves the search experience by allowing users to complete searches they started to type.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<p>if you need to use the “%” character for your <code>keyword</code>, pl
        example: albert einstein
      location_code:
        type: integer
        required: false
        note: <p><em>search engine location code</em><p><strong>required field if you don't specify</strong> <code>location_name</code>;<p>you can receive the list of available locations of the search engines with their <code>location_code</code> by making a separate request to <code>https://api.dataforseo.com/v3
        example: 2840
      language_code:
        type: string
        required: false
        note: '<p><em>search engine language code</em><p><strong>required field if you don''t specify</strong> <code>language_name</code><p><strong>if you use this field, you don''t need to specify <code>language_name</code></strong>;<p>you can receive the list of available languages of the search engine with their '
        example: en
      cursor_pointer:
        type: integer
        required: false
        note: '<p><em>search bar cursor pointer</em><p>optional field<p>the horizontal numerical position of the cursor pointer within the keyword in the search bar;<p>by modifying the position of the cursor pointer, you will obtain different autocomplete suggestions for the same seed keyword;<p>minimal value: <co'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_code</code></strong>;<p>you can receive the list of available locations of the search engine wit
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify</strong> <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong>;<p>you can receive the list of available languages of the search engine wit
      client:
        type: string
        required: false
        note: <p><em>search client for autocomplete</em><p>optional field<p>autocomplete results may differ depending on the search client;<p>possible values:<p><code>chrome</code> — used when google search is opened in google chrome;<p><code>chrome-omni</code> — used in the address bar in chrome;<p><code>gws-wiz
        example: gws-wiz-serp
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
      client: gws-wiz-serp
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-autocomplete-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-dataset-info-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/dataset_info/live/advanced
  name: "Google Dataset details by dataset id, live"
  summary: Live Google Dataset Info provides real-time data on the dataset you specify in the request.
  input:
    bodyType: json
    body:
      dataset_id:
        type: string
        required: false
        note: <p><em>ID of the dataset</em><p><strong>required field</strong><p>you can find dataset ID in the dataset URL or <code>dataset</code> item of <a href="https://docs.dataforseo.com/v3/serp/google/dataset_search/live/advanced">Google Dataset Search</a> result<p>example:<p><code>L2cvMTFqbl85ZHN6MQ==</cod
        example: L2cvMTFqbl85ZHN6MQ==
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p>optional field<p>if you use this field, you don't need to specify <code>language_name</code><p>possible value:<p><code class="long-string">en</code></p>
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>possible value: <code>desktop</code></p>'
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p>optional field<p>if you use this field, you don't need to specify <code>language_code</code><p>possible value:<p><code class="long-string">English</code></p>
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>possible values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code></p>'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - dataset_id: L2cvMTFqbl85ZHN6MQ==
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-dataset-info-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-dataset-search-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/dataset_search/live/advanced
  name: "Google Dataset search results for a keyword, live"
  summary: Live Google Dataset Search provides real-time data on the top 20 Google Dataset search engine results.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<p>if you need to use the “%” character for your <code>keyword</code>, pl
        example: water quality
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p>optional field if you don't specify <code>language_name</code><p>if you use this field, you don't need to specify <code>language_name</code><p>possible value:<p><code class="long-string">en</code></p>
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p>default value: <code>20</code><p>max value: <code>200</code></p><br><strong>Your account will be billed per each SERP containing up to 20 results;</strong><p>Setting depth above 20 may result in additional charges if the search'
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>possible value: <code>desktop</code></p>'
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p>optional field<p>if you use this field, you don't need to specify <code>language_code</code><p>possible value:<p><code class="long-string">English</code></p>
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code></p>'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      last_updated:
        type: string
        required: false
        note: '<p><em>last time the dataset was updated</em><p>optional field<p>possible values: <code>1m</code>, <code>1y</code>, <code>3y</code></p>'
        example: 1m
      file_formats:
        type: array
        required: false
        note: '<p><em>file formats of the dataset</em><p>optional field<p>possible values: <code>other</code>, <code>archive</code>, <code>text</code>, <code>image</code>, <code>document</code>, <code>tabular</code></p>'
        example:
        - archive
        - image
      usage_rights:
        type: string
        required: false
        note: '<p><em>usage rights of the dataset</em><p>optional field<p>possible values: <code>commercial</code>, <code>noncommercial</code></p>'
        example: noncommercial
      is_free:
        type: boolean
        required: false
        note: '<p><em>indicates whether displayed datasets are free</em><p>optional field<p>possible values: <code>true</code>, <code>false</code></p>'
        example: true
      topics:
        type: array
        required: false
        note: '<p><em>dataset topics</em><p>optional field<p>possible values: <code>humanities</code>, <code>social_sciences</code>, <code>life_sciences</code>, <code>agriculture</code>, <code>natural_sciences</code>, <code>geo</code>, <code>computer</code>, <code>architecture_and_urban_planning</code>, <code>engi'
        example:
        - natural_sciences
        - geo
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: water quality
      last_updated: 1m
      file_formats:
      - archive
      - image
      usage_rights: noncommercial
      is_free: true
      topics:
      - natural_sciences
      - geo
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-dataset-search-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-events-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/events/live/advanced
  name: "Google Events results for a keyword and location, live"
  summary: Live Google Events SERP provides real-time data from Google Events Search for the specified keyword and location.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character '+' will be decoded to a space character)<p>if you need to use the "%" character for your <code>keyword</code>, pl
        example: concerts
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
      language_code:
        type: string
        required: false
        note: '<p><em>search engine language code</em><p>optional field<p><strong>if you use this field, you don''t need to specify <code>language_name</code></strong><p>by default, language settings correspond to one of the local official languages of the specified location<p>you can receive the list of available '
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p>default value: <code>10</code><p>max value: <code>200</code></p><br><strong>Your account will be billed per each SERP containing up to 10 results;</strong><p>Setting depth above 10 may result in additional charges if the search'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
        example: Los Angeles,California,United States
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p>optional field<p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>by default, language settings correspond to one of the local official languages of the specified location<p>you can receive the list of av
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p><strong>note that this API provides results for desktop only</strong><p>choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code></p>'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      max_crawl_pages:
        type: integer
        required: false
        note: '<p><em>page crawl limit</em><p>optional field<p>number of search results pages to crawl<p>max value: <code>100</code><p><strong>Note:</strong> the <code>max_crawl_pages</code> and <code>depth</code> parameters complement each other;<p>learn more at <a href="https://dataforseo.com/help-center/what-is'
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
      date_range:
        type: string
        required: false
        note: '<p><em>date range to get events for</em><p>optional field<p>if you do not use this field, we will return all events<p>possible values: <code>today</code>, <code>tomorrow</code>, <code>week</code>, <code>weekend</code>, <code>next_week</code>, <code>month</code>, <code>next_month</code></p>'
        example: today
      se_domain:
        type: string
        required: false
        note: <p><em>search engine domain</em><p>optional field<p>we choose the relevant search engine domain automatically according to the location you specify<p>however, you can set a custom search engine domain in this field<p>example:<p><code><em>google.co.uk</em></code>, <code><em>google.com.au</em></code>,
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: concerts
      location_name: Los Angeles,California,United States
      date_range: today
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-events-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-finance-explore-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/finance_explore/live/advanced
  name: "Google Finance Explore tab results, live"
  summary: Live Google Finance Explore provides real-time data from the ‘Explore’ tab of Google Finance.
  input:
    bodyType: json
    body:
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify <code>location_name</code></strong><p><strong>if you use this field, you don''t need to specify <code>location_name</code></strong><p>you can receive the list of available locations of the search engines with their '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify <code>language_name</code></strong><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>possible value: <code>desktop</code></p>'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify <code>location_code</code></strong><p><strong>if you use this field, you don't need to specify <code>location_code</code></strong><p>you can receive the list of available locations of the search engine with
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify <code>language_code</code></strong><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine with
        example: English
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>possible values: <code>windows</code></p>'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      news_type:
        type: string
        required: false
        note: '<p><em>financial news filters</em><p>optional field<p>possible values: <code>top_stories</code>, <code>local_market</code>, <code>world_markets</code><p>default value: <code>top_stories</code><p><strong>Note:</strong> if you specify <code>local_market</code> or <code>world_markets</code>, the charge'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_name: English
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-finance-explore-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-finance-markets-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/finance_markets/live/advanced
  name: "Google Finance Markets tab results, live"
  summary: Live Google Finance Markets provides real-time data from the ‘Markets’ tab of Google Finance.
  input:
    bodyType: json
    body:
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify <code>location_name</code></strong><p><strong>if you use this field, you don''t need to specify <code>location_name</code></strong><p>you can receive the list of available locations of the search engines with their '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify <code>language_name</code></strong><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>possible value: <code>desktop</code></p>'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify <code>location_code</code></strong><p><strong>if you use this field, you don't need to specify <code>location_code</code></strong><p>you can receive the list of available locations of the search engine with
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify <code>language_code</code> </strong><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine wit
        example: English
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>possible values: <code>windows</code></p>'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      market_type:
        type: string
        required: false
        note: '<p><em>type of google finance market</em><p>optional field<p>possible values: <code>most-active</code>, <code>indexes</code>, <code>indexes/americas</code>, <code>indexes/europe-middle-east-africa</code>, <code>indexes/asia-pacific</code>, <code>gainers</code>, <code>losers</code>, <code>climate-lea'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_name: English
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-finance-markets-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-finance-quote-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/finance_quote/live/advanced
  name: "Google Finance quote for a ticker, live"
  summary: Live Google Finance Quote provides real-time data from the ‘Quote’ tab of Google Finance.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>ticker or stock symbol</em><p><strong>required field</strong><p>in this field you can pass the ticker symbol of publicly traded shares of a particular stock or security on a particular stock exchange;<p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field;<p
        example: CLW00:NYMEX
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify <code>location_name</code></strong><p><strong>if you use this field, you don''t need to specify <code>location_name</code></strong><p>you can receive the list of available locations of the search engines with their '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify <code>language_name</code></strong><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>possible value: <code>desktop</code></p>'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify <code>location_code</code></strong><p><strong>if you use this field, you don't need to specify <code>location_code</code></strong><p>you can receive the list of available locations of the search engine with
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify <code>language_code</code> </strong><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine wit
        example: English
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>possible values: <code>windows</code></p>'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      window:
        type: string
        required: false
        note: '<p><em>time window for <code>google_finance_quote</code> graph</em><p>optional field<p>possible values: <code>1D</code>, <code>5D</code>, <code>1M</code>, <code>6M</code>, <code>YTD</code>, <code>1Y</code>, <code>5Y</code>, <code>MAX</code><p>default value: <code>1D</code><p><strong>Note:</strong> i'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: CLW00:NYMEX
      location_code: 2840
      language_name: English
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-finance-quote-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-finance-ticker-search-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/finance_ticker_search/live/advanced
  name: "Google Finance ticker search by company name, live"
  summary: Live Google Finance Ticker Search allows you to search for financial instruments available on Google Finance along with additional information.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>company or financial instrument name</em><p><strong>required field</strong><p>in this field, you can enter the name of a company or financial instrument to search for relevant tickers;<p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field;<p>all %## will be
        example: DJ
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify <code>location_name</code></strong><p><strong>if you use this field, you don''t need to specify <code>location_name</code></strong><p>you can receive the list of available locations of the search engines with their '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify <code>language_name</code></strong><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify <code>location_code</code></strong><p><strong>if you use this field, you don't need to specify <code>location_code</code></strong><p>you can receive the list of available locations of the search engine with
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify <code>language_code</code> </strong><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine wit
        example: English
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      category:
        type: string
        required: false
        note: '<p><em>category of financial instruments to search for</em><p>optional field<p>possible values: <code>all</code>, <code>stock</code>, <code>index</code>, <code>mutual_fund</code>, <code>currency</code>, <code>futures</code><p>default value: <code>all</code></p>'
        example: all
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_name: English
      location_code: 2840
      category: all
      keyword: DJ
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-finance-ticker-search-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-images-live-advanced
  tier: extended
  platform: google
  method: POST
  path: /serp/google/images/live/advanced
  name: "Google Images results for a keyword, live"
  summary: Live Google Images SERP provides real-time data on top 100 images results for the specified keyword, search engine, and location.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character '+' will be decoded to a space character)<p>if you need to use the "%" character for your <code>keyword</code>, pl
        example: albert einstein
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify</strong> <code>language_name</code><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
        example: en
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p>default value: <code>100</code><p>max value: <code>200</code></p><br><strong>Your account will be billed per each SERP containing up to 100 results;</strong><p>Setting depth above 100 may result in additional charges if the sea'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify</strong> <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine with
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p><strong>note that this API provides results for desktop only</strong><p>choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code></p>'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      max_crawl_pages:
        type: integer
        required: false
        note: '<p><em>page crawl limit</em><p>optional field<p>number of search results pages to crawl<p>max value: <code>100</code><p><strong>Note:</strong> the <code>max_crawl_pages</code> and <code>depth</code> parameters complement each other;<p>learn more at <a href="https://dataforseo.com/help-center/what-is'
      search_param:
        type: string
        required: false
        note: <p><em>additional parameters of the search query</em><p>optional field<p><a href="https://dataforseo.com/what-are-google-search-parameters-and-how-to-use-them-with-serp-api.html">Get the list of available parameters and additional details here.</a></p>
      url:
        type: string
        required: false
        note: <p><em>direct URL of the search query</em><p>optional field<p>you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, w
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
      se_domain:
        type: string
        required: false
        note: <p><em>search engine domain</em><p>optional field<p>We choose the relevant search engine domain automatically according to the location and language you specify. However, you can set a custom search engine domain in this field<p>example:<p><code><code></code></code><em>google.co.uk</em>, <code><code
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-images-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-jobs-task-post
  tier: extended
  platform: google
  method: POST
  path: /serp/google/jobs/task_post
  name: "Google Jobs postings for a keyword (async task)"
  summary: This endpoint will provide you with SERP data from the Google Jobs search engine.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<p>if you need to use the “%” character for your <code>keyword</code>, pl
        example: .net developer
      location_code:
        type: integer
        required: false
        note: <p><em>search engine location code</em><p><strong>required field if you don't specify</strong> <code>location_name</code>;<p>you can receive the list of available locations of the search engines with their <code>location_code</code> by making a separate request to <code>https://api.dataforseo.com/v3
        example: 2840
      language_code:
        type: string
        required: false
        note: '<p><em>search engine language code</em><p><strong>required field if you don''t specify</strong> <code>language_name</code><p><strong>if you use this field, you don''t need to specify <code>language_name</code></strong>;<p>you can receive the list of available languages of the search engine with their '
        example: en
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP;<p>default value: <code>10</code><p>max value: <code>200</code></p><br><strong>Your account will be billed per each SERP containing up to 10 results;</strong><p>Setting depth above 10 may result in additional charges if the searc'
      priority:
        type: integer
        required: false
        note: <p><em>task priority</em><p>optional field<p>can take the following values:<p>1 – normal execution priority (set by default);<p>2 – high execution priority</p><br>You will be additionally charged for the tasks with high execution priority;<p>The cost can be calculated on the <a title="Pricing" href=
      pingback_url:
        type: string
        required: false
        note: <p><em>notification URL of a completed task</em><p>optional field<p>when a task is completed we will notify you by GET request sent to the URL you have specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as urlencoded <code>$tag</code> variable. We will set the necessa
      postback_url:
        type: string
        required: false
        note: <p><em>URL for sending task results</em><p>optional field<p>once the task is completed, we will send a POST request with its results compressed in the <code>gzip</code> format to the <code>postback_url</code> you specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as u
      postback_data:
        type: string
        required: false
        note: <p><em>postback_url datatype</em><p><strong>required field if you specify <code>postback_url</code></strong><p>corresponds to the datatype that will be sent to your server<p>possible values:<p><code>regular</code>, <code>advanced</code>, <code>html</code></p>
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_code</code></strong>;<p>you can receive the list of available locations of the search engine wit
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify</strong> <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong>;<p>you can receive the list of available languages of the search engine wit
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      location_radius:
        type: string
        required: false
        note: <p><em>location search radius</em><p>optional field<p>location search radius in kilometers;<p><strong>Note:</strong> for countries that use the imperial system of units, you will need to convert miles to kilometers by multiplying the value in miles by <code>1.609</code>;<p>if value is not specified,
      employment_type:
        type: array
        required: false
        note: <p><em>employment contract type</em><p>optional field<p>type of employment contract for which the search results will be returned;<p>possible values:<p><code>fulltime</code>, <code>partime</code>, <code>contractor</code>, <code>intern</code></p>
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: .net developer
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-jobs-task-post.json
  observed_cost: 0.0006
- id: dataforseo.x.serp-google-local-finder-live-advanced
  capability: google.serp.local
  platform: google
  tier: extended
  method: POST
  path: /serp/google/local_finder/live/advanced
  name: "Google Local Finder results for a keyword, live"
  summary: Live Google Local_finder SERP provides real-time search engine results for the specified keyword and location.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<p>if you need to use the “%” character for your <code>keyword</code>, pl
        example: local nail services
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify</strong> <code>language_name</code><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
        example: en
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p>default value for <code>desktop</code>: <code>20</code><p>max value for <code>desktop</code>: <code>100</code></p><br><p>default value for <code>mobile</code>: <code>10</code><p>max value for <code>mobile</code>: <code>100</cod'
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>can take the values:<code>desktop</code>, <code>mobile</code><p>default value: <code>desktop</code></p>'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify</strong> <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine with
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>if you specify <code>desktop</code> in the <code>device</code> field, choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code><p>if you specify <code>mobile</code> in the <code>device</code'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      priority:
        type: integer
        required: false
        note: <p><em>task priority</em><p>optional field<p>can take the following values:<p>1 – normal execution priority (set by default)<p>2 – high execution priority</p><br>You will be additionally charged for the tasks with high execution priority.<p>The cost can be calculated on the <a title="Pricing" href="
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
      min_rating:
        type: number
        required: false
        note: '<p><em>filter results by minimum rating</em><p>optional field<p>possible values for <code>desktop</code>: <code>3.5</code>, <code>4</code>, <code>4.5</code>;<p>possible values for <code>mobile</code>: <code>2</code>, <code>2.5</code>, <code>3</code>, <code>3.5</code>, <code>4</code>, <code>4.5</code'
        example: 4.5
      time_filter:
        type: string
        required: false
        note: '<p><em>filter results by open hours</em><p>optional field<p>using this field, you can filter places in the results by the time a place is open for visitors<p>note that Google may also provide results that do not match this filter<p>possible values: <code>"open_now"</code>, <code>"24_hours"</code>, <'
        example: monday
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: local nail services
      min_rating: 4.5
      time_filter: monday
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-local-finder-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-locations-country
  kind: utility
  tier: extended
  platform: google
  method: GET
  path: /serp/google/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Google SERP location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/serp/google/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.serp-google-maps-live-advanced
  capability: google.serp.maps
  platform: google
  tier: extended
  method: POST
  path: /serp/google/maps/live/advanced
  name: "Google Maps results for a keyword and location, live"
  summary: Live Google Maps SERP provides real-time data on top 100 search engine results for the specified keyword, search engine, and location.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character '+' will be decoded to a space character)<p>if you need to use the "%" character for your <code>keyword</code>, pl
        example: albert einstein
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify</strong> <code>language_name</code><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
        example: en
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p>default value: <code>100</code><p>max value: <code>700</code></p><br><strong>Your account will be billed per each SERP containing up to 100 results;</strong><p>Setting depth above 100 may result in additional charges <a href="h'
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>can take the values:<code>desktop</code>, <code>mobile</code><p>default value: <code>desktop</code><p><strong>note:</strong> for <code>mobile</code> device, only 20 results are returned for every SERP</p>'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify</strong> <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine with
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>if you specify <code>desktop</code> in the <code>device</code> field, choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code><p>if you specify <code>mobile</code> in the <code>device</code'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      max_crawl_pages:
        type: integer
        required: false
        note: '<p><em>page crawl limit</em><p>optional field<p>number of search results pages to crawl<p>max value: <code>100</code><p><strong>Note:</strong> the <code>max_crawl_pages</code> and <code>depth</code> parameters complement each other;<p>learn more at <a href="https://dataforseo.com/help-center/what-is'
      url:
        type: string
        required: false
        note: <p><em>direct URL of the search query</em><p>optional field<p>you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, w
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
      se_domain:
        type: string
        required: false
        note: <p><em>search engine domain</em><p>optional field<p>we choose the relevant search engine domain automatically according to the location and language you specify<p>however, you can set a custom search engine domain in this field<p>example:<p><code><em>google.co.uk</em></code>, <code><em>google.com.au
      search_this_area:
        type: boolean
        required: false
        note: '<p><em>show results from the displayed area</em><p>optional field<p>can take the values:<code>true</code>, <code>false</code><p>default value: <code>true</code></p><br><p>if set to <code>false</code>, the <code>search_this_area</code> mode will be turned off</p><br><p><strong>Note:</strong> if the <'
      search_places:
        type: boolean
        required: false
        note: <p><em>search places mode</em><p>optional field<p>the search places mode allows to obtain Google Maps results on a certain place (e.g., Apple Store in New York)<p>however, due to the pecularities of our data mining algorithm, this mode might interfere with some local-intent queries - and display res
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-maps-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-news-live-advanced
  capability: google.serp.news
  platform: google
  tier: extended
  method: POST
  path: /serp/google/news/live/advanced
  name: "Google News results for a keyword, live"
  summary: Live Google News SERP provides real-time data on top search engine results for the specified keyword, search engine, and location.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character '+' will be decoded to a space character)<p>if you need to use the "%" character for your <code>keyword</code>, pl
        example: android
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify</strong> <code>language_name</code><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
        example: en
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p>default value: <code>10</code><p>max value: <code>200</code></p><br><strong>Your account will be billed per each SERP containing up to 10 results;</strong><p>Setting depth above 10 may result in additional charges if the search'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify</strong> <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine with
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p><strong>note that this API provides results for desktop only</strong><p>choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code></p>'
      max_crawl_pages:
        type: integer
        required: false
        note: '<p><em>page crawl limit</em><p>optional field<p>number of search results pages to crawl<p>max value: <code>100</code><p><strong>Note:</strong> the <code>max_crawl_pages</code> and <code>depth</code> parameters complement each other;<p>learn more at <a href="https://dataforseo.com/help-center/what-is'
      search_param:
        type: string
        required: false
        note: <p><em>additional parameters of the search query</em><p>optional field<p><a href="https://dataforseo.com/help-center/google-search-engine-parameters-and-how-to-use-them">Get the list of available parameters and additional details here.</a></p>
      calculate_rectangles:
        type: boolean
        required: false
        note: <p><em>calculate pixel rankings for SERP elements in advanced results</em><p>optional field<p>pixel ranking refers to the distance between the result snippet and top left corner of the screen;<p><a href="https://dataforseo.com/help-center/pixel-ranking-in-serp-api">Visit Help Center to learn more&gt
      browser_screen_width:
        type: integer
        required: false
        note: <p><em>browser screen width</em><p>optional field<p>you can set a custom browser screen width to calculate pixel rankings for a particular device;<p>by default, the parameter is set to <code>1920</code>;<p><strong>Note:</strong> to use this parameter, set <code>calculate_rectangles</code> to <code>t
      browser_screen_height:
        type: integer
        required: false
        note: <p><em>browser screen height</em><p>optional field<p>you can set a custom browser screen height to calculate pixel rankings for a particular device;<p>by default, the parameter is set to <code>1080</code>;<p><strong>Note:</strong> to use this parameter, set <code>calculate_rectangles</code> to <code
      browser_screen_resolution_ratio:
        type: integer
        required: false
        note: <p><em>browser screen resolution ratio</em><p>optional field<p>you can set a custom browser screen resolution ratio to calculate pixel rankings for a particular device;<p>by default, the parameter is set to <code>1</code>;<p><strong>Note:</strong> to use this parameter, set <code>calculate_rectangle
      url:
        type: string
        required: false
        note: <p><em>direct URL of the search query</em><p>optional field<p>you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, w
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
      se_domain:
        type: string
        required: false
        note: <p><em>search engine domain</em><p>optional field<p>we choose the relevant search engine domain automatically according to the location and language you specify<p>however, you can set a custom search engine domain in this field<p>example:<p><code><em>google.co.uk</em></code>, <code><em>google.com.au
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: android
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-news-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-google-organic-live-advanced
  capability: google.serp.organic
  platform: google
  tier: extended
  method: POST
  path: /serp/google/organic/live/advanced
  name: "Google organic results for a keyword — full SERP features"
  summary: Live SERP provides real-time data on top search engine results for the specified keyword, search engine, and location.
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: <p><em>keyword</em><p><strong>required field</strong><p>you can specify <strong>up to 700 characters</strong> in the <code>keyword</code> field<p>all %## will be decoded (plus character ‘+’ will be decoded to a space character)<p>if you need to use the “%” character for your <code>keyword</code>, pl
        example: albert einstein
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p>optional field if you specify <code>language_name</code><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <code>language_code</cod
        example: en
      depth:
        type: integer
        required: false
        note: '<p><em>parsing depth</em><p>optional field<p>number of results in SERP<p><strong>default value: <code>10</code></strong><p>max value: <code>200</code></p><br><strong>Your account will be billed per each SERP containing up to 10 results;</strong><p>Setting depth above 10 may result in additional char'
      device:
        type: string
        required: false
        note: '<p><em>device type</em><p>optional field<p>return results for a specific device type<p>can take the values:<code>desktop</code>, <code>mobile</code><p>default value: <code>desktop</code></p>'
      load_async_ai_overview:
        type: boolean
        required: false
        note: '<p><em>load asynchronous ai overview</em><p>optional field<p>set to <code>true</code> to obtain <code>ai_overview</code> items is SERPs even if they are loaded asynchronously;<p>if set to <code>false</code>, you will only obtain <code>ai_overview</code> items from cache;<p>default value: <code>false'
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p>optional field if you specify <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine with their <code>language_n
      os:
        type: string
        required: false
        note: '<p><em>device operating system</em><p>optional field<p>if you specify <code>desktop</code> in the <code>device</code> field, choose from the following values: <code>windows</code>, <code>macos</code><p>default value: <code>windows</code><p>if you specify <code>mobile</code> in the <code>device</code'
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_type:
        type: string
        required: false
        note: <p><em>target match type</em><p><strong>required field if <code>stop_crawl_on_match</code> is specified</strong><p>type of match for the <code>match_value</code><p>possible values:<p><code>domain</code> – specific domain or subdomain<p><code>with_subdomains</code> – main domain and subdomains<p><cod
      match_value:
        type: string
        required: false
        note: '<p><em>target domain, subdomain, or wildcard value</em><p><strong>required field if <code>stop_crawl_on_match</code> is specified</strong><p>specify a target domain, subdomain, or wildcard value;<p><strong>Note:</strong> domain or subdomain must be specified without a request protocol;<p>example: <c'
      max_crawl_pages:
        type: integer
        required: false
        note: '<p><em>page crawl limit</em><p>optional field<p>number of search results pages to crawl<p>max value: <code>100</code><p><strong>Note:</strong> you will be charged for each page crawled (10 organic results per page);<p>learn more about pricing on our <a href="https://dataforseo.com/pricing/serp/googl'
      search_param:
        type: string
        required: false
        note: <p><em>additional parameters of the search query</em><p>optional field<p><a href="https://dataforseo.com/help-center/google-search-engine-parameters-and-how-to-use-them">get the list of available parameters and additional details here</a></p><br><strong>Note:</strong> the following search engine par
      remove_from_url:
        type: array
        required: false
        note: '<p><em>remove specific parameters from URLs</em><p>optional field<p>using this field, you can specify up to 10 parameters to remove from URLs in the result<p>example:<p><code>"remove_from_url": ["srsltid"]</code><p><strong>Note:</strong> if the <code>target</code> field is specified, the specified U'
      people_also_ask_click_depth:
        type: integer
        required: false
        note: '<p><em>clicks on the corresponding element</em><p>optional field<p>specify the click depth on the <code>people_also_ask</code> element to get additional <code>people_also_ask_element</code> items;<p><strong>Note</strong> your account will be billed $0.00015 extra for each click;<p>if the element is '
      group_organic_results:
        type: boolean
        required: false
        note: <p><em>display related results</em><p>optional field<p>if set to <code>true</code>, the <code>related_result</code> element in the response will be provided as a snippet of its parent organic result;<p>if set to <code>false</code>, the <code>related_result</code> element will be provided as a separa
      calculate_rectangles:
        type: boolean
        required: false
        note: <p><em>calcualte pixel rankings for SERP elements in advanced results</em><p>optional field<p>pixel ranking refers to the distance between the result snippet and top left corner of the screen;<p><a href="https://dataforseo.com/help-center/pixel-ranking-in-serp-api">Visit Help Center to learn more&gt
        example: true
      browser_screen_width:
        type: integer
        required: false
        note: '<p><em>browser screen width</em><p>optional field<p>you can set a custom browser screen width to calculate pixel rankings for a particular device;<p>can be specified within the following range: <code>240-9999</code>;<p>by default, the parameter is set to:<p><code>1920</code> for <code>desktop</code>'
      browser_screen_height:
        type: integer
        required: false
        note: '<p><em>browser screen height</em><p>optional field<p>you can set a custom browser screen height to calculate pixel rankings for a particular device;<p>can be specified within the following range: <code>240-9999</code>;<p>by default, the parameter is set to:<p><code>1080</code> for <code>desktop</cod'
      browser_screen_resolution_ratio:
        type: integer
        required: false
        note: '<p><em>browser screen resolution ratio</em><p>optional field<p>you can set a custom browser screen resolution ratio to calculate pixel rankings for a particular device;<p>can be specified within the following range: <code>0.5-3</code>;<p>by default, the parameter is set to:<p><code>1</code> for <cod'
      url:
        type: string
        required: false
        note: <p><em>direct URL of the search query</em><p>optional field<p>you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, w
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p>optional field if you specify <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location_coordinate</code> para
      se_domain:
        type: string
        required: false
        note: <p><em>search engine domain</em><p>optional field<p>we choose the relevant search engine domain automatically according to the location and language you specify<p>however, you can set a custom search engine domain in this field<p>example:<p><em>google.co.uk</em>, <code><code></code></code><em>google
      target:
        type: string
        required: false
        note: <p><em>target domain, subdomain, or webpage to get results for</em><p>optional field<p>a domain or a subdomain should be specified without <code>https://</code> and <code>www.</code><p>note that the results of <code>target</code>-specific tasks will only include SERP elements that contain a <code>ur
      target_search_mode:
        type: string
        required: false
        note: '<p><em>target matching mode</em><p>optional field<p>to enable this parameter, <code>stop_crawl_on_match</code> must also be enabled<p>defines how the crawl should stop when multiple targets are specified in <code>stop_crawl_on_match</code><p>possible values: <code>all</code>, <code>any</code><p><cod'
      find_targets_in:
        type: array
        required: false
        note: <p><em>SERP element types to check for targets</em><p>optional field<p>to enable this parameter, <code>stop_crawl_on_match</code> must also be enabled<p>specifies which SERP element types should be checked for target matches<p>if not specified, all first-level elements with <code>url</code> and <cod
      ignore_targets_in:
        type: array
        required: false
        note: '<p><em>SERP element types to exclude from target search</em><p>optional field<p>to enable this parameter, <code>stop_crawl_on_match</code> must also be enabled<p>specifies which SERP element types should be excluded when searching for target matches<p>possible values: <code>organic</code>, <code>pai'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
      calculate_rectangles: true
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-organic-live-advanced.json
  observed_cost: 0.004
- id: dataforseo.x.serp-google-search-by-image-task-post
  tier: extended
  platform: google
  method: POST
  path: /serp/google/search_by_image/task_post
  name: "Google reverse image search by image URL (async task)"
  summary: Google Search By Image SERP API provides up to top 100 search engine results based on the image you specified.
  input:
    bodyType: json
    body:
      image_url:
        type: string
        required: false
        note: <p><em>URL of the image</em><p><strong>required field</strong><p>the results will be based on the image you specified in this field<p>example:<p><code class="long-string">https://upload.wikimedia.org/wikipedia/commons/e/ed/Elon_Musk_Royal_Society.jpg</code></p>
        example: https://dataforseo.com/wp-content/uploads/2016/11/data_for_seo_light_429.png
      location_code:
        type: integer
        required: false
        note: '<p><em>search engine location code</em><p><strong>required field if you don''t specify</strong> <code>location_name</code>&nbsp;or <code>location_coordinate</code><p><strong>if you use this field, you don''t need to specify <code>location_name</code> or <code>location_coordinate</code></strong><p>you '
        example: 2840
      language_code:
        type: string
        required: false
        note: <p><em>search engine language code</em><p><strong>required field if you don't specify</strong> <code>language_name</code><p><strong>if you use this field, you don't need to specify <code>language_name</code></strong><p>you can receive the list of available languages of the search engine with their <
        example: en
      pingback_url:
        type: string
        required: false
        note: <p><em>notification URL of a completed task</em><p>optional field<p>when a task is completed we will notify you by GET request sent to the URL you have specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as urlencoded <code>$tag</code> variable. We will set the necessa
      postback_url:
        type: string
        required: false
        note: <p><em>URL for sending task results</em><p>optional field<p>once the task is completed, we will send a POST request with its results compressed in the <code>gzip</code> format to the <code>postback_url</code> you specified<p>you can use the ‘$id’ string as a <code>$id</code> variable and ‘$tag’ as u
      postback_data:
        type: string
        required: false
        note: <p><em>postback_url datatype</em><p><strong>required field if you specify <code>postback_url</code></strong><p>corresponds to the datatype that will be sent to your server<p>possible values:<p><code>advanced</code>, <code>html</code></p>
      priority:
        type: integer
        required: false
        note: <p><em>task priority</em><p>optional field<p>can take the following values:<p>1 – normal execution priority (set by default)<p>2 – high execution priority</p><br>You will be additionally charged for the tasks with high execution priority.<p>The cost can be calculated on the <a title="Pricing" href="
      location_name:
        type: string
        required: false
        note: <p><em>full name of search engine location</em><p><strong>required field if you don't specify</strong> <code>location_code</code> or <code>location_coordinate</code><p><strong>if you use this field, you don't need to specify <code>location_code</code> or <code>location_coordinate</code></strong><p>y
      language_name:
        type: string
        required: false
        note: <p><em>full name of search engine language</em><p><strong>required field if you don't specify</strong> <code>language_code</code><p><strong>if you use this field, you don't need to specify <code>language_code</code></strong><p>you can receive the list of available languages of the search engine with
      tag:
        type: string
        required: false
        note: <p><em>user-defined task identifier</em><p>optional field<p><em>the character limit is 255</em><p>you can use this parameter to identify the task and match it with the result<p>you will find the specified <code>tag</code> value in the <code>data</code> object of the response</p>
      max_crawl_pages:
        type: integer
        required: false
        note: '<p><em>page crawl limit</em><p>optional field<p>number of search results pages to crawl<p>max value: <code>100</code><p><strong>Note:</strong> the <code>max_crawl_pages</code> and <code>depth</code> parameters complement each other;<p>learn more at <a href="https://dataforseo.com/help-center/what-is'
      search_param:
        type: string
        required: false
        note: <p><em>additional parameters of the search query</em><p>optional field<p><a href="https://dataforseo.com/help-center/google-search-engine-parameters-and-how-to-use-them">get the list of available parameters and additional details here</a></p>
      calculate_rectangles:
        type: boolean
        required: false
        note: <p><em>calculate pixel rankings for SERP elements in advanced results</em><p>optional field<p>pixel ranking refers to the distance between the result snippet and top left corner of the screen;<p><a href="https://dataforseo.com/help-center/pixel-ranking-in-serp-api">Visit Help Center to learn more&gt
      browser_screen_width:
        type: integer
        required: false
        note: <p><em>browser screen width</em><p>optional field<p>you can set a custom browser screen width to calculate pixel rankings for a particular device;<p>by default, the parameter is set to <code>1920</code>;<p><strong>Note:</strong> to use this parameter, set <code>calculate_rectangles</code> to <code>t
      browser_screen_height:
        type: integer
        required: false
        note: <p><em>browser screen height</em><p>optional field<p>you can set a custom browser screen height to calculate pixel rankings for a particular device;<p>by default, the parameter is set to <code>1080</code>;<p><strong>Note:</strong> to use this parameter, set <code>calculate_rectangles</code> to <code
      browser_screen_resolution_ratio:
        type: integer
        required: false
        note: <p><em>browser screen resolution ratio</em><p>optional field<p>you can set a custom browser screen resolution ratio to calculate pixel rankings for a particular device;<p>by default, the parameter is set to <code>1</code>;<p><strong>Note:</strong> to use this parameter, set <code>calculate_rectangle
      location_coordinate:
        type: string
        required: false
        note: <p><em>GPS coordinates of a location</em><p><strong>required field if you don't specify</strong> <code>location_name</code>&nbsp;or <code>location_code</code><p><strong>if you use this field, you don't need to specify <code>location_name</code> or <code>location_code</code></strong><p><code>location
      se_domain:
        type: string
        required: false
        note: <p><em>search engine domain</em><p>optional field<p>we choose the relevant search engine domain automatically according to the location and language you specify<p>however, you can set a custom search engine domain in this field<p>example:<p><code><em>google.co.uk</em></code>, <code><em>google.com.au
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      image_url: https://dataforseo.com/wp-content/uploads/2016/11/data_for_seo_light_429.png
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-google-search-by-image-task-post.json
  observed_cost: 0.0006
- id: dataforseo.x.serp-screenshot
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: google
  method: POST
  path: /serp/screenshot
  name: "Screenshot of a fetched SERP by task id"
  summary: Using the Live Page Screenshot endpoint, you can capture a screenshot of any SERP page.
  docs_url: https://docs.dataforseo.com/v3/serp/screenshot/
  input:
    bodyType: json
    body:
      task_id:
        type: string
        required: false
        note: task identifier required field unique identifier of the associated task in the UUID format you will be able to use it within 7 days to request the results of the task at any time
        example: 06211235-0696-0139-1000-36727fbd3c90
      browser_preset:
        type: string
        required: false
        note: 'browser resolution preset optional field browser preset associated with a certain device type can take the following values: desktop, tablet, mobile Note: by default, browser preset corresponds to the device type specified in the POST request'
      browser_screen_width:
        type: integer
        required: false
        note: 'width of the browser resolution optional field can be specified in the following range: 240-9999 default value for desktop: 1920 default value for mobile: 390 default value for table: 1024'
      browser_screen_height:
        type: integer
        required: false
        note: 'height of the browser resolution optional field can be specified in the following range: 240-9999 default value for desktop: 1080 default value for mobile: 844 default value for table: 1366'
      browser_screen_scale_factor:
        type: number
        required: false
        note: 'browser scale factor optional field can be specified in the following range: 0.5-3 default value for desktop: 1 default value for mobile: 3 default value for table: 2'
        example: 0.5
      page:
        type: integer
        required: false
        note: 'number of SERP pages optional field if depth in the corresponding Task POST request exceeds 10 results (or 1 SERP page), specify the number of SERP pages to screenshot; default value: 1'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - task_id: 06211235-0696-0139-1000-36727fbd3c90
      browser_screen_scale_factor: 0.5
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-screenshot.json
  observed_cost: 0.004
- id: dataforseo.x.app-data-google-app-info-task-post
  tier: extended
  platform: google-play
  method: POST
  path: /app_data/google/app_info/task_post
  name: "Google Play app details by app id (async task)"
  summary: This endpoint will provide you with information about the Google Play application specified in the app_id field of the POST request.
  docs_url: https://docs.dataforseo.com/v3/app_data/google/app_info/task_post/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the app required field ID of the mobile application on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL https://play.google.com/store/apps/details?id=org.telegram.messenger the id is org.telegram.messenger'
        example: org.telegram.messenger
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if language_code is not specified if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/google/languag
      language_code:
        type: string
        required: false
        note: search engine language code required field if language_name is not specified if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/google/languages
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced, html'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: org.telegram.messenger
      location_code: 2840
      language_code: en
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-google-app-info-task-post.json
  observed_cost: 0.0006
- id: dataforseo.x.app-data-google-app-list-task-post
  tier: extended
  platform: google-play
  method: POST
  path: /app_data/google/app_list/task_post
  name: "Google Play top chart apps by collection (async task)"
  summary: This endpoint will provide you with a list of mobile applications published in the top charts on the Google Play platform.
  docs_url: https://docs.dataforseo.com/v3/app_data/google/app_list/task_post/
  input:
    bodyType: json
    body:
      app_collection:
        type: string
        required: false
        note: 'app collection required field app collection on Google Play from which apps will be collected; you can specify the following values: featured, topselling_paid, topselling_free, topselling_new_free, topselling_new_paid, topgrossing, movers_shakers Note: if featured is selected, the app_category param'
        example: topselling_free
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if language_code is not specified if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/google/languag
      language_code:
        type: string
        required: false
        note: search engine language code required field if language_name is not specified if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/google/languages
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of apps to be returned in the API response; we strongly recommend setting the parsing depth in the multiples of 100, because our system processes 100 results in a row; default value: 100; maximum value: 200; Your account will be billed per each SERP containing up '
        example: 100
      app_category:
        type: string
        required: false
        note: 'application category on Google Play optional field you can filter the results by app category; example: family; you can receive the full list of available categories by making a separate request to https://api.dataforseo.com/v3/app_data/google/categories Note: app_category cannot be used if app_coll'
      age_rating:
        type: string
        required: false
        note: 'filter results by age rating optional field you can use this field to filter the results by age rating; possible types of filtering: ages_up_to_5 — return apps approved for children up to 5 years old; ages_6_8 — return apps approved for children from 6 to 8 years old; ages_9_12 — return apps approve'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced, html'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_collection: topselling_free
      location_code: 2840
      language_code: en
      depth: 100
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-google-app-list-task-post.json
  observed_cost: 0.0012
- id: dataforseo.x.app-data-google-app-listings-search-live
  tier: extended
  platform: google-play
  method: POST
  path: /app_data/google/app_listings/search/live
  name: "Search Google Play apps — rating, reviews count, price"
  summary: 'This endpoint will provide you with a list of apps published on Google Play along with additional information: its ID, icon, reviews count, rating, price, and other data.'
  docs_url: https://docs.dataforseo.com/v3/app_data/google/app_listings/search/live/
  input:
    bodyType: json
    body:
      categories:
        type: array
        required: false
        note: app categories optional field the categories you specify are used to search for app listings; you can get the full list of available app listing categories by this link you can specify up to 10 categories
        example:
        - Tools
      description:
        type: string
        required: false
        note: keyword in the app’s description optional field keywords that occur in the description of the app; can contain up to 200 characters
        example: vpn
      title:
        type: string
        required: false
        note: keyword in the app’s title optional field keywords that occur in the title of the app; can contain up to 200 characters
        example: vpn
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % ope'
        example:
        - - item.rating.value
          - '>'
          - 4.5
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting parameter example:'
        example:
        - item.installs_count,asc
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned apps optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned apps optional field default value: 0 if you specify the 10 value, the first ten entities in the results array will be omitted and the data will be provided for the successive entities Note: we recommend using this parameter only when retrieving up to 10,000 re'
      offset_token:
        type: string
        required: false
        note: token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 100,000 results in a single request; by specifying the unique offset_token value from the response array, you will get the su
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - title: vpn
      description: vpn
      categories:
      - Tools
      order_by:
      - item.installs_count,asc
      filters:
      - - item.rating.value
        - '>'
        - 4.5
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-google-app-listings-search-live.json
  observed_cost: 0.11
- id: dataforseo.x.app-data-google-app-reviews-task-post
  tier: extended
  platform: google-play
  method: POST
  path: /app_data/google/app_reviews/task_post
  name: "Google Play reviews for an app by app id (async task)"
  summary: This endpoint will provide you with reviews published on the Google Play platform for the app specified in the app_id field.
  docs_url: https://docs.dataforseo.com/v3/app_data/google/app_reviews/task_post/
  input:
    bodyType: json
    body:
      app_id:
        type: string
        required: false
        note: 'id of the app required field ID of the mobile application on Google Play; you can find the ID in the URL of every app listed on Google Play; example: https://play.google.com/store/apps/details?id=org.telegram.messenger'
        example: org.telegram.messenger
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/google/langua
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/google/language
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of reviews to be returned in the API response; we strongly recommend setting the parsing depth in the multiples of 150, because our system processes 150 reviews in a row; default value: 150; maximum value: 100000; Your account will be billed per each SERP containi'
        example: 150
      rating:
        type: integer
        required: false
        note: 'filter reviews by rating optional field you can use this field to filter the results; possible types of filtering: 5 — return reviews with five-star rating only; 4 — return reviews with four-star rating only; 3 — return reviews with three-star rating only; 2 — return reviews with two-star rating onl'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field you can use this field to sort the results; possible types of sorting: newest — sort by the most recent reviews; most_relevant — sort by the most relevant reviews; default rule: most_relevant'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced, html'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - app_id: org.telegram.messenger
      location_code: 2840
      language_code: en
      depth: 150
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-google-app-reviews-task-post.json
  observed_cost: 0.00075
- id: dataforseo.x.app-data-google-app-searches-task-post
  tier: extended
  platform: google-play
  method: POST
  path: /app_data/google/app_searches/task_post
  name: "Google Play apps ranking for a keyword (async task)"
  summary: This endpoint will provide you with a list of apps ranking on Google Play for the specified keyword.
  docs_url: https://docs.dataforseo.com/v3/app_data/google/app_searches/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field; all %## will be decoded (plus character ‘+’ will be decoded to a space character); if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keywor
        example: vpn
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to https://api.dataforseo.co
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engine with their location_code by making a separate request to https://api.dataforseo.com/v3/app
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of search engine language optional field if you use this field, you don’t need to specify language_code you can receive the list of available languages with language_name by making a separate request to https://api.dataforseo.com/v3/app_data/google/languages example: English'
      language_code:
        type: string
        required: false
        note: 'search engine language code optional field if you use this field, you don’t need to specify language_name you can receive the list of available languages with their language_code by making a separate request to https://api.dataforseo.com/v3/app_data/google/languages example: en'
        example: en
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results to be returned to be returned from the Google Play SERP; we strongly recommend setting the parsing depth in the multiples of 30, because our system processes 30 results in a row; default value: 30; maximum value: 200; Your account will be billed per eac'
        example: 30
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the datatype that will be sent to your server possible values: advanced, html'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: vpn
      location_code: 2840
      language_code: en
      depth: 30
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.app-data-google-app-searches-task-post.json
  observed_cost: 0.0012
- id: dataforseo.x.app-data-google-locations-country
  kind: utility
  tier: extended
  platform: google-play
  method: GET
  path: /app_data/google/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Google Play location codes by country"
  summary: By calling this endpoint you will receive the list of Google locations supported in App Data API.
  docs_url: https://docs.dataforseo.com/v3/app_data/google/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.serp-naver-organic-task-post
  tier: extended
  platform: naver
  method: POST
  path: /serp/naver/organic/task_post
  name: "Naver organic search results for a keyword (async task)"
  summary: Naver SERP API provides top 15 search engine results.
  docs_url: https://docs.dataforseo.com/v3/serp/naver/organic/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keyword,
        example: albert einstein
      url:
        type: string
        required: false
        note: 'direct URL of the search query optional field you can specify a direct URL and we will sort it out to the necessary fields in most cases, we wouldn’t recommend using this method; example: https://search.naver.com/search.naver?where=nexearch&sm=top_hty&fbm=1&ie=utf8&query=iphone'
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 15 max value: 700 Your account will be billed per each SERP containing up to 15 results; Setting depth above 15 may result in additional charges if the search engine returns more than 15 results; The cost can be calculated on the '
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl default value: 1 max value: 100 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      device:
        type: string
        required: false
        note: 'device type optional field return results for a specific device type can take the values:desktop, mobile default value: desktop'
        example: desktop
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android'
      se_domain:
        type: string
        required: false
        note: 'search engine domain optional field we choose the relevant search engine domain automatically however, you can set a custom search engine domain in this field example: search.naver.com'
      search_param:
        type: string
        required: false
        note: additional parameters of the search query optional field get the list of available parameters and additional details here
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_value:
        type: string
        required: false
        note: 'target domain, subdomain, or wildcard value required field if stop_crawl_on_match is specified specify a target domain, subdomain, or wildcard value; Note: domain or subdomain must be specified without a request protocol; example: "match_value": "dataforseo.com", "match_value": "/blog/post-*"'
      match_type:
        type: string
        required: false
        note: 'target match type required field if stop_crawl_on_match is specified type of match for the match_value possible values: domain – specific domain or subdomain with_subdomains – main domain and subdomains wildcard – wildcard pattern'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: some_string_123
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
        example: https://your-server.com/postbackscript.php
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the function you used for setting a task possible values: regular, advanced, html'
        example: regular
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: albert einstein
      device: desktop
      tag: some_string_123
      postback_url: https://your-server.com/postbackscript.php
      postback_data: regular
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-naver-organic-task-post.json
  observed_cost: 0.0006
  capability: naver.serp.organic
- id: dataforseo.x.ai-optimization-perplexity-llm-responses-live
  tier: extended
  platform: ai-search
  method: POST
  path: /ai_optimization/perplexity/llm_responses/live
  name: "Perplexity answer to a prompt, live"
  summary: Live Perplexity LLM Responses endpoint allows you to retrieve structured responses from a specific Perplexity AI model, based on the input parameters.
  input:
    bodyType: json
    body:
      user_prompt:
        type: string
        required: false
        note: <em>prompt for the AI model</em><br><strong>required field</strong><br>the question or task you want to send to the AI model;<br>you can specify <strong>up to 500 characters</strong> in the <code>user_prompt</code> field
        example: provide information on how relevant the amusement park business is in France now
      model_name:
        type: string
        required: false
        note: <em>name of the AI model</em><br><strong>required field</strong><br><code>model_name</code >consists of the actual model name and version name;<br>if the basic model name is specified, its latest version will be set by default;<br>you can receive the list of available LLM models by making a separate
        example: sonar
      max_output_tokens:
        type: integer
        required: false
        note: '<em>maximum number of tokens in the AI response</em><br>optional field<br>minimum value: <code>1</code><br>maximum value: <code>4096</code>;<br>default value: <code>2048</code>;<br><strong>Note:</strong> if the reasoning model is specified in the request, the output token count may exceed the specif'
        example: 200
      temperature:
        type: number
        required: false
        note: '<em>randomness of the AI response</em><br>optional field<br>higher values make output more diverse <br>lower values make output more focused<br>minimum value: <code>0</code><br>maximum value: <code>1.9</code><br>default value: <code>0.77</code>'
        example: 0.3
      top_p:
        type: number
        required: false
        note: '<em>diversity of the AI response</em><br>optional field <br>controls diversity of the response by limiting token selection<br>minimum value: <code>0</code><br>maximum value: <code>1</code> <br>default value: <code>0.9</code>'
        example: 0.5
      web_search_country_iso_code:
        type: string
        required: false
        note: '<em>country code for web search localization</em><br>optional field<br>specify the country ISO code to get localized web search results<br><strong>Note:</strong> available only for Perplexity Sonar models<br>example: <code>US</code>'
        example: FR
      system_message:
        type: string
        required: false
        note: <em>instructions for the AI behavior</em><br>optional field<br>defines the AI's role, tone, or specific behavior <br>you can specify <strong>up to 500 characters</strong> in the <code>system_message</code> field
        example: communicate as if we are in a business meeting
      message_chain:
        type: array
        required: false
        note: 'conversation history optional field array of message objects representing previous conversation turns; each object must contain: role string with either user or ai role; message string with message content (max 500 characters); you can specify maximum of 10 message objects in the array; Note: for Pe'
        example:
        - role: user
          message: Hello, what’s up?
        - role: ai
          message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - system_message: communicate as if we are in a business meeting
      message_chain:
      - role: user
        message: Hello, what’s up?
      - role: ai
        message: Hello! I’m doing well, thank you. How can I assist you today? Are there any specific topics or projects you’d like to discuss in our meeting?
      max_output_tokens: 200
      temperature: 0.3
      top_p: 0.5
      web_search_country_iso_code: FR
      model_name: sonar
      user_prompt: provide information on how relevant the amusement park business is in France now
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.ai-optimization-perplexity-llm-responses-live.json
  observed_cost: 0.005861
  capability: ai-search.perplexity.answer
- id: dataforseo.x.serp-seznam-locations-country
  kind: utility
  tier: extended
  platform: seznam
  method: GET
  path: /serp/seznam/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Seznam location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/serp/seznam/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.serp-seznam-organic-task-post
  tier: extended
  platform: seznam
  method: POST
  path: /serp/seznam/organic/task_post
  name: "Seznam organic search results for a keyword (async task)"
  summary: Seznam SERP API provides top 10 search engine results from one of the most popular search engines in the Czech Republic.
  docs_url: https://docs.dataforseo.com/v3/serp/seznam/organic/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keyword,
        example: albert einstein
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to the https://api.dataforse
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to the https://api.dataforseo.com/v
        example: 2203
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: cs
      url:
        type: string
        required: false
        note: direct URL of the search query optional field you can specify a direct URL and we will sort it out to the necessary fields; note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL; in most cases, we wouldn’t recom
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 10; maximum value: 500; Your account will be billed per each SERP containing up to 10 results; Setting depth above 10 may result in additional charges if the search engine returns more than 10 results; The cost can be calculated o'
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl default value: 1 max value: 10 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      device:
        type: string
        required: false
        note: 'device type optional field return results for a specific device type can take the values:desktop, mobile default value: desktop'
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android'
      se_domain:
        type: string
        required: false
        note: 'search engine domain optional field we choose the relevant search engine domain automatically however, you can set a custom search engine domain in this field example: search.seznam.cz'
      search_param:
        type: string
        required: false
        note: additional parameters of the search query optional field
      calculate_rectangles:
        type: boolean
        required: false
        note: 'calculate pixel rankings for SERP elements in advanced results optional field pixel ranking refers to the distance between the result snippet and top left corner of the screen; Visit Help Center to learn more>> by default, the parameter is set to false Note: if set to true, the charge per task will '
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_value:
        type: string
        required: false
        note: 'target domain, subdomain, or wildcard value required field if stop_crawl_on_match is specified specify a target domain, subdomain, or wildcard value; Note: domain or subdomain must be specified without a request protocol; example: "match_value": "dataforseo.com", "match_value": "/blog/post-*"'
      match_type:
        type: string
        required: false
        note: 'target match type required field if stop_crawl_on_match is specified type of match for the match_value possible values: domain – specific domain or subdomain with_subdomains – main domain and subdomains wildcard – wildcard pattern'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      postback_data:
        type: string
        required: false
        note: 'postback_url datatype required field if you specify postback_url corresponds to the function you used for setting a task possible values: regular, advanced, html'
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: cs
      location_code: 2203
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-seznam-organic-task-post.json
  observed_cost: 0.0006
  capability: seznam.serp.organic
- id: dataforseo.x.business-data-tripadvisor-locations-country
  kind: utility
  tier: extended
  platform: tripadvisor
  method: GET
  path: /business_data/tripadvisor/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Tripadvisor location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/business_data/tripadvisor/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.business-data-tripadvisor-reviews-task-post
  tier: extended
  platform: tripadvisor
  method: POST
  path: /business_data/tripadvisor/reviews/task_post
  name: "Tripadvisor reviews for a business (async task)"
  summary: This endpoint provides results from the “Reviews” element on the Tripadvisor platform.
  docs_url: https://docs.dataforseo.com/v3/business_data/tripadvisor/reviews/task_post/
  input:
    bodyType: json
    body:
      url_path:
        type: string
        required: false
        note: 'URL path of the business entity required field if you do not specify keyword URL path to the Tripadvisor page of the business entity; examples: Hotel_Review-g60763-d23462501-Reviews-Margaritaville_Times_Square-New_York_City_New_York.html https://www.tripadvisor.com/Hotel_Review-g60763-d23462501-Revi'
        example: Hotel_Review-g60763-d23462501-Reviews-Margaritaville_Times_Square-New_York_City_New_York.html
      keyword:
        type: string
        required: false
        note: keyword required field if you do not specify url_path the keyword you specify should indicate a name of an existing business or prominent place on Tripadvisor; you can specify up to 700 characters in the keyword filed; all %## will be decoded (plus character ‘+’ will be decoded to a space character)
      location_name:
        type: string
        required: false
        note: 'full name of search engine location required field if you don’t specify location_code or url_path you can receive the list of available locations with location_name by making a separate request to the https://api.dataforseo.com/v3/business_data/tripadvisor/locations example: London,England,United Ki'
      location_code:
        type: integer
        required: false
        note: 'search engine location code required field if you don’t specify location_name or url_path you can receive the list of available locations with location_code by making a separate request to the https://api.dataforseo.com/v3/business_data/tripadvisor/locations example: 1003854'
        example: 1003854
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      language_name:
        type: string
        required: false
        note: full name of search engine language optional field if you use this field, your account will be charged for one extra request you can receive the list of available languages with language_name by making a separate request to the https://api.dataforseo.com/v3/business_data/tripadvisor/languages exampl
      language_code:
        type: string
        required: false
        note: 'search engine language code optional field if you use this field, your account will be charged for one extra request you can receive the list of available languages with language_code by making a separate request to the https://api.dataforseo.com/v3/business_data/tripadvisor/languages example: en Yo'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of reviews in SERP; we strongly recommend setting the parsing depth in the multiples of ten, because our systems processes ten reviews in a row; default value: 10; max value: 4490 Your account will be billed per each SERP containing up to 10 results; Setting depth'
      ratings:
        type: array
        required: false
        note: 'Tripadvisor traveler rating for a place of interest optional field rating based on the written reviews by a traveler after they visited a place. possible values: excellent, very_good, average, poor, terrible you can specify several values at once'
      visit_type:
        type: array
        required: false
        note: 'filter by type of travelers who left a review optional field possible values: families, couples, solo, business, friends you can specify several values at once'
      months:
        type: array
        required: false
        note: 'filter by months when a traveler made a visit optional field possible values: january, february, march, april, may, april, june, july, august, september, october, november, december you can specify several values at once'
      search_reviews_keyword:
        type: string
        required: false
        note: 'search reviews containing a specified keyword example: dessert'
      sort_by:
        type: string
        required: false
        note: 'results sorting parameters optional field you can use this field to sort the results; possible types of sorting: most_recent detailed_reviews'
      translate_reviews:
        type: boolean
        required: false
        note: 'translate reviews according to the URL path optional field if set to true, returned reviews will be translated to the language matching the specified url_path; for example, if url_path contains tripadvisor.it and translate_reviews is true, reviews will be translated to the Italian language; default '
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: some_string_123
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
        example: https://your-server.com/pingback.php?id=$id&tag=$tag
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - url_path: Hotel_Review-g60763-d23462501-Reviews-Margaritaville_Times_Square-New_York_City_New_York.html
      location_code: 1003854
      pingback_url: https://your-server.com/pingback.php?id=$id&tag=$tag
      tag: some_string_123
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-tripadvisor-reviews-task-post.json
  observed_cost: 0.00075
  capability: tripadvisor.business.reviews
- id: dataforseo.x.business-data-tripadvisor-search-task-post
  tier: extended
  platform: tripadvisor
  method: POST
  path: /business_data/tripadvisor/search/task_post
  name: "Search Tripadvisor business profiles (async task)"
  summary: This endpoint provides a list of business profiles listed on the Tripadvisor platform.
  docs_url: https://docs.dataforseo.com/v3/business_data/tripadvisor/search/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field the keyword you specify should indicate a business category, company name, or a prominent place; you can specify up to 700 characters in the keyword filed; all %## will be decoded (plus character ‘+’ will be decoded to a space character); if you need to use the “%” character f
        example: pizza
      location_name:
        type: string
        required: false
        note: 'full name of search engine location required field if you don’t specify location_code you can receive the list of available locations with location_name by making a separate request to the https://api.dataforseo.com/v3/business_data/tripadvisor/locations example: London,England,United Kingdom'
      location_code:
        type: integer
        required: false
        note: 'search engine location code required field if you don’t specify location_name you can receive the list of available locations with location_code by making a separate request to the https://api.dataforseo.com/v3/business_data/tripadvisor/locations example: 1003854'
        example: 1003854
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of search results to be returned from the API response we strongly recommend setting the parsing depth in the multiples of thirty because our systems processes thirty search results in a row; default value: 30; maximum value: 210 Your account will be billed per ea'
        example: 30
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: pizza
      location_code: 1003854
      depth: 30
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-tripadvisor-search-task-post.json
  observed_cost: 0.00075
  capability: tripadvisor.search.businesses
- id: dataforseo.x.business-data-trustpilot-reviews-task-post
  tier: extended
  platform: trustpilot
  method: POST
  path: /business_data/trustpilot/reviews/task_post
  name: "Trustpilot reviews for a business by domain (async task)"
  summary: This endpoint provides reviews published on the Trustpilot platform for the local establishment specified in the domain field.
  docs_url: https://docs.dataforseo.com/v3/business_data/trustpilot/reviews/task_post/
  input:
    bodyType: json
    body:
      domain:
        type: string
        required: false
        note: 'domain of the local establishment required field domain of the local establishment on Trustpilot; you can find the domain in the URL of every business listed on Trustpilot example: www.thepearlsource.com https://www.trustpilot.com/review/www.thepearlsource.com'
        example: www.thepearlsource.com
      sort_by:
        type: string
        required: false
        note: 'results sorting parameter optional field you can use this field to sort the results; possible sorting parameters: recency — most recent reviews first; relevance — most relevant reviews first; default value: relevance'
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of reviews to be returned from the API response we strongly recommend setting the parsing depth in the multiples of twenty, because our system processes twenty reviews in a row default value: 20 maximum value: 200 Your account will be billed per each SERP containi'
        example: 40
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - domain: www.thepearlsource.com
      depth: 40
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-trustpilot-reviews-task-post.json
  observed_cost: 0.0015
  capability: trustpilot.business.reviews
- id: dataforseo.x.business-data-trustpilot-search-task-post
  tier: extended
  platform: trustpilot
  method: POST
  path: /business_data/trustpilot/search/task_post
  name: "Search Trustpilot business profiles (async task)"
  summary: This endpoint provides a list of business profiles listed on the Trustpilot platform.
  docs_url: https://docs.dataforseo.com/v3/business_data/trustpilot/search/task_post/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field the keyword you specify should indicate a business category or company name; you can specify up to 700 characters in the keyword filed; all %## will be decoded (plus character ‘+’ will be decoded to a space character); if you need to use the “%” character for your keyword, ple
        example: pizza restaurant
      priority:
        type: integer
        required: false
        note: 'task priority optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the Pricing page.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of search results to be returned from the API response we strongly recommend setting the parsing depth in the multiples of twenty because our systems processes twenty search results in a row; default value: 10; maximum value: 140 Your account will be billed per ea'
        example: 20
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      postback_url:
        type: string
        required: false
        note: URL for sending task results optional field once the task is completed, we will send a POST request with its results compressed in the gzip format to the postback_url you specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: pizza restaurant
      depth: 20
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-trustpilot-search-task-post.json
  observed_cost: 0.0015
  capability: trustpilot.search.businesses
- id: dataforseo.x.backlinks-bulk-backlinks-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/bulk_backlinks/live
  name: "Backlink counts for many domains or pages"
  summary: This endpoint will provide you with the number of backlinks pointing to domains, subdomains, and pages specified in the targets array.
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: '<em>domains, subdomains or webpages to get the number of backlinks for</em><br><strong>required field</strong><br>you can set up to 1000 domains, subdomains or webpages<br>the domain or subdomain should be specified without <code>https://</code> and <code>www.</code><br>the page should be specified '
        example:
        - forbes.com
        - cnn.com
        - bbc.com
        - yelp.com
        - https://www.apple.com/iphone/
        - https://ahrefs.com/blog/
        - ibm.com
        - https://variety.com/
        - https://stackoverflow.com/
        - www.trustpilot.com
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - forbes.com
      - cnn.com
      - bbc.com
      - yelp.com
      - https://www.apple.com/iphone/
      - https://ahrefs.com/blog/
      - ibm.com
      - https://variety.com/
      - https://stackoverflow.com/
      - www.trustpilot.com
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-bulk-backlinks-live.json
  observed_cost: 0.02436
- id: dataforseo.x.backlinks-bulk-new-lost-backlinks-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/bulk_new_lost_backlinks/live
  name: "New and lost backlink counts for many domains"
  summary: This endpoint will provide you with the number of new and lost backlinks for the domains, subdomains, and pages specified in the targets array.
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: <em>domains, subdomains or webpages to get new & lost backlinks for</em><br><strong>required field</strong><br>you can set up to 1000 domains, subdomains or webpages<br>the domain or subdomain should be specified without `https://` and `www.` the page should be specified with absolute URL (including
        example:
        - forbes.com
        - cnn.com
        - bbc.com
        - yelp.com
        - https://www.apple.com/iphone/
        - https://ahrefs.com/blog/
        - ibm.com
        - https://variety.com/
        - https://stackoverflow.com/
        - www.trustpilot.com
      date_from:
        type: string
        required: false
        note: <em>starting date of the time range</em><br>optional field<br>this field indicates the date which will be used as a threshold for new and lost backlinks;<br>the backlinks that appeared in our index after the specified date will be considered as new;<br>the backlinks that weren't found after the spec
        example: '2021-01-01'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - forbes.com
      - cnn.com
      - bbc.com
      - yelp.com
      - https://www.apple.com/iphone/
      - https://ahrefs.com/blog/
      - ibm.com
      - https://variety.com/
      - https://stackoverflow.com/
      - www.trustpilot.com
      date_from: '2021-01-01'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-bulk-new-lost-backlinks-live.json
  observed_cost: 0
- id: dataforseo.x.backlinks-bulk-new-lost-referring-domains-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/bulk_new_lost_referring_domains/live
  name: "New and lost referring domain counts for many domains"
  summary: This endpoint will provide you with the number of referring domains pointing to the domains, subdomains and pages specified in the targets array.
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: <em>domains, subdomains or webpages to get new & lost referring domains for</em><br><strong>required field</strong><br>you can set up to 1000 domains, subdomains or webpages<br>the domain or subdomain should be specified without <code>https://</code> and <code>www.</code><br>the page should be speci
        example:
        - forbes.com
        - cnn.com
        - bbc.com
        - yelp.com
        - https://www.apple.com/iphone/
        - https://ahrefs.com/blog/
        - ibm.com
        - https://variety.com/
        - https://stackoverflow.com/
        - www.trustpilot.com
      date_from:
        type: string
        required: false
        note: <em>starting date of the time range</em><br>optional field<br>this field indicates the date which will be used as a threshold for new and lost referring domains;<br>the referring domains that appeared in our index after the specified date will be considered as new;<br>the referring domains that were
        example: '2023-09-01'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - forbes.com
      - cnn.com
      - bbc.com
      - yelp.com
      - https://www.apple.com/iphone/
      - https://ahrefs.com/blog/
      - ibm.com
      - https://variety.com/
      - https://stackoverflow.com/
      - www.trustpilot.com
      date_from: '2023-09-01'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-bulk-new-lost-referring-domains-live.json
  observed_cost: 0
- id: dataforseo.x.backlinks-bulk-pages-summary-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/bulk_pages_summary/live
  name: "Backlink summary for up to 1000 pages or domains"
  summary: This endpoint will provide you with a comprehensive overview of backlinks and related data for a bulk of up to 1000 pages, domains, or subdomains.
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: <em>domains, subdomains or webpages to get summary data for</em><br><strong>required field</strong><br>a domain or a subdomain should be specified without <code>https://</code> and <code>www.</code><br>a page should be specified with absolute URL (including <code>http://</code> or <code>https://</co
        example:
        - https://dataforseo.com/solutions
        - https://dataforseo.com/about-us
      include_subdomains:
        type: boolean
        required: false
        note: '<em>indicates if the subdomains of the <code>target</code> will be included in the search</em><br>optional field<br>if set to <code>false</code>, the subdomains will be ignored<br>default value: <code>true</code>'
      rank_scale:
        type: string
        required: false
        note: <em>defines the scale used for calculating and displaying the <code>rank</code>, <code>domain_from_rank</code>, and <code>page_from_rank</code> values</em><br>optional field<p>you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale<p>possible values:<br><cod
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - https://dataforseo.com/solutions
      - https://dataforseo.com/about-us
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-bulk-pages-summary-live.json
  observed_cost: 0.024072
- id: dataforseo.x.backlinks-bulk-referring-domains-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/bulk_referring_domains/live
  name: "Referring domain counts for many domains or pages"
  summary: This endpoint will provide you with the number of referring domains pointing to domains, subdomains, and pages specified in the targets array.
  docs_url: https://docs.dataforseo.com/v3/backlinks/bulk_referring_domains/live/
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: domains, subdomains or webpages to get the number of referring domains for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) exampl
        example:
        - forbes.com
        - cnn.com
        - bbc.com
        - yelp.com
        - https://www.apple.com/iphone/
        - https://ahrefs.com/blog/
        - ibm.com
        - https://variety.com/
        - https://stackoverflow.com/
        - www.trustpilot.com
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - forbes.com
      - cnn.com
      - bbc.com
      - yelp.com
      - https://www.apple.com/iphone/
      - https://ahrefs.com/blog/
      - ibm.com
      - https://variety.com/
      - https://stackoverflow.com/
      - www.trustpilot.com
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-bulk-referring-domains-live.json
  observed_cost: 0.02436
- id: dataforseo.x.backlinks-bulk-spam-score-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/bulk_spam_score/live
  name: "Spam scores for many domains or pages"
  summary: This endpoint will provide you with spam scores of the domains, subdomains, and pages you specified in the targets array.
  docs_url: https://docs.dataforseo.com/v3/backlinks/bulk_spam_score/live/
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: 'domains, subdomains or webpages to get rank for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) example: "targets": [ "forbes.com'
        example:
        - forbes.com
        - cnn.com
        - bbc.com
        - yelp.com
        - https://www.apple.com/iphone/
        - https://ahrefs.com/blog/
        - ibm.com
        - https://variety.com/
        - https://stackoverflow.com/
        - www.trustpilot.com
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - forbes.com
      - cnn.com
      - bbc.com
      - yelp.com
      - https://www.apple.com/iphone/
      - https://ahrefs.com/blog/
      - ibm.com
      - https://variety.com/
      - https://stackoverflow.com/
      - www.trustpilot.com
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-bulk-spam-score-live.json
  observed_cost: 0.02436
- id: dataforseo.x.backlinks-domain-intersection-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/domain_intersection/live
  name: "Domains linking to several targets at once"
  summary: This endpoint will provide you with the list of domains pointing to the specified websites.
  input:
    bodyType: json
    body:
      targets:
        type: object
        required: false
        note: <em>domains, subdomains or webpages to get links for</em><br><strong>required field</strong><br>you can set up to 20 domains, subdomains or webpages<br>a domain or a subdomain should be specified without <code>https://</code> and <code>www.</code><br>a page should be specified with absolute URL (inc
        example:
          '1': moz.com
          '2': ahrefs.com
      exclude_targets:
        type: array
        required: false
        note: '<em>domains, subdomains or webpages you want to exclude</em><br>optional field<br>you can specify up to 10 domains, subdomains or webpages<br>if you use this array, results will contain the referring domains that link to <code>targets</code> but don''t link to <code>exclude_targets</code><br>example:'
        example:
        - semrush.com
      filters:
        type: array
        required: false
        note: '<em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>regex</code>, '
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
        example:
        - 1.backlinks,desc
      offset:
        type: integer
        required: false
        note: '<em>offset in the array of returned results</em><br>optional field<br>default value: <code>0</code><br>if you specify the <code>10</code> value, the first ten backlinks in the results array will be omitted and the data will be provided for the successive backlinks'
      limit:
        type: integer
        required: false
        note: '<em>the maximum number of returned results</em><br>optional field<br>default value: <code>100</code><br>maximum value: <code>1000</code>'
        example: 5
      internal_list_limit:
        type: integer
        required: false
        note: <em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the following arrays:<br><code>referring_links_tld</code><br><code>referring_links_types</code><br><code>referring_links_attributes</code><br><code>referring_
      backlinks_status_type:
        type: string
        required: false
        note: '<em>set what backlinks to return and count</em><br>optional field<br>you can use this field to choose what backlinks will be returned and used for aggregated metrics for your <code>targets</code>;<br>possible values: <br><code>all</code> - all backlinks will be returned and counted;<br><code>live</c'
      backlinks_filters:
        type: array
        required: false
        note: <em>filter the backlinks of your <code>target</code></em><br>optional field<br>you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your <code>target</code><br>you can filter the backlinks by all fields available in the response of <a
      include_subdomains:
        type: boolean
        required: false
        note: '<em>indicates if the subdomains of the <code>target</code> will be included in the search</em><br>optional field<br>if set to <code>false</code>, the subdomains will be ignored<br>default value: <code>true</code>'
        example: false
      include_indirect_links:
        type: boolean
        required: false
        note: <em>indicates if indirect links to the <code>targets</code> will be included in the results</em><br>optional field<br>if set to <code>true</code>, the results will include data on indirect links pointing to a page that either redirects to a target, or points to a canonical page<br>if set to <code>fa
      exclude_internal_backlinks:
        type: boolean
        required: false
        note: '<em>indicates whether the backlinks from subdomains of the <code>target</code> are excluded</em><br>optional field<br>if set to <code>false</code>, the backlinks from subdomains of the <code>target</code> will be omitted and you won''t receive the same domain in the response;<br>default value: <code>'
        example: true
      intersection_mode:
        type: string
        required: false
        note: '<em>indicates whether to intersect backlinks</em><br>optional field<br>use this field to intersect or merge results for the specified domains<br>possible values: <code>all</code>, <code>partial</code><br><code>all</code> - results are based on all backlinks;<br><code>partial</code> - results are bas'
      rank_scale:
        type: string
        required: false
        note: <em>defines the scale used for calculating and displaying the <code>rank</code>, <code>domain_from_rank</code>, and <code>page_from_rank</code> values</em><br>optional field<p>you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale<p>possible values:<br><cod
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
        '1': moz.com
        '2': ahrefs.com
      include_subdomains: false
      exclude_targets:
      - semrush.com
      limit: 5
      order_by:
      - 1.backlinks,desc
      exclude_internal_backlinks: true
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-domain-intersection-live.json
  observed_cost: 0.02418
- id: dataforseo.x.backlinks-domain-pages-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/domain_pages/live
  name: "Pages of a domain with backlinks per page"
  summary: This endpoint will provide you with a detailed overview of domain pages with backlink data for each page.
  docs_url: https://docs.dataforseo.com/v3/backlinks/domain_pages/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: 'domain or subdomain required field a domain or a subdomain should be specified without https:// and www. example: forbes.com'
        example: forbes.com
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned pages optional field default value: 100 maximum value: 1000'
        example: 5
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned pages optional field default value: 0 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages'
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 1'
      backlinks_status_type:
        type: string
        required: false
        note: 'set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost –'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, <>, in, not_in, like, not_like, ilike, not_ilike, match, not_match'
        example:
        - - page_summary.backlinks
          - '>'
          - 5
        - and
        - - page
          - like
          - '%sites%'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["pa'
      backlinks_filters:
        type: array
        required: false
        note: filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can in
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      exclude_internal_backlinks:
        type: boolean
        required: false
        note: 'indicates if internal backlinks from subdomains to the target will be excluded from the results optional field if set to true, the results will not include data on internal backlinks from subdomains of the same domain as target if set to false, internal links will be included in the results default '
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_t'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: forbes.com
      limit: 5
      filters:
      - - page_summary.backlinks
        - '>'
        - 5
      - and
      - - page
        - like
        - '%sites%'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-domain-pages-live.json
  observed_cost: 0.02418
- id: dataforseo.x.backlinks-domain-pages-summary-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/domain_pages_summary/live
  name: "Backlink summary for every page of a domain"
  summary: This endpoint will provide you with detailed summary data on all backlinks and related metrics for each page of the target domain or subdomain you specify.
  docs_url: https://docs.dataforseo.com/v3/backlinks/domain_pages_summary/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain, subdomain or webpage to get summary data for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://)
        example: forbes.com
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned anchors optional field default value: 100 maximum value: 1000'
        example: 4
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned anchors optional field default value: 0 if you specify the 10 value, the first ten anchors in the results array will be omitted and the data will be provided for the successive anchors'
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 1'
      backlinks_status_type:
        type: string
        required: false
        note: 'set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, <>, in, not_in, like, not_like, ilike, not_ilike, match, not_match'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["ba'
        example:
        - backlinks,desc
      backlinks_filters:
        type: array
        required: false
        note: filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can in
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains of the target domain will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      include_indirect_links:
        type: boolean
        required: false
        note: indicates if indirect links to the target will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to the target, or points to a canonical page if set to false, indirect links will be ignored default value
      exclude_internal_backlinks:
        type: boolean
        required: false
        note: 'indicates whether the backlinks from subdomains of the target are excluded optional field if set to false, backlinks from the subdomains of the target domain will be ommited and you won’t receive the same domain in the response; default value: true'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_t'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: forbes.com
      limit: 4
      order_by:
      - backlinks,desc
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-domain-pages-summary-live.json
  observed_cost: 0.024144
- id: dataforseo.x.backlinks-history-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/history/live
  name: "Backlink history for a domain back to 2019"
  summary: This endpoint will provide you with historical backlinks data back to the beginning of 2019.
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: <em>domain</em><br><strong>required field</strong><br>a domain should be specified without <code>https://</code> and <code>www.</code>
        example: cnn.com
      date_from:
        type: string
        required: false
        note: '<em>starting date of the time range</em><br>optional field<br>minimum value <code>2019-01-01</code><br>if you don''t specify this field, the minimum value will be used by default<br>date format: <code>"yyyy-mm-dd"</code><br>example:<br><code>"2019-01-15"</code>'
        example: '2020-01-01'
      date_to:
        type: string
        required: false
        note: '<em>ending date of the time range</em><br>optional field<br>if you don''t specify this field, the today''s date will be used by default<br>date format: <code>"yyyy-mm-dd"</code><br>example:<br><code>"2019-01-15"</code>'
        example: '2021-01-01'
      rank_scale:
        type: string
        required: false
        note: <em>defines the scale used for calculating and displaying the <code>rank</code>, <code>domain_from_rank</code>, and <code>page_from_rank</code> values</em><br>optional field<p>you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale<p>possible values:<br><cod
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: cnn.com
      date_from: '2020-01-01'
      date_to: '2021-01-01'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-history-live.json
  observed_cost: 0.024324
- id: dataforseo.x.backlinks-index
  kind: utility
  tier: extended
  platform: web
  method: GET
  path: /backlinks/index
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Backlink index totals — backlinks, domains, pages"
  summary: This endpoint will provide you with the total number of backlinks, domains, and pages our database contains for the moment when you make a request.
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.backlinks-page-intersection-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/page_intersection/live
  name: "Pages linking to several targets at once"
  summary: This endpoint will provide you with the list of referring pages pointing to the specified targets.
  docs_url: https://docs.dataforseo.com/v3/backlinks/page_intersection/live/
  input:
    bodyType: json
    body:
      targets:
        type: object
        required: false
        note: 'domains, subdomains or webpages to get links for required field you can set up to 20 domains, subdomains or webpages a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) example: "targets": { "1": "http://p'
        example:
          '1': football.com
          '2': fifa.com
      exclude_targets:
        type: array
        required: false
        note: 'domains, subdomains or webpages you want to exclude optional field you can set up to 10 domains, subdomains or webpages if you use this array, results will contain the referring pages that link to targets but don’t link to exclude_targets example: "exclude_targets": [ "bbc.com", "https://www.apple.c'
        example:
        - skysports.com
      backlinks_status_type:
        type: string
        required: false
        note: 'set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your targets; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned an'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, <>, in, not_in, like, not_like, ilike, not_ilike, match, not_match'
        example:
        - - 2.domain_from_rank
          - '>'
          - 400
        - and
        - - 1.dofollow
          - '='
          - true
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["ra'
        example:
        - 1.rank,desc
      offset:
        type: integer
        required: false
        note: 'offset in the results array of the returned backlinks optional field default value: 0 if you specify the 10 value, the first ten backlinks in the results array will be omitted and the data will be provided for the successive backlinks'
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned backlinks optional field default value: 100 maximum value: 1000'
        example: 5
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: attributes domain_from_platform_type default value: 10 maximum value: 1000'
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains of the targets will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      include_indirect_links:
        type: boolean
        required: false
        note: 'indicates if indirect links to the targets will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to a target, or points to a canonical page if set to false, indirect links will be ignored default value:'
      exclude_internal_backlinks:
        type: boolean
        required: false
        note: indicates if internal backlinks from subdomains to the target will be excluded from the results optional field if set to true, the results will not include data on internal backlinks from subdomains of the same domain as target if set to false, internal links will be included in the result default v
      intersection_mode:
        type: string
        required: false
        note: 'indicates whether to intersect backlinks optional field use this field to intersect or merge results for the specified URLs possible values: all, partial all – results are based on all backlinks; partial – results are based on the intersecting backlinks only; default value: all'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_t'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
        '1': football.com
        '2': fifa.com
      exclude_targets:
      - skysports.com
      limit: 5
      order_by:
      - 1.rank,desc
      filters:
      - - 2.domain_from_rank
        - '>'
        - 400
      - and
      - - 1.dofollow
        - '='
        - true
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-page-intersection-live.json
  observed_cost: 0.02418
- id: dataforseo.x.backlinks-referring-networks-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/referring_networks/live
  name: "Referring IPs and subnets for a domain or page"
  summary: This endpoint will provide you with a detailed overview of referring IPs and subnets pointing to the target you specify.
  docs_url: https://docs.dataforseo.com/v3/backlinks/referring_networks/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: domain, subdomain or webpage to get referring networks for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://)
        example: backlinko.com
      network_address_type:
        type: string
        required: false
        note: 'indicates the type of network to get data for optional field possible values: ip, subnet default value: ip'
        example: subnet
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned networks optional field default value: 100 maximum value: 1000'
        example: 5
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned networks optional field default value: 0 if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive pages'
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 1'
      backlinks_status_type:
        type: string
        required: false
        note: 'set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, <>, in, not_in, like, not_like, ilike, not_ilike, match, not_match'
        example:
        - backlinks
        - '>'
        - 100
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["ba'
        example:
        - rank,desc
      backlinks_filters:
        type: array
        required: false
        note: filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can in
        example:
        - dofollow
        - '='
        - true
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      include_indirect_links:
        type: boolean
        required: false
        note: indicates if indirect links to the target will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to the target, or points to a canonical page if set to false, indirect links will be ignored default value
      exclude_internal_backlinks:
        type: boolean
        required: false
        note: 'indicates whether the backlinks from subdomains of the target are excluded optional field if set to false, the backlinks from subdomains of the target will be ommited and you won’t receive the same domain in the response; default value: true'
        example: true
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_t'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: backlinko.com
      network_address_type: subnet
      limit: 5
      order_by:
      - rank,desc
      exclude_internal_backlinks: true
      backlinks_filters:
      - dofollow
      - '='
      - true
      filters:
      - backlinks
      - '>'
      - 100
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-referring-networks-live.json
  observed_cost: 0.02418
- id: dataforseo.x.backlinks-timeseries-new-lost-summary-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/timeseries_new_lost_summary/live
  name: "New and lost backlinks over time for a domain"
  summary: This endpoint will provide you with the number of new and lost backlinks and referring domains for the domain specified in the target field.
  docs_url: https://docs.dataforseo.com/v3/backlinks/timeseries_new_lost_summary/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: 'domain to get data for required field a domain should be specified without https:// and www. example: "forbes.com"'
        example: dataforseo.com
      date_from:
        type: string
        required: false
        note: starting date of the time range optional field this field indicates the date which will be used as a threshold for new and lost backlinks and referring domains; the backlinks and referring domains that appeared in our index after the specified date will be considered as new; the backlinks and referr
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default minimum value shouldn’t preceed the date specified in the date_from maximum value: today’s date date format: "yyyy-mm-dd" example: "2021-01-15"'
        example: '2026-06-24'
      group_range:
        type: string
        required: false
        note: 'time range which will be used to group the results optional field default value: month possible values: day, week, month, year note: for day, we will return items corresponding to all dates between and including date_from and date_to; for week/month/year, we will return items corresponding to full w'
        example: month
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
      date_from: '2026-04-24'
      date_to: '2026-06-24'
      group_range: month
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-timeseries-new-lost-summary-live.json
  observed_cost: 0.024108
- id: dataforseo.x.backlinks-timeseries-summary-live
  tier: extended
  platform: web
  method: POST
  path: /backlinks/timeseries_summary/live
  name: "Backlink totals over time for a domain, by date range"
  summary: This endpoint will provide you with an overview of backlink data for the target domain available during a period between the two indicated dates.
  docs_url: https://docs.dataforseo.com/v3/backlinks/timeseries_summary/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: 'domain to get data for required field a domain should be specified without https:// and www. example: "forbes.com"'
        example: dataforseo.com
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field this field indicates the date which will be used as a threshold for summary data; minimum value: 2019-01-30 maximum value shouldn’t exceed the date specified in the date_to date format: "yyyy-mm-dd" example: "2021-01-01"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default minimum value shouldn’t preceed the date specified in the date_from maximum value: today’s date date format: "yyyy-mm-dd" example: "2021-01-15"'
        example: '2026-06-24'
      group_range:
        type: string
        required: false
        note: 'time range which will be used to group the results optional field default value: month possible values: day, week, month, year note: for day, we will return items corresponding to all dates between and including date_from and date_to; for week/month/year, we will return items corresponding to full w'
        example: month
      include_subdomains:
        type: boolean
        required: false
        note: 'indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_t'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
      date_from: '2026-04-24'
      date_to: '2026-06-24'
      group_range: month
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.backlinks-timeseries-summary-live.json
  observed_cost: 0.024108
- id: dataforseo.x.business-data-business-listings-categories-aggregation-live
  tier: extended
  platform: web
  method: POST
  path: /business_data/business_listings/categories_aggregation/live
  name: "Business listing counts by related category"
  summary: Business Listings Categories Aggregation endpoint provides results containing information about groups of related categories along with the number of entities in each category.
  input:
    bodyType: json
    body:
      categories:
        type: array
        required: false
        note: <em>business categories</em><br>optional field<br>the categories you specify are used to search for business listings;<br>if you don't use this field, we will return business listings found in the specified location;<br>you can specify <strong>up to 10 categories</strong>
        example:
        - pizza_restaurant
      description:
        type: string
        required: false
        note: <em>description of the element in SERP</em><br>optional field<br>the description of the business entity for which the results are collected;<br>can contain up to 200 characters
        example: pizza
      title:
        type: string
        required: false
        note: <em>title of the element in SERP</em><br>optional field<br>the name of the business entity for which the results are collected;<br>can contain up to 200 characters
        example: pizza
      is_claimed:
        type: boolean
        required: false
        note: <em>indicates whether the business is verified by its owner on Google Maps</em><br>optional field
        example: true
      location_coordinate:
        type: string
        required: false
        note: '<em>GPS coordinates of a location</em><br>optional field<br><code>location_coordinate</code> parameter should be specified in the <em>"latitude,longitude,radius"</em> format<br>the maximum number of decimal digits for <em>"latitude"</em> and <em>"longitude"</em>: 7<br>the minimum value for <em>"radi'
        example: 53.476225,-2.243572,10
      initial_dataset_filters:
        type: array
        required: false
        note: '<em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>regex</code>, '
        example:
        - - rating.value
          - '>'
          - 3
      internal_list_limit:
        type: integer
        required: false
        note: '<em>maximum number of elements within internal arrays</em><br>optional field<br>you can use this field to limit the number of elements within the aggregated categories<br>default value: <code>10</code>'
      limit:
        type: integer
        required: false
        note: '<em>the maximum number of returned businesses</em><br>optional field<br>default value: <code>100</code><br>maximum value: <code>1000</code>'
        example: 3
      offset:
        type: integer
        required: false
        note: <em>the maximum number of returned businesses</em><br>optional field
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - categories:
      - pizza_restaurant
      description: pizza
      title: pizza
      is_claimed: true
      location_coordinate: 53.476225,-2.243572,10
      initial_dataset_filters:
      - - rating.value
        - '>'
        - 3
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-business-listings-categories-aggregation-live.json
  observed_cost: 0.01308
- id: dataforseo.x.business-data-business-listings-search-live
  tier: extended
  platform: web
  method: POST
  path: /business_data/business_listings/search/live
  name: "Search Google Maps businesses by category and location"
  summary: Business Listings Search API provides results containing information about business entities listed on Google Maps in the specified categories.
  input:
    bodyType: json
    body:
      categories:
        type: array
        required: false
        note: <em>business categories</em><br>optional field<br>the categories you specify are used to search for business listings;<br>if you don't use this field, we will return business listings found in the specified location;<br>you can specify <strong>up to 10 categories</strong>
        example:
        - pizza_restaurant
      description:
        type: string
        required: false
        note: <em>description of the element in SERP</em><br>optional field<br>the description of the business entity for which the results are collected;<br>can contain up to 200 characters
        example: pizza
      title:
        type: string
        required: false
        note: <em>title of the element in SERP</em><br>optional field<br>the name of the business entity for which the results are collected;<br>can contain up to 200 characters
        example: pizza
      is_claimed:
        type: boolean
        required: false
        note: <em>indicates whether the business is verified by its owner on Google Maps</em><br>optional field
        example: true
      location_coordinate:
        type: string
        required: false
        note: '<em>GPS coordinates of a location</em><br>optional field<br><code>location_coordinate</code> parameter should be specified in the <em>"latitude,longitude,radius"</em> format<br>the maximum number of decimal digits for <em>"latitude"</em> and <em>"longitude"</em>: 7<br>the value of <em>"radius"</em> '
        example: 53.476225,-2.243572,10
      filters:
        type: array
        required: false
        note: '<em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>regex</code>, '
        example:
        - - rating.value
          - '>'
          - 3
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
        example:
        - rating.value,desc
      limit:
        type: integer
        required: false
        note: '<em>the maximum number of returned businesses</em><br>optional field<br>default value: <code>100</code><br>maximum value: <code>1000</code>'
        example: 3
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of returned businesses</em><br>optional field<br>default value: <code>0</code><br>if you specify the <code>10</code> value, the first ten entities in the results array will be omitted and the data will be provided for the successive entities<br><strong>Note:</strong> '
      offset_token:
        type: string
        required: false
        note: <em>token for subsequent requests</em><br>optional field<br>provided in the identical filed of the response to each request;<br>use this parameter to avoid timeouts while trying to obtain over 100,000 results in a single request;<br>by specifying the unique <code>offset_token</code> value from the r
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - categories:
      - pizza_restaurant
      description: pizza
      title: pizza
      is_claimed: true
      location_coordinate: 53.476225,-2.243572,10
      order_by:
      - rating.value,desc
      filters:
      - - rating.value
        - '>'
        - 3
      limit: 3
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-business-listings-search-live.json
  observed_cost: 0.01308
- id: dataforseo.x.business-data-social-media-pinterest-live
  tier: extended
  platform: pinterest
  method: POST
  path: /business_data/social_media/pinterest/live
  name: "Pinterest pins made from a list of URLs"
  summary: Social Media Pinterest API will provide you with data on pins made from the specified URLs.
  docs_url: https://docs.dataforseo.com/v3/business_data/social_media/pinterest/live/
  input:
    bodyType: json
    body:
      targets:
        type: array
        required: false
        note: 'target URLs required field target page should be specified with its absolute URL (including http:// or https://) example: https://dataforseo.com/ Note: you can specify 10 targets maximum. You will be charged per earch URL you specify in this array'
        example:
        - https://www.simplyrecipes.com/recipes/grilled_salmon_with_cucumber_mango_salsa/
        - https://tasty.co/recipe/classic-lasagna
        - https://www.allrecipes.com/recipe/255263/sicilian-roasted-chicken/
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: some_string_123
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - targets:
      - https://www.simplyrecipes.com/recipes/grilled_salmon_with_cucumber_mango_salsa/
      - https://tasty.co/recipe/classic-lasagna
      - https://www.allrecipes.com/recipe/255263/sicilian-roasted-chicken/
      tag: some_string_123
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.business-data-social-media-pinterest-live.json
  observed_cost: 0.00012
- id: dataforseo.x.content-analysis-category-trends-live
  tier: extended
  platform: web
  method: POST
  path: /content_analysis/category_trends/live
  name: "Citation trends for a category over a date range"
  summary: This endpoint will provide you with data on all citations in the target category for the indicated date range.
  docs_url: https://docs.dataforseo.com/v3/content_analysis/category_trends/live/
  input:
    bodyType: json
    body:
      category_code:
        type: integer
        required: false
        note: target category code required field to obtain a full list of available categories, refer to the Categories endpoint
        example: 10994
      page_type:
        type: array
        required: false
        note: 'target page types optional field use this parameter to filter the dataset by page types possible values: "ecommerce", "news", "blogs", "message-boards", "organization"'
      search_mode:
        type: string
        required: false
        note: 'results grouping type optional field possible grouping types: as_is – returns data on all citations for the target category_code one_per_domain – returns data on one citation of the category_code per domain default value: as_is'
        example: as_is
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: top_domains text_categories page_categories countries languages default value: 1 maximum value: 20'
      date_from:
        type: string
        required: false
        note: 'starting date of the time range required field minimum value: 2022-10-31 date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15"'
      date_group:
        type: string
        required: false
        note: 'time range which will be used to group the results optional field default value: month possible values: day, week, month'
        example: month
      initial_dataset_filters:
        type: array
        required: false
        note: 'initial dataset filtering parameters optional field initial filtering parameters that apply to fields in the Search endpoint; you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_reg'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - category_code: 10994
      search_mode: as_is
      date_from: '2026-04-24'
      date_group: month
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.content-analysis-category-trends-live.json
  observed_cost: 0.024144
- id: dataforseo.x.content-analysis-phrase-trends-live
  tier: extended
  platform: web
  method: POST
  path: /content_analysis/phrase_trends/live
  name: "Citation trends for a keyword over a date range"
  summary: This endpoint will provide you with data on all citations of the target keyword for the indicated date range.
  docs_url: https://docs.dataforseo.com/v3/content_analysis/phrase_trends/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: 'target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note: to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: "keyword": "\"tesla palo alto\"" learn more about rules and limitations of keyword and keywo'
        example: logitech
      keyword_fields:
        type: object
        required: false
        note: 'target keyword fields and target keywords optional field use this parameter to filter the dataset by keywords that certain fields should contain; fields you can specify: title, main_title, previous_title, snippet you can indicate several fields; Note: to match an exact phrase instead of a stand-alon'
      page_type:
        type: array
        required: false
        note: 'target page types optional field use this parameter to filter the dataset by page types possible values: "ecommerce", "news", "blogs", "message-boards", "organization"'
      search_mode:
        type: string
        required: false
        note: 'results grouping type optional field possible grouping types: as_is – returns data on all citations for the target keyword one_per_domain – returns data on one citation of the keyword per domain default value: as_is'
        example: as_is
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: top_domains text_categories page_categories countries languages default value: 1 maximum value: 20'
      date_from:
        type: string
        required: false
        note: 'starting date of the time range required field date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15"'
      date_group:
        type: string
        required: false
        note: 'time range which will be used to group the results optional field default value: month possible values: day, week, month'
        example: month
      initial_dataset_filters:
        type: array
        required: false
        note: 'initial dataset filtering parameters optional field initial filtering parameters that apply to fields in the Search endpoint; you can add several filters at once (8 filters maximum); you should set a logical operator and, or between the conditions; the following operators are supported: regex, not_r'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: logitech
      search_mode: as_is
      date_from: '2026-04-24'
      date_group: month
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.content-analysis-phrase-trends-live.json
  observed_cost: 0.024144
- id: dataforseo.x.content-analysis-rating-distribution-live
  tier: extended
  platform: web
  method: POST
  path: /content_analysis/rating_distribution/live
  name: "Rating distribution of citations for a keyword"
  summary: This endpoint will provide you with rating distribution data for the keyword and other parameters specified in the request.
  docs_url: https://docs.dataforseo.com/v3/content_analysis/rating_distribution/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: 'target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note: to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: "keyword": "\"tesla palo alto\"" learn more about rules and limitations of keyword and keywo'
        example: logitech
      keyword_fields:
        type: object
        required: false
        note: 'target keyword fields and target keywords optional field use this parameter to filter the dataset by keywords that certain fields should contain; fields you can specify: title, main_title, previous_title, snippet you can indicate several fields; Note: to match an exact phrase instead of a stand-alon'
      page_type:
        type: array
        required: false
        note: 'target page types optional field use this parameter to filter the dataset by page types possible values: "ecommerce", "news", "blogs", "message-boards", "organization"'
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: top_domains text_categories page_categories countries languages default value: 1 maximum value: 20'
        example: 10
      search_mode:
        type: string
        required: false
        note: 'results grouping type optional field possible grouping types: as_is – returns all citations for the target keyword one_per_domain – returns one citation of the keyword per domain default value: as_is'
        example: as_is
      positive_connotation_threshold:
        type: number
        required: false
        note: positive connotation threshold optional field specified as the probability index threshold for positive sentiment related to the citation content if you specify this field, connotation_types object in the response will only contain data on citations with positive sentiment probability more than or e
      sentiments_connotation_threshold:
        type: number
        required: false
        note: sentiment connotation threshold optional field specified as the probability index threshold for sentiment connotations related to the citation content if you specify this field, sentiment_connotations object in the response will only contain data on citations where the probability per each sentiment
      initial_dataset_filters:
        type: array
        required: false
        note: 'initial dataset filtering parameters optional field initial filtering parameters that apply to fields in the Search endpoint you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_rege'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: logitech
      search_mode: as_is
      internal_list_limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.content-analysis-rating-distribution-live.json
  observed_cost: 0.024
- id: dataforseo.x.content-analysis-search-live
  tier: extended
  platform: web
  method: POST
  path: /content_analysis/search/live
  name: "Search citations of a keyword"
  summary: This endpoint will provide you with detailed citation data available for the target keyword.
  docs_url: https://docs.dataforseo.com/v3/content_analysis/search/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: 'target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note: to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: "keyword": "\"tesla palo alto\"" learn more about rules and limitations of keyword and keywo'
        example: logitech
      keyword_fields:
        type: object
        required: false
        note: 'target keyword fields and target keywords optional field use this parameter to filter the dataset by keywords that certain fields should contain; fields you can specify: title, main_title, previous_title, snippet you can indicate several fields; Note: to match an exact phrase instead of a stand-alon'
        example:
          snippet: logitech
      page_type:
        type: array
        required: false
        note: 'target page types optional field use this parameter to filter the dataset by page types possible values: "ecommerce", "news", "blogs", "message-boards", "organization"'
        example:
        - ecommerce
        - news
        - blogs
        - message-boards
        - organization
      search_mode:
        type: string
        required: false
        note: 'results grouping type optional field possible grouping types: as_is – returns all citations for the target keyword one_per_domain – returns one citation of the keyword per domain default value: as_is'
        example: as_is
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned citations optional field default value: 100 maximum value: 1000'
        example: 10
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like,not_like, match, not_match you '
        example:
        - main_domain
        - '='
        - reviewfinder.ca
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["co'
        example:
        - content_info.sentiment_connotations.anger,desc
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned citations optional field default value: 0 if you specify the 10 value, the first ten citations in the results array will be omitted and the data will be provided for the successive citations Note: we recommend using this parameter only when retrieving up to 10'
      offset_token:
        type: string
        required: false
        note: 'offset token for subsequent requests optional field provided in the identical field of the response to each request; use this parameter to avoid timeouts while trying to obtain over 10,000 results in a single request; by specifying the unique offset_token value from the response array, you will get '
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the domain_rank, and url_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank va'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword_fields:
        snippet: logitech
      keyword: logitech
      page_type:
      - ecommerce
      - news
      - blogs
      - message-boards
      - organization
      search_mode: as_is
      filters:
      - main_domain
      - '='
      - reviewfinder.ca
      order_by:
      - content_info.sentiment_connotations.anger,desc
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.content-analysis-search-live.json
  observed_cost: 0.024036
- id: dataforseo.x.content-analysis-sentiment-analysis-live
  tier: extended
  platform: web
  method: POST
  path: /content_analysis/sentiment_analysis/live
  name: "Sentiment of citations for a keyword"
  summary: This endpoint will provide you with sentiment analysis data for the citations available for the target keyword.
  docs_url: https://docs.dataforseo.com/v3/content_analysis/sentiment_analysis/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: 'target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note: to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: "keyword": "\"tesla palo alto\"" learn more about rules and limitations of keyword and keywo'
        example: logitech
      keyword_fields:
        type: object
        required: false
        note: 'target keyword fields and target keywords optional field use this parameter to filter the dataset by keywords that certain fields should contain; fields you can specify: title, main_title, previous_title, snippet you can indicate several fields; Note: to match an exact phrase instead of a stand-alon'
      page_type:
        type: array
        required: false
        note: 'target page types optional field use this parameter to filter the dataset by page types possible values: "ecommerce", "news", "blogs", "message-boards", "organization"'
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: top_domains text_categories page_categories countries languages default value: 1 maximum value: 20'
        example: 1
      positive_connotation_threshold:
        type: number
        required: false
        note: positive connotation threshold optional field specified as the probability index threshold for positive sentiment related to the citation content if you specify this field, connotation_types object in the response will only contain data on citations with positive sentiment probability more than or e
      sentiments_connotation_threshold:
        type: number
        required: false
        note: sentiment connotation threshold optional field specified as the probability index threshold for sentiment connotations related to the citation content if you specify this field, sentiment_connotations object in the response will only contain data on citations where the probability per each sentiment
      initial_dataset_filters:
        type: array
        required: false
        note: 'initial dataset filtering parameters optional field initial filtering parameters that apply to fields in the Search endpoint you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_rege'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: logitech
      internal_list_limit: 1
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.content-analysis-sentiment-analysis-live.json
  observed_cost: 0.024036
- id: dataforseo.x.content-analysis-summary-live
  tier: extended
  platform: web
  method: POST
  path: /content_analysis/summary/live
  name: "Citation overview for a keyword"
  summary: This endpoint will provide you with an overview of citation data available for the target keyword.
  docs_url: https://docs.dataforseo.com/v3/content_analysis/summary/live/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: 'target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note: to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: "keyword": "\"tesla palo alto\"" learn more about rules and limitations of keyword and keywo'
        example: logitech
      keyword_fields:
        type: object
        required: false
        note: 'target keyword fields and target keywords optional field use this parameter to filter the dataset by keywords that certain fields should contain; fields you can specify: title, main_title, previous_title, snippet you can indicate several fields; Note: to match an exact phrase instead of a stand-alon'
      page_type:
        type: array
        required: false
        note: 'target page types optional field use this parameter to filter the dataset by page types possible values: "ecommerce", "news", "blogs", "message-boards", "organization"'
        example:
        - ecommerce
        - news
        - blogs
        - message-boards
        - organization
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: top_domains text_categories page_categories countries languages default value: 1 maximum value: 20'
        example: 8
      positive_connotation_threshold:
        type: number
        required: false
        note: positive connotation threshold optional field specified as the probability index threshold for positive sentiment related to the citation content if you specify this field, connotation_types object in the response will only contain data on citations with positive sentiment probability more than or e
        example: 0.5
      sentiments_connotation_threshold:
        type: number
        required: false
        note: sentiment connotation threshold optional field specified as the probability index threshold for sentiment connotations related to the citation content if you specify this field, sentiment_connotations object in the response will only contain data on citations where the probability per each sentiment
      initial_dataset_filters:
        type: array
        required: false
        note: 'initial dataset filtering parameters optional field initial filtering parameters that apply to fields in the Search endpoint you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_rege'
      rank_scale:
        type: string
        required: false
        note: 'defines the scale used for calculating and displaying the rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keyword: logitech
      page_type:
      - ecommerce
      - news
      - blogs
      - message-boards
      - organization
      internal_list_limit: 8
      positive_connotation_threshold: 0.5
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.content-analysis-summary-live.json
  observed_cost: 0.024036
- id: dataforseo.x.domain-analytics-technologies-aggregation-technologies-live
  tier: extended
  platform: web
  method: POST
  path: /domain_analytics/technologies/aggregation_technologies/live
  name: "Technologies used alongside a given technology"
  summary: The Aggregation Technologies endpoint will provide you with a list of the most popular technologies websites use alongside the technologies you specify.
  docs_url: https://docs.dataforseo.com/v3/domain_analytics/technologies/aggregation_technologies/live/
  input:
    bodyType: json
    body:
      group:
        type: string
        required: false
        note: 'id of the target technology group required field if you don’t specify technology, category or keyword at least one field (group, category, keyword, technology) must be set you can find the full list of technology group ids on this page example: "marketing"'
      category:
        type: string
        required: false
        note: 'id of the target technology category required field if you don’t specify group, keyword or technology at least one field (group, category, keyword, technology) must be set you can find the full list of technology category ids on this page example: "crm"'
      technology:
        type: string
        required: false
        note: 'target technology required field if you don’t specify group, keyword or category at least one field (group, category, keyword, technology) must be set you can find the full list of technologies on this page example: "Salesforce"'
        example: Nginx
      keyword:
        type: string
        required: false
        note: 'target keyword in the domain’s meta keywords required field if you don’t specify group, category or technology at least one field (group, category, keyword, technology) must be set UTF-8 encoding example: "seo"learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs i'
        example: WordPress
      mode:
        type: string
        required: false
        note: 'search mode optional field possible search mode types: as_is – search for results exactly matching the specified group ids, category ids, or technology names entry – search for results matching a part of the specified group ids, category ids, or technology names default value: as_is'
        example: entry
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in, like,not_like you can use the % operator with like and'
        example:
        - - country_iso_code
          - '='
          - US
        - and
        - - domain_rank
          - '>'
          - 800
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the following values to sort the results: groups_count, categories_count, technologies_count possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to se'
        example:
        - groups_count,desc
      internal_groups_list_limit:
        type: integer
        required: false
        note: 'maximum number of returned technology groups optional field you can use this field to limit the number of items with identical "group" in the results default value: 5 minimum value: 1 maximum value: 10000'
      internal_categories_list_limit:
        type: integer
        required: false
        note: 'maximum number of returned technology categories within the same group optional field you can use this field to limit the number of items with identical "category" in the results default value: 5 minimum value: 1 maximum value: 10000'
      internal_technologies_list_limit:
        type: integer
        required: false
        note: 'maximum number of returned technologies within the same category optional field you can use this field to limit the number of items with identical "technology" in the results default value: 10 minimum value: 1 maximum value: 10000'
      internal_list_limit:
        type: integer
        required: false
        note: maximum number of items with identical "category", "group", and "technology" optional field if you use this field, the values specified in internal_groups_list_limit, internal_categories_list_limit and internal_technologies_list_limit will be ignored; you can use this field to limit the number of it
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned technologies optional field default value: 100 maximum value: 10000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned domains optional field default value: 0 maximum value: 9999 if you specify the 10 value, the first ten technologies in the results array will be omitted and the data will be provided for the successive technologies'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - mode: entry
      technology: Nginx
      keyword: WordPress
      filters:
      - - country_iso_code
        - '='
        - US
      - and
      - - domain_rank
        - '>'
        - 800
      order_by:
      - groups_count,desc
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.domain-analytics-technologies-aggregation-technologies-live.json
  observed_cost: 0.0132
- id: dataforseo.x.domain-analytics-technologies-domain-technologies-live
  tier: extended
  platform: web
  method: POST
  path: /domain_analytics/technologies/domain_technologies/live
  name: "Technologies a website uses, by domain"
  summary: Using this endpoint you will get a list of technologies used in a particular domain.
  docs_url: https://docs.dataforseo.com/v3/domain_analytics/technologies/domain_technologies/live/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: 'target domain required field domain name of the website to analyze Note: results will be returned for the specified domain only'
        example: dataforseo.com
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.domain-analytics-technologies-domain-technologies-live.json
  observed_cost: 0.012
- id: dataforseo.x.domain-analytics-technologies-domains-by-html-terms-live
  tier: extended
  platform: web
  method: POST
  path: /domain_analytics/technologies/domains_by_html_terms/live
  name: "Find domains by HTML terms on their homepage"
  summary: This endpoint provides domains based on the HTML terms they use on their homepage.
  docs_url: https://docs.dataforseo.com/v3/domain_analytics/technologies/domains_by_html_terms/live/
  input:
    bodyType: json
    body:
      search_terms:
        type: array
        required: false
        note: target search terms required field specify target HTML elements, tags, attributes, their content or all of the above if you specify more than one search term, you will receive only the domains containing all of the specified terms in the HTML code of their homepage maximum number of search terms you
        example:
        - data-attrid
      keywords:
        type: array
        required: false
        note: 'target keywords in the domain’s title, description or meta keywords optional field UTF-8 encoding maximum number of keywords you can specify: 10 example: ["seo","software"] learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article'
      mode:
        type: string
        required: false
        note: 'search mode optional field possible search mode types: strict_entry – search for results exactly matching the order, intervals and separators in the specified search terms entry – search for results ignoring the order, intervals and separators in the specified search terms default value: entry'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % operator with like an'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field available fields: domain_rank, domain, last_visited, country_iso_code, language_code, content_language_code possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to'
        example:
        - last_visited,desc
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned domains optional field default value: 100 maximum value: 10000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned domains optional field default value: 0 if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive domains; Note: the maximum value is 9999, the sum of limit and offset must not exce'
        example: 0
      offset_token:
        type: string
        required: false
        note: token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 100,000 results in a single request; by specifying the unique offset_token value from the response array, you will get the su
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - search_terms:
      - data-attrid
      order_by:
      - last_visited,desc
      limit: 10
      offset: 0
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.domain-analytics-technologies-domains-by-html-terms-live.json
  observed_cost: 0.024
- id: dataforseo.x.domain-analytics-technologies-domains-by-technology-live
  tier: extended
  platform: web
  method: POST
  path: /domain_analytics/technologies/domains_by_technology/live
  name: "Find domains using a given technology"
  summary: This endpoint provides domains based on the technology they use.
  docs_url: https://docs.dataforseo.com/v3/domain_analytics/technologies/domains_by_technology/live/
  input:
    bodyType: json
    body:
      technology_paths:
        type: array
        required: false
        note: 'target technology paths required field if you don’t specify groups, technologies, keywords or categories at least one field (technology_paths, groups, technologies, keywords or categories) must be set; each technology path should be specified as a separate object containing “path” and “name”, where '
      groups:
        type: array
        required: false
        note: 'ids of the target technology groups required field if you don’t specify technologies, technology_paths, keywords or categories you can find the full list of technology group ids on this page note: you can specify up to 10 technology groups in this array example: ["sales", "marketing"]'
      categories:
        type: array
        required: false
        note: 'ids of the target technology categories required field if you don’t specify groups, technology_paths, keywords or technologies you can find the full list of technology category ids on this page note: you can specify up to 10 technology categories in this array example: ["payment_processors","crm"]'
      technologies:
        type: array
        required: false
        note: 'target technologies required field if you don’t specify groups, technology_paths, keywords or categories you can find the full list of technologies you can specify here on this page note: you can specify up to 10 technologies in this array example: ["Google Pay","Salesforce"]'
        example:
        - Nginx
      keywords:
        type: array
        required: false
        note: 'target keywords in the domain’s title, description or meta keywords required field if you don’t specify groups, technology_paths, technologies or categories optional field you can specify the maximum of 10 keywords; UTF-8 encoding; example: ["seo","software"] learn more about rules and limitations o'
      mode:
        type: string
        required: false
        note: 'search mode optional field possible search mode types: as_is – search for results exactly matching the specified group ids, category ids, or technology names entry – search for results matching a part of the specified group ids, category ids, or technology names default value: as_is'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % operator with like an'
        example:
        - - country_iso_code
          - '='
          - US
        - and
        - - domain_rank
          - '>'
          - 800
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field available fields: domain_rank, domain, last_visited, country_iso_code, language_code, content_language_code possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to'
        example:
        - last_visited,desc
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned domains optional field default value: 100 maximum value: 10000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned domains optional field default value: 0 if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive domains; Note: the maximum value is 9999, the sum of limit and offset must not exce'
      offset_token:
        type: string
        required: false
        note: token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 100,000 results in a single request; by specifying the unique offset_token value from the response array, you will get the su
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - technologies:
      - Nginx
      filters:
      - - country_iso_code
        - '='
        - US
      - and
      - - domain_rank
        - '>'
        - 800
      order_by:
      - last_visited,desc
      limit: 10
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.domain-analytics-technologies-domains-by-technology-live.json
  observed_cost: 0.024
- id: dataforseo.x.domain-analytics-technologies-technologies
  kind: utility
  tier: extended
  platform: web
  method: GET
  path: /domain_analytics/technologies/technologies
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Full list of technologies by group and category"
  summary: This endpoint will provide you with the full list of available technologies structured by technology groups and categories each particular technology belongs to.
  docs_url: https://docs.dataforseo.com/v3/domain_analytics/technologies/technologies/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.domain-analytics-technologies-technologies-summary-live
  tier: extended
  platform: web
  method: POST
  path: /domain_analytics/technologies/technologies_summary/live
  name: "Domain counts for a technology, by country and language"
  summary: The Technologies Summary endpoint will provide you with the number of domains across different countries and languages that use the specified technology names, technology groups, or technology categories.
  docs_url: https://docs.dataforseo.com/v3/domain_analytics/technologies/technologies_summary/live/
  input:
    bodyType: json
    body:
      technology_paths:
        type: array
        required: false
        note: target technology paths required field if you don’t specify groups, technologies and categories each technology path should be specified as a separate object containing “path” and “name”, where “path” is specified as “$group_id.$category_id” and “name” – as the name of the target technology; each ob
      groups:
        type: array
        required: false
        note: 'ids of the target technology groups required field if you don’t specify technologies, technology_paths, categories, or keywords you can find the full list of technology group ids on this page note: you can specify up to 10 technology groups in this array example: ["sales", "marketing"]'
      categories:
        type: array
        required: false
        note: 'ids of the target technology categories required field if you don’t specify groups, technology_paths, technologies, or keywords you can find the full list of technology category ids on this page note: you can specify up to 10 technology categories in this array example: ["payment_processors","crm"]'
      technologies:
        type: array
        required: false
        note: 'target technologies required field if you don’t specify groups, technology_paths, categories, or keywords you can find the full list of technologies you can specify here on this page note: you can specify up to 10 technologies in this array example: ["Google Pay","Salesforce"]'
        example:
        - Ngi
      keywords:
        type: array
        required: false
        note: 'target keywords in the domain’s title, description or meta keywords required field if you don’t specify groups, technology_paths, categories, or technologies you can specify the maximum of 10 keywords; UTF-8 encoding; example: ["seo","software"] learn more about rules and limitations of keyword and '
        example:
        - WordPress
      mode:
        type: string
        required: false
        note: 'search mode optional field possible search mode types: as_is – search for results exactly matching the specified group ids, category ids, or technology names entry – search for results matching a part of the specified group ids, category ids, or technology names default value: as_is'
        example: entry
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: <, <=, >, >=, =, <>, in, not_in, like,not_like you can use the % operator with like and'
        example:
        - - country_iso_code
          - '='
          - US
        - and
        - - domain_rank
          - '>'
          - 800
      internal_list_limit:
        type: integer
        required: false
        note: 'maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: countries, languages, content_languages, keywords default value: 10 minimum value: 1 maximum value: 10000'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - mode: entry
      technologies:
      - Ngi
      keywords:
      - WordPress
      filters:
      - - country_iso_code
        - '='
        - US
      - and
      - - domain_rank
        - '>'
        - 800
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.domain-analytics-technologies-technologies-summary-live.json
  observed_cost: 0.0132
- id: dataforseo.x.domain-analytics-technologies-technology-stats-live
  tier: extended
  platform: web
  method: POST
  path: /domain_analytics/technologies/technology_stats/live
  name: "Domain counts for a technology over time"
  summary: The Technology Stats endpoint will provide you with historical data on the number of domains across different countries and languages that use the specified technology.
  docs_url: https://docs.dataforseo.com/v3/domain_analytics/technologies/technology_stats/live/
  input:
    bodyType: json
    body:
      technology:
        type: string
        required: false
        note: 'target technology required field you can find the full list of technologies you can specify here on this page example: "Salesforce"'
        example: jQuery
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field minimum value: 2022-10-31 if you don’t specify this field, the minimum value will be used by default date format: "yyyy-mm-dd" example: "2023-06-01"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2023-01-15"'
        example: '2026-06-24'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - technology: jQuery
      date_from: '2026-04-24'
      date_to: '2026-06-24'
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.domain-analytics-technologies-technology-stats-live.json
  observed_cost: 0.0144
- id: dataforseo.x.domain-analytics-whois-overview-live
  tier: extended
  platform: web
  method: POST
  path: /domain_analytics/whois/overview/live
  name: "Whois records with backlink and traffic stats, filtered"
  summary: This endpoint will provide you with Whois data enriched with backlink stats, and ranking and traffic info from organic and paid search results.
  input:
    bodyType: json
    body:
      limit:
        type: integer
        required: false
        note: '<em>the maximum number of returned domains</em><br>optional field<br>default value: <code>100</code><br>maximum value: <code>1000</code>'
        example: 2
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of returned items</em><br>optional field<br>default value: <code>0</code><br>if you specify the <code>10</code> value, the first ten <code>items</code> in the results array will be omitted and the data will be provided for the successive items;<br><strong>Note:</stron'
      offset_token:
        type: string
        required: false
        note: <em>token for subsequent requests</em><br>optional field<br>provided in the identical filed of the response to each request;<br>use this parameter to avoid timeouts while trying to obtain over 100,000 results in a single request;<br>by specifying the unique <code>offset_token</code> value from the r
      filters:
        type: array
        required: false
        note: '<em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>regex</code>, '
        example:
        - - epp_status_codes
          - in
          - - client_transfer_prohibited
            - client_update_prohibited
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - limit: 2
      filters:
      - - epp_status_codes
        - in
        - - client_transfer_prohibited
          - client_update_prohibited
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.domain-analytics-whois-overview-live.json
  observed_cost: 0.1224
- id: dataforseo.x.keywords-data-clickstream-data-bulk-search-volume-live
  tier: extended
  platform: web
  method: POST
  path: /keywords_data/clickstream_data/bulk_search_volume/live
  name: "Clickstream search volume for up to 1000 keywords"
  summary: The Bulk Clickstream Search Volume endpoint of DataForSEO Keywords Data API is designed to provide clickstream-based search volume data for up to 1000 keywords in a single Live request.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/clickstream_data/bulk_search_volume/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; each keyword should be at least 3 characters long; the keywords will be converted to lowercase format; Note: certain symbols and characters (e.g., UTF symbols, emojis) are not allowed to lea'
        example:
        - you tube
        - youtube
        - youtub
      location_name:
        type: string
        required: false
        note: 'full name of the location required field if you don’t specify location_code Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_name by making a separate request to the https://api.dataforseo.com/v3/keywords_data/c'
      location_code:
        type: integer
        required: false
        note: 'location code required field if you don’t specify location_name Note: it is required to specify either location_name or location_code you can receive the list of available locations with their location_code by making a separate request to the https://api.dataforseo.com/v3/keywords_data/clickstream_d'
        example: 2840
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: test-tag
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      tag: test-tag
      keywords:
      - you tube
      - youtube
      - youtub
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-clickstream-data-bulk-search-volume-live.json
  observed_cost: 0.01236
- id: dataforseo.x.keywords-data-clickstream-data-dataforseo-search-volume-live
  tier: extended
  platform: web
  method: POST
  path: /keywords_data/clickstream_data/dataforseo_search_volume/live
  name: "Search volume for keywords, clickstream optional"
  summary: ClickstreamDataDataforseoSearchVolumeLive
  docs_url: https://docs.dataforseo.com/v3/keywords_data/clickstream_data/dataforseo_search_volume/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000 the keywords will be converted to lowercase format Note: certain symbols and characters (e.g., UTF symbols, emojis) are not allowed to learn more about which symbols and characters can be use'
        example:
        - you tube
        - youtube
        - youtub
      location_name:
        type: string
        required: false
        note: 'full name of search engine location required field if you don’t specify location_code you can receive the list of available locations with location_name by making a separate request to https://api.dataforseo.com/v3/keywords_data/clickstream_data/locations_and_languages example: United Kingdom'
      location_code:
        type: integer
        required: false
        note: 'search engine location code required field if you don’t specify location_name if you use this field, you can receive the list of available locations with location_code by making a separate request to the https://api.dataforseo.com/v3/keywords_data/clickstream_data/locations_and_languages example: 28'
        example: 2840
      language_name:
        type: string
        required: false
        note: 'full name of search engine language required field if don’t specify language_code you can receive the list of available languages with their language_name by making a separate request to the https://api.dataforseo.com/v3/keywords_data/clickstream_data/locations_and_languages example: English'
      language_code:
        type: string
        required: false
        note: 'search engine language code required field if don’t specify language_name you can receive the list of available languages with their language_code by making a separate request to the https://api.dataforseo.com/v3/keywords_data/clickstream_data/locations_and_languages example: en'
        example: en
      use_clickstream:
        type: boolean
        required: false
        note: 'use clickstream data to provide results optional field if set to true, you will get DataForSEO search volume values based on clickstream data; if set to false, Bing search volume data will be used to calculate DataForSEO search volume; default value: true; Note: Bing search volume is available for l'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: test-tag
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - location_code: 2840
      language_code: en
      tag: test-tag
      keywords:
      - you tube
      - youtube
      - youtub
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-clickstream-data-dataforseo-search-volume-live.json
  observed_cost: 0.18
- id: dataforseo.x.keywords-data-clickstream-data-global-search-volume-live
  tier: extended
  platform: web
  method: POST
  path: /keywords_data/clickstream_data/global_search_volume/live
  name: "Global clickstream search volume for up to 1000 keywords"
  summary: The Clickstream Global Search Volume endpoint of DataForSEO Keywords Data API is designed to provide clickstream-based search volume data for up to 1000 keywords in a single Live request.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/clickstream_data/global_search_volume/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; each keyword should be at least 3 characters long; the keywords will be converted to lowercase format; Note: certain symbols and characters (e.g., UTF symbols, emojis) are not allowed to lea'
        example:
        - you tube
        - youtube
        - youtub
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: test-tag
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - tag: test-tag
      keywords:
      - you tube
      - youtube
      - youtub
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-clickstream-data-global-search-volume-live.json
  observed_cost: 0.18
- id: dataforseo.x.keywords-data-dataforseo-trends-demography-live
  tier: extended
  platform: web
  method: POST
  path: /keywords_data/dataforseo_trends/demography/live
  name: "Keyword popularity by age and gender, DataForSEO Trends"
  summary: This endpoint will provide you with the demographic breakdown (by age and gender) of keyword popularity per each specified term based on DataForSEO Trends data.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/dataforseo_trends/demography/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field the maximum number of keywords you can specify: 5 avoid symbols and special characters (e.g., UTF symbols, emojis); specifying non-Latin characters, you’ll get data for the countries where they are used learn more about rules and limitations of keyword and keywords fields in '
        example:
        - rugby
        - cricket
      location_name:
        type: string
        required: false
        note: full name of search engine location optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to
        example: United States
      location_code:
        type: integer
        required: false
        note: 'search engine location code optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to https:'
      type:
        type: string
        required: false
        note: type of element
        example: web
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the current day and month of the preceding year will be used by default minimal value for the web type: 2004-01-01 minimal value for other types: 2008-01-01 date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-06-24'
      time_range:
        type: string
        required: false
        note: 'preset time ranges optional field if you specify date_from or date_to parameters, this field will be ignored when setting a task possible values for all type parameters: past_4_hours, past_day, past_7_days, past_30_days, past_90_days, past_12_months, past_5_years'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keywords:
      - rugby
      - cricket
      date_from: '2026-04-24'
      date_to: '2026-06-24'
      type: web
      location_name: United States
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-dataforseo-trends-demography-live.json
  observed_cost: 0.0024
- id: dataforseo.x.keywords-data-dataforseo-trends-explore-live
  capability: google.keywords.trends
  platform: google
  tier: extended
  method: POST
  path: /keywords_data/dataforseo_trends/explore/live
  name: "DataForSEO Trends keyword popularity over time"
  summary: This endpoint will provide you with the keyword popularity data from DataForSEO Trends.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/dataforseo_trends/explore/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field the maximum number of keywords you can specify: 5 learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article'
        example:
        - iphone 14
        - samsung s23
      location_name:
        type: string
        required: false
        note: full name of search engine location optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to
      location_code:
        type: integer
        required: false
        note: 'search engine location code optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to https:'
        example: 2840
      type:
        type: string
        required: false
        note: type of element
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the current day and month of the preceding year will be used by default minimal value for the web type: 2004-01-01 minimal value for other types: 2008-01-01 date format: "yyyy-mm-dd" example: "2019-01-15"'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15"'
      time_range:
        type: string
        required: false
        note: 'preset time ranges optional field if you specify date_from or date_to parameters, this field will be ignored when setting a task possible values for all type parameters: past_4_hours, past_day, past_7_days, past_30_days, past_90_days, past_12_months, past_5_years'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keywords:
      - iphone 14
      - samsung s23
      location_code: 2840
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-dataforseo-trends-explore-live.json
  observed_cost: 0.0012
- id: dataforseo.x.keywords-data-dataforseo-trends-locations-country
  kind: utility
  tier: extended
  platform: web
  method: GET
  path: /keywords_data/dataforseo_trends/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "DataForSEO Trends location codes by country"
  summary: You will receive the list of DataForSEO Trends locations by calling this API.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/dataforseo_trends/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.keywords-data-dataforseo-trends-merged-data-live
  tier: extended
  platform: web
  method: POST
  path: /keywords_data/dataforseo_trends/merged_data/live
  name: "Keyword popularity merged across trend sources"
  summary: This endpoint will provide you with the keyword popularity data from DataForSEO Trends.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/dataforseo_trends/merged_data/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field the maximum number of keywords you can specify: 5 avoid symbols and special characters (e.g., UTF symbols, emojis); specifying non-Latin characters, you’ll get data for the countries where they are used learn more about rules and limitations of keyword and keywords fields in '
        example:
        - rugby
        - cricket
      location_name:
        type: string
        required: false
        note: full name of search engine location optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to
        example: United States
      location_code:
        type: integer
        required: false
        note: 'search engine location code optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to https:'
      type:
        type: string
        required: false
        note: type of element
        example: web
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the current day and month of the preceding year will be used by default minimal value for the web type: 2004-01-01 minimal value for other types: 2008-01-01 date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-06-24'
      time_range:
        type: string
        required: false
        note: 'preset time ranges optional field if you specify date_from or date_to parameters, this field will be ignored when setting a task possible values for all type parameters: past_4_hours, past_day, past_7_days, past_30_days, past_90_days, past_12_months, past_5_years'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keywords:
      - rugby
      - cricket
      date_from: '2026-04-24'
      date_to: '2026-06-24'
      type: web
      location_name: United States
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-dataforseo-trends-merged-data-live.json
  observed_cost: 0.006
- id: dataforseo.x.keywords-data-dataforseo-trends-subregion-interests-live
  tier: extended
  platform: web
  method: POST
  path: /keywords_data/dataforseo_trends/subregion_interests/live
  name: "Keyword popularity by subregion, DataForSEO Trends"
  summary: This endpoint will provide you with location-specific keyword popularity data from DataForSEO Trends.
  docs_url: https://docs.dataforseo.com/v3/keywords_data/dataforseo_trends/subregion_interests/live/
  input:
    bodyType: json
    body:
      keywords:
        type: array
        required: false
        note: 'keywords required field the maximum number of keywords you can specify: 5 avoid symbols and special characters (e.g., UTF symbols, emojis); specifying non-Latin characters, you’ll get data for the countries where they are used learn more about rules and limitations of keyword and keywords fields in '
        example:
        - rugby
        - cricket
      location_name:
        type: string
        required: false
        note: full name of search engine location optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to
        example: United States
      location_code:
        type: integer
        required: false
        note: 'search engine location code optional field if you don’t use this field, you will recieve global results if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to https:'
      type:
        type: string
        required: false
        note: type of element
        example: web
      date_from:
        type: string
        required: false
        note: 'starting date of the time range optional field if you don’t specify this field, the current day and month of the preceding year will be used by default minimal value for the web type: 2004-01-01 minimal value for other types: 2008-01-01 date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-04-24'
      date_to:
        type: string
        required: false
        note: 'ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15"'
        example: '2026-06-24'
      time_range:
        type: string
        required: false
        note: 'preset time ranges optional field if you specify date_from or date_to parameters, this field will be ignored when setting a task possible values for all type parameters: past_4_hours, past_day, past_7_days, past_30_days, past_90_days, past_12_months, past_5_years'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - keywords:
      - rugby
      - cricket
      date_from: '2026-04-24'
      date_to: '2026-06-24'
      type: web
      location_name: United States
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.keywords-data-dataforseo-trends-subregion-interests-live.json
  observed_cost: 0.0024
- id: dataforseo.x.on-page-content-parsing
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/content_parsing
  name: "Parsed page content from a crawl, by task id and URL"
  summary: This endpoint allows parsing the content on any page you specify and will return the structured content of the target page, including link URLs, anchors, headings, and textual content.
  docs_url: https://docs.dataforseo.com/v3/on_page/content_parsing/
  input:
    bodyType: json
    body:
      url:
        type: string
        required: false
        note: 'URL of the content to parse required field URL of the page to parse example: https://dataforseo.com/blog/a-versatile-alternative-to-google-trends-exploring-the-power-of-dataforseo-trends-api'
        example: https://dataforseo.com/blog/a-versatile-alternative-to-google-trends-exploring-the-power-of-dataforseo-trends-api
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint note: the enable_content_parsing parameter in the POST request must be set to true example: "07131248-1535-0216-1000-17384017ad04"'
        example: 11161551-1535-0216-0000-500b3f307f92
      markdown_view:
        type: boolean
        required: false
        note: 'return page content as markdown optional field if set to true, the markdown-formatted content of the page will be returned in the page_as_markdown field of the response; default value: false'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - url: https://dataforseo.com/blog/a-versatile-alternative-to-google-trends-exploring-the-power-of-dataforseo-trends-api
      id: 11161551-1535-0216-0000-500b3f307f92
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.on-page-content-parsing.json
  observed_cost: 0
- id: dataforseo.x.on-page-content-parsing-live
  tier: extended
  platform: web
  method: POST
  path: /on_page/content_parsing/live
  name: "Parse a page into links, headings and text, live"
  summary: This endpoint allows parsing the content on any page you specify and will return the structured content of the target page, including link URLs, anchors, headings, and textual content.
  docs_url: https://docs.dataforseo.com/v3/on_page/content_parsing/live/
  input:
    bodyType: json
    body:
      url:
        type: string
        required: false
        note: 'URL of the content to parse required field URL of the page to parse example: https://www.fujielectric.com/'
        example: https://dataforseo.com/blog/a-versatile-alternative-to-google-trends-exploring-the-power-of-dataforseo-trends-api
      custom_user_agent:
        type: string
        required: false
        note: 'custom user agent optional field custom user agent for crawling a website example: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.116 Safari/537.36 default value: Mozilla/5.0 (compatible; RSiteAuditor)'
      browser_preset:
        type: string
        required: false
        note: 'preset for browser screen parameters optional field if you use this field, you don’t need to indicate browser_screen_width, browser_screen_height, browser_screen_scale_factor possible values: desktop, mobile, tablet desktop preset will apply the following values: browser_screen_width: 1920 browser_s'
      browser_screen_width:
        type: integer
        required: false
        note: 'browser screen width optional field you can set a custom browser screen width to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; Note: to use this parameter, set enable_javascript or enable_browser_rendering to true minim'
      browser_screen_height:
        type: integer
        required: false
        note: 'browser screen height optional field you can set a custom browser screen height to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; Note: to use this parameter, set enable_javascript or enable_browser_rendering to true min'
      browser_screen_scale_factor:
        type: number
        required: false
        note: 'browser screen scale factor optional field you can set a custom browser screen resolution ratio to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; Note: to use this parameter, set enable_javascript or enable_browser_rende'
      store_raw_html:
        type: boolean
        required: false
        note: 'store HTML of a crawled page optional field set to true if you want to get the HTML of the page using the OnPage Raw HTML endpoint default value: false'
      disable_cookie_popup:
        type: boolean
        required: false
        note: 'disable the cookie popup optional field set to true if you want to disable the popup requesting cookie consent from the user; default value: false'
      accept_language:
        type: string
        required: false
        note: 'language header for accessing the website optional field all locale formats are supported (xx, xx-XX, xxx-XX, etc.) Note: if you do not specify this parameter, some websites may deny access; in this case, pages will be returned with the "type":"broken in the response array'
      enable_javascript:
        type: boolean
        required: false
        note: 'load javascript on a page optional field set to true if you want to load the scripts available on a page default value: false Note: if you use this parameter, additional charges will apply; learn more about the cost of tasks with this parameter in our help article; the cost can be calculated on the '
      enable_browser_rendering:
        type: boolean
        required: false
        note: 'emulate browser rendering to measure Core Web Vitals optional field by using this parameter you will be able to emulate a browser when loading a web page; enable_browser_rendering loads styles, images, fonts, animations, videos, and other resources on a page; default value: false set to true to obta'
      enable_xhr:
        type: boolean
        required: false
        note: 'enable XMLHttpRequest on a page optional field set to true if you want our crawler to request data from a web server using the XMLHttpRequest object default value: false if you use this field, enable_javascript must be set to true;'
      switch_pool:
        type: boolean
        required: false
        note: switch proxy pool optional field if true, additional proxy pools will be used to obtain the requested data; the parameter can be used if a multitude of tasks is set simultaneously, resulting in occasional rate-limit and/or site_unreachable errors
      ip_pool_for_scan:
        type: string
        required: false
        note: 'proxy pool optional field you can choose a location of the proxy pool that will be used to obtain the requested data; the parameter can be used if page content is inaccessible in one of the locations, resulting in occasional site_unreachable errors possible values: us, de'
      markdown_view:
        type: boolean
        required: false
        note: 'return page content as markdown optional field if set to true, the markdown-formatted content of the page will be returned in the page_as_markdown field of the response; default value: false'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-duplicate-content
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/duplicate_content
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Pages with duplicate content in a crawl, by URL"
  summary: This endpoint returns a list of pages that have content similar to the page specified in the request.
  docs_url: https://docs.dataforseo.com/v3/on_page/duplicate_content/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 07281559-0695-0216-0000-c269be8b7592
      url:
        type: string
        required: false
        note: page URL required field specify the initial page you want to receive duplicate content for
        example: https://www.etsy.com/
      similarity:
        type: integer
        required: false
        note: content similarity score by default, the content is considered duplicate if the value is greater than or equals 6 you can specify any similarity score in the 0-to-10 range
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned pages optional field default value: 100 maximum value: 1000'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned pages optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-duplicate-tags
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/duplicate_tags
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Pages with duplicate title or meta tags in a crawl"
  summary: This endpoint returns a list of pages that contain duplicate title or description tags.
  docs_url: https://docs.dataforseo.com/v3/on_page/duplicate_tags/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 07281559-0695-0216-0000-c269be8b7592
      type:
        type: string
        required: false
        note: type of element
        example: duplicate_description
      accumulator:
        type: string
        required: false
        note: tag value optional field specify a title or description here if you want to receive a list of duplicate pages that contains this tag
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned pages optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned pages optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-keyword-density
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/keyword_density
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Keyword density on a crawled site or page"
  summary: This endpoint will provide you with keyword density and keyword frequency data for terms appearing on the specified website or web page.
  docs_url: https://docs.dataforseo.com/v3/on_page/keyword_density/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 09101923-1535-0216-0000-2389a8854b70
      keyword_length:
        type: integer
        required: false
        note: 'number of words for a keyword required field possible values: 1, 2, 3, 4, 5'
        example: 2
      url:
        type: string
        required: false
        note: page URL optional field if you do not specify a page here, the results will be provided for the whole website if you use this field, the API response will contain only keywords from the specified page a page should be specified with absolute URL (including http:// or https://)
        example: https://dataforseo.com/
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned keywords optional field default value: 100 maximum value: 1000'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, <>, in, not_in, like, not_like you can use the % operator with lik'
        example:
        - frequency
        - '>'
        - 5
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["fr'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-lighthouse-audits
  kind: utility
  tier: extended
  platform: web
  method: GET
  path: /on_page/lighthouse/audits
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Available Lighthouse audit checks"
  summary: The OnPage Lighthouse API is based on Google’s open-source Lighthouse project and provides data on the quality of web pages.
  docs_url: https://docs.dataforseo.com/v3/on_page/lighthouse/audits/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.on-page-lighthouse-live-json
  tier: extended
  platform: web
  method: POST
  path: /on_page/lighthouse/live/json
  name: "Lighthouse audit for a URL, live"
  summary: The OnPage Lighthouse API is based on Google’s open-source Lighthouse project for measuring the quality of web pages and web apps.
  input:
    bodyType: json
    body:
      url:
        type: string
        required: false
        note: <em>target URL</em><br><strong>required field</strong><br>target page should be specified with its absolute URL (including http:// or https://)<br>example:<br><code>https://dataforseo.com/</code>
        example: https://dataforseo.com
      for_mobile:
        type: boolean
        required: false
        note: '<em>applies mobile emulation</em><br>optional field<br>if set to <code>true</code>, Lighthouse will use mobile device and screen emulation to test the page against mobile environment<br>if set to <code>false</code>, the results will be provided for desktop<br>default value: <code>false</code>'
        example: true
      categories:
        type: array
        required: false
        note: <em>categories of Lighthouse audits</em><br>optional field<br>each category is a collection of audits and audit groups that applies weighting and scoring to the section (<a href="https://github.com/GoogleChrome/lighthouse/blob/master/docs/architecture.md#auditreport-terminology" rel="noopener norefe
      audits:
        type: array
        required: false
        note: <em>Lighthouse audits</em><br>optional field<br>audits are individual tests Lighthouse runs for each specific feature/optimization/metric to produce a numeric score (<a href="https://github.com/GoogleChrome/lighthouse/blob/master/docs/architecture.md#components--terminology" rel="noopener noreferrer
      version:
        type: string
        required: false
        note: <em>lighthouse version</em><br>optional field<br>you can obtain the results specific to a certain Lighthouse version by specifying its number<br>the list of available versions is available through the <a href="/v3/on_page/lighthouse/versions/" rel="noopener noreferrer" target="_blank">Lighthouse Ver
      language_name:
        type: string
        required: false
        note: <em>lighthouse language name</em><br>optional field<br>you can receive the list of available languages of the search engine with their <code>language_name</code> by making a separate request to <code>https://api.dataforseo.com/v3/on_page/lighthouse/languages</code><br>default value:<br><code class="
      language_code:
        type: string
        required: false
        note: <em>lighthouse language code</em><br>optional field<br>you can receive the list of available languages of the search engine with their <code>language_code</code> by making a separate request to <code>https://api.dataforseo.com/v3/on_page/lighthouse/languages</code><br>default value:<br><code class="
      custom_user_agent:
        type: string
        required: false
        note: <em>custom user agent</em><br>optional field<br>specify the custom user agent used by the browser when running the Lighthouse audit;<br>can be specified with up to 254 characters;
      browser_screen_width:
        type: integer
        required: false
        note: '<em>browser screen width</em><br>optional field<br>set the screen width of the browser used for the Lighthouse audit to emulate a specific device;<br>can be specified within the following range: <code>240–9999</code>;'
      browser_screen_height:
        type: integer
        required: false
        note: '<em>browser screen height</em><br>optional field<br>set the screen height of the browser used for the Lighthouse audit to emulate a specific device;<br>can be specified within the following range: <code>240–9999</code>;'
      browser_screen_scale_factor:
        type: number
        required: false
        note: '<em>browser screen scale factor</em><br>optional field<br>set the device pixel ratio of the browser used for the Lighthouse audit;<br>can be specified within the following range: <code>0.5–3</code>;'
      browser_network_throttling_method:
        type: string
        required: false
        note: <em>browser network throttling method</em><br>optional field<br>defines the method used to apply throttling during the Lighthouse audit;<br>possible vaules:<br><code>simulate</code> - calculates estimated performance metrics without applying explicit throttling;<br><code>devtools</code> - applies th
      browser_cpu_throttling_multiplier:
        type: number
        required: false
        note: <em>browser CPU throttling multiplier</em><br><strong>required if <code>browser_network_throttling_method</code> is set to <code>devtools</code>;</strong><br>set the CPU throttling multiplier to simulate device performance conditions during the Lighthouse audit;<br>can be specified within the follow
      browser_network_throttling:
        type: string
        required: false
        note: '<em>browser network throttling</em><br><strong>required if <code>browser_network_throttling_method</code> is set to <code>devtools</code>;</strong><br>set the network throttling profile to simulate connection speed conditions during the Lighthouse audit;<br>possible values: <code>no_throttling</code'
      tag:
        type: string
        required: false
        note: <em>user-defined task identifier</em><br>optional field<br><em>the character limit is 255</em><br>you can use this parameter to identify the task and match it with the result<br>you will find the specified <code>tag</code> value in the <code>data</code> object of the response
        example: some_string_123
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-links
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/links
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Internal and external links from a site crawl"
  summary: This endpoint will provide you with a list of internal and external links detected on a target website.
  docs_url: https://docs.dataforseo.com/v3/on_page/links/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 07281559-0695-0216-0000-c269be8b7592
      page_from:
        type: string
        required: false
        note: relative page URL optional field if you use this field, the API response will contain only links from the specified page note that in this field you can specify relative URLs only
        example: /apis/google-trends-api
      page_to:
        type: string
        required: false
        note: relative page URL optional field if you use this field, the API response will contain only internal links pointing to the specified page note that in this field you can specify relative URLs only
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned links optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned links optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten links in the results array will be omitted and the data will be provided for the successive links'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, <>, in, not_in, like, not_like you can use the % operator with lik'
        example:
        - - dofollow
          - '='
          - true
        - and
        - - direction
          - '='
          - external
      search_after_token:
        type: string
        required: false
        note: token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 20,000 results in a single request; by specifying the unique search_after_token value from the response array, you will get t
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-microdata
  tier: extended
  platform: web
  method: POST
  path: /on_page/microdata
  name: "Validate JSON-LD and microdata on a crawled page"
  summary: This endpoint is designed to validate structured JSON-LD data and Microdata.
  docs_url: https://docs.dataforseo.com/v3/on_page/microdata/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: "07131248-1535-0216-1000-17384017ad04"'
        example: 02241700-1535-0216-0000-034137259bc1
      url:
        type: string
        required: false
        note: 'resource URL required field you can get this URL in the response of the Pages endpoint example: https://dataforseo.com/apis'
        example: https://dataforseo.com/apis
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-non-indexable
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/non_indexable
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Non-indexable pages found in a site crawl"
  summary: This endpoint returns a list of pages that are blocked from being indexed by Google and other search engines through robots.txt, HTTP headers, or meta tags settings.
  docs_url: https://docs.dataforseo.com/v3/on_page/non_indexable/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 07281559-0695-0216-0000-c269be8b7592
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned pages optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned pages optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % ope'
        example:
        - - reason
          - '='
          - robots_txt
        - and
        - - url
          - like
          - '%go%'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-page-screenshot
  tier: extended
  platform: web
  method: POST
  path: /on_page/page_screenshot
  cost: {type: per_result, value: 0.0048, currency: USD, per: 1, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', confidence: verified, note: 'read from our own account rate card'}
  name: "Full-page screenshot of any URL"
  summary: Using this endpoint, you can capture a full high-quality screenshot of any webpage.
  docs_url: https://docs.dataforseo.com/v3/on_page/page_screenshot/
  input:
    bodyType: json
    body:
      url:
        type: string
        required: false
        note: 'page url required field absolute URL of the page to snap note: if the URL you indicate here returns a 404 status code or the indicated value is not a valid URL, you will obtain "error_message":"Screenshot is empty" in the response array'
        example: https://dataforseo.com/apis
      accept_language:
        type: string
        required: false
        note: 'language header for accessing the website optional field all locale formats are supported (xx, xx-XX, xxx-XX, etc.) note: if you do not specify this parameter, some websites may deny access; in this case, you will obtain "error_message":"Screenshot is empty" in the response array'
      custom_user_agent:
        type: string
        required: false
        note: 'custom user agent optional field custom user agent for crawling a website example: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.116 Safari/537.36 default value: Mozilla/5.0 (compatible; RSiteAuditor)'
      browser_preset:
        type: string
        required: false
        note: 'preset for browser screen parameters optional field if you use this field, you don’t need to indicate browser_screen_width, browser_screen_height, browser_screen_scale_factor possible values: desktop, mobile, tablet desktop preset will apply the following values: browser_screen_width: 1920 browser_s'
      browser_screen_width:
        type: integer
        required: false
        note: 'browser screen width optional field you can set a custom browser screen width to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; minimum value, in pixels: 240 maximum value, in pixels: 9999'
      browser_screen_height:
        type: integer
        required: false
        note: 'browser screen height optional field you can set a custom browser screen height to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; minimum value, in pixels: 240 maximum value, in pixels: 9999'
      browser_screen_scale_factor:
        type: number
        required: false
        note: 'browser screen scale factor optional field you can set a custom browser screen resolution ratio to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; minimum value: 0.5 maximum value: 3'
      full_page_screenshot:
        type: boolean
        required: false
        note: 'take a screenshot of the full page optional field set to false if you want to capture only the part of the page displayed before scrolling default value: true'
      disable_cookie_popup:
        type: boolean
        required: false
        note: 'disable the cookie popup optional field set to true if you want to disable the popup requesting cookie consent from the user; default value: false'
      switch_pool:
        type: boolean
        required: false
        note: switch proxy pool optional field if true, additional proxy pools will be used to obtain the requested data; the parameter can be used if a multitude of tasks is set simultaneously, resulting in occasional rate-limit and/or site_unreachable errors
      ip_pool_for_scan:
        type: string
        required: false
        note: 'proxy pool optional field you can choose a location of the proxy pool that will be used to obtain the requested data; the parameter can be used if page content is inaccessible in one of the locations, resulting in occasional site_unreachable errors possible values: us, de'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-pages
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/pages
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Crawled pages with on-page checks, by task id"
  summary: This endpoint returns a list of crawled pages with on-page check-ups and other metrics related to the page performance.
  docs_url: https://docs.dataforseo.com/v3/on_page/pages/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 07281559-0695-0216-0000-c269be8b7592
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned pages optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned pages optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % ope'
        example:
        - - resource_type
          - '='
          - html
        - and
        - - meta.scripts_count
          - '>'
          - 40
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["me'
        example:
        - meta.content.plain_text_word_count,desc
      search_after_token:
        type: string
        required: false
        note: token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 20,000 results in a single request; by specifying the unique search_after_token value from the response array, you will get t
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-pages-by-resource
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/pages_by_resource
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Pages using a given resource, from a site crawl"
  summary: This endpoint will return the list of pages where a specific resource is located.
  docs_url: https://docs.dataforseo.com/v3/on_page/page_by_resource/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 02241700-1535-0216-0000-034137259bc1
      url:
        type: string
        required: false
        note: 'resource URL required field you can get this URL in the response of the Resources endpoint example: https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js'
        example: https://www.etsy.com/about/jobs.workco2018.js?
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned pages optional field default value: 100 maximum value: 1000'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned pages optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % ope'
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["me'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-raw-html
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/raw_html
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Raw HTML of a crawled page, by task id and URL"
  summary: This endpoint returns the HTML of a page you indicate in the request.
  docs_url: https://docs.dataforseo.com/v3/on_page/raw_html/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 07281559-0695-0216-0000-c269be8b7592
      url:
        type: string
        required: false
        note: 'page url required field the absolute URL of a page to request HTML Note: this field is optional if the task was set using the Instant Pages endpoint'
        example: https://dataforseo.com/apis
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-redirect-chains
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/redirect_chains
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Redirect chains found in a site crawl"
  summary: Redirect chains occur when there are at least two redirects between the initial URL and the destination URL.
  docs_url: https://docs.dataforseo.com/v3/on_page/redirect_chains/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 03051327-4536-0216-1000-3b458a2cfcca
      url:
        type: string
        required: false
        note: page URL optional field absolute URL of the target page if you use this field, the API response will return only redirect chains which contain the specified URL
        example: https://test_rdr.dataforseo.com/a/
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned redirect chains optional field default value: 100 maximum value: 1000'
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned redirect chains optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten redirect chains in the results array will be omitted and the data will be provided for the successive redirect chains'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can use only one filtering parameter with this endpoint the following filtering parameter is supported: is_redirect_loop the following operators are supported: regex, not_regex, =, <> examples: ["is_redirect_loop","=","true"] ["is_redirect_loo'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-resources
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/resources
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Site resources from a crawl — images, scripts, broken"
  summary: This endpoint will provide you with a list of resources, including images, scripts, stylesheets, and broken elements.
  docs_url: https://docs.dataforseo.com/v3/on_page/resources/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 07281559-0695-0216-0000-c269be8b7592
      url:
        type: string
        required: false
        note: page URL optional field specify this field if you want to get the resources for a specific page note that to obtain resource’s meta from a particular URL, you should specify the URL in this field; if you do not indicate a url when setting a task, resource’s meta in the results will be returned based
      limit:
        type: integer
        required: false
        note: 'the maximum number of returned resources optional field default value: 100 maximum value: 1000'
        example: 10
      offset:
        type: integer
        required: false
        note: 'offset in the results array of returned resources optional field default value: 0 maximum value: 2000000 if you specify the 10 value, the first ten resources in the results array will be omitted and the data will be provided for the successive resources'
      filters:
        type: array
        required: false
        note: 'array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, <, <=, >, >=, =, <>, in, not_in, like, not_like you can use the % ope'
        example:
        - - resource_type
          - '='
          - image
        - and
        - - size
          - '>'
          - 100000
      relevant_pages_filters:
        type: array
        required: false
        note: filter the resources by relevant pages optional field you can use this field to obtain resources from pages matching to the defined parameters you can apply the same filters here as available for the pages endpoint you can add several filters at once (8 filters maximum) you should set a logical oper
      order_by:
        type: array
        required: false
        note: 'results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["si'
        example:
        - size,desc
      search_after_token:
        type: string
        required: false
        note: token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 20,000 results in a single request; by specifying the unique search_after_token value from the response array, you will get t
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-summary-id
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: GET
  path: /on_page/summary/{id}
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Site crawl summary and issues, by task id"
  summary: Using this function, you can get the overall information on a website as well as drill down into exact on-page issues of a website that has been scanned.
  docs_url: https://docs.dataforseo.com/v3/on_page/summary/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.on-page-task-post
  tier: extended
  platform: web
  method: POST
  path: /on_page/task_post
  name: "Start a site crawl for on-page audit, by domain"
  summary: OnPage API checks websites for 60+ customizable on-page parameters defines and displays all found flaws and opportunities for optimization so that you can easily fix them.
  docs_url: https://docs.dataforseo.com/v3/on_page/task_post/
  input:
    bodyType: json
    body:
      target:
        type: string
        required: false
        note: target domain required field domain name should be specified without https:// and www. if you specify the page URL, the results will be returned for the domain included in the URL
        example: dataforseo.com
      max_crawl_pages:
        type: integer
        required: false
        note: 'crawled pages limit required field the number of pages to crawl on the specified domain Note: if you set max_crawl_pages to 1 and do not specify start_url or set a homepage in it, the following sitewide checks will be disabled: test_canonicalization, enable_www_redirect_check, test_hidden_server_sig'
        example: 10
      start_url:
        type: string
        required: false
        note: 'the first url to crawl optional field Note: you should specify an absolute URL if you want to crawl a single page, specify its URL in this field and additionally set the max_crawl_pages parameter to 1 you can also use the live Instant Pages endpoint to get page-specific data'
      force_sitewide_checks:
        type: boolean
        required: false
        note: 'enable sitewide checks when crawling a single page optional field set to true to get data on sitewide checks when crawling a single page; default value: false'
      priority_urls:
        type: array
        required: false
        note: 'urls to be crawled bypassing the queue optional field URLs specified in this array will be crawled in the first instance, bypassing the crawling queue; Note: you should specify the absolute URL; you can specify up to 20 URLs; all URLs in the array must belong to the target domain; subdomains will be'
      max_crawl_depth:
        type: integer
        required: false
        note: crawl depth optional field the linking depth of the pages to crawl; for example, starting page of the crawl is level 0, pages that have links from that page are level 1, etc.
      crawl_delay:
        type: integer
        required: false
        note: 'delay between hits, ms optional field the custom delay between crawler hits to the server default value: 2000'
      store_raw_html:
        type: boolean
        required: false
        note: 'store HTML of crawled pages optional field set to true if you want to get the HTML of the page using the OnPage Raw HTML endpoint default value: false'
      enable_content_parsing:
        type: boolean
        required: false
        note: 'parse content on crawled pages optional field set to true to use the OnPage Content Parsing endpoint default value: false'
      support_cookies:
        type: boolean
        required: false
        note: 'support cookies on crawled pages optional field set to true to support cookies when crawling the pages default value: false'
      accept_language:
        type: string
        required: false
        note: 'language header for accessing the website optional field all locale formats are supported (xx, xx-XX, xxx-XX, etc.) Note: if you do not specify this parameter, some websites may deny access; in this case, pages will be returned with the "type":"broken in the response array'
      custom_robots_txt:
        type: string
        required: false
        note: 'custom robots.txt settings optional field example: Disallow: /directory1/'
      robots_txt_merge_mode:
        type: string
        required: false
        note: 'merge with or override robots.txt settings optional field possible values: merge, override; set to override if you want to ignore website crawling restrictions and other robots.txt settings default value: merge; Note: if set to override, specify the custom_robots_txt parameter'
      custom_user_agent:
        type: string
        required: false
        note: 'custom user agent optional field custom user agent for crawling a website example: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/83.0.4103.116 Safari/537.36 default value: Mozilla/5.0 (compatible; RSiteAuditor)'
      browser_preset:
        type: string
        required: false
        note: 'preset for browser screen parameters optional field if you use this field, you don’t need to indicate browser_screen_width, browser_screen_height, browser_screen_scale_factorpossible values: desktop, mobile, tabletdesktop preset will apply the following values:browser_screen_width: 1920 browser_scre'
      browser_screen_width:
        type: integer
        required: false
        note: 'browser screen width optional field you can set a custom browser screen width to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; Note: to use this parameter, set enable_javascript or enable_browser_rendering to trueminimu'
      browser_screen_height:
        type: integer
        required: false
        note: 'browser screen height optional field you can set a custom browser screen height to perform an audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; Note: to use this parameter, set enable_javascript or enable_browser_rendering to truem'
      browser_screen_scale_factor:
        type: number
        required: false
        note: 'browser screen scale factor optional field you can set a custom browser screen resolution ratio to perform audit for a particular device; if you use this field, you don’t need to indicate browser_preset as it will be ignored; Note: to use this parameter, set enable_javascript or enable_browser_rende'
      respect_sitemap:
        type: boolean
        required: false
        note: 'respect sitemap when crawling optional field set to true if you want to follow the order of pages indicated in the primary sitemap when crawling; default value: false Note: if set to true, the click_depth value in the API response will equal 0; the max_crawl_depth field of the request will be ignore'
      custom_sitemap:
        type: string
        required: false
        note: 'custom sitemap url optional field the URL of the page where the alternative sitemap is located Note: if you want to use this parameter, respect_sitemap should be true'
      crawl_sitemap_only:
        type: boolean
        required: false
        note: 'crawl only pages indicated in the sitemap optional field set to true if you want to crawl only the pages indicated in the sitemap if you set this parameter to true and do not specify custom_sitemap, we will crawl the default sitemap default value: false Note: if you want to use this parameter, respe'
      load_resources:
        type: boolean
        required: false
        note: 'load resources optional field set to true if you want to load image, stylesheets, scripts, and broken resources default value: false Note: if you use this parameter, additional charges will apply; learn more about the cost of tasks with this parameter in our help article; the cost can be calculated '
        example: true
      enable_www_redirect_check:
        type: boolean
        required: false
        note: 'check if the domain implemented the www redirection optional field set to true if you want to check if the requested domain implemented the www to non-www or non-www to www redirect; default value: false'
      enable_javascript:
        type: boolean
        required: false
        note: 'load javascript on a page optional field set to true if you want to load the scripts available on a page default value: false Note: if you use this parameter, additional charges will apply; learn more about the cost of tasks with this parameter in our help article; the cost can be calculated on the '
        example: true
      enable_xhr:
        type: boolean
        required: false
        note: 'enable XMLHttpRequest on a page optional field set to true if you want our crawler to request data from a web server using the XMLHttpRequest object default value: false;if you use this field, enable_javascript must be set to true;'
      enable_browser_rendering:
        type: boolean
        required: false
        note: 'emulate browser rendering to measure Core Web Vitals optional field by using this parameter you will be able to emulate a browser when loading a web page; enable_browser_rendering loads styles, images, fonts, animations, videos, and other resources on a page; default value: false set to true to obta'
      disable_cookie_popup:
        type: boolean
        required: false
        note: 'disable the cookie popup optional field set to true if you want to disable the popup requesting cookie consent from the user; default value: false'
      custom_js:
        type: string
        required: false
        note: 'custom javascript optional field Note that the execution time for the script you enter here should be 700 ms maximum, for example, you can use the following JS snippet to check if the website contains Google Tag Manager as a scr attribute: let meta = { haveGoogleAnalytics: false, haveTagManager: fal'
        example: meta = {}; meta.url = document.URL; meta;
      validate_micromarkup:
        type: boolean
        required: false
        note: 'enable microdata validation optional field set to true if you want to use the OnPage API Microdata endpoint default value: false'
      allow_subdomains:
        type: boolean
        required: false
        note: 'include pages on subdomains optional field set to true if you want to crawl all subdomains of a target website default value: false'
      allowed_subdomains:
        type: array
        required: false
        note: 'subdomains to crawl optional field specify subdomains that you want to crawl example: ["blog.site.com", "my.site.com", "shop.site.com"] Note: to use this parameter, the allow_subdomains parameter should be set to false; otherwise, the content of allowed_subdomains field will be ignored and the resul'
      disallowed_subdomains:
        type: array
        required: false
        note: 'subdomains not to crawl optional field specify subdomains that you don’t want to crawl example: ["status.site.com", "docs.site.com"] Note: to use this parameter, the allow_subdomains parameter should be set to true'
      check_spell:
        type: boolean
        required: false
        note: 'check spelling optional field set to true to check spelling on a website using Hunspell library default value: false'
      check_spell_language:
        type: string
        required: false
        note: 'language of the spell check optional field supported languages: ‘hy’, ‘eu’, ‘bg’, ‘ca’, ‘hr’, ‘cs’, ‘da’, ‘nl’, ‘en’, ‘eo’, ‘et’, ‘fo’, ‘fa’, ‘fr’, ‘fy’, ‘gl’, ‘ka’, ‘de’, ‘el’, ‘he’, ‘hu’, ‘is’, ‘ia’, ‘ga’, ‘it’, ‘rw’, ‘la’, ‘lv’, ‘lt’, ‘mk’, ‘mn’, ‘ne’, ‘nb’, ‘nn’, ‘pl’, ‘pt’, ‘ro’, ‘gd’, ‘sr’, ‘s'
      check_spell_exceptions:
        type: array
        required: false
        note: 'words excluded from spell check optional field specify the words that you want to exclude from spell check maximum word length: 100 characters maximum amount of words: 1000 example: "SERP", "minifiers", "JavaScript"'
      calculate_keyword_density:
        type: boolean
        required: false
        note: 'calculate keyword density for the target domain optional field set to true if you want to calculate keyword density for website pages default value: false Note: if you use this parameter, additional charges will apply; learn more about the cost of tasks with this parameter in our help article once t'
      checks_threshold:
        type: object
        required: false
        note: 'custom threshold values for checks optional field you can specify custom threshold values for the parameters included in the checks object of OnPage API responses; Note: only integer threshold values can be modified; for example, the high_loading_time and large_page_size parameters are set to 3 seco'
      disable_sitewide_checks:
        type: array
        required: false
        note: 'prevent certain sitewide checks from running optional field specify the following checks to prevent them from running on the target website: "test_page_not_found" "test_canonicalization" "test_https_redirect" "test_directory_browsing"example: "disable_sitewide_checks": ["test_directory_browsing", "t'
      disable_page_checks:
        type: array
        required: false
        note: 'prevent certain page checks from running optional field specify certain checks to prevent them from running and impacting the onpage_scoreexample: "disable_page_checks": ["is_5xx_code", "is_4xx_code"]'
      switch_pool:
        type: boolean
        required: false
        note: switch proxy pool optional field if true, additional proxy pools will be used to obtain the requested data; the parameter can be used if a multitude of tasks is set simultaneously, resulting in occasional rate-limit and/or site_unreachable errors
      return_despite_timeout:
        type: boolean
        required: false
        note: 'return data on pages despite the timeout error optional field if true, the data will be provided on pages that failed to load within 120 seconds and responded with a timeout error; default value: false'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
        example: some_string_123
      pingback_url:
        type: string
        required: false
        note: notification URL of a completed task optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a $id variable and ‘$tag’ as urlencoded $tag variable. We will set the necessary values before sending the request. exampl
        example: https://your-server.com/pingscript?id=$id&tag=$tag
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - target: dataforseo.com
      max_crawl_pages: 10
      load_resources: true
      enable_javascript: true
      custom_js: meta = {}; meta.url = document.URL; meta;
      tag: some_string_123
      pingback_url: https://your-server.com/pingscript?id=$id&tag=$tag
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.on-page-task-post.json
  observed_cost: 0.018
- id: dataforseo.x.on-page-uncrawlable-resources
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/uncrawlable_resources
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Uncrawlable resources found in a site crawl"
  summary: This endpoint returns a list of resources detected on the target website that could not be crawled due to a content type inconsistency.
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: <em>ID of the task</em><br><strong>required field</strong><br>you can get this ID in the response of the <a href="/v3/on_page/task_post/">Task POST</a> endpoint<br>example:<br>"07131248-1535-0216-1000-17384017ad04"
        example: 07281559-0695-0216-0000-c269be8b7592
      limit:
        type: integer
        required: false
        note: '<em>the maximum number of returned uncrawlable resources</em><br>optional field<br>default value: <code>100</code><br>maximum value: <code>1000</code>'
        example: 10
      offset:
        type: integer
        required: false
        note: '<em>offset in the results array of returned uncrawlable resources</em><br>optional field<br>default value: <code>0</code><br> maximum value: <code>2000000</code><br>if you specify the <code>10</code> value, the first ten invalid resources in the results array will be omitted and the data will be pro'
      order_by:
        type: array
        required: false
        note: <em>results sorting rules</em><br>optional field<br>you can use the same values as in the <code>filters</code> array to sort the results<br>possible sorting types:<br><code>asc</code> - results will be sorted in the ascending order<br><code>desc</code> - results will be sorted in the descending orde
      filters:
        type: array
        required: false
        note: '<em>array of results filtering parameters</em><br>optional field<br><strong>you can add several filters at once (8 filters maximum)</strong><br>you should set a logical operator <code>and</code>, <code>or</code> between the conditions<br>the following operators are supported:<br><code>regex</code>, '
        example:
        - - meta.content_type
          - '='
          - image/jpeg
        - and
        - - url
          - like
          - '%go%'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.on-page-waterfall
  platform_blocked: "Shared-key task ownership is not available for this legacy endpoint; connect your own DataForSEO key."
  tier: extended
  platform: web
  method: POST
  path: /on_page/waterfall
  cost: {type: free, value: 0, currency: USD, unit: call, source: rate_card_api, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Page load waterfall timings for a crawled page"
  summary: This endpoint is designed to provide you with the page speed insights.
  docs_url: https://docs.dataforseo.com/v3/on_page/waterfall/
  input:
    bodyType: json
    body:
      id:
        type: string
        required: false
        note: 'ID of the task required field you can get this ID in the response of the Task POST endpoint example: “07131248-1535-0216-1000-17384017ad04”'
        example: 08101204-0696-0216-0000-644a7b21a48a
      url:
        type: string
        required: false
        note: page URL required field specify the pages you want to receive timing for
        example: https://dataforseo.com/tag/broken-links
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  untestable: needs the id of a completed on_page crawl (task_post plus polling); the one-shot route of this family is verified via dataforseo.x.on-page-instant-pages
- id: dataforseo.x.serp-yahoo-locations-country
  kind: utility
  tier: extended
  platform: yahoo
  method: GET
  path: /serp/yahoo/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "Yahoo location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/serp/yahoo/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.serp-yahoo-organic-live-advanced
  tier: extended
  platform: yahoo
  method: POST
  path: /serp/yahoo/organic/live/advanced
  name: "Yahoo organic results for a keyword — full SERP features"
  summary: Live SERP provides real-time data on top search engine results.
  docs_url: https://docs.dataforseo.com/v3/serp/yahoo/organic/live/advanced/
  input:
    bodyType: json
    body:
      url:
        type: string
        required: false
        note: direct URL of the search query optional field you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, we wouldn’t recom
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keyword,
        example: albert einstein
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      device:
        type: string
        required: false
        note: 'device type optional field return results for a specific device type can take the values:desktop, mobile default value: desktop'
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android'
      se_domain:
        type: string
        required: false
        note: 'search engine domain optional field we choose the relevant search engine domain automatically according to the location and language you specify however, you can set a custom search engine domain in this field example: au.search.yahoo.com, uk.search.yahoo.com, ca.search.yahoo.com, etc.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 6 max value: 200 Your account will be billed per each SERP; Each Yahoo SERP can contain fewer than 10 results, so setting depth above the default value may result in additional charges ; The cost can be calculated on the Pricing p'
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl default value: 1 max value: 100 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      target:
        type: string
        required: false
        note: target domain, subdomain, or webpage to get results for optional field a domain or a subdomain should be specified without https:// and www. note that the results of target-specific tasks will only include SERP elements that contain a url string; you can also use a wildcard (‘*’) character to specif
      search_param:
        type: string
        required: false
        note: additional parameters of the search query optional field get the list of available parameters and additional details here
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_value:
        type: string
        required: false
        note: 'target domain, subdomain, or wildcard value required field if stop_crawl_on_match is specified specify a target domain, subdomain, or wildcard value; Note: domain or subdomain must be specified without a request protocol; example: "match_value": "dataforseo.com", "match_value": "/blog/post-*"'
      match_type:
        type: string
        required: false
        note: 'target match type required field if stop_crawl_on_match is specified type of match for the match_value possible values: domain – specific domain or subdomain with_subdomains – main domain and subdomains wildcard – wildcard pattern'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-yahoo-organic-live-advanced.json
  observed_cost: 0.002
  capability: yahoo.serp.organic
- id: dataforseo.x.serp-yahoo-organic-live-regular
  tier: extended
  platform: yahoo
  method: POST
  path: /serp/yahoo/organic/live/regular
  name: "Yahoo organic search results for a keyword, live"
  summary: Live Yahoo SERP provides real-time data on search engine results for the specified keyword, search engine, and location.
  docs_url: https://docs.dataforseo.com/v3/serp/yahoo/organic/live/regular/
  input:
    bodyType: json
    body:
      url:
        type: string
        required: false
        note: direct URL of the search query optional field you can specify a direct URL and we will sort it out to the necessary fields. Note that this method is the most difficult for our API to process and also requires you to specify the exact language and location in the URL. In most cases, we wouldn’t recom
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keyword,
        example: albert einstein
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code or location_coordinate if you use this field, you don’t need to specify location_code or location_coordinate you can receive the list of available locations of the search engine with their location_name by making a
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name or location_coordinate if you use this field, you don’t need to specify location_name or location_coordinate you can receive the list of available locations of the search engines with their location_code by making a separa
        example: 2840
      location_coordinate:
        type: string
        required: false
        note: GPS coordinates of a location required field if you don’t specify location_name or location_code if you use this field, you don’t need to specify location_name or location_code location_coordinate parameter should be specified in the “latitude,longitude,radius” format the maximum number of decimal d
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      device:
        type: string
        required: false
        note: 'device type optional field can take the values:desktop, mobile default value: desktop'
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android'
      se_domain:
        type: string
        required: false
        note: 'search engine domain optional field we choose the relevant search engine domain automatically according to the location and language you specify however, you can set a custom search engine domain in this field example: au.search.yahoo.com, uk.search.yahoo.com, ca.search.yahoo.com, etc.'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 6 max value: 200 Your account will be billed per each SERP; Each Yahoo SERP can contain fewer than 10 results, so setting depth above the default value may result in additional charges ; The cost can be calculated on the Pricing p'
      max_crawl_pages:
        type: integer
        required: false
        note: 'page crawl limit optional field number of search results pages to crawl default value: 1 max value: 100 Note: the max_crawl_pages and depth parameters complement each other; learn more at our help center'
      target:
        type: string
        required: false
        note: target domain, subdomain, or webpage to get results for optional field a domain or a subdomain should be specified without https:// and www. note that the results of target-specific tasks will only include SERP elements that contain a url string; you can also use a wildcard (‘*’) character to specif
      search_param:
        type: string
        required: false
        note: additional parameters of the search query optional field get the list of available parameters and additional details here
      stop_crawl_on_match:
        type: array
        required: false
        note: 'array of targets to stop crawling optional field if specified, the response will contain SERP results up to and including the specified match_value; you can specify up to 10 target values in this array example: "stop_crawl_on_match":[{"match_value":"dataforseo.com","match_type":"with_subdomains"}] l'
      match_value:
        type: string
        required: false
        note: 'target domain, subdomain, or wildcard value required field if stop_crawl_on_match is specified; required field if stop_crawl_on_match is specified specify a target domain, subdomain, or wildcard value; Note: domain or subdomain must be specified without a request protocol; example: "match_value": "d'
      match_type:
        type: string
        required: false
        note: 'target match type required field if stop_crawl_on_match is specified type of match for the match_value possible values: domain – specific domain or subdomain with_subdomains – main domain and subdomains wildcard – wildcard pattern'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: albert einstein
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-yahoo-organic-live-regular.json
  observed_cost: 0.002
  capability: yahoo.serp.organic
- id: dataforseo.x.serp-youtube-locations-country
  kind: utility
  tier: extended
  platform: youtube
  method: GET
  path: /serp/youtube/locations/{country}
  cost: {type: free, value: 0, currency: USD, unit: call, source: observed, source_url: 'https://api.dataforseo.com/v3/appendix/user_data', checked: '2026-08-07', note: 'DataForSEO reports cost 0 for this route on our own account — reference/utility data, not metered'}
  name: "YouTube location codes by country"
  summary: You will receive the list of locations by this API call.
  docs_url: https://docs.dataforseo.com/v3/serp/youtube/locations/
  untestable: the OpenAPI spec ships no example request body for this route, so no test request could be generated
- id: dataforseo.x.serp-youtube-organic-live-advanced
  tier: extended
  platform: youtube
  method: POST
  path: /serp/youtube/organic/live/advanced
  name: "YouTube search results for a keyword, live"
  summary: Live SERP provides real-time data on the top 20 blocks of YouTube search engine results.
  docs_url: https://docs.dataforseo.com/v3/serp/youtube/organic/live/advanced/
  input:
    bodyType: json
    body:
      keyword:
        type: string
        required: false
        note: keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword, please specify it as “%25”; if you need to use the “+” character for your keyword,
        example: audi
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don't specify location_name if you use this field, you don't need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to the https://api.dataforseo.com/v
        example: 2840
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don't specify language_name if you use this field, you don't need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      device:
        type: string
        required: false
        note: 'device type optional field return results for a specific device type available values: desktop, mobile'
      block_depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of blocks of results in SERP default value: 20 max value: 200 Note: your account will be billed per each SERP containing up to 20 results; thus, setting a block depth above 20 may result in additional charges if the search engine returns more than 20 results; if t'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don't specify location_code if you use this field, you don't need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to the https://api.dataforse
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don't specify language_code if you use this field, you don't need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      os:
        type: string
        required: false
        note: 'device operating system optional field if you specify desktop in the device field, choose from the following values: windows, macos default value: windows if you specify mobile in the device field, choose from the following values: android, ios default value: android'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      search_param:
        type: string
        required: false
        note: 'additional parameters of the search query optional field example: sp=EgIQAg%253D%253D'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      keyword: audi
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-youtube-organic-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-youtube-video-comments-live-advanced
  tier: extended
  platform: youtube
  method: POST
  path: /serp/youtube/video_comments/live/advanced
  name: "YouTube comments for a video by video id, live"
  summary: Live YouTube Comments provides real-time data on comments on the video you specify in the request.
  docs_url: https://docs.dataforseo.com/v3/serp/youtube/video_comments/live/advanced/
  input:
    bodyType: json
    body:
      video_id:
        type: string
        required: false
        note: 'ID of the video required field you can find video ID in the URL or ‘youtube_video’ item of YouTube Organic result example: vQXvyV0zIP4'
        example: vQXvyV0zIP4
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don’t specify location_code if you use this field, you don’t need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to the https://api.dataforse
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don’t specify location_name if you use this field, you don’t need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to the https://api.dataforseo.com/v
        example: 2840
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don’t specify language_code if you use this field, you don’t need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don’t specify language_name if you use this field, you don’t need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      device:
        type: string
        required: false
        note: 'device type optional field only value: desktop'
      os:
        type: string
        required: false
        note: 'device operating system optional field choose from the following values: windows, macos default value: windows'
      depth:
        type: integer
        required: false
        note: 'parsing depth optional field number of results in SERP default value: 20 max value: 200 Note: your account will be billed per each SERP containing up to 20 results; thus, setting a depth above 20 may result in additional charges if the search engine returns more than 20 results; if the specified dep'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      video_id: vQXvyV0zIP4
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-youtube-video-comments-live-advanced.json
  observed_cost: 0.002
- id: dataforseo.x.serp-youtube-video-info-live-advanced
  tier: extended
  platform: youtube
  method: POST
  path: /serp/youtube/video_info/live/advanced
  name: "YouTube video details by video id, live"
  summary: Live YouTube Video Info provides real-time data on the video you specify in the request.
  docs_url: https://docs.dataforseo.com/v3/serp/youtube/video_info/live/advanced/
  input:
    bodyType: json
    body:
      video_id:
        type: string
        required: false
        note: 'ID of the video required field you can find video ID in the URL or ''youtube_video'' item of YouTube Organic result example: vQXvyV0zIP4'
        example: vQXvyV0zIP4
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don't specify location_name if you use this field, you don't need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to the https://api.dataforseo.com/v
        example: 2840
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don't specify language_name if you use this field, you don't need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      device:
        type: string
        required: false
        note: 'device type optional field only value: desktop'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don't specify location_code if you use this field, you don't need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to the https://api.dataforse
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don't specify language_code if you use this field, you don't need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      os:
        type: string
        required: false
        note: 'device operating system optional field choose from the following values: windows, macos default value: windows'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      video_id: vQXvyV0zIP4
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-youtube-video-info-live-advanced.json
  observed_cost: 0.006
- id: dataforseo.x.serp-youtube-video-subtitles-live-advanced
  tier: extended
  platform: youtube
  method: POST
  path: /serp/youtube/video_subtitles/live/advanced
  name: "YouTube subtitles for a video by video id, live"
  summary: Live YouTube Subtitles provides real-time data on subtitles in the video you specify in the request.
  docs_url: https://docs.dataforseo.com/v3/serp/youtube/video_subtitles/live/advanced/
  input:
    bodyType: json
    body:
      video_id:
        type: string
        required: false
        note: 'ID of the video required field you can find video ID in the URL or ''youtube_video'' item of YouTube Organic result example: Y8Wu4rSNJms'
        example: Y8Wu4rSNJms
      location_code:
        type: integer
        required: false
        note: search engine location code required field if you don't specify location_name if you use this field, you don't need to specify location_name you can receive the list of available locations of the search engines with their location_code by making a separate request to the https://api.dataforseo.com/v
        example: 2840
      language_code:
        type: string
        required: false
        note: search engine language code required field if you don't specify language_name if you use this field, you don't need to specify language_name you can receive the list of available languages of the search engine with their language_code by making a separate request to the https://api.dataforseo.com/v3
        example: en
      device:
        type: string
        required: false
        note: 'device type optional field only value: desktop'
      location_name:
        type: string
        required: false
        note: full name of search engine location required field if you don't specify location_code if you use this field, you don't need to specify location_code you can receive the list of available locations of the search engine with their location_name by making a separate request to the https://api.dataforse
      language_name:
        type: string
        required: false
        note: full name of search engine language required field if you don't specify language_code if you use this field, you don't need to specify language_code you can receive the list of available languages of the search engine with their language_name by making a separate request to the https://api.dataforse
      os:
        type: string
        required: false
        note: 'device operating system optional field choose from the following values: windows, macos default value: windows'
      tag:
        type: string
        required: false
        note: user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response
      subtitles_language:
        type: string
        required: false
        note: language code of original text you can get the language code from YouTube Video Info result
      subtitles_translate_language:
        type: string
        required: false
        note: 'language code of translated text possible values: "az", "ay", "ak", "sq", "am", "en", "ar", "hy", "as", "af", "eu", "be", "bn", "my", "bg", "bs", "bho", "cy", "hu", "vi", "haw", "ht", "gl", "lg", "el", "ka", "gn", "gu", "gd", "da", "fy", "zu", "iw", "ig", "yi", "id", "ga", "is", "es", "it", "yo", "k'
    note: the POST body is an ARRAY of task objects with these fields — one object per task
  test_request:
    body:
    - language_code: en
      location_code: 2840
      video_id: Y8Wu4rSNJms
    bodyType: json
  expect:
    json_path: tasks.0.status_code
    equals: 20000
  verified: '2026-07-28'
  example_response: examples/dataforseo.x.serp-youtube-video-subtitles-live-advanced.json
  observed_cost: 0.006
