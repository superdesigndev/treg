# openhandle - EXTENDED tier: the provider's full endpoint surface, machine-generated by
# scripts/catalog_ingest.py. Do not hand-edit; re-run the script instead. Entries start with
# no capability, no verification and no example response - verification stamps and reviewed
# `capability` mappings (with their platform correction) are added later and carried across
# re-ingests by id via carry_verification. Routes curated in core openhandle.yaml are excluded here.
# Every documented operation outside the core catalog is included, including free Test-data discovery.
# Cross-platform URL dispatch and synthetic-fixture utilities share the creators platform.
# Generated test requests use real public identifiers; synthetic OpenAPI examples only answer on Test keys.
# Paid routes carry the published $0.003 entry rate; verified stamps come from Live calls with a real key.

provider: openhandle
source:
  method: openapi
  spec_urls:
  - https://api.openhandle.dev/openapi.json
  ingested: '2026-09-05'
endpoints:
- id: openhandle.x.get-v1-test-data
  tier: extended
  platform: creators
  scope: any_account
  kind: utility
  method: GET
  path: /v1/test-data
  summary: Search the synthetic test-data catalog
  input:
    queryParams:
      platform:
        type: string
        required: false
        enum:
        - instagram
        - tiktok
        - twitter
        note: Provider-specific public-data filter for this operation.
      resource:
        type: string
        required: false
        enum:
        - profile
        - post
        - comment
        - hashtag
        - location
        - music
        - category
        - list
        - entity
        note: Provider-specific public-data filter for this operation.
      operation:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      traits:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      status:
        type: integer
        required: false
        note: Provider-specific public-data filter for this operation.
      errorCode:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      search:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      limit:
        type: integer
        required: false
        default: 100
        minimum: 1
        maximum: 100
        example: '100'
        note: Maximum number of results to return.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: free
    value: 0
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://api.openhandle.dev/openapi.json
    checked: '2026-09-05'
    confidence: documented
    note: Public, unmetered synthetic-fixture discovery.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      limit: 1
  unverified: Public response returned HTTP 200, but operation metadata contains undocumented fields and null values where OpenAPI requires arrays.
- id: openhandle.x.get-v1-test-data-id
  tier: extended
  platform: creators
  scope: any_account
  kind: utility
  method: GET
  path: /v1/test-data/{id}
  summary: Get one synthetic catalog entry
  input:
    pathParams:
      id:
        type: string
        required: true
        example: instagram.profile.northstar-forge
        note: Provider-specific public-data filter for this operation.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: free
    value: 0
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://api.openhandle.dev/openapi.json
    checked: '2026-09-05'
    confidence: documented
    note: Public, unmetered synthetic-fixture discovery.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      id: instagram.profile.northstar-forge
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.post-v1-urls-fetch
  tier: extended
  platform: creators
  scope: any_account
  kind: utility
  method: POST
  path: /v1/urls/fetch
  summary: Fetch the resource at a social URL
  input:
    bodyType: json
    body:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
      url:
        type: string
        required: true
        minLength: 1
        note: Supported social URL, with or without an https:// scheme.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    body:
      url: https://www.instagram.com/instagram/
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-comments-identifier-likers
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/comments/{identifier}/likers
  summary: List comment likers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      media_id:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Opaque native media ID associated with the selected resource.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '18470057866113934'
    queryParams:
      media_id: '3978880184454783667'
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-comments-identifier-likers.json
- id: openhandle.x.get-v1-instagram-hashtags-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/hashtags/{identifier}
  summary: Get a hashtag
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: syntheticvolume
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: instagram
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-hashtags-identifier.json
- id: openhandle.x.get-v1-instagram-hashtags-identifier-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/hashtags/{identifier}/posts
  summary: List hashtag posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: syntheticvolume
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - recent
        - top
        default: recent
        example: recent
        note: Result ordering for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: instagram
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-hashtags-identifier-posts.json
- id: openhandle.x.get-v1-instagram-hashtags-identifier-reels
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/hashtags/{identifier}/reels
  summary: List hashtag reels
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: instagram
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-hashtags-identifier-reels.json
- id: openhandle.x.get-v1-instagram-highlights-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/highlights/{identifier}
  summary: Get a highlight
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '18142207969557132'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-highlights-identifier.json
- id: openhandle.x.get-v1-instagram-locations-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/locations/{identifier}
  summary: Get a location
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '384695225271379'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-locations-identifier.json
- id: openhandle.x.get-v1-instagram-locations-identifier-guides
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/locations/{identifier}/guides
  summary: List location guides
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '384695225271379'
    queryParams:
      freshness: 30d
  unverified: http 404
- id: openhandle.x.get-v1-instagram-locations-identifier-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/locations/{identifier}/posts
  summary: List location posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - recent
        - top
        default: recent
        example: recent
        note: Result ordering for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '384695225271379'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-locations-identifier-posts.json
- id: openhandle.x.get-v1-instagram-music-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/music/{identifier}
  summary: Get a music
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000003'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '27499285159675348'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-music-identifier.json
- id: openhandle.x.get-v1-instagram-music-identifier-stream
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/music/{identifier}/stream
  summary: List music stream pages
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '27499285159675348'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-music-identifier-stream.json
- id: openhandle.x.get-v1-instagram-posts-identifier-clip-metadata
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/clip-metadata
  summary: Get clip metadata
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-clip-metadata.json
- id: openhandle.x.get-v1-instagram-posts-identifier-comment-metadata
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comment-metadata
  summary: Get comment metadata
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-comment-metadata.json
- id: openhandle.x.get-v1-instagram-posts-identifier-comment-moderation
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comment-moderation
  summary: Check comment moderation
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      comment:
        type: string
        required: true
        minLength: 1
        example: synthetic comment
        note: Provider-specific public-data filter for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      comment: great post
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-comment-moderation.json
- id: openhandle.x.get-v1-instagram-posts-identifier-comments
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comments
  summary: List post comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-comments.json
- id: openhandle.x.get-v1-instagram-posts-identifier-comments-comment-id-replies
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comments/{comment_id}/replies
  summary: List comment replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      comment_id:
        type: string
        required: true
        minLength: 1
        example: '910200000001'
        note: Opaque native comment ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
      comment_id: '18470057866113934'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-comments-comment-id-replies.json
- id: openhandle.x.get-v1-instagram-posts-identifier-insights
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/insights
  summary: Get post insights
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-insights.json
- id: openhandle.x.get-v1-instagram-posts-identifier-likers
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/likers
  summary: List post likers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-likers.json
- id: openhandle.x.get-v1-instagram-posts-identifier-oembed
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/oembed
  summary: Get post oEmbed data
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-oembed.json
- id: openhandle.x.get-v1-instagram-posts-identifier-tagged-users
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/tagged-users
  summary: List tagged users
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-tagged-users.json
- id: openhandle.x.get-v1-instagram-posts-identifier-template
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/template
  summary: Get post template
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-posts-identifier-template.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-about
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/about
  summary: Get profile about information
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-about.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-business-recommendations
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/business-recommendations
  summary: List business recommendations
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-business-recommendations.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-followers
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/followers
  summary: List profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-followers.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-followers-search
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/followers/search
  summary: Search profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      q: instagram
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-followers-search.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-following
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/following
  summary: List profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-following.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-following-search
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/following/search
  summary: Search profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      q: instagram
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-following-search.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-highlights
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/highlights
  summary: List profile highlights
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-highlights.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-pinned-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/pinned-posts
  summary: List profile pinned posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-pinned-posts.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-related
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/related
  summary: List profile related
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-related.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-reposts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/reposts
  summary: List profile reposts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-reposts.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-stories
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/stories
  summary: List profile stories
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-stories.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-suggested
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/suggested
  summary: List profile suggested
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-suggested.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-tagged-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/tagged-posts
  summary: List profile tagged posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-tagged-posts.json
- id: openhandle.x.get-v1-instagram-profiles-identifier-videos
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/videos
  summary: List profile videos
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-profiles-identifier-videos.json
- id: openhandle.x.get-v1-instagram-search-hashtags
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/hashtags
  summary: Search Instagram hashtags
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-search-hashtags.json
- id: openhandle.x.get-v1-instagram-search-music
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/music
  summary: Search Instagram music
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-search-music.json
- id: openhandle.x.get-v1-instagram-search-places
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/places
  summary: Search Instagram places
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-search-places.json
- id: openhandle.x.get-v1-instagram-search-reels
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/reels
  summary: Search Instagram reels
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-search-reels.json
- id: openhandle.x.get-v1-instagram-search-top
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/top
  summary: Search all Instagram results
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  verified: '2026-09-05'
  example_response: examples/openhandle.x.get-v1-instagram-search-top.json
- id: openhandle.x.get-v1-instagram-stories-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/stories/{identifier}
  summary: Get a story
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-effects
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/effects
  summary: Get effects in a batch
  input:
    queryParams:
      ids:
        type: string
        required: true
        minLength: 1
        example: '950000000007'
        note: Comma-separated opaque native IDs.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-effects-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/effects/{identifier}
  summary: Get an effect
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-effects-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/effects/{identifier}/posts
  summary: List effect posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-hashtags-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/hashtags/{identifier}
  summary: Get a hashtag
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: fyp
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-hashtags-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/hashtags/{identifier}/posts
  summary: List hashtag posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: fyp
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-live-events-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/live/events/{identifier}
  summary: Get a live event
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-live-feed
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/live/feed
  summary: List live feed
  input:
    queryParams:
      draw_room_id:
        type: string
        required: false
        minLength: 1
        note: Provider-specific public-data filter for this operation.
      max_time:
        type: string
        required: false
        minLength: 1
        note: Provider-specific public-data filter for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-live-rooms-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/live/rooms/{identifier}
  summary: Get live room info
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      user_id:
        type: string
        required: true
        minLength: 1
        example: '920000000001'
        note: Opaque native user ID required by this provider surface.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-live-rooms-identifier-ranking-types
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/live/rooms/{identifier}/ranking-types
  summary: List live ranking types
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      user_id:
        type: string
        required: true
        minLength: 1
        example: '920000000001'
        note: Opaque native user ID required by this provider surface.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-live-rooms-identifier-rankings
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/live/rooms/{identifier}/rankings
  summary: Get a live ranking
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      user_id:
        type: string
        required: true
        minLength: 1
        example: '920000000001'
        note: Opaque native user ID required by this provider surface.
      rank_type:
        type: string
        required: true
        minLength: 1
        example: daily
        note: Provider ranking type returned by the ranking-types operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-locations-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/locations/{identifier}
  summary: Get a location
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-locations-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/locations/{identifier}/posts
  summary: List location posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-music-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/music/{identifier}
  summary: Get a music
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000003'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-music-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/music/{identifier}/posts
  summary: List music posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000003'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-posts-identifier-comments
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/posts/{identifier}/comments
  summary: List post comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '7606449212716305678'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-posts-identifier-comments-comment-id
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/posts/{identifier}/comments/{comment_id}
  summary: Get a comment
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      comment_id:
        type: string
        required: true
        minLength: 1
        example: '920200000000000001'
        note: Opaque native comment ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-posts-identifier-comments-comment-id-replies
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/posts/{identifier}/comments/{comment_id}/replies
  summary: List comment replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      comment_id:
        type: string
        required: true
        minLength: 1
        example: '920200000000000001'
        note: Opaque native comment ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-followers
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/followers
  summary: List profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-following
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/following
  summary: List profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-liked-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/liked-posts
  summary: List profile liked posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-music
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/music
  summary: List profile music
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-playlists
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/playlists
  summary: List profile playlists
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-playlists-playlist-id
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/playlists/{playlist_id}
  summary: Get a playlist
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      playlist_id:
        type: string
        required: true
        minLength: 1
        example: '950000000006'
        note: Opaque native playlist ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-playlists-playlist-id-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/playlists/{playlist_id}/posts
  summary: List playlist posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      playlist_id:
        type: string
        required: true
        minLength: 1
        example: '950000000006'
        note: Opaque native playlist ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-qr-code
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/qr-code
  summary: Get a profile QR code
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-stories
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/stories
  summary: List profile stories
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-comment-suggestions
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/comment-suggestions
  summary: Search comment suggestions
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-hashtags
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/hashtags
  summary: Search Tiktok hashtags
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-insights
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/insights
  summary: Search insights
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-live
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/live
  summary: Search live streams
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-locations
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/locations
  summary: Search Tiktok locations
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-music
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/music
  summary: Search Tiktok music
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-top
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/top
  summary: Search all TikTok results
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-shop-deals-flash-sale
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/deals/flash-sale
  summary: List flash-sale products
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-shop-deals-new-user
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/deals/new-user
  summary: List new-user deals
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-shop-home
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/home
  summary: List shop home sections
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-shop-live-identifier-products
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/live/{identifier}/products
  summary: List livestream products
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      room_id:
        type: string
        required: true
        minLength: 1
        example: '950000000009'
        note: Opaque native live-room ID.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-products-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/products/{identifier}
  summary: Get a shop product
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-products-identifier-reviews
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/products/{identifier}/reviews
  summary: List product reviews
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-profiles-identifier-page
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/profiles/{identifier}/page
  summary: List seller page products
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      tab_id:
        type: string
        required: false
        minLength: 1
        note: Provider-specific public-data filter for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-profiles-identifier-products
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/profiles/{identifier}/products
  summary: List profile shop products
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-profiles-identifier-tabs
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/profiles/{identifier}/tabs
  summary: List seller tabs
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-recommendations
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/recommendations
  summary: List recommended shop products
  input:
    queryParams:
      category_id:
        type: string
        required: true
        minLength: 1
        example: '950000000008'
        note: Opaque native category ID.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-search
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/search
  summary: Search shop products
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-shop-sellers-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/sellers/{identifier}
  summary: Get a seller
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-sellers-identifier-categories
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/sellers/{identifier}/categories
  summary: List seller categories
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-sellers-identifier-categories-category-id-products
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/sellers/{identifier}/categories/{category_id}/products
  summary: List seller category products
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      category_id:
        type: string
        required: true
        minLength: 1
        example: '950000000008'
        note: Opaque native category ID.
    queryParams:
      product_source_type:
        type: string
        required: true
        minLength: 1
        example: '0'
        note: Provider product-source type returned by the seller surface.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-sellers-identifier-products
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/sellers/{identifier}/products
  summary: List seller products
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort_field:
        type: string
        required: false
        minLength: 1
        note: Provider-specific public-data filter for this operation.
      sort_order:
        type: string
        required: false
        minLength: 1
        note: Provider-specific public-data filter for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-shop-sellers-identifier-sort-types
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/shop/sellers/{identifier}/sort-types
  summary: List seller product sort types
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-stories-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/stories/{identifier}
  summary: Get a story
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-trending-categories
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/trending/categories
  summary: List trending categories
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-trending-insights
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/trending/insights
  summary: List trending insights
  input:
    queryParams:
      tab:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Provider tab identifier for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-trending-music
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/trending/music
  summary: List trending music
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Test response returned HTTP 200, but data was an array where OpenAPI requires an object. Live verification remains pending.
- id: openhandle.x.get-v1-tiktok-trending-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/trending/posts
  summary: List trending posts
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-lists-identifier
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/lists/{identifier}
  summary: Get a Twitter list
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000005'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-twitter-lists-identifier-members
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/lists/{identifier}/members
  summary: List Twitter list members
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000005'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-twitter-lists-identifier-posts
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/lists/{identifier}/posts
  summary: List Twitter list posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000005'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-twitter-posts-identifier-comments
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/posts/{identifier}/comments
  summary: List post comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '940100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '1808168603721650364'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-posts-identifier-comments-comment-id-replies
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/posts/{identifier}/comments/{comment_id}/replies
  summary: List comment replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '940100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      comment_id:
        type: string
        required: true
        minLength: 1
        example: '940200000000000001'
        note: Opaque native comment ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-twitter-posts-identifier-likers
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/posts/{identifier}/likers
  summary: List post likers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '940100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '1808168603721650364'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-posts-identifier-reposters
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/posts/{identifier}/reposters
  summary: List post reposters
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '940100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '1808168603721650364'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-followers
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/followers
  summary: List profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-following
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/following
  summary: List profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-lists
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/lists
  summary: List profile lists
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-media
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/media
  summary: List profile media
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-replies
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/replies
  summary: List profile replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-search-media
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/search/media
  summary: Search Twitter media
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      type:
        type: string
        required: true
        enum:
        - image
        - video
        example: image
        note: Media type filter.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: NASA
      type: image
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-search-suggestions
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/search/suggestions
  summary: Search suggestions
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.003
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-05'
    confidence: documented
    note: Published entry rate per answered request; the Openhandle-List-Price header reported 0.003 on 2026-09-05. Volume tiers ($0.0025 above 10,000 requests a month, $0.0015 above 100,000) and cache hits (24h $0.0005, 7d $0.0001, 30d free) cost less and are margin. Confirmed not-found and private profiles are billed; invalid input, upstream failures and rate limits are not.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: NASA
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
