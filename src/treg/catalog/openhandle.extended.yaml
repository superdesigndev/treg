# openhandle - EXTENDED tier: the provider's full endpoint surface, machine-generated by
# scripts/catalog_ingest.py. Do not hand-edit; re-run the script instead. Entries start with
# no capability, no verification and no example response - verification stamps and reviewed
# `capability` mappings (with their platform correction) are added later and carried across
# re-ingests by id via carry_verification. Routes curated in core openhandle.yaml are excluded here.
# Every documented operation outside the core catalog is included, including free Test-data discovery.
# Cross-platform URL dispatch and synthetic-fixture utilities share the creators platform.
# Generated test requests use real public identifiers; synthetic OpenAPI examples only answer on Test keys.
# Paid routes carry the published $0.0025 prepaid entry rate; verified stamps come from Live calls with a real key.

provider: openhandle
source:
  method: openapi
  spec_urls:
  - https://api.openhandle.dev/openapi.json
  ingested: '2026-09-21'
  version: 1.1.0
endpoints:
- id: openhandle.x.get-v1-test-data
  tier: extended
  platform: creators
  scope: any_account
  kind: utility
  method: GET
  path: /v1/test-data
  summary: Search the synthetic test-data catalog
  input:
    queryParams:
      platform:
        type: string
        required: false
        enum:
        - instagram
        - tiktok
        - twitter
        - reddit
        note: Provider-specific public-data filter for this operation.
      resource:
        type: string
        required: false
        enum:
        - profile
        - post
        - comment
        - hashtag
        - location
        - music
        - category
        - list
        - entity
        - subreddit
        - rule
        - wiki
        - trophy
        note: Provider-specific public-data filter for this operation.
      operation:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      traits:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      status:
        type: integer
        required: false
        note: Provider-specific public-data filter for this operation.
      errorCode:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      search:
        type: string
        required: false
        note: Provider-specific public-data filter for this operation.
      limit:
        type: integer
        required: false
        default: 100
        minimum: 1
        maximum: 100
        example: '100'
        note: Maximum number of results to return.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: free
    value: 0
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://api.openhandle.dev/openapi.json
    checked: '2026-09-21'
    confidence: documented
    note: Public, unmetered synthetic-fixture discovery.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      limit: 1
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-test-data-id
  tier: extended
  platform: creators
  scope: any_account
  kind: utility
  method: GET
  path: /v1/test-data/{id}
  summary: Get one synthetic catalog entry
  input:
    pathParams:
      id:
        type: string
        required: true
        example: instagram.profile.northstar-forge
        note: Provider-specific public-data filter for this operation.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: free
    value: 0
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://api.openhandle.dev/openapi.json
    checked: '2026-09-21'
    confidence: documented
    note: Public, unmetered synthetic-fixture discovery.
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      id: instagram.profile.northstar-forge
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.post-v1-urls-fetch
  tier: extended
  platform: creators
  scope: any_account
  kind: utility
  method: POST
  path: /v1/urls/fetch
  summary: Fetch the resource at a social URL
  input:
    bodyType: json
    body:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
      url:
        type: string
        required: true
        minLength: 1
        note: Supported social URL, with or without an https:// scheme.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    body:
      url: https://www.instagram.com/instagram/
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-comments-identifier-likers
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/comments/{identifier}/likers
  summary: List comment likers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      media_id:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Opaque native media ID associated with the selected resource.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '18470057866113934'
    queryParams:
      media_id: '3978880184454783667'
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-hashtags-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/hashtags/{identifier}
  summary: Get a hashtag
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: syntheticvolume
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: instagram
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-hashtags-identifier-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/hashtags/{identifier}/posts
  summary: List hashtag posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: syntheticvolume
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - recent
        - top
        default: recent
        example: recent
        note: Result ordering for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: instagram
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-hashtags-identifier-reels
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/hashtags/{identifier}/reels
  summary: List hashtag reels
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: instagram
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-highlights-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/highlights/{identifier}
  summary: Get a highlight
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '18142207969557132'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-locations-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/locations/{identifier}
  summary: Get a location
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '384695225271379'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-locations-identifier-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/locations/{identifier}/posts
  summary: List location posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - recent
        - top
        default: recent
        example: recent
        note: Result ordering for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '384695225271379'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-music-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/music/{identifier}
  summary: Get a music
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000003'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '27499285159675348'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-music-identifier-stream
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/music/{identifier}/stream
  summary: List music stream pages
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '27499285159675348'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-clip-metadata
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/clip-metadata
  summary: Get clip metadata
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-comment-metadata
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comment-metadata
  summary: Get comment metadata
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-comment-moderation
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comment-moderation
  summary: Check comment moderation
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      comment:
        type: string
        required: true
        minLength: 1
        example: synthetic comment
        note: Provider-specific public-data filter for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      comment: great post
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-comments
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comments
  summary: List post comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-comments-comment-id-replies
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/comments/{comment_id}/replies
  summary: List comment replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      comment_id:
        type: string
        required: true
        minLength: 1
        example: '910200000001'
        note: Opaque native comment ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
      comment_id: '18470057866113934'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-insights
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/insights
  summary: Get post insights
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-likers
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/likers
  summary: List post likers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-oembed
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/oembed
  summary: Get post oEmbed data
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-tagged-users
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/tagged-users
  summary: List tagged users
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-posts-identifier-template
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/posts/{identifier}/template
  summary: Get post template
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: Dc30nJeRKKz
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-about
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/about
  summary: Get profile about information
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-business-recommendations
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/business-recommendations
  summary: List business recommendations
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-followers
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/followers
  summary: List profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-followers-search
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/followers/search
  summary: Search profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      q: instagram
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-following
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/following
  summary: List profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-following-search
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/following/search
  summary: Search profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      q: instagram
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-highlights
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/highlights
  summary: List profile highlights
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-pinned-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/pinned-posts
  summary: List profile pinned posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-reposts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/reposts
  summary: List profile reposts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-stories
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/stories
  summary: List profile stories
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-suggested
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/suggested
  summary: List profile suggested
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-tagged-posts
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/tagged-posts
  summary: List profile tagged posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-profiles-identifier-videos
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/profiles/{identifier}/videos
  summary: List profile videos
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@northstar_forge_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@instagram'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-search-hashtags
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/hashtags
  summary: Search Instagram hashtags
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-search-music
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/music
  summary: Search Instagram music
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-search-places
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/places
  summary: Search Instagram places
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-search-reels
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/reels
  summary: Search Instagram reels
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-search-top
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/search/top
  summary: Search all Instagram results
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: instagram
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-instagram-stories-identifier
  tier: extended
  platform: instagram
  scope: any_account
  kind: data
  method: GET
  path: /v1/instagram/stories/{identifier}
  summary: Get a story
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '910100000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-reddit-domains-identifier-posts
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/domains/{identifier}/posts
  summary: List domain posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: example.com
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - hot
        - new
        - top
        - rising
        - controversial
        default: hot
        example: hot
        note: Result ordering for this operation.
      t:
        type: string
        required: false
        enum:
        - hour
        - day
        - week
        - month
        - year
        - all
        note: Provider-specific public-data filter for this operation.
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        note: Maximum number of results to return.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: python.org
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-posts-identifier
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/posts/{identifier}
  summary: Get a post
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-reddit-posts-identifier-comments
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/posts/{identifier}/comments
  summary: List post comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - confidence
        - top
        - new
        - controversial
        - old
        - qa
        default: confidence
        example: confidence
        note: Result ordering for this operation.
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        note: Maximum number of results to return.
      depth:
        type: integer
        required: false
        minimum: 0
        maximum: 10
        note: Provider-specific public-data filter for this operation.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-reddit-posts-identifier-duplicates
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/posts/{identifier}/duplicates
  summary: List duplicate posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        note: Maximum number of results to return.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-reddit-profiles-identifier-comments
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/profiles/{identifier}/comments
  summary: List profile comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@synthetic_reddit'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - new
        - hot
        - top
        - controversial
        default: new
        example: new
        note: Result ordering for this operation.
      t:
        type: string
        required: false
        enum:
        - hour
        - day
        - week
        - month
        - year
        - all
        note: Provider-specific public-data filter for this operation.
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        note: Maximum number of results to return.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@spez'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-profiles-identifier-moderated
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/profiles/{identifier}/moderated
  summary: List moderated subreddits
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@synthetic_reddit'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@spez'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-profiles-identifier-trophies
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/profiles/{identifier}/trophies
  summary: List profile trophies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@synthetic_reddit'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@spez'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-search-profiles
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/search/profiles
  summary: Search profiles
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        example: synthetic
        note: Maximum number of results to return.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: python
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-subreddits-new
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/subreddits/new
  summary: List new subreddits
  input:
    queryParams:
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        note: Maximum number of results to return.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-subreddits-popular
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/subreddits/popular
  summary: List popular subreddits
  input:
    queryParams:
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        note: Maximum number of results to return.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-subreddits-identifier-rules
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/subreddits/{identifier}/rules
  summary: List subreddit rules
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: python
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-subreddits-identifier-wiki-pages
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/subreddits/{identifier}/wiki-pages
  summary: List wiki pages
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: python
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-subreddits-identifier-wiki-pages-page
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/subreddits/{identifier}/wiki-pages/{page}
  summary: Get a wiki page
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      page:
        type: string
        required: true
        minLength: 1
        example: index
        note: Provider-specific public-data filter for this operation.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: python
      page: index
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-reddit-trending-posts
  tier: extended
  platform: reddit
  scope: any_account
  kind: data
  method: GET
  path: /v1/reddit/trending/posts
  summary: List trending posts
  input:
    queryParams:
      sort:
        type: string
        required: false
        enum:
        - hot
        - new
        - top
        - rising
        - controversial
        - best
        default: hot
        example: hot
        note: Result ordering for this operation.
      t:
        type: string
        required: false
        enum:
        - hour
        - day
        - week
        - month
        - year
        - all
        note: Provider-specific public-data filter for this operation.
      limit:
        type: integer
        required: false
        default: 25
        minimum: 1
        maximum: 100
        note: Maximum number of results to return.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-effects
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/effects
  summary: Get effects in a batch
  input:
    queryParams:
      ids:
        type: string
        required: true
        minLength: 1
        example: '950000000007'
        note: Comma-separated opaque native IDs.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-effects-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/effects/{identifier}
  summary: Get an effect
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-effects-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/effects/{identifier}/posts
  summary: List effect posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-hashtags-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/hashtags/{identifier}
  summary: Get a hashtag
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: fyp
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-hashtags-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/hashtags/{identifier}/posts
  summary: List hashtag posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: fyp
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-locations-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/locations/{identifier}
  summary: Get a location
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '22535865201205278'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-locations-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/locations/{identifier}/posts
  summary: List location posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000002'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '22535865201205278'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-music-identifier
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/music/{identifier}
  summary: Get a music
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000003'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '7240110572024645634'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-music-identifier-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/music/{identifier}/posts
  summary: List music posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000003'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '7240110572024645634'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-posts-identifier-comments
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/posts/{identifier}/comments
  summary: List post comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '7606449212716305678'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-posts-identifier-comments-comment-id-replies
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/posts/{identifier}/comments/{comment_id}/replies
  summary: List comment replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '920100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      comment_id:
        type: string
        required: true
        minLength: 1
        example: '920200000000000001'
        note: Opaque native comment ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '7606449212716305678'
      comment_id: '7606779946837164820'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-followers
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/followers
  summary: List profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-following
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/following
  summary: List profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-liked-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/liked-posts
  summary: List profile liked posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-playlists
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/playlists
  summary: List profile playlists
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@tiktok'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-playlists-playlist-id
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/playlists/{playlist_id}
  summary: Get a playlist
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      playlist_id:
        type: string
        required: true
        minLength: 1
        example: '950000000006'
        note: Opaque native playlist ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-profiles-identifier-playlists-playlist-id-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/profiles/{identifier}/playlists/{playlist_id}/posts
  summary: List playlist posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@pixel_orchard_tt_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      playlist_id:
        type: string
        required: true
        minLength: 1
        example: '950000000006'
        note: Opaque native playlist ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-tiktok-search-hashtags
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/hashtags
  summary: Search Tiktok hashtags
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-locations
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/locations
  summary: Search Tiktok locations
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-search-music
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/search/music
  summary: Search Tiktok music
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: tiktok
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-trending-categories
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/trending/categories
  summary: List trending categories
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-trending-music
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/trending/music
  summary: List trending music
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-tiktok-trending-posts
  tier: extended
  platform: tiktok
  scope: any_account
  kind: data
  method: GET
  path: /v1/tiktok/trending/posts
  summary: List trending posts
  input:
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-lists-identifier-posts
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/lists/{identifier}/posts
  summary: List Twitter list posts
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '950000000005'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  unverified: No real public identifier is on file for this route's required input; not called.
- id: openhandle.x.get-v1-twitter-posts-identifier-comments
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/posts/{identifier}/comments
  summary: List post comments
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '940100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '1808168603721650364'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-posts-identifier-comments-comment-id-replies
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/posts/{identifier}/comments/{comment_id}/replies
  summary: List comment replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '940100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
      comment_id:
        type: string
        required: true
        minLength: 1
        example: '940200000000000001'
        note: Opaque native comment ID.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '1808168603721650364'
      comment_id: '1808170286425964893'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-posts-identifier-reposters
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/posts/{identifier}/reposters
  summary: List post reposters
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '940100000000000001'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '1808168603721650364'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-followers
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/followers
  summary: List profile followers
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-following
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/following
  summary: List profile following
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-media
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/media
  summary: List profile media
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-profiles-identifier-replies
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/profiles/{identifier}/replies
  summary: List profile replies
  input:
    pathParams:
      identifier:
        type: string
        required: true
        minLength: 1
        example: '@copperfield_lab_x_test'
        note: Public resource reference. Profile paths accept a username with an @ prefix, such as @openai, or the stable platform ID. Post paths accept the platform post ID or shortcode.
    queryParams:
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    pathParams:
      identifier: '@NASA'
    queryParams:
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-search-media
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/search/media
  summary: Search Twitter media
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      type:
        type: string
        required: true
        enum:
        - image
        - video
        example: image
        note: Media type filter.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: NASA
      type: image
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
- id: openhandle.x.get-v1-twitter-search-suggestions
  tier: extended
  platform: x
  scope: any_account
  kind: data
  method: GET
  path: /v1/twitter/search/suggestions
  summary: Search suggestions
  input:
    queryParams:
      q:
        type: string
        required: true
        minLength: 1
        example: synthetic
        note: Public search query.
      freshness:
        type: string
        required: false
        enum:
        - live
        - 24h
        - 7d
        - 30d
        default: 24h
        example: 24h
        note: Maximum accepted data age. The default is 24h.
      cursor:
        type: string
        required: false
        note: Opaque cursor returned by the previous page.
    note: OpenAPI examples can refer to synthetic Test fixtures. Use real public identifiers with a Live key. freshness=30d makes cache hits free; cache misses still fetch and bill Live data.
  cost:
    type: per_call
    value: 0.0025
    currency: USD
    per: 1
    unit: call
    source: docs
    source_url: https://openhandle.dev/pricing.md
    checked: '2026-09-21'
    confidence: documented
    note: 'Published prepaid entry rate per answered request: $0.0025 for the first 100,000 requests per month, then $0.002. Cache hits cost less: 24h $0.0005, 7d $0.0001, 30d free; cache misses fetch and bill Live data. Confirmed not-found and private profiles are billed; invalid input, provider failures and rate limits are not.'
  docs_url: https://openhandle.dev/docs/api-reference
  test_request:
    queryParams:
      q: NASA
      freshness: 30d
  unverified: Imported from OpenAPI; a Live call has not verified this route yet.
