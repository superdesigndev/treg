# openrouter - EXTENDED tier: the provider's full endpoint surface, machine-generated by
# scripts/catalog_ingest.py. Do not hand-edit; re-run the script instead. Entries start with
# no capability, no verification and no example response - verification stamps and reviewed
# names and kinds are added later and carried across; capability mappings stay in core
# re-ingests by id via carry_verification. Routes curated in core openrouter.yaml are excluded here.
# Generated from the live video model rate card. Each row fixes one model on POST /videos.
# pricing_skus become a first-match reserve table; terminal usage.cost remains authoritative.

provider: openrouter
source:
  spec_urls:
  - https://openrouter.ai/api/v1/videos/models
  ingested: '2026-09-02'
async:
  id_from: id
  poll:
    endpoint: openrouter.video-gen.task.status
    param:
      in: pathParams
      name: id
  status:
    path: status
    success:
    - completed
    failure:
    - failed
    - cancelled
    - expired
  result:
    fetch: openrouter.video-gen.result.retrieve
    fetch_param:
      in: pathParams
      name: id
      value_from: id
  interval: 30
endpoints:
- id: openrouter.x.alibaba-happyhorse-1-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Alibaba: HappyHorse 1.0'
  summary: HappyHorse 1.0 is a video generation model from Alibaba. It generates short videos from a text prompt, a single starting image, or a set of reference images, with output up...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - alibaba/happyhorse-1.0
        example: alibaba/happyhorse-1.0
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 3
        max: 15
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
        - '4:3'
        - '3:4'
        - '21:9'
        - '9:21'
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: alibaba/happyhorse-1.0
        body.resolution: 1080p
      value: 0.1694
      times: body.duration
    - when:
        body.model: alibaba/happyhorse-1.0
        body.resolution: 720p
      value: 0.0988
      times: body.duration
    fallback:
      value: 2.541000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/alibaba/happyhorse-1.0
- id: openrouter.x.alibaba-happyhorse-1-1
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Alibaba: HappyHorse 1.1'
  summary: HappyHorse 1.1 is a video generation model from Alibaba. It generates short videos from a text prompt, a single starting image, or a set of reference images, with output up...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - alibaba/happyhorse-1.1
        example: alibaba/happyhorse-1.1
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 3
        max: 15
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
        - '4:3'
        - '3:4'
        - '21:9'
        - '9:21'
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: alibaba/happyhorse-1.1
        body.resolution: 1080p
      value: 0.1278
      times: body.duration
    - when:
        body.model: alibaba/happyhorse-1.1
        body.resolution: 720p
      value: 0.0988
      times: body.duration
    fallback:
      value: 1.917000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/alibaba/happyhorse-1.1
- id: openrouter.x.alibaba-wan-2-6
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Alibaba: Wan 2.6'
  summary: Alibaba's most advanced video generation model, supporting over 10 visual creation capabilities in a unified system. Wan 2.6 generates 1080p video at 24fps from text, images, reference videos, or audio,...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - alibaba/wan-2.6
        example: alibaba/wan-2.6
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 5
        max: 10
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: alibaba/wan-2.6
        body.resolution: 1080p
      value: 0.15
      times: body.duration
    - when:
        body.model: alibaba/wan-2.6
        body.resolution: 720p
      value: 0.1
      times: body.duration
    - when:
        body.model: alibaba/wan-2.6
      value: 0.04
      times: body.duration
    fallback:
      value: 1.500000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/alibaba/wan-2.6
- id: openrouter.x.alibaba-wan-2-7
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Alibaba: Wan 2.7'
  summary: Wan 2.7 is a video generation model from Alibaba. It supports text-to-video, image-to-video with first and last frame control, and reference-to-video, where multiple reference images guide the style and content...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - alibaba/wan-2.7
        example: alibaba/wan-2.7
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 2
        max: 10
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
        - '4:3'
        - '3:4'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: alibaba/wan-2.7
      value: 0.1
      times: body.duration
    fallback:
      value: 1.000000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/alibaba/wan-2.7
- id: openrouter.x.alibaba-wan-3-0-prime
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Alibaba: Wan 3.0 Prime'
  summary: Wan 3.0 Prime is a fast-mode variant of [Wan 3.0](https://openrouter.ai/alibaba/wan-3.0) from Alibaba. It supports text-to-video and first-frame image-to-video generation.
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - alibaba/wan-3.0-prime
        example: alibaba/wan-3.0-prime
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 2
        max: 30
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '4:3'
        - '1:1'
        - '3:4'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: alibaba/wan-3.0-prime
        body.resolution: 1080p
      value: 0.28
      times: body.duration
    - when:
        body.model: alibaba/wan-3.0-prime
        body.resolution: 480p
      value: 0.068
      times: body.duration
    - when:
        body.model: alibaba/wan-3.0-prime
        body.resolution: 720p
      value: 0.14
      times: body.duration
    fallback:
      value: 8.400000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/alibaba/wan-3.0-prime
- id: openrouter.x.black-forest-labs-flux-3-video
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Black Forest Labs: FLUX.3 Video'
  summary: FLUX.3 Video is a video generation model from Black Forest Labs. It supports text-to-video, image-guided generation with opening and closing keyframes, and video continuation workflows, making it suited for controlled...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - black-forest-labs/flux-3-video
        example: black-forest-labs/flux-3-video
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 5
        max: 20
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '21:9'
        enum:
        - '21:9'
        - '16:9'
        - '4:3'
        - '1:1'
        - '3:4'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: black-forest-labs/flux-3-video
        body.resolution: 1080p
      value: 0.53
      times: body.duration
    - when:
        body.model: black-forest-labs/flux-3-video
        body.resolution: 720p
      value: 0.41
      times: body.duration
    - when:
        body.model: black-forest-labs/flux-3-video
      value: 0.17
      times: body.duration
    fallback:
      value: 10.600000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/black-forest-labs/flux-3-video
- id: openrouter.x.black-forest-labs-flux-video-upscale
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Black Forest Labs: FLUX Video Upscale'
  summary: FLUX Video Upscale is a video upscaling model from Black Forest Labs. It enlarges a single source video by 1.5× to 3× while preserving its duration, with an optional prompt...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - black-forest-labs/flux-video-upscale
        example: black-forest-labs/flux-video-upscale
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 5
        max: 30
      generate_audio:
        type: boolean
        required: false
        default: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      cents_per_megapixel_second_creative: '10.5'
      cents_per_megapixel_second_precise: '7.5'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/black-forest-labs/flux-video-upscale
- id: openrouter.x.bytedance-seedance-1-5-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'ByteDance: Seedance 1.5 Pro'
  summary: ByteDance's next-generation audio-visual generation model with a 4.5B parameter Dual-Branch Diffusion Transformer architecture. Seedance 1.5 Pro generates video and audio simultaneously in a single unified pass - eliminating the timing...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - bytedance/seedance-1-5-pro
        example: bytedance/seedance-1-5-pro
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 12
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '1:1'
        enum:
        - '1:1'
        - '3:4'
        - '9:16'
        - '9:21'
        - '4:3'
        - '16:9'
        - '21:9'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      video_tokens: '0.0000024'
      video_tokens_without_audio: '0.0000012'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/bytedance/seedance-1-5-pro
- id: openrouter.x.bytedance-seedance-2-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'ByteDance: Seedance 2.0'
  summary: Seedance 2.0 is a video generation model from ByteDance. It supports text-to-video, image-to-video with first and last frame control, and multimodal reference-to-video. It is particularly strong at preserving character consistency,...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - bytedance/seedance-2.0
        example: bytedance/seedance-2.0
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 15
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
        - 1080p
        - 4K
      aspect_ratio:
        type: string
        required: false
        default: '1:1'
        enum:
        - '1:1'
        - '3:4'
        - '9:16'
        - '4:3'
        - '16:9'
        - '21:9'
        - '9:21'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      video_tokens: '0.000007'
      video_tokens_1080p: '0.0000077'
      video_tokens_1080p_with_video_input: '0.0000047'
      video_tokens_4k: '0.000004'
      video_tokens_4k_with_video_input: '0.0000024'
      video_tokens_with_video_input: '0.0000043'
      video_tokens_without_audio: '0.000007'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/bytedance/seedance-2.0
- id: openrouter.x.bytedance-seedance-2-0-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'ByteDance: Seedance 2.0 Fast'
  summary: Seedance 2.0 Fast is a video generation model from ByteDance. It supports text-to-video, image-to-video with first and last frame control, and multimodal reference-to-video. It prioritizes generation speed and lower cost...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - bytedance/seedance-2.0-fast
        example: bytedance/seedance-2.0-fast
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 15
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '1:1'
        enum:
        - '1:1'
        - '3:4'
        - '9:16'
        - '4:3'
        - '16:9'
        - '21:9'
        - '9:21'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      video_tokens: '0.0000042'
      video_tokens_with_video_input: '0.000002475'
      video_tokens_without_audio: '0.0000042'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/bytedance/seedance-2.0-fast
- id: openrouter.x.bytedance-seedance-2-0-mini
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'ByteDance: Seedance 2.0 Mini'
  summary: Seedance 2.0 Mini is a video generation model from ByteDance. It supports text-to-video, image-to-video with first and last frame control, and multimodal reference-to-video with image, video, and audio inputs. It...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - bytedance/seedance-2.0-mini
        example: bytedance/seedance-2.0-mini
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 15
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '1:1'
        enum:
        - '1:1'
        - '3:4'
        - '9:16'
        - '4:3'
        - '16:9'
        - '21:9'
        - '9:21'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      video_tokens: '0.0000035'
      video_tokens_with_video_input: '0.0000021'
      video_tokens_without_audio: '0.0000035'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/bytedance/seedance-2.0-mini
- id: openrouter.x.bytedance-seedance-2-5
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'ByteDance: Seedance 2.5'
  summary: Seedance 2.5 is a video generation model from ByteDance. It is suited for long-form storytelling, multimodal reference-based generation, video editing, and video extension. It supports first-frame and first-and-last-frame control, up...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - bytedance/seedance-2.5
        example: bytedance/seedance-2.5
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 30
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '4:3'
        - '1:1'
        - '3:4'
        - '9:16'
        - '21:9'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      video_tokens: '0.0000107'
      video_tokens_with_video_input: '0.0000064'
      video_tokens_without_audio: '0.0000107'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/bytedance/seedance-2.5
- id: openrouter.x.google-veo-3-1
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Google: Veo 3.1'
  summary: Google's state-of-the-art video generation model, built for maximum visual fidelity in final production cuts. Veo 3.1 generates high-quality 1080p video from text or image prompts with native synchronized audio -...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - google/veo-3.1
        example: google/veo-3.1
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 8
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
        - 4K
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: google/veo-3.1
        body.resolution: 4K
        body.generate_audio: false
      value: 0.4
      times: body.duration
    - when:
        body.model: google/veo-3.1
        body.resolution: 4K
        body.generate_audio: true
      value: 0.6
      times: body.duration
    - when:
        body.model: google/veo-3.1
        body.generate_audio: false
      value: 0.2
      times: body.duration
    - when:
        body.model: google/veo-3.1
        body.generate_audio: true
      value: 0.4
      times: body.duration
    fallback:
      value: 4.800000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/google/veo-3.1
- id: openrouter.x.google-veo-3-1-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Google: Veo 3.1 Fast'
  summary: Google's mid-tier video generation model balancing speed and quality. Veo 3.1 Fast generates high-quality video from text or image prompts with native synchronized audio, offering faster turnaround than Veo 3.1...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - google/veo-3.1-fast
        example: google/veo-3.1-fast
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 8
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
        - 4K
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: google/veo-3.1-fast
        body.resolution: 4K
        body.generate_audio: false
      value: 0.25
      times: body.duration
    - when:
        body.model: google/veo-3.1-fast
        body.resolution: 720p
        body.generate_audio: false
      value: 0.08
      times: body.duration
    - when:
        body.model: google/veo-3.1-fast
        body.resolution: 4K
        body.generate_audio: true
      value: 0.3
      times: body.duration
    - when:
        body.model: google/veo-3.1-fast
        body.resolution: 720p
        body.generate_audio: true
      value: 0.1
      times: body.duration
    - when:
        body.model: google/veo-3.1-fast
        body.generate_audio: false
      value: 0.1
      times: body.duration
    - when:
        body.model: google/veo-3.1-fast
        body.generate_audio: true
      value: 0.12
      times: body.duration
    fallback:
      value: 2.400000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/google/veo-3.1-fast
- id: openrouter.x.google-veo-3-1-lite
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Google: Veo 3.1 Lite'
  summary: Google's most cost-effective video generation model, designed for high-volume applications and rapid iteration. Veo 3.1 Lite generates 720p and 1080p video from text or image prompts with native synchronized audio...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - google/veo-3.1-lite
        example: google/veo-3.1-lite
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 8
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: google/veo-3.1-lite
        body.resolution: 720p
        body.generate_audio: false
      value: 0.03
      times: body.duration
    - when:
        body.model: google/veo-3.1-lite
        body.resolution: 720p
        body.generate_audio: true
      value: 0.05
      times: body.duration
    - when:
        body.model: google/veo-3.1-lite
        body.generate_audio: false
      value: 0.05
      times: body.duration
    - when:
        body.model: google/veo-3.1-lite
        body.generate_audio: true
      value: 0.08
      times: body.duration
    fallback:
      value: 0.640000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/google/veo-3.1-lite
- id: openrouter.x.heygen-avatar-iv
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'HeyGen: Avatar IV'
  summary: 'HeyGen: Avatar IV is an image-to-video model that animates a single photo into an expressive, lip-synced talking-head video. Rather than only matching mouth shapes to words, it interprets the vocal...'
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - heygen/avatar-iv
        example: heygen/avatar-iv
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 5
        max: 30
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
      generate_audio:
        type: boolean
        required: false
        default: false
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: heygen/avatar-iv
      value: 0.05
      times: body.duration
    fallback:
      value: 1.500000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/heygen/avatar-iv
- id: openrouter.x.kwaivgi-kling-v3-0-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Kling: Video v3.0 Pro'
  summary: Kling v3.0 Pro is Kuaishou's premium video generation model, offering higher visual quality than the Standard tier. It supports text-to-video and image-to-video workflows, with first-frame and last-frame control for precise...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - kwaivgi/kling-v3.0-pro
        example: kwaivgi/kling-v3.0-pro
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 3
        max: 15
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: kwaivgi/kling-v3.0-pro
        body.generate_audio: true
      value: 0.168
      times: body.duration
    - when:
        body.model: kwaivgi/kling-v3.0-pro
        body.resolution: 720p
      value: 0.112
      times: body.duration
    - when:
        body.model: kwaivgi/kling-v3.0-pro
      value: 0.112
      times: body.duration
    fallback:
      value: 2.520000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/kwaivgi/kling-v3.0-pro
- id: openrouter.x.kwaivgi-kling-v3-0-std
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Kling: Video v3.0 Standard'
  summary: Kling v3.0 Standard is a video generation model from Kuaishou. It supports text-to-video and image-to-video workflows, with first-frame and last-frame control for guided scene composition. Clips range from 3 to...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - kwaivgi/kling-v3.0-std
        example: kwaivgi/kling-v3.0-std
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 3
        max: 15
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: kwaivgi/kling-v3.0-std
        body.generate_audio: true
      value: 0.126
      times: body.duration
    - when:
        body.model: kwaivgi/kling-v3.0-std
        body.resolution: 720p
      value: 0.084
      times: body.duration
    - when:
        body.model: kwaivgi/kling-v3.0-std
      value: 0.084
      times: body.duration
    fallback:
      value: 1.890000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/kwaivgi/kling-v3.0-std
- id: openrouter.x.kwaivgi-kling-video-o1
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Kling: Video O1'
  summary: Kling Video O1 is a video generation model from Kuaishou. It supports text and image inputs with video output, enabling text-to-video and image-to-video workflows. It is suited for cinematic content...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - kwaivgi/kling-video-o1
        example: kwaivgi/kling-video-o1
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 5
        max: 10
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: kwaivgi/kling-video-o1
      value: 0.112
      times: body.duration
    fallback:
      value: 1.120000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/kwaivgi/kling-video-o1
- id: openrouter.x.minimax-hailuo-2-3
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'MiniMax: Hailuo 2.3'
  summary: Hailuo 2.3 is a video generation model from MiniMax. It accepts text prompts and reference images as input and generates video output, supporting both text-to-video and image-to-video workflows. It is...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - minimax/hailuo-2.3
        example: minimax/hailuo-2.3
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 6
        max: 10
      resolution:
        type: string
        required: false
        default: 1080p
        enum:
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
      generate_audio:
        type: boolean
        required: false
        default: false
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: minimax/hailuo-2.3
      value: 0.0817
      times: body.duration
    fallback:
      value: 0.817000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/minimax/hailuo-2.3
- id: openrouter.x.minimax-hailuo-3
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'MiniMax: H3'
  summary: MiniMax H3 is a lightweight, open-weights video generation model from MiniMax. It is designed for precise multimodal editing and controlled content generation, including instruction-guided edits, text and brand rendering, and...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - minimax/hailuo-3
        example: minimax/hailuo-3
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 5
        max: 15
      resolution:
        type: string
        required: false
        default: 2K
        enum:
        - 2K
      aspect_ratio:
        type: string
        required: false
        default: '21:9'
        enum:
        - '21:9'
        - '16:9'
        - '4:3'
        - '1:1'
        - '3:4'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      duration_seconds: '0.13'
      reference_images: '0.04'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/minimax/hailuo-3
- id: openrouter.x.openai-sora-2-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'OpenAI: Sora 2 Pro'
  summary: OpenAI's flagship video generation model, delivering production-quality video with physics-accurate motion, synchronized audio, and world-state persistence across shots. Sora 2 Pro follows intricate multi-shot instructions while maintaining consistent spatial relationships...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - openai/sora-2-pro
        example: openai/sora-2-pro
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 4
        max: 20
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: true
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: openai/sora-2-pro
        body.resolution: 1080p
      value: 0.5
      times: body.duration
    - when:
        body.model: openai/sora-2-pro
        body.resolution: 720p
      value: 0.3
      times: body.duration
    - when:
        body.model: openai/sora-2-pro
      value: 0.5
      times: body.duration
    fallback:
      value: 10.000000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/openai/sora-2-pro
- id: openrouter.x.runway-aleph-2
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Runway: Aleph 2.0'
  summary: Runway Aleph 2.0 is an in-context video editing model from Runway. It applies text instructions and keyframe-guided edits across existing footage while preserving details that are not meant to change....
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - runway/aleph-2
        example: runway/aleph-2
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 5
        max: 30
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '4:3'
        - '3:2'
        - '1:1'
        - '2:3'
        - '3:4'
        - '9:16'
        - '21:9'
      generate_audio:
        type: boolean
        required: false
        default: false
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: runway/aleph-2
      value: 0.28
      times: body.duration
    fallback:
      value: 8.400000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/runway/aleph-2
- id: openrouter.x.runway-gen-4-5
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'Runway: Gen-4.5'
  summary: Runway Gen-4.5 is a video generation model from Runway for text-to-video and image-to-video workflows. It is designed for cinematic scene creation with strong motion quality, visual fidelity, and prompt adherence....
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - runway/gen-4.5
        example: runway/gen-4.5
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 2
        max: 10
      resolution:
        type: string
        required: false
        default: 720p
        enum:
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
      generate_audio:
        type: boolean
        required: false
        default: false
    bodyType: json
  cost:
    type: per_success
    table:
    - when:
        body.model: runway/gen-4.5
      value: 0.12
      times: body.duration
    fallback:
      value: 1.200000001
      note: The highest live SKU multiplied by the model's maximum duration is the global usage-settlement reservation ceiling.
    currency: USD
    settle: usage
    usage:
      path: usage.cost
      unit: usd
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    confidence: documented
    note: Table rows quote the live rate card. Indistinguishable mode-specific SKUs collapse to the highest rate. The matched row is reserved and the terminal usage.cost settles; observed charges can include a minimum or fee absent from pricing_skus, so a settle may exceed its reserve (reconcile lists overruns).
  docs_url: https://openrouter.ai/runway/gen-4.5
- id: openrouter.x.x-ai-grok-imagine-video
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'SpaceXAI: Grok Imagine Video'
  summary: Grok Imagine Video is SpaceXAI's fast, text-, image-, and reference-conditioned video generation model. It produces short videos (1–15 seconds, 24 fps) at 480p or 720p across seven aspect ratios -...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - x-ai/grok-imagine-video
        example: x-ai/grok-imagine-video
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 1
        max: 15
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
        - '4:3'
        - '3:4'
        - '3:2'
        - '2:3'
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      cents_per_image_input: '0.2'
      cents_per_video_output_second_480p: '5'
      cents_per_video_output_second_720p: '7'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/x-ai/grok-imagine-video
- id: openrouter.x.x-ai-grok-imagine-video-1-5
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /videos
  name: 'SpaceXAI: Grok Imagine Video 1.5'
  summary: Grok Imagine Video 1.5 is a video generation model from SpaceXAI. It creates videos from text prompts, with an optional starting image to guide the scene. It can direct subject...
  input:
    body:
      model:
        type: string
        required: true
        enum:
        - x-ai/grok-imagine-video-1.5
        example: x-ai/grok-imagine-video-1.5
      prompt:
        type: string
        required: true
        example: A paper boat crosses a quiet pond at sunrise.
      duration:
        type: integer
        required: false
        default: 1
        max: 15
      resolution:
        type: string
        required: false
        default: 480p
        enum:
        - 480p
        - 720p
        - 1080p
      aspect_ratio:
        type: string
        required: false
        default: '16:9'
        enum:
        - '16:9'
        - '9:16'
        - '1:1'
        - '4:3'
        - '3:4'
        - '3:2'
        - '2:3'
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    source: rate_card_api
    source_url: https://openrouter.ai/api/v1/videos/models
    checked: '2026-09-02'
    rate_card:
      cents_per_image_input: '1'
      cents_per_video_output_second_1080p: '25'
      cents_per_video_output_second_480p: '8'
      cents_per_video_output_second_720p: '14'
    note: The live rate card includes a token, image-input, reference-image, or megapixel-second dimension that the request schema cannot bound with one declarative times field; this row remains BYOK-only.
  docs_url: https://openrouter.ai/x-ai/grok-imagine-video-1.5
