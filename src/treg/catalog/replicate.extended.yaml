# replicate - EXTENDED tier: the provider's full endpoint surface, machine-generated by
# scripts/catalog_ingest.py. Do not hand-edit; re-run the script instead. Entries start with
# no capability, no verification and no example response - verification stamps and reviewed
# names and kinds are added later and carried across; capability mappings stay in core
# re-ingests by id via carry_verification. Routes curated in core replicate.yaml are excluded here.
# Generated from Replicate's maintained text-to-image, text-to-video, and image-to-video collections.
# Only official models are included. Input fields come from latest_version.openapi_schema.
# Generated rows intentionally carry no price; an unpriced row is BYOK-only.

provider: replicate
source:
  spec_urls:
  - https://api.replicate.com/v1/collections/text-to-image
  - https://api.replicate.com/v1/collections/text-to-video
  - https://api.replicate.com/v1/collections/image-to-video
  ingested: '2026-09-01'
async:
  id_from: id
  poll:
    url_from: urls.get
    url_hosts:
    - api.replicate.com
  status:
    path: status
    success:
    - succeeded
    failure:
    - failed
    - canceled
  result:
    path: output
  interval: 2
endpoints:
- id: replicate.x.black-forest-labs-flux-1-1-pro-ultra
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-1.1-pro-ultra/predictions
  name: black-forest-labs/flux-1.1-pro-ultra
  summary: FLUX1.1 [pro] in ultra and raw modes. Images are up to 4 megapixels. Use raw mode for realism.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          image_prompt:
            type: string
            description: Image to use with Flux Redux. This is used together with the text prompt to guide the generation towards the composition of the image_prompt. Must be jpeg, png, gif, or webp.
            required: false
          image_prompt_strength:
            type: number
            description: Blend between the prompt and the image prompt.
            default: 0.1
            min: 0
            max: 1
            required: false
          aspect_ratio:
            description: Aspect ratio for the generated image
            default: '1:1'
            required: false
          safety_tolerance:
            type: integer
            description: Safety tolerance, 1 is most strict and 6 is most permissive
            default: 2
            min: 1
            max: 6
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          raw:
            type: boolean
            description: Generate less processed, more natural-looking images
            default: false
            required: false
          output_format:
            description: Format of the output images.
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-1.1-pro-ultra
- id: replicate.x.black-forest-labs-flux-2-flex
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-2-flex/predictions
  name: black-forest-labs/flux-2-flex
  summary: Max-quality image generation and editing with support for ten reference images
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          input_images:
            type: array
            description: List of input images for image-to-image generation. Maximum 10 images. Must be jpeg, png, gif, or webp.
            default: []
            required: false
          aspect_ratio:
            description: Aspect ratio for the generated image. Use 'match_input_image' to match the first input image's aspect ratio.
            default: '1:1'
            required: false
          resolution:
            description: 'Resolution in megapixels. Up to 4 MP is possible, but 2 MP or below is recommended. The maximum image size is 2048x2048, which means that high-resolution images may not respect the resolution if aspect ratio is not 1:1.


              Resolution is not used when aspect_ratio is ''custom''. When aspect_ratio is ''match_input_image'', use ''match_input_image'' to match the input image''s resolution (clamped to 0.5-4 MP).'
            default: 1 MP
            required: false
          width:
            type: integer
            description: Width of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16 (if it's not, it will be rounded to nearest multiple of 16).
            min: 256
            max: 2048
            required: false
          height:
            type: integer
            description: Height of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16 (if it's not, it will be rounded to nearest multiple of 16).
            min: 256
            max: 2048
            required: false
          safety_tolerance:
            type: integer
            description: Safety tolerance, 1 is most strict and 5 is most permissive
            default: 2
            min: 1
            max: 5
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          prompt_upsampling:
            type: boolean
            description: Automatically modify the prompt for more creative generation
            default: true
            required: false
          steps:
            type: integer
            description: Number of inference steps
            default: 30
            min: 1
            max: 50
            required: false
          guidance:
            type: number
            description: Guidance scale for generation. Controls how closely the output follows the prompt
            default: 4.5
            min: 1.5
            max: 10
            required: false
          output_format:
            description: Format of the output images.
            default: webp
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-2-flex
- id: replicate.x.black-forest-labs-flux-2-klein-4b
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-2-klein-4b/predictions
  name: black-forest-labs/flux-2-klein-4b
  summary: Very fast image generation and editing model. 4 steps distilled, sub-second inference for production and near real-time applications.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation.
            required: true
          images:
            type: array
            description: List of input images for image-to-image generation. Maximum 5 images. Must be jpeg, png, gif, or webp.
            default: []
            required: false
          aspect_ratio:
            description: Aspect ratio for the generated image. Use 'match_input_image' to match the aspect ratio of the first input image.
            default: '1:1'
            required: false
          output_megapixels:
            description: Resolution of the output image in megapixels
            default: '1'
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          go_fast:
            type: boolean
            description: Run faster predictions with additional optimizations.
            default: false
            required: false
          output_format:
            description: Format of the output images
            default: jpg
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs.
            default: 95
            min: 0
            max: 100
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated images.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-2-klein-4b
- id: replicate.x.black-forest-labs-flux-2-max
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-2-max/predictions
  name: black-forest-labs/flux-2-max
  summary: The highest fidelity image model from Black Forest Labs
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          input_images:
            type: array
            description: List of input images for image-to-image generation. Maximum 8 images. Must be jpeg, png, gif, or webp.
            default: []
            required: false
          aspect_ratio:
            description: Aspect ratio for the generated image. Use 'match_input_image' to match the first input image's aspect ratio.
            default: '1:1'
            required: false
          resolution:
            description: 'Resolution in megapixels. Up to 4 MP is possible, but 2 MP or below is recommended. The maximum image size is 2048x2048, which means that high-resolution images may not respect the resolution if aspect ratio is not 1:1.


              Resolution is not used when aspect_ratio is ''custom''. When aspect_ratio is ''match_input_image'', use ''match_input_image'' to match the input image''s resolution (clamped to 0.5-4 MP).'
            default: 1 MP
            required: false
          width:
            type: integer
            description: Width of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16 (if it's not, it will be rounded to nearest multiple of 16).
            min: 256
            max: 2048
            required: false
          height:
            type: integer
            description: Height of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16 (if it's not, it will be rounded to nearest multiple of 16).
            min: 256
            max: 2048
            required: false
          safety_tolerance:
            type: integer
            description: Safety tolerance, 1 is most strict and 5 is most permissive
            default: 2
            min: 1
            max: 5
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          output_format:
            description: Format of the output images.
            default: webp
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-2-max
- id: replicate.x.black-forest-labs-flux-2-pro
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-2-pro/predictions
  name: black-forest-labs/flux-2-pro
  summary: High-quality image generation and editing with support for eight reference images
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          input_images:
            type: array
            description: List of input images for image-to-image generation. Maximum 8 images. Must be jpeg, png, gif, or webp.
            default: []
            required: false
          aspect_ratio:
            description: Aspect ratio for the generated image. Use 'match_input_image' to match the first input image's aspect ratio.
            default: '1:1'
            required: false
          resolution:
            description: 'Resolution in megapixels. Up to 4 MP is possible, but 2 MP or below is recommended. The maximum image size is 2048x2048, which means that high-resolution images may not respect the resolution if aspect ratio is not 1:1.


              Resolution is not used when aspect_ratio is ''custom''. When aspect_ratio is ''match_input_image'', use ''match_input_image'' to match the input image''s resolution (clamped to 0.5-4 MP).'
            default: 1 MP
            required: false
          width:
            type: integer
            description: Width of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16 (if it's not, it will be rounded to nearest multiple of 16).
            min: 256
            max: 2048
            required: false
          height:
            type: integer
            description: Height of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16 (if it's not, it will be rounded to nearest multiple of 16).
            min: 256
            max: 2048
            required: false
          safety_tolerance:
            type: integer
            description: Safety tolerance, 1 is most strict and 5 is most permissive
            default: 2
            min: 1
            max: 5
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          output_format:
            description: Format of the output images.
            default: webp
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-2-pro
- id: replicate.x.black-forest-labs-flux-dev-lora
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-dev-lora/predictions
  name: black-forest-labs/flux-dev-lora
  summary: A version of flux-dev, a text to image model, that supports fast fine-tuned lora inference
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Prompt for generated image
            required: true
          aspect_ratio:
            description: Aspect ratio for the generated image
            default: '1:1'
            required: false
          image:
            type: string
            description: Input image for image to image mode. The aspect ratio of your output will match this image
            required: false
          prompt_strength:
            type: number
            description: Prompt strength when using img2img. 1.0 corresponds to full destruction of information in image
            default: 0.8
            min: 0
            max: 1
            required: false
          num_outputs:
            type: integer
            description: Number of outputs to generate
            default: 1
            min: 1
            max: 4
            required: false
          num_inference_steps:
            type: integer
            description: Number of denoising steps. Recommended range is 28-50, and lower number of steps produce lower quality outputs, faster.
            default: 28
            min: 1
            max: 50
            required: false
          guidance:
            type: number
            description: Guidance for generated image
            default: 3
            min: 0
            max: 10
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          output_format:
            description: Format of the output images
            default: webp
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated images.
            default: false
            required: false
          go_fast:
            type: boolean
            description: Run faster predictions with model optimized for speed (currently fp8 quantized); disable to run in original bf16. Note that outputs will not be deterministic when this is enabled, even if you set a seed.
            default: true
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Supports Replicate models in the format <owner>/<username> or <owner>/<username>/<version>, HuggingFace URLs in the format huggingface.co/<owner>/<model-name>[/<lora-weights-file.safetensors>], CivitAI URLs in the format civitai.com/models/<id>[/<model-name>], or arbitrary .safetensors URLs from the Internet, including signed URLs. For example, 'fofr/flux-pixar-cars'. Civit AI and HuggingFace LoRAs may require an API token to access, which you can provide in the `civitai_api_token` and `hf_api_token` inputs respectively.
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied. Sane results between 0 and 1 for base inference. For go_fast we apply a 1.5x multiplier to this value; we've generally seen good performance when scaling the base value by that amount. You may still need to experiment to find the best value for your particular lora.
            default: 1
            min: -1
            max: 3
            required: false
          extra_lora:
            type: string
            description: Load LoRA weights. Supports Replicate models in the format <owner>/<username> or <owner>/<username>/<version>, HuggingFace URLs in the format huggingface.co/<owner>/<model-name>, CivitAI URLs in the format civitai.com/models/<id>[/<model-name>], or arbitrary .safetensors URLs from the Internet. For example, 'fofr/flux-pixar-cars'
            required: false
          extra_lora_scale:
            type: number
            description: Determines how strongly the extra LoRA should be applied. Sane results between 0 and 1 for base inference. For go_fast we apply a 1.5x multiplier to this value; we've generally seen good performance when scaling the base value by that amount. You may still need to experiment to find the best value for your particular lora.
            default: 1
            min: -1
            max: 3
            required: false
          megapixels:
            description: Approximate number of megapixels for generated image
            default: '1'
            required: false
          hf_api_token:
            type: string
            description: HuggingFace API token. If you're using a hf lora that needs authentication, you'll need to provide an API token.
            required: false
          civitai_api_token:
            type: string
            description: Civitai API token. If you're using a civitai lora that needs authentication, you'll need to provide an API token.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-dev-lora
- id: replicate.x.black-forest-labs-flux-dev
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-dev/predictions
  name: black-forest-labs/flux-dev
  summary: A 12 billion parameter rectified flow transformer capable of generating images from text descriptions
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Prompt for generated image
            required: true
          aspect_ratio:
            description: Aspect ratio for the generated image
            default: '1:1'
            required: false
          image:
            type: string
            description: Input image for image to image mode. The aspect ratio of your output will match this image
            required: false
          prompt_strength:
            type: number
            description: Prompt strength when using img2img. 1.0 corresponds to full destruction of information in image
            default: 0.8
            min: 0
            max: 1
            required: false
          num_outputs:
            type: integer
            description: Number of outputs to generate
            default: 1
            min: 1
            max: 4
            required: false
          num_inference_steps:
            type: integer
            description: Number of denoising steps. Recommended range is 28-50, and lower number of steps produce lower quality outputs, faster.
            default: 28
            min: 1
            max: 50
            required: false
          guidance:
            type: number
            description: Guidance for generated image
            default: 3
            min: 0
            max: 10
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          output_format:
            description: Format of the output images
            default: webp
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated images.
            default: false
            required: false
          go_fast:
            type: boolean
            description: Run faster predictions with model optimized for speed (currently fp8 quantized); disable to run in original bf16. Note that outputs will not be deterministic when this is enabled, even if you set a seed.
            default: true
            required: false
          megapixels:
            description: Approximate number of megapixels for generated image
            default: '1'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-dev
- id: replicate.x.black-forest-labs-flux-kontext-max
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-kontext-max/predictions
  name: black-forest-labs/flux-kontext-max
  summary: A premium text-based image editing model that delivers maximum performance and improved typography generation for transforming images through natural language prompts
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the generated image. Use 'match_input_image' to match the aspect ratio of the input image.
            default: match_input_image
            required: false
          output_format:
            description: Output format for the generated image
            default: png
            required: false
          input_image:
            type: string
            description: Image to use as reference. Must be jpeg, png, gif, or webp.
            required: false
          prompt:
            type: string
            description: Text description of what you want to generate, or the instruction on how to edit the given image.
            required: true
          prompt_upsampling:
            type: boolean
            description: Automatic prompt improvement
            default: false
            required: false
          safety_tolerance:
            type: integer
            description: Safety tolerance, 0 is most strict and 6 is most permissive. 2 is currently the maximum allowed when input images are used.
            default: 2
            min: 0
            max: 6
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-kontext-max
- id: replicate.x.black-forest-labs-flux-kontext-pro
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-kontext-pro/predictions
  name: black-forest-labs/flux-kontext-pro
  summary: A state-of-the-art text-based image editing model that delivers high-quality outputs with excellent prompt following and consistent results for transforming images through natural language
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the generated image. Use 'match_input_image' to match the aspect ratio of the input image.
            default: match_input_image
            required: false
          output_format:
            description: Output format for the generated image
            default: png
            required: false
          input_image:
            type: string
            description: Image to use as reference. Must be jpeg, png, gif, or webp.
            required: false
          prompt:
            type: string
            description: Text description of what you want to generate, or the instruction on how to edit the given image.
            required: true
          prompt_upsampling:
            type: boolean
            description: Automatic prompt improvement
            default: false
            required: false
          safety_tolerance:
            type: integer
            description: Safety tolerance, 0 is most strict and 6 is most permissive. 2 is currently the maximum allowed when input images are used.
            default: 2
            min: 0
            max: 6
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-kontext-pro
- id: replicate.x.black-forest-labs-flux-pro
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/black-forest-labs/flux-pro/predictions
  name: black-forest-labs/flux-pro
  summary: State-of-the-art image generation with top of the line prompt following, visual quality, image detail and output diversity.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio for the generated image
            default: '1:1'
            required: false
          output_format:
            description: Format of the output images.
            default: webp
            required: false
          guidance:
            type: number
            description: Controls the balance between adherence to the text prompt and image quality/diversity. Higher values make the output more closely match the prompt but may reduce overall image quality. Lower values allow for more creative freedom but might produce results less relevant to the prompt.
            default: 3
            min: 2
            max: 5
            required: false
          height:
            type: integer
            description: 'Height of the generated image in text-to-image mode. Only used when aspect_ratio=custom. Must be a multiple of 32 (if it''s not, it will be rounded to nearest multiple of 32). Note: Ignored in img2img and inpainting modes.'
            min: 256
            max: 1440
            required: false
          image_prompt:
            type: string
            description: Image to use with Flux Redux. This is used together with the text prompt to guide the generation towards the composition of the image_prompt. Must be jpeg, png, gif, or webp.
            required: false
          interval:
            type: number
            description: Deprecated
            default: 2
            min: 1
            max: 4
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          prompt_upsampling:
            type: boolean
            description: Automatically modify the prompt for more creative generation
            default: false
            required: false
          safety_tolerance:
            type: integer
            description: Safety tolerance, 1 is most strict and 6 is most permissive
            default: 2
            min: 1
            max: 6
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          steps:
            type: integer
            description: Deprecated
            default: 25
            min: 1
            max: 50
            required: false
          width:
            type: integer
            description: 'Width of the generated image in text-to-image mode. Only used when aspect_ratio=custom. Must be a multiple of 32 (if it''s not, it will be rounded to nearest multiple of 32). Note: Ignored in img2img and inpainting modes.'
            min: 256
            max: 1440
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/black-forest-labs/flux-pro
- id: replicate.x.bria-fibo
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/bria/fibo/predictions
  name: bria/fibo
  summary: SOTA Open source model trained on licensed data, transforming intent into structured control for precise, high-quality AI image generation in enterprise and agentic workflows.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          image:
            type: string
            description: Image file
            required: false
          structured_prompt:
            type: string
            description: Structured prompt (JSON string). Use a structured_prompt from a previous generation's response or the /v2/structured_prompt/generate endpoint for precise refinement.
            default: ''
            required: false
          negative_prompt:
            type: string
            description: Negative prompt for image generation
            required: false
          guidance_scale:
            type: integer
            description: Guidance scale (1-10)
            min: 3
            max: 5
            required: false
          aspect_ratio:
            description: Aspect ratio for expansion. Either aspect_ratio or canvas_size with original_image_size/location must be provided. Can be a predefined string like '1:1', '16:9' etc. or a custom float between 0.5 and 3.0
            default: '1:1'
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bria/fibo
- id: replicate.x.bria-image-3-2
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/bria/image-3.2/predictions
  name: bria/image-3.2
  summary: Commercial-ready, trained entirely on licensed data, text-to-image model. With only 4B parameters provides exceptional aesthetics and text rendering. Evaluated to be on par to other leading models in the market
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt for image generation
            required: false
          aspect_ratio:
            description: Aspect ratio for expansion. Either aspect_ratio or canvas_size with original_image_size/location must be provided. Can be a predefined string like '1:1', '16:9' etc. or a custom float between 0.5 and 3.0
            default: '1:1'
            required: false
          guidance_scale:
            type: number
            description: Guidance scale (1-10)
            min: 3
            max: 5
            required: false
          prompt_enhancement:
            type: boolean
            description: Enhance prompt for more creative output
            default: false
            required: false
          enhance_image:
            type: boolean
            description: Enhance image details and clarity
            default: false
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bria/image-3.2
- id: replicate.x.bytedance-seedream-3
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/bytedance/seedream-3/predictions
  name: bytedance/seedream-3
  summary: A text-to-image model with support for native high-resolution (2K) image generation
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Image aspect ratio. Set to 'custom' to specify width and height.
            default: '16:9'
            required: false
          size:
            description: Big images will have their longest dimension be 2048px. Small images will have their shortest dimension be 512px. Regular images will always be 1 megapixel. Ignored if aspect ratio is custom.
            default: regular
            required: false
          guidance_scale:
            type: number
            description: Prompt adherence. Higher = more literal.
            default: 2.5
            min: 1
            max: 10
            required: false
          height:
            type: integer
            description: Image height
            default: 2048
            min: 512
            max: 2048
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          width:
            type: integer
            description: Image width
            default: 2048
            min: 512
            max: 2048
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedream-3
- id: replicate.x.bytedance-seedream-4-5
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/bytedance/seedream-4.5/predictions
  name: bytedance/seedream-4.5
  summary: 'Seedream 4.5: Upgraded Bytedance image model with stronger spatial understanding and world knowledge'
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation. Maximum 4000 characters. BytePlus recommends keeping prompts under 600 English words for best results.
            required: true
          image_input:
            type: array
            description: Input image(s) for image-to-image generation. List of 1-14 images for single or multi-reference generation.
            default: []
            required: false
          size:
            description: 'Image resolution: 2K (2048px), 4K (4096px), or ''custom'' for specific dimensions. Note: 1K resolution is not supported in Seedream 4.5.'
            default: 2K
            required: false
          aspect_ratio:
            description: Image aspect ratio. Only used when size is not 'custom'. Use 'match_input_image' to automatically match the input image's aspect ratio.
            default: match_input_image
            required: false
          width:
            type: integer
            description: 'Custom image width (only used when size=''custom''). Range: 1024-4096 pixels.'
            default: 2048
            min: 1024
            max: 4096
            required: false
          height:
            type: integer
            description: 'Custom image height (only used when size=''custom''). Range: 1024-4096 pixels.'
            default: 2048
            min: 1024
            max: 4096
            required: false
          sequential_image_generation:
            description: Group image generation mode. 'disabled' generates a single image. 'auto' lets the model decide whether to generate multiple related images (e.g., story scenes, character variations).
            default: disabled
            required: false
          max_images:
            type: integer
            description: 'Maximum number of images to generate when sequential_image_generation=''auto''. Range: 1-15. Total images (input + generated) cannot exceed 15.'
            default: 1
            min: 1
            max: 15
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable the safety checker for generated images. When enabled, input moderation is relaxed to only block illegal content (CSAM). Use responsibly.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedream-4.5
- id: replicate.x.bytedance-seedream-4
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/bytedance/seedream-4/predictions
  name: bytedance/seedream-4
  summary: Unified text-to-image generation and precise single-sentence editing at up to 4K resolution
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          image_input:
            type: array
            description: Input image(s) for image-to-image generation. List of 1-10 images for single or multi-reference generation.
            default: []
            required: false
          size:
            description: 'Image resolution: 1K (1024px), 2K (2048px), 4K (4096px), or ''custom'' for specific dimensions.'
            default: 2K
            required: false
          aspect_ratio:
            description: Image aspect ratio. Only used when size is not 'custom'. Use 'match_input_image' to automatically match the input image's aspect ratio.
            default: match_input_image
            required: false
          width:
            type: integer
            description: 'Custom image width (only used when size=''custom''). Range: 1024-4096 pixels.'
            default: 2048
            min: 1024
            max: 4096
            required: false
          height:
            type: integer
            description: 'Custom image height (only used when size=''custom''). Range: 1024-4096 pixels.'
            default: 2048
            min: 1024
            max: 4096
            required: false
          sequential_image_generation:
            description: Group image generation mode. 'disabled' generates a single image. 'auto' lets the model decide whether to generate multiple related images (e.g., story scenes, character variations).
            default: disabled
            required: false
          max_images:
            type: integer
            description: 'Maximum number of images to generate when sequential_image_generation=''auto''. Range: 1-15. Total images (input + generated) cannot exceed 15.'
            default: 1
            min: 1
            max: 15
            required: false
          enhance_prompt:
            type: boolean
            description: Enable prompt enhancement for higher quality results, this will take longer to generate.
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedream-4
- id: replicate.x.bytedance-seedream-5-lite
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/bytedance/seedream-5-lite/predictions
  name: bytedance/seedream-5-lite
  summary: 'Seedream 5.0 lite: image generation with built-in reasoning, example-based editing, and deep domain knowledge'
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          image_input:
            type: array
            description: Input image(s) for image-to-image generation. List of 1-14 images for single or multi-reference generation.
            default: []
            required: false
          size:
            description: 'Image resolution: 2K (2048px) or 3K (3072px).'
            default: 2K
            required: false
          aspect_ratio:
            description: Image aspect ratio. Use 'match_input_image' to automatically match the input image's aspect ratio.
            default: match_input_image
            required: false
          sequential_image_generation:
            description: Group image generation mode. 'disabled' generates a single image. 'auto' lets the model decide whether to generate multiple related images (e.g., story scenes, character variations).
            default: disabled
            required: false
          max_images:
            type: integer
            description: 'Maximum number of images to generate when sequential_image_generation=''auto''. Range: 1-15. Total images (input + generated) cannot exceed 15.'
            default: 1
            min: 1
            max: 15
            required: false
          output_format:
            description: Output image format.
            default: png
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedream-5-lite
- id: replicate.x.google-imagen-3-fast
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/imagen-3-fast/predictions
  name: google/imagen-3-fast
  summary: A faster and cheaper Imagen 3 model, for when price or speed are more important than final image quality
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: '1:1'
            required: false
          safety_filter_level:
            description: block_low_and_above is strictest, block_medium_and_above blocks some prompts, block_only_high is most permissive but some prompts will still be blocked
            default: block_only_high
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/imagen-3-fast
- id: replicate.x.google-imagen-3
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/imagen-3/predictions
  name: google/imagen-3
  summary: Google's highest quality text-to-image model, capable of generating images with detail, rich lighting and beauty
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: '1:1'
            required: false
          safety_filter_level:
            description: block_low_and_above is strictest, block_medium_and_above blocks some prompts, block_only_high is most permissive but some prompts will still be blocked
            default: block_only_high
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/imagen-3
- id: replicate.x.google-imagen-4-fast
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/imagen-4-fast/predictions
  name: google/imagen-4-fast
  summary: Use this fast version of Imagen 4 when speed and cost are more important than quality
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: '1:1'
            required: false
          safety_filter_level:
            description: block_low_and_above is strictest, block_medium_and_above blocks some prompts, block_only_high is most permissive but some prompts will still be blocked
            default: block_only_high
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/imagen-4-fast
- id: replicate.x.google-imagen-4-ultra
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/imagen-4-ultra/predictions
  name: google/imagen-4-ultra
  summary: Use this ultra version of Imagen 4 when quality matters more than speed and cost
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: '1:1'
            required: false
          image_size:
            description: Resolution of the generated image
            default: 1K
            required: false
          safety_filter_level:
            description: block_low_and_above is strictest, block_medium_and_above blocks some prompts, block_only_high is most permissive but some prompts will still be blocked
            default: block_only_high
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/imagen-4-ultra
- id: replicate.x.google-imagen-4
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/imagen-4/predictions
  name: google/imagen-4
  summary: Google's Imagen 4 flagship model
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: '1:1'
            required: false
          image_size:
            description: Resolution of the generated image
            default: 1K
            required: false
          safety_filter_level:
            description: block_low_and_above is strictest, block_medium_and_above blocks some prompts, block_only_high is most permissive but some prompts will still be blocked
            default: block_only_high
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/imagen-4
- id: replicate.x.google-nano-banana-2
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/nano-banana-2/predictions
  name: google/nano-banana-2
  summary: Google's fast image generation model with conversational editing, multi-image fusion, and character consistency
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: A text description of the image you want to generate
            required: true
          image_input:
            type: array
            description: Input images to transform or use as reference (supports up to 14 images)
            default: []
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: match_input_image
            required: false
          resolution:
            description: Resolution of the generated image. Higher resolutions take longer to generate.
            default: 1K
            required: false
          google_search:
            type: boolean
            description: Use Google Web Search grounding to generate images based on real-time information (e.g. weather, sports scores, recent events).
            default: false
            required: false
          image_search:
            type: boolean
            description: Use Google Image Search grounding to find web images as visual context for generation. When enabled, web search is also used automatically.
            default: false
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/nano-banana-2
- id: replicate.x.google-nano-banana-pro
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/nano-banana-pro/predictions
  name: google/nano-banana-pro
  summary: Google's state of the art image generation and editing model 🍌🍌
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: A text description of the image you want to generate
            required: true
          image_input:
            type: array
            description: Input images to transform or use as reference (supports up to 14 images)
            default: []
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: match_input_image
            required: false
          resolution:
            description: Resolution of the generated image
            default: 2K
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
          safety_filter_level:
            description: block_low_and_above is strictest, block_medium_and_above blocks some prompts, block_only_high is most permissive but some prompts will still be blocked
            default: block_only_high
            required: false
          allow_fallback_model:
            type: boolean
            description: Fallback to another model (currently bytedance/seedream-5) if Nano Banana Pro is at capacity.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/nano-banana-pro
- id: replicate.x.google-nano-banana
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/google/nano-banana/predictions
  name: google/nano-banana
  summary: Google's latest image editing model in Gemini 2.5
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: A text description of the image you want to generate
            required: true
          image_input:
            type: array
            description: Input images to transform or use as reference (supports multiple images)
            default: []
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: match_input_image
            required: false
          output_format:
            description: Format of the output image
            default: jpg
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/nano-banana
- id: replicate.x.ideogram-ai-ideogram-v2-turbo
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/ideogram-ai/ideogram-v2-turbo/predictions
  name: ideogram-ai/ideogram-v2-turbo
  summary: A fast image model with state of the art inpainting, prompt comprehension and text rendering.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio. Ignored if a resolution or inpainting image is given.
            default: '1:1'
            required: false
          resolution:
            description: Resolution. Overrides aspect ratio. Ignored if an inpainting image is given.
            default: None
            required: false
          magic_prompt_option:
            description: Magic Prompt will interpret your prompt and optimize it to maximize variety and quality of the images generated. You can also use it to write prompts in different languages.
            default: Auto
            required: false
          style_type:
            description: The styles help define the specific aesthetic of the image you want to generate.
            default: None
            required: false
          image:
            type: string
            description: An image file to use for inpainting. You must also use a mask.
            required: false
          mask:
            type: string
            description: A black and white image. Black pixels are inpainted, white pixels are preserved. The mask will be resized to match the image size.
            required: false
          negative_prompt:
            type: string
            description: Things you do not want to see in the generated image.
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            max: 2147483647
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/ideogram-ai/ideogram-v2-turbo
- id: replicate.x.ideogram-ai-ideogram-v2
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/ideogram-ai/ideogram-v2/predictions
  name: ideogram-ai/ideogram-v2
  summary: An excellent image model with state of the art inpainting, prompt comprehension and text rendering
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio. Ignored if a resolution or inpainting image is given.
            default: '1:1'
            required: false
          resolution:
            description: Resolution. Overrides aspect ratio. Ignored if an inpainting image is given.
            default: None
            required: false
          magic_prompt_option:
            description: Magic Prompt will interpret your prompt and optimize it to maximize variety and quality of the images generated. You can also use it to write prompts in different languages.
            default: Auto
            required: false
          style_type:
            description: The styles help define the specific aesthetic of the image you want to generate.
            default: None
            required: false
          image:
            type: string
            description: An image file to use for inpainting. You must also use a mask.
            required: false
          mask:
            type: string
            description: A black and white image. Black pixels are inpainted, white pixels are preserved. The mask will be resized to match the image size.
            required: false
          negative_prompt:
            type: string
            description: Things you do not want to see in the generated image.
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            max: 2147483647
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/ideogram-ai/ideogram-v2
- id: replicate.x.ideogram-ai-ideogram-v2a-turbo
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/ideogram-ai/ideogram-v2a-turbo/predictions
  name: ideogram-ai/ideogram-v2a-turbo
  summary: Like Ideogram v2 turbo, but now faster and cheaper
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio. Ignored if a resolution or inpainting image is given.
            default: '1:1'
            required: false
          resolution:
            description: Resolution. Overrides aspect ratio. Ignored if an inpainting image is given.
            default: None
            required: false
          magic_prompt_option:
            description: Magic Prompt will interpret your prompt and optimize it to maximize variety and quality of the images generated. You can also use it to write prompts in different languages.
            default: Auto
            required: false
          style_type:
            description: The styles help define the specific aesthetic of the image you want to generate.
            default: None
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            max: 2147483647
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/ideogram-ai/ideogram-v2a-turbo
- id: replicate.x.ideogram-ai-ideogram-v2a
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/ideogram-ai/ideogram-v2a/predictions
  name: ideogram-ai/ideogram-v2a
  summary: Like Ideogram v2, but faster and cheaper
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio. Ignored if a resolution or inpainting image is given.
            default: '1:1'
            required: false
          resolution:
            description: Resolution. Overrides aspect ratio. Ignored if an inpainting image is given.
            default: None
            required: false
          magic_prompt_option:
            description: Magic Prompt will interpret your prompt and optimize it to maximize variety and quality of the images generated. You can also use it to write prompts in different languages.
            default: Auto
            required: false
          style_type:
            description: The styles help define the specific aesthetic of the image you want to generate.
            default: None
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            max: 2147483647
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/ideogram-ai/ideogram-v2a
- id: replicate.x.ideogram-ai-ideogram-v3-balanced
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/ideogram-ai/ideogram-v3-balanced/predictions
  name: ideogram-ai/ideogram-v3-balanced
  summary: Balance speed, quality and cost. Ideogram v3 creates images with stunning realism, creative designs, and consistent styles
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio. Ignored if a resolution or inpainting image is given.
            default: '1:1'
            required: false
          resolution:
            description: Resolution. Overrides aspect ratio. Ignored if an inpainting image is given.
            default: None
            required: false
          magic_prompt_option:
            description: Magic Prompt will interpret your prompt and optimize it to maximize variety and quality of the images generated. You can also use it to write prompts in different languages.
            default: Auto
            required: false
          image:
            type: string
            description: An image file to use for inpainting. You must also use a mask.
            required: false
          mask:
            type: string
            description: A black and white image. Black pixels are inpainted, white pixels are preserved. The mask will be resized to match the image size.
            required: false
          style_type:
            description: The styles help define the specific aesthetic of the image you want to generate.
            default: None
            required: false
          style_reference_images:
            type: array
            description: A list of images to use as style references.
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            max: 2147483647
            required: false
          style_preset:
            description: Apply a predefined artistic style to the generated image (V3 models only).
            default: None
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/ideogram-ai/ideogram-v3-balanced
- id: replicate.x.ideogram-ai-ideogram-v3-quality
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/ideogram-ai/ideogram-v3-quality/predictions
  name: ideogram-ai/ideogram-v3-quality
  summary: The highest quality Ideogram v3 model. v3 creates images with stunning realism, creative designs, and consistent styles
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio. Ignored if a resolution or inpainting image is given.
            default: '1:1'
            required: false
          resolution:
            description: Resolution. Overrides aspect ratio. Ignored if an inpainting image is given.
            default: None
            required: false
          magic_prompt_option:
            description: Magic Prompt will interpret your prompt and optimize it to maximize variety and quality of the images generated. You can also use it to write prompts in different languages.
            default: Auto
            required: false
          image:
            type: string
            description: An image file to use for inpainting. You must also use a mask.
            required: false
          mask:
            type: string
            description: A black and white image. Black pixels are inpainted, white pixels are preserved. The mask will be resized to match the image size.
            required: false
          style_type:
            description: The styles help define the specific aesthetic of the image you want to generate.
            default: None
            required: false
          style_reference_images:
            type: array
            description: A list of images to use as style references.
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            max: 2147483647
            required: false
          style_preset:
            description: Apply a predefined artistic style to the generated image (V3 models only).
            default: None
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/ideogram-ai/ideogram-v3-quality
- id: replicate.x.ideogram-ai-ideogram-v3-turbo
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/ideogram-ai/ideogram-v3-turbo/predictions
  name: ideogram-ai/ideogram-v3-turbo
  summary: Turbo is the fastest and cheapest Ideogram v3. v3 creates images with stunning realism, creative designs, and consistent styles
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio. Ignored if a resolution or inpainting image is given.
            default: '1:1'
            required: false
          resolution:
            description: Resolution. Overrides aspect ratio. Ignored if an inpainting image is given.
            default: None
            required: false
          magic_prompt_option:
            description: Magic Prompt will interpret your prompt and optimize it to maximize variety and quality of the images generated. You can also use it to write prompts in different languages.
            default: Auto
            required: false
          image:
            type: string
            description: An image file to use for inpainting. You must also use a mask.
            required: false
          mask:
            type: string
            description: A black and white image. Black pixels are inpainted, white pixels are preserved. The mask will be resized to match the image size.
            required: false
          style_type:
            description: The styles help define the specific aesthetic of the image you want to generate.
            default: None
            required: false
          style_reference_images:
            type: array
            description: A list of images to use as style references.
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            max: 2147483647
            required: false
          style_preset:
            description: Apply a predefined artistic style to the generated image (V3 models only).
            default: None
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/ideogram-ai/ideogram-v3-turbo
- id: replicate.x.leonardoai-lucid-origin
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/leonardoai/lucid-origin/predictions
  name: leonardoai/lucid-origin
  summary: Artistic and high-quality visuals with improved prompt adherence, diversity, and definition
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          aspect_ratio:
            description: Aspect ratio of the output image
            default: '16:9'
            required: false
          generation_mode:
            description: Generation mode
            default: standard
            required: false
          contrast:
            description: Contrast level
            default: medium
            required: false
          prompt_enhance:
            type: boolean
            description: Whether to enhance the prompt
            default: true
            required: false
          num_images:
            type: integer
            description: Number of images to generate
            default: 1
            min: 1
            max: 8
            required: false
          style:
            description: Style to use for the output image
            default: none
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/leonardoai/lucid-origin
- id: replicate.x.luma-photon-flash
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/luma/photon-flash/predictions
  name: luma/photon-flash
  summary: Accelerated variant of Photon prioritizing speed while maintaining quality
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: '16:9'
            required: false
          character_reference:
            type: string
            description: Character reference image to guide generation
            required: false
          character_reference_url:
            type: string
            description: 'Deprecated: Use character_reference instead'
            required: false
          image_reference:
            type: string
            description: Reference image to guide generation
            required: false
          image_reference_url:
            type: string
            description: 'Deprecated: Use image_reference instead'
            required: false
          image_reference_weight:
            type: number
            description: Weight of the reference image. Larger values will make the reference image have a stronger influence on the generated image.
            default: 0.85
            min: 0
            max: 1
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          style_reference:
            type: string
            description: Style reference image to guide generation
            required: false
          style_reference_url:
            type: string
            description: 'Deprecated: Use style_reference instead'
            required: false
          style_reference_weight:
            type: number
            description: Weight of the style reference image
            default: 0.85
            min: 0
            max: 1
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/photon-flash
- id: replicate.x.luma-photon
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/luma/photon/predictions
  name: luma/photon
  summary: High-quality image generation model optimized for creative professional workflows and ultra-high fidelity outputs
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: '16:9'
            required: false
          image_reference_url:
            type: string
            description: URL of a reference image to guide generation
            required: false
          image_reference_weight:
            type: number
            description: Weight of the reference image. Larger values will make the reference image have a stronger influence on the generated image.
            default: 0.85
            min: 0
            max: 1
            required: false
          style_reference_url:
            type: string
            description: URL of a style reference image
            required: false
          style_reference_weight:
            type: number
            description: Weight of the style reference image
            default: 0.85
            min: 0
            max: 1
            required: false
          character_reference_url:
            type: string
            description: URL of a character reference image
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/photon
- id: replicate.x.minimax-image-01
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/minimax/image-01/predictions
  name: minimax/image-01
  summary: Minimax's first image model, with character reference support
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Image aspect ratio
            default: '1:1'
            required: false
          number_of_images:
            type: integer
            description: Number of images to generate
            default: 1
            min: 1
            max: 9
            required: false
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          prompt_optimizer:
            type: boolean
            description: Use prompt optimizer
            default: true
            required: false
          subject_reference:
            type: string
            description: An optional character reference image (human face) to use as the subject in the generated image(s).
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/minimax/image-01
- id: replicate.x.openai-gpt-image-1-5
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/openai/gpt-image-1.5/predictions
  name: openai/gpt-image-1.5
  summary: OpenAI's latest image generation model with better instruction following and adherence to prompts
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: A text description of the desired image
            required: true
          openai_api_key:
            type: string
            description: Your OpenAI API key (optional - uses proxy if not provided)
            required: false
          aspect_ratio:
            description: The aspect ratio of the generated image
            default: '1:1'
            required: false
          input_fidelity:
            description: Control how much effort the model will exert to match the style and features, especially facial features, of input images
            default: low
            required: false
          input_images:
            type: array
            description: A list of images to use as input for the generation
            required: false
          number_of_images:
            type: integer
            description: Number of images to generate (1-10)
            default: 1
            min: 1
            max: 10
            required: false
          quality:
            description: The quality of the generated image
            default: auto
            required: false
          background:
            description: Set whether the background is transparent or opaque or choose automatically
            default: auto
            required: false
          output_compression:
            type: integer
            description: Compression level (0-100%)
            default: 90
            min: 0
            max: 100
            required: false
          output_format:
            description: Output format
            default: webp
            required: false
          moderation:
            description: Content moderation level
            default: auto
            required: false
          user_id:
            type: string
            description: An optional unique identifier representing your end-user. This helps OpenAI monitor and detect abuse.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/openai/gpt-image-1.5
- id: replicate.x.openai-gpt-image-2
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/openai/gpt-image-2/predictions
  name: openai/gpt-image-2
  summary: OpenAI's state-of-the-art image generation model. Create and edit images from text with strong instruction following, sharp text rendering, and detailed editing.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: A text description of the desired image
            required: true
          openai_api_key:
            type: string
            description: Your OpenAI API key (optional - uses proxy if not provided)
            default: null
            required: false
          aspect_ratio:
            description: The size of the generated image. Use 'auto' to let the model pick. Sizes above 2560x1440 are experimental and may give more variable results.
            default: '1:1'
            required: false
          input_images:
            type: array
            description: A list of images to use as input for the generation
            default: null
            required: false
          number_of_images:
            type: integer
            description: Number of images to generate (1-10)
            default: 1
            min: 1
            max: 10
            required: false
          quality:
            description: The quality of the generated image
            default: auto
            required: false
          background:
            description: Set whether the background is transparent or opaque or choose automatically
            default: auto
            required: false
          output_compression:
            type: integer
            description: Compression level (0-100%)
            default: 90
            min: 0
            max: 100
            required: false
          output_format:
            description: Output format
            default: webp
            required: false
          moderation:
            description: Content moderation level
            default: auto
            required: false
          user_id:
            type: string
            description: An optional unique identifier representing your end-user. This helps OpenAI monitor and detect abuse.
            default: null
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/openai/gpt-image-2
- id: replicate.x.prunaai-flux-fast
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/prunaai/flux-fast/predictions
  name: prunaai/flux-fast
  summary: This is the fastest Flux endpoint in the world.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          speed_mode:
            description: Speed optimization level
            default: Extra Juiced 🔥 (more speed)
            required: false
          aspect_ratio:
            description: Aspect ratio of the output image
            default: '1:1'
            required: false
          output_format:
            description: Output format
            default: jpg
            required: false
          guidance:
            type: number
            description: Guidance scale
            default: 3.5
            required: false
          image_size:
            type: integer
            description: Base image size (longest side)
            default: 1024
            required: false
          num_inference_steps:
            type: integer
            description: Number of inference steps
            default: 28
            required: false
          output_quality:
            type: integer
            description: Output quality (for jpg and webp)
            default: 80
            min: 1
            max: 100
            required: false
          prompt:
            type: string
            description: Prompt
            required: true
          seed:
            type: integer
            description: Seed
            default: -1
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/flux-fast
- id: replicate.x.prunaai-hidream-l1-fast
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/prunaai/hidream-l1-fast/predictions
  name: prunaai/hidream-l1-fast
  summary: This is an optimised version of the hidream-l1 model using the pruna ai optimisation toolkit!
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Prompt
            required: true
          model_type:
            type: string
            description: Model type
            default: fast
            required: false
          speed_mode:
            description: Speed optimization level
            default: Extra Juiced 🚀 (even more speed)
            required: false
          resolution:
            description: Output resolution
            default: 1024 × 1024 (Square)
            required: false
          aspect_ratio:
            type: string
            description: Legacy aspect ratio input. Ignored; use resolution instead.
            default: ''
            required: false
          seed:
            type: integer
            description: Random seed (-1 for random)
            default: -1
            required: false
          output_format:
            description: Output format
            default: webp
            required: false
          output_quality:
            type: integer
            description: Output quality (for jpg and webp)
            default: 100
            min: 1
            max: 100
            required: false
          negative_prompt:
            type: string
            description: Negative prompt for generated image. Leave blank to use the default negative prompt.
            default: ''
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/hidream-l1-fast
- id: replicate.x.prunaai-p-image-lora
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/prunaai/p-image-lora/predictions
  name: prunaai/p-image-lora
  summary: Use trained LoRAs from the https://replicate.com/prunaai/p-image-trainer. Find or contribute LoRAs here https://huggingface.co/collections/PrunaAI/p-image-loras
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation.
            required: true
          aspect_ratio:
            description: Aspect ratio for the generated image.
            default: '16:9'
            required: false
          width:
            type: integer
            description: Width of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16.
            min: 256
            max: 1440
            required: false
          height:
            type: integer
            description: Height of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16.
            min: 256
            max: 1440
            required: false
          prompt_upsampling:
            type: boolean
            description: upsample the prompt with an llm
            default: false
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation.
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated images.
            default: false
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Supports HuggingFace URLs in the format huggingface.co/<owner>/<model-name>[/<lora-weights-file.safetensors>]. HuggingFace LoRAs may require an API token to access, which you can provide in the `hf_api_token` input.
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied. 0.5 usually works well for most LoRAs.
            default: 0.5
            min: -1
            max: 3
            required: false
          hf_api_token:
            type: string
            description: HuggingFace API token. If you're using a HuggingFace LoRAs that needs authentication, you'll need to provide an API token.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/p-image-lora
- id: replicate.x.prunaai-p-image
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/prunaai/p-image/predictions
  name: prunaai/p-image
  summary: A sub 1 second text-to-image model built for production use cases.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation.
            required: true
          aspect_ratio:
            description: Aspect ratio for the generated image.
            default: '16:9'
            required: false
          width:
            type: integer
            description: Width of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16.
            min: 256
            max: 1440
            required: false
          height:
            type: integer
            description: Height of the generated image. Only used when aspect_ratio=custom. Must be a multiple of 16.
            min: 256
            max: 1440
            required: false
          prompt_upsampling:
            type: boolean
            description: Upsample the prompt with an LLM.
            default: false
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation.
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated images.
            default: false
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Supports HuggingFace URLs in the format huggingface.co/<owner>/<model-name>[/<lora-weights-file.safetensors>]. HuggingFace LoRAs may require an API token to access, which you can provide in the `hf_api_token` input.
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied. 0.5 usually works well for most LoRAs.
            default: 0.5
            min: -1
            max: 3
            required: false
          hf_api_token:
            type: string
            description: HuggingFace API token. If you're using a HuggingFace LoRAs that needs authentication, you'll need to provide an API token.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/p-image
- id: replicate.x.prunaai-wan-2-2-image
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/prunaai/wan-2.2-image/predictions
  name: prunaai/wan-2.2-image
  summary: This model generates beautiful cinematic 2 megapixel images in 3-4 seconds and is derived from the Wan 2.2 model through optimisation techniques from the pruna package
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          juiced:
            type: boolean
            description: Faster inference with additional optimizations.
            default: false
            required: false
          aspect_ratio:
            description: Aspect ratio for the generated image
            default: '16:9'
            required: false
          megapixels:
            description: Approximate number of megapixels for generated image
            default: 2
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          output_format:
            description: Format of the output images
            default: jpg
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 1
            max: 100
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/wan-2.2-image
- id: replicate.x.prunaai-z-image-turbo
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/prunaai/z-image-turbo/predictions
  name: prunaai/z-image-turbo
  summary: Z-Image Turbo is a super fast text-to-image model of 6B parameters developed by Tongyi-MAI.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          height:
            type: integer
            description: Height of the generated image
            default: 1024
            min: 64
            max: 2048
            required: false
          width:
            type: integer
            description: Width of the generated image
            default: 1024
            min: 64
            max: 2048
            required: false
          num_inference_steps:
            type: integer
            description: Number of inference steps.
            default: 8
            min: 1
            max: 50
            required: false
          guidance_scale:
            type: number
            description: Guidance scale. Should be 0 for Turbo models
            default: 0
            min: 0
            max: 20
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          go_fast:
            type: boolean
            description: Unused compatibility field; this endpoint always runs the bf16 model.
            default: false
            required: false
          output_format:
            description: Format of the output images
            default: jpg
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
          num_outputs:
            type: integer
            description: Number of output images to generate (currently only 1 is supported).
            default: 1
            min: 1
            max: 4
            required: false
          safety_tolerance:
            type: integer
            description: Unused compatibility field for Flux-style clients; this model has no safety checker.
            default: 2
            min: 1
            max: 6
            required: false
          model_version:
            type: string
            description: Unused compatibility field; this endpoint always runs Z-Image-Turbo.
            required: false
          negative_prompt:
            type: string
            description: Unused compatibility field; Z-Image-Turbo is guidance-free (guidance_scale=0) and does not use negative prompts.
            required: false
          aspect_ratio:
            type: string
            description: Unused compatibility field; set width/height directly instead.
            required: false
          megapixels:
            type: string
            description: Unused compatibility field; set width/height directly instead.
            required: false
          disable_safety_checker:
            type: boolean
            description: Unused compatibility field; this model has no safety checker.
            default: true
            required: false
          enable_safety_checker:
            type: boolean
            description: Unused compatibility field; this model has no safety checker.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/z-image-turbo
- id: replicate.x.quiverai-arrow-1-1-max
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/quiverai/arrow-1.1-max/predictions
  name: quiverai/arrow-1.1-max
  summary: Generate high-fidelity SVG graphics from text prompts with Quiver's Arrow 1.1 Max model. Tuned for dense illustrations, detailed logos, and technical diagrams.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text description of the desired SVG.
            required: true
          instructions:
            type: string
            description: Additional style or formatting guidance, separate from the prompt.
            default: ''
            required: false
          references:
            type: array
            description: Optional reference images (PNG, JPEG, or WebP) to guide generation. Arrow 1.1 supports up to 4; Arrow 1.1 Max supports up to 16.
            default: []
            required: false
          temperature:
            type: number
            description: Sampling temperature. Lower is more deterministic.
            default: 1
            min: 0
            max: 2
            required: false
          top_p:
            type: number
            description: Nucleus sampling probability.
            default: 1
            min: 0
            max: 1
            required: false
          presence_penalty:
            type: number
            description: Penalty for tokens already present in prior output.
            default: 0
            min: -2
            max: 2
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/quiverai/arrow-1.1-max
- id: replicate.x.quiverai-arrow-1-1
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/quiverai/arrow-1.1/predictions
  name: quiverai/arrow-1.1
  summary: Generate editable SVG graphics from text prompts with Quiver's Arrow 1.1 model. Great for icons, logos, and illustrations.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text description of the desired SVG.
            required: true
          instructions:
            type: string
            description: Additional style or formatting guidance, separate from the prompt.
            default: ''
            required: false
          references:
            type: array
            description: Optional reference images (PNG, JPEG, or WebP) to guide generation. Arrow 1.1 supports up to 4; Arrow 1.1 Max supports up to 16.
            default: []
            required: false
          temperature:
            type: number
            description: Sampling temperature. Lower is more deterministic.
            default: 1
            min: 0
            max: 2
            required: false
          top_p:
            type: number
            description: Nucleus sampling probability.
            default: 1
            min: 0
            max: 1
            required: false
          presence_penalty:
            type: number
            description: Penalty for tokens already present in prior output.
            default: 0
            min: -2
            max: 2
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/quiverai/arrow-1.1
- id: replicate.x.qwen-qwen-image
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/qwen/qwen-image/predictions
  name: qwen/qwen-image
  summary: An image generation foundation model in the Qwen series that achieves significant advances in complex text rendering.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Prompt for generated image
            required: true
          enhance_prompt:
            type: boolean
            description: Enhance the prompt with positive magic.
            default: false
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Only works with text to image pipeline. Supports arbitrary .safetensors URLs, tar files, and zip files from the Internet (for example, 'https://huggingface.co/flymy-ai/qwen-image-lora/resolve/main/pytorch_lora_weights.safetensors', 'https://example.com/lora_weights.tar.gz', or 'https://example.com/lora_weights.zip')
            required: false
          replicate_weights:
            type: string
            description: Load LoRA weights from Replicate training. Only works with text to image pipeline. Supports arbitrary .safetensors URLs, tar files, and zip files from the Internet.
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied.
            default: 1
            required: false
          extra_lora_weights:
            type: array
            description: Additional LoRA weights as an array of URLs. Same formats supported as lora_weights (e.g., ['https://huggingface.co/flymy-ai/qwen-image-lora/resolve/main/pytorch_lora_weights.safetensors', 'https://huggingface.co/flymy-ai/qwen-image-realism-lora/resolve/main/flymy_realism.safetensors'])
            required: false
          extra_lora_scale:
            type: array
            description: Scales for additional LoRAs as an array of numbers (e.g., 0.5, 0.7). Must match the number of weights in extra_lora_weights.
            required: false
          image:
            type: string
            description: Input image for img2img pipeline
            required: false
          strength:
            type: number
            description: Strength for img2img pipeline
            default: 0.9
            min: 0
            max: 1
            required: false
          negative_prompt:
            type: string
            description: Negative prompt for generated image
            default: ' '
            required: false
          aspect_ratio:
            description: Aspect ratio for the generated image
            default: '16:9'
            required: false
          image_size:
            description: Image size for the generated image
            default: optimize_for_quality
            required: false
          go_fast:
            type: boolean
            description: Run faster predictions with additional optimizations.
            default: true
            required: false
          num_inference_steps:
            type: integer
            description: Number of denoising steps. Recommended range is 28-50, and lower number of steps produce lower quality outputs, faster.
            default: 30
            min: 1
            max: 50
            required: false
          guidance:
            type: number
            description: Guidance for generated image. Lower values can give more realistic images. Good values to try are 2, 2.5, 3 and 3.5
            default: 3
            min: 0
            max: 10
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          output_format:
            description: Format of the output images
            default: webp
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 80
            min: 0
            max: 100
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated images.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/qwen/qwen-image
- id: replicate.x.recraft-ai-recraft-v3-svg
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/recraft-ai/recraft-v3-svg/predictions
  name: recraft-ai/recraft-v3-svg
  summary: Recraft V3 SVG (code-named red_panda) is a text-to-image model with the ability to generate high quality SVG images including logotypes, and icons. The model supports a wide list of styles.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: Not set
            required: false
          size:
            description: Width and height of the generated image. Size is ignored if an aspect ratio is set.
            default: 1024x1024
            required: false
          style:
            description: Style of the generated image.
            default: any
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/recraft-ai/recraft-v3-svg
- id: replicate.x.recraft-ai-recraft-v3
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/recraft-ai/recraft-v3/predictions
  name: recraft-ai/recraft-v3
  summary: Recraft V3 (code-named red_panda) is a text-to-image model with the ability to generate long texts, and images in a wide list of styles. As of today, it is SOTA in image generation, proven by the Text-to-Image Benchmark by Artificial Analysis
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: Not set
            required: false
          size:
            description: Width and height of the generated image. Size is ignored if an aspect ratio is set.
            default: 1024x1024
            required: false
          style:
            description: Style of the generated image.
            default: any
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/recraft-ai/recraft-v3
- id: replicate.x.recraft-ai-recraft-v4-pro-svg
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/recraft-ai/recraft-v4-pro-svg/predictions
  name: recraft-ai/recraft-v4-pro-svg
  summary: Generate detailed SVG vector graphics from text prompts. Recraft V4 Pro's design taste with more geometric detail and finer paths - clean layers, editable output, and scalable to any size.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation (up to 10,000 characters)
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: Not set
            required: false
          size:
            description: Width and height of the generated image. Size is ignored if an aspect ratio is set.
            default: 2048x2048
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/recraft-ai/recraft-v4-pro-svg
- id: replicate.x.recraft-ai-recraft-v4-pro
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/recraft-ai/recraft-v4-pro/predictions
  name: recraft-ai/recraft-v4-pro
  summary: Recraft's latest image generation model at ~2048px resolution. Same design taste and prompt accuracy as V4, with higher resolution for print-ready and large-scale work.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation (up to 10,000 characters)
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: Not set
            required: false
          size:
            description: Width and height of the generated image. Size is ignored if an aspect ratio is set.
            default: 2048x2048
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/recraft-ai/recraft-v4-pro
- id: replicate.x.recraft-ai-recraft-v4-styles-pro-svg
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/recraft-ai/recraft-v4-styles-pro-svg/predictions
  name: recraft-ai/recraft-v4-styles-pro-svg
  summary: Recraft V4 Styles Pro SVG generates high-quality editable vector images that match a reusable style or attached reference images.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation (up to 10,000 characters)
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: Not set
            required: false
          size:
            description: Width and height of the generated image. Size is ignored if an aspect ratio is set.
            default: 2048x2048
            required: false
          style_id:
            type: string
            description: Reusable Recraft style ID created with the Create style endpoint. Mutually exclusive with style_reference_images.
            default: ''
            required: false
          style_reference_images:
            type: array
            description: PNG, JPG, or WEBP reference images used to create a private style for this generation. Supports 1-10 images. Mutually exclusive with style_id.
            required: true
          style_match:
            description: How closely the generated image should follow the style.
            default: precise
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/recraft-ai/recraft-v4-styles-pro-svg
- id: replicate.x.recraft-ai-recraft-v4-svg
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/recraft-ai/recraft-v4-svg/predictions
  name: recraft-ai/recraft-v4-svg
  summary: Generate production-ready SVG vector images from text prompts. Recraft V4's design taste applied to vector output - clean geometry, structured layers, and editable paths.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation (up to 10,000 characters)
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated image
            default: Not set
            required: false
          size:
            description: Width and height of the generated image. Size is ignored if an aspect ratio is set.
            default: 1024x1024
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/recraft-ai/recraft-v4-svg
- id: replicate.x.sourceful-riverflow-2-0-pro
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/sourceful/riverflow-2.0-pro/predictions
  name: sourceful/riverflow-2.0-pro
  summary: Agentic image model optimized for robust, high-precision generations supporting font control
  input:
    body:
      input:
        type: object
        required: true
        properties:
          instruction:
            type: string
            description: Generation instruction (text-to-image or edit). Minimum 2 characters.
            required: true
          init_images:
            type: array
            description: 'Optional init images (i2i). URLs or uploads. Pro: up to 10, Fast: up to 4.'
            required: false
          super_resolution_refs:
            type: array
            description: Optional reference images for detail fix (up to 4). Used in i2i and refsr.
            required: false
          resolution:
            description: 'Target resolution: 1K, 2K, or 4K.'
            default: 1K
            required: false
          aspect_ratio:
            description: Aspect ratio for output. auto uses model choice.
            default: auto
            required: false
          output_format:
            description: Output image format. API returns webp; png will be converted in Python.
            default: webp
            required: false
          font_urls:
            type: array
            description: Optional font URLs (ttf/otf/woff/woff2). Up to 2.
            required: false
          font_texts:
            type: array
            description: Text strings matching font_urls (≤300 chars each).
            required: false
          transparency:
            type: boolean
            description: Enable transparent background when supported.
            default: false
            required: false
          enhance_prompt:
            type: boolean
            description: Let model enhance the instruction.
            default: false
            required: false
          max_iterations:
            type: integer
            description: Number of internal reasoning iterations (1-3).
            default: 3
            min: 1
            max: 3
            required: false
          safety_checker:
            type: boolean
            description: Enable Sourceful safety checker (t2i).
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/sourceful/riverflow-2.0-pro
- id: replicate.x.stability-ai-stable-diffusion-3-5-large-turbo
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/stability-ai/stable-diffusion-3.5-large-turbo/predictions
  name: stability-ai/stable-diffusion-3.5-large-turbo
  summary: A text-to-image model that generates high-resolution images with fine details. It supports various artistic styles and produces diverse outputs from the same prompt, with a focus on fewer inference steps
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: The aspect ratio of your output image. This value is ignored if you are using an input image.
            default: '1:1'
            required: false
          output_format:
            description: Format of the output images
            default: webp
            required: false
          cfg:
            type: number
            description: The guidance scale tells the model how similar the output should be to the prompt.
            default: 1
            min: 1
            max: 10
            required: false
          image:
            type: string
            description: Input image for image to image mode. The aspect ratio of your output will match this image.
            required: false
          negative_prompt:
            type: string
            description: What you do not want to see in the image
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          prompt_strength:
            type: number
            description: Prompt strength (or denoising strength) when using image to image. 1.0 corresponds to full destruction of information in image.
            default: 0.85
            min: 0
            max: 1
            required: false
          seed:
            type: integer
            description: Set a seed for reproducibility. Random by default.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/stability-ai/stable-diffusion-3.5-large-turbo
- id: replicate.x.stability-ai-stable-diffusion-3-5-large
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/stability-ai/stable-diffusion-3.5-large/predictions
  name: stability-ai/stable-diffusion-3.5-large
  summary: A text-to-image model that generates high-resolution images with fine details. It supports various artistic styles and produces diverse outputs from the same prompt, thanks to Query-Key Normalization.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: The aspect ratio of your output image. This value is ignored if you are using an input image.
            default: '1:1'
            required: false
          output_format:
            description: Format of the output images
            default: webp
            required: false
          cfg:
            type: number
            description: The guidance scale tells the model how similar the output should be to the prompt.
            default: 5
            min: 1
            max: 10
            required: false
          image:
            type: string
            description: Input image for image to image mode. The aspect ratio of your output will match this image.
            required: false
          negative_prompt:
            type: string
            description: What you do not want to see in the image
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          prompt_strength:
            type: number
            description: Prompt strength (or denoising strength) when using image to image. 1.0 corresponds to full destruction of information in image.
            default: 0.85
            min: 0
            max: 1
            required: false
          seed:
            type: integer
            description: Set a seed for reproducibility. Random by default.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/stability-ai/stable-diffusion-3.5-large
- id: replicate.x.stability-ai-stable-diffusion-3-5-medium
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/stability-ai/stable-diffusion-3.5-medium/predictions
  name: stability-ai/stable-diffusion-3.5-medium
  summary: 2.5 billion parameter image model with improved MMDiT-X architecture
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: The aspect ratio of your output image. This value is ignored if you are using an input image.
            default: '1:1'
            required: false
          output_format:
            description: Format of the output images
            default: webp
            required: false
          cfg:
            type: number
            description: The guidance scale tells the model how similar the output should be to the prompt.
            default: 5
            min: 1
            max: 10
            required: false
          image:
            type: string
            description: Input image for image to image mode. The aspect ratio of your output will match this image.
            required: false
          negative_prompt:
            type: string
            description: What you do not want to see in the image
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          prompt_strength:
            type: number
            description: Prompt strength (or denoising strength) when using image to image. 1.0 corresponds to full destruction of information in image.
            default: 0.85
            min: 0
            max: 1
            required: false
          seed:
            type: integer
            description: Set a seed for reproducibility. Random by default.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/stability-ai/stable-diffusion-3.5-medium
- id: replicate.x.tencent-hunyuan-image-3
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/tencent/hunyuan-image-3/predictions
  name: tencent/hunyuan-image-3
  summary: A powerful native multimodal model for image generation (PrunaAI squeezed)
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          aspect_ratio:
            description: Aspect ratio for the generated image
            default: '1:1'
            required: false
          go_fast:
            type: boolean
            description: Run faster predictions with additional optimizations.
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          output_format:
            description: Format of the output images
            default: webp
            required: false
          output_quality:
            type: integer
            description: Quality when saving the output images, from 0 to 100. 100 is best quality, 0 is lowest quality. Not relevant for .png outputs
            default: 95
            min: 0
            max: 100
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated images.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/tencent/hunyuan-image-3
- id: replicate.x.wan-video-wan-2-7-image-pro
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.7-image-pro/predictions
  name: wan-video/wan-2.7-image-pro
  summary: Generate and edit high-quality images with Alibaba's Wan 2.7 Pro with 4K output, thinking mode, text-to-image, multi-image editing, and image set generation
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation or editing
            required: true
          images:
            type: array
            description: Input images for editing, style transfer, or multi-reference generation (up to 9 images, jpg/png/bmp/webp). When provided, the model operates in image editing mode.
            default: []
            required: false
          size:
            description: Output image resolution. '1K', '2K', and '4K' auto-size based on input images. 4K is only available for text-to-image.
            default: 2K
            required: false
          num_outputs:
            type: integer
            description: Number of images to generate (1-4 for standard mode, 1-12 for image set mode)
            default: 1
            min: 1
            max: 4
            required: false
          image_set_mode:
            type: boolean
            description: Generate a coherent set of related images from a single prompt (e.g. a character across seasons). When enabled, num_outputs can be up to 12.
            default: false
            required: false
          thinking_mode:
            type: boolean
            description: Enable enhanced reasoning for improved image quality. Only applies to text-to-image (no input images, no image set mode). Increases generation time.
            default: true
            required: false
          seed:
            type: integer
            description: 'Random seed for reproducible generation. Range: 0-2147483647'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.7-image-pro
- id: replicate.x.wan-video-wan-2-7-image
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.7-image/predictions
  name: wan-video/wan-2.7-image
  summary: Generate and edit images with Alibaba's Wan 2.7
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation or editing
            required: true
          images:
            type: array
            description: Input images for editing, style transfer, or multi-reference generation (up to 9 images, jpg/png/bmp/webp). When provided, the model operates in image editing mode.
            default: []
            required: false
          size:
            description: Output image resolution. '1K' and '2K' auto-size based on input images.
            default: 2K
            required: false
          num_outputs:
            type: integer
            description: Number of images to generate (1-4 for standard mode, 1-12 for image set mode)
            default: 1
            min: 1
            max: 4
            required: false
          image_set_mode:
            type: boolean
            description: Generate a coherent set of related images from a single prompt (e.g. a character across seasons). When enabled, num_outputs can be up to 12.
            default: false
            required: false
          thinking_mode:
            type: boolean
            description: Enable enhanced reasoning for improved image quality. Only applies to text-to-image (no input images, no image set mode). Increases generation time.
            default: true
            required: false
          seed:
            type: integer
            description: 'Random seed for reproducible generation. Range: 0-2147483647'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.7-image
- id: replicate.x.xai-grok-imagine-image
  tier: extended
  platform: image-gen
  domain: models
  method: POST
  path: /models/xai/grok-imagine-image/predictions
  name: xai/grok-imagine-image
  summary: SOTA image model from xAI
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation or editing
            required: true
          image:
            type: string
            description: Input image for editing. When provided, the model edits this image based on the prompt. Supports jpg, jpeg, png, webp.
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated image. Ignored when editing an image.
            default: '1:1'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/xai/grok-imagine-image
- id: replicate.x.alibaba-happyhorse-1-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/alibaba/happyhorse-1.0/predictions
  name: alibaba/happyhorse-1.0
  summary: Alibaba's Happy Horse 1.0 generates videos from text prompts or animates a single image into video. Supports 720p and 1080p, 3-15 second durations, and five aspect ratios.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation. Optional when an image is provided.
            default: ''
            required: false
          image:
            type: string
            description: First-frame image to animate. When provided, the model runs in image-to-video mode. Accepts jpg/png/bmp/webp, ≤10MB, aspect ratio between 1:2.5 and 2.5:1, each side ≥300px.
            required: false
          resolution:
            description: Output video resolution
            default: 1080p
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video. Only applies to text-to-video - when an image is provided, the image's aspect ratio is used.
            default: '16:9'
            required: false
          duration:
            description: Duration of the generated video in seconds
            default: 5
            required: false
          seed:
            type: integer
            description: 'Random seed for reproducible generation. Range: 0-2147483647'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/alibaba/happyhorse-1.0
- id: replicate.x.alibaba-happyhorse-1-1
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/alibaba/happyhorse-1.1/predictions
  name: alibaba/happyhorse-1.1
  summary: Alibaba's Happy Horse 1.1 generates videos from text, animates a single image, or builds a video from multiple reference images. Supports 720p and 1080p, 3-15 second durations, and five aspect ratios.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation. Optional when a single image is provided (image-to-video). Required for text-to-video and reference-to-video. When providing multiple reference images, refer to them in the prompt as [Image 1], [Image 2], etc.
            default: ''
            required: false
          images:
            type: array
            description: Input images. Provide no images for text-to-video, one image to animate it as the first frame (image-to-video), or multiple images (up to 9) for reference-to-video. Accepts jpg/png/bmp/webp, <=10MB each, aspect ratio between 1:2.5 and 2.5:1, each side >=300px.
            default: []
            required: false
          resolution:
            description: Output video resolution
            default: 1080p
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video. Only applies to text-to-video and reference-to-video. When a single image is provided (image-to-video), the image's aspect ratio is used.
            default: '16:9'
            required: false
          duration:
            description: Duration of the generated video in seconds
            default: 5
            required: false
          seed:
            type: integer
            description: 'Random seed for reproducible generation. Range: 0-2147483647'
            default: null
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/alibaba/happyhorse-1.1
- id: replicate.x.alibaba-wan-3
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/alibaba/wan-3/predictions
  name: alibaba/wan-3
  summary: (50% off until Aug 30!) - Wan 3.0 generates video from a text prompt or a starting image, with cinematic motion and support for 480p, 720p, and 1080p output up to 30 seconds.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Optional first-frame image to animate into a video (jpg/png/bmp/webp, ≤10MB). When provided, the video is generated from this image guided by the prompt.
            default: null
            required: false
          negative_prompt:
            type: string
            description: Negative prompt - describes content that should not appear in the video
            default: ''
            required: false
          resolution:
            description: Output video resolution
            default: 1080p
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video. 'adaptive' lets the model choose the best ratio for the prompt. Ignored when an image is provided (the input image's aspect ratio is used).
            default: adaptive
            required: false
          duration:
            type: integer
            description: Duration of the generated video in seconds
            default: 5
            min: 2
            max: 30
            required: false
          enable_prompt_expansion:
            type: boolean
            description: Automatically expand and optimize the prompt for better results. Improves quality for short prompts but increases latency.
            default: true
            required: false
          seed:
            type: integer
            description: 'Random seed for reproducible generation. Range: 0-2147483647'
            default: null
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/alibaba/wan-3
- id: replicate.x.bytedance-dreamactor-m2-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/bytedance/dreamactor-m2.0/predictions
  name: bytedance/dreamactor-m2.0
  summary: Animate any character, humans, cartoons, animals, even non-humans, from a single image + driving video
  input:
    body:
      input:
        type: object
        required: true
        properties:
          image:
            type: string
            description: 'Input image containing a human subject. Supported formats: JPEG, JPG, PNG. Max size: 4.7 MB. Resolution: 480x480 to 1920x1080.'
            required: true
          video:
            type: string
            description: 'Template video whose motion, facial expressions, and lip movements will be applied to the image subject. Supported formats: MP4, MOV, WebM. Max duration: 30 seconds. Resolution: 200x200 to 2048x1440.'
            required: true
          cut_first_second:
            type: boolean
            description: Whether to crop the first second of the output video (removes the 1-second transition at the beginning).
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/dreamactor-m2.0
- id: replicate.x.bytedance-seedance-1-pro-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/bytedance/seedance-1-pro-fast/predictions
  name: bytedance/seedance-1-pro-fast
  summary: A faster and cheaper version of Seedance 1 Pro
  input:
    body:
      input:
        type: object
        required: true
        properties:
          resolution:
            description: Video resolution
            default: 1080p
            required: false
          aspect_ratio:
            description: Video aspect ratio. Ignored if an image is used.
            default: '16:9'
            required: false
          fps:
            description: Frame rate (frames per second)
            default: 24
            required: false
          camera_fixed:
            type: boolean
            description: Whether to fix camera position
            default: false
            required: false
          duration:
            type: integer
            description: Video duration in seconds
            default: 5
            min: 2
            max: 12
            required: false
          image:
            type: string
            description: Input image for image-to-video generation
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedance-1-pro-fast
- id: replicate.x.bytedance-seedance-1-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/bytedance/seedance-1-pro/predictions
  name: bytedance/seedance-1-pro
  summary: A pro version of Seedance that offers text-to-video and image-to-video support for 5s or 10s videos, at 480p and 1080p resolution
  input:
    body:
      input:
        type: object
        required: true
        properties:
          resolution:
            description: Video resolution
            default: 1080p
            required: false
          aspect_ratio:
            description: Video aspect ratio. Ignored if an image is used.
            default: '16:9'
            required: false
          fps:
            description: Frame rate (frames per second)
            default: 24
            required: false
          camera_fixed:
            type: boolean
            description: Whether to fix camera position
            default: false
            required: false
          duration:
            type: integer
            description: Video duration in seconds
            default: 5
            min: 2
            max: 12
            required: false
          image:
            type: string
            description: Input image for image-to-video generation
            required: false
          last_frame_image:
            type: string
            description: Input image for last frame generation. This only works if an image start frame is given too.
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedance-1-pro
- id: replicate.x.bytedance-seedance-1-5-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/bytedance/seedance-1.5-pro/predictions
  name: bytedance/seedance-1.5-pro
  summary: A joint audio-video model that accurately follows complex instructions.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Input image for image-to-video generation
            required: false
          last_frame_image:
            type: string
            description: Input image for last frame generation. This only works if an image start frame is given too.
            required: false
          duration:
            type: integer
            description: Video duration in seconds
            default: 5
            min: 2
            max: 12
            required: false
          aspect_ratio:
            description: Video aspect ratio. Ignored if an image is used.
            default: '16:9'
            required: false
          fps:
            description: Frame rate (frames per second)
            default: 24
            required: false
          camera_fixed:
            type: boolean
            description: Whether to fix camera position
            default: false
            required: false
          generate_audio:
            type: boolean
            description: Generate audio synchronized with the video. When enabled, the model outputs a video with audio that matches the visuals.
            default: false
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedance-1.5-pro
- id: replicate.x.bytedance-seedance-2-0-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/bytedance/seedance-2.0-fast/predictions
  name: bytedance/seedance-2.0-fast
  summary: A faster variant of Seedance 2.0 for quicker video generation with multimodal inputs and native audio.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Input image for image-to-video generation (first frame). Cannot be combined with reference images.
            required: false
          last_frame_image:
            type: string
            description: Input image for last frame generation. Only works if a first frame image is also provided. Cannot be combined with reference images.
            required: false
          reference_images:
            type: array
            description: Reference images (up to 9) for character consistency, style guidance, and scene composition. Cannot be used together with first/last frame images. You can reference them in your prompt as [Image1], [Image2], etc.
            default: []
            required: false
          reference_videos:
            type: array
            description: Reference videos (up to 3, total duration max 15s) for motion transfer, style reference, and editing. Reference them in your prompt as [Video1], [Video2], etc.
            default: []
            required: false
          reference_audios:
            type: array
            description: Reference audio files (up to 3, total duration max 15s) for audio-driven generation and lip-sync. Requires at least one reference image or video. Reference them in your prompt as [Audio1], [Audio2], etc.
            default: []
            required: false
          duration:
            type: integer
            description: Video duration in seconds. Set to -1 for intelligent duration (model picks the best length).
            default: 5
            min: -1
            max: 15
            required: false
          resolution:
            description: Video resolution.
            default: 720p
            required: false
          aspect_ratio:
            description: Video aspect ratio. Set to 'adaptive' to let the model choose the best ratio based on inputs.
            default: '16:9'
            required: false
          generate_audio:
            type: boolean
            description: Generate synchronized audio with the video, including dialogue (use double quotes in prompt), sound effects, and background music.
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedance-2.0-fast
- id: replicate.x.bytedance-seedance-2-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/bytedance/seedance-2.0/predictions
  name: bytedance/seedance-2.0
  summary: ByteDance's multimodal video generation model with native audio, multimodal reference inputs, and intelligent duration control.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation. Maximum 4000 characters. BytePlus recommends keeping prompts under 600 English words for best results.
            required: true
          image:
            type: string
            description: Input image for image-to-video generation (first frame). Cannot be combined with reference images.
            default: null
            required: false
          last_frame_image:
            type: string
            description: Input image for last frame generation. Only works if a first frame image is also provided. Cannot be combined with reference images.
            default: null
            required: false
          reference_images:
            type: array
            description: Reference images (up to 9) for character consistency, style guidance, and scene composition. Cannot be used together with first/last frame images. You can reference them in your prompt as [Image1], [Image2], etc.
            default: []
            required: false
          reference_videos:
            type: array
            description: Reference videos (up to 3, total duration max 15s) for motion transfer, style reference, and editing. Reference them in your prompt as [Video1], [Video2], etc.
            default: []
            required: false
          reference_audios:
            type: array
            description: Reference audio files (up to 3, total duration max 15s) for audio-driven generation and lip-sync. Requires at least one reference image or video. Reference them in your prompt as [Audio1], [Audio2], etc.
            default: []
            required: false
          duration:
            type: integer
            description: Video duration in seconds. Set to -1 for intelligent duration (model picks the best length).
            default: 5
            min: -1
            max: 15
            required: false
          resolution:
            description: Video resolution. 4K outputs 10-bit H.265/HEVC at high bitrate.
            default: 720p
            required: false
          aspect_ratio:
            description: Video aspect ratio. Set to 'adaptive' to let the model choose the best ratio based on inputs.
            default: '16:9'
            required: false
          generate_audio:
            type: boolean
            description: Generate synchronized audio with the video, including dialogue (use double quotes in prompt), sound effects, and background music.
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation.
            default: null
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/bytedance/seedance-2.0
- id: replicate.x.google-veo-2
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/google/veo-2/predictions
  name: google/veo-2
  summary: State of the art video generation model. Veo 2 can faithfully follow simple and complex instructions, and convincingly simulates real-world physics as well as a wide range of visual styles.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: false
          image:
            type: string
            description: Input image to start generating from. Ideal images are 16:9 or 9:16 and 1280x720 or 720x1280, depending on the aspect ratio you choose.
            required: false
          aspect_ratio:
            description: Video aspect ratio
            default: '16:9'
            required: false
          duration:
            description: Video duration in seconds
            default: 5
            required: false
          seed:
            type: integer
            description: Random seed. Omit for random generations
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/veo-2
- id: replicate.x.google-veo-3-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/google/veo-3-fast/predictions
  name: google/veo-3-fast
  summary: A faster and cheaper version of Google’s Veo 3 video model, with audio
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          aspect_ratio:
            description: Video aspect ratio
            default: '16:9'
            required: false
          duration:
            description: Video duration in seconds
            default: 8
            required: false
          image:
            type: string
            description: Input image to start generating from. Ideal images are 16:9 or 9:16 and 1280x720 or 720x1280, depending on the aspect ratio you choose.
            required: false
          negative_prompt:
            type: string
            description: Description of what to exclude from the generated video
            required: false
          resolution:
            description: Resolution of the generated video
            default: 1080p
            required: false
          generate_audio:
            type: boolean
            description: Generate audio with the video
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Omit for random generations
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/veo-3-fast
- id: replicate.x.google-veo-3-1
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/google/veo-3.1/predictions
  name: google/veo-3.1
  summary: New and improved version of Veo 3, with higher-fidelity video, context-aware audio, reference image and last frame support
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          aspect_ratio:
            description: Video aspect ratio
            default: '16:9'
            required: false
          duration:
            description: Video duration in seconds
            default: 8
            required: false
          image:
            type: string
            description: Input image to start generating from. Ideal images are 16:9 or 9:16 and 1280x720 or 720x1280, depending on the aspect ratio you choose.
            required: false
          last_frame:
            type: string
            description: Ending image for interpolation. When provided with an input image, creates a transition between the two images.
            required: false
          reference_images:
            type: array
            description: 1 to 3 reference images for subject-consistent generation (reference-to-video, or R2V). Reference images only work with 16:9 aspect ratio and 8-second duration. Last frame is ignored if reference images are provided.
            default: []
            required: false
          negative_prompt:
            type: string
            description: Description of what to exclude from the generated video
            required: false
          resolution:
            description: Resolution of the generated video
            default: 1080p
            required: false
          generate_audio:
            type: boolean
            description: Generate audio with the video
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Omit for random generations
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/veo-3.1
- id: replicate.x.google-veo-3
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/google/veo-3/predictions
  name: google/veo-3
  summary: 'Sound on: Google’s flagship Veo 3 text to video model, with audio'
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          aspect_ratio:
            description: Video aspect ratio
            default: '16:9'
            required: false
          duration:
            description: Video duration in seconds
            default: 8
            required: false
          image:
            type: string
            description: Input image to start generating from. Ideal images are 16:9 or 9:16 and 1280x720 or 720x1280, depending on the aspect ratio you choose.
            required: false
          negative_prompt:
            type: string
            description: Description of what to exclude from the generated video
            required: false
          resolution:
            description: Resolution of the generated video
            default: 1080p
            required: false
          generate_audio:
            type: boolean
            description: Generate audio with the video
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Omit for random generations
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/google/veo-3
- id: replicate.x.kwaivgi-kling-o1
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-o1/predictions
  name: kwaivgi/kling-o1
  summary: Modify an existing video through natural-language commands, changing subjects, environments, and visual style while preserving the original motion and timing.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation. Can include references like <<<image_1>>>, <<<video_1>>> to reference inputs.
            required: true
          start_image:
            type: string
            description: First frame image for the video. Supports .jpg/.jpeg/.png, max 10MB.
            required: false
          end_image:
            type: string
            description: Last frame image for the video. Requires start_image to be set. Supports .jpg/.jpeg/.png, max 10MB.
            required: false
          reference_images:
            type: array
            description: Reference images for elements, scenes, or styles (up to 7 without video, 4 with video). Supports .jpg/.jpeg/.png.
            required: false
          reference_video:
            type: string
            description: Reference video for style, camera movement, or as base for editing. Supports .mp4/.mov, 3-10s duration, max 200MB.
            required: false
          video_reference_type:
            description: 'How to use the reference video: ''feature'' for style/camera reference, ''base'' for video editing.'
            default: feature
            required: false
          keep_original_sound:
            type: boolean
            description: Whether to keep the original sound from the reference video.
            default: true
            required: false
          mode:
            description: Video generation mode. 'std' is cost-effective, 'pro' has higher quality.
            default: pro
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video. Required for text-to-video. Ignored when using first frame image or video editing.
            default: '16:9'
            required: false
          duration:
            description: 'Video duration in seconds. For text/image-to-video: 5 or 10. With video reference (feature type): 3-10. Ignored for video editing (base type).'
            default: 5
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-o1
- id: replicate.x.kwaivgi-kling-v1-6-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v1.6-pro/predictions
  name: kwaivgi/kling-v1.6-pro
  summary: Generate 5s and 10s videos in 1080p resolution
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the video. Ignored if start_image is provided.
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          cfg_scale:
            type: number
            description: Flexibility in video generation; The higher the value, the lower the model's degree of flexibility, and the stronger the relevance to the user's prompt.
            default: 0.5
            min: 0
            max: 1
            required: false
          end_image:
            type: string
            description: Last frame of the video. A start or end image is required.
            required: false
          negative_prompt:
            type: string
            description: Things you do not want to see in the video
            default: ''
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          reference_images:
            type: array
            description: Reference images to use in video generation (up to 4 images). Also known as scene elements.
            required: false
          start_image:
            type: string
            description: First frame of the video. A start or end image is required.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v1.6-pro
- id: replicate.x.kwaivgi-kling-v1-6-standard
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v1.6-standard/predictions
  name: kwaivgi/kling-v1.6-standard
  summary: Generate 5s and 10s videos in 720p resolution at 30fps
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the video. Ignored if start_image is provided.
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          cfg_scale:
            type: number
            description: Flexibility in video generation; The higher the value, the lower the model's degree of flexibility, and the stronger the relevance to the user's prompt.
            default: 0.5
            min: 0
            max: 1
            required: false
          negative_prompt:
            type: string
            description: Things you do not want to see in the video
            default: ''
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          reference_images:
            type: array
            description: Reference images to use in video generation (up to 4 images). Also known as scene elements.
            required: false
          start_image:
            type: string
            description: First frame of the video
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v1.6-standard
- id: replicate.x.kwaivgi-kling-v2-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v2.0/predictions
  name: kwaivgi/kling-v2.0
  summary: Generate 5s and 10s videos in 720p resolution
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the video. Ignored if start_image is provided.
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          cfg_scale:
            type: number
            description: Flexibility in video generation; The higher the value, the lower the model's degree of flexibility, and the stronger the relevance to the user's prompt.
            default: 0.5
            min: 0
            max: 1
            required: false
          negative_prompt:
            type: string
            description: Things you do not want to see in the video
            default: ''
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          start_image:
            type: string
            description: First frame of the video (optional)
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v2.0
- id: replicate.x.kwaivgi-kling-v2-1-master
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v2.1-master/predictions
  name: kwaivgi/kling-v2.1-master
  summary: A premium version of Kling v2.1 with superb dynamics and prompt adherence. Generate 1080p 5s and 10s videos from text or an image
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the video. Ignored if start_image is provided.
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          negative_prompt:
            type: string
            description: Things you do not want to see in the video
            default: ''
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          start_image:
            type: string
            description: First frame of the video
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v2.1-master
- id: replicate.x.kwaivgi-kling-v2-1
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v2.1/predictions
  name: kwaivgi/kling-v2.1
  summary: Use Kling v2.1 to generate 5s and 10s videos in 720p and 1080p resolution from a starting image (image-to-video)
  input:
    body:
      input:
        type: object
        required: true
        properties:
          mode:
            description: Standard has a resolution of 720p, pro is 1080p. Both are 24fps.
            default: standard
            required: false
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          end_image:
            type: string
            description: Last frame of the video (pro mode is required when this parameter is set)
            required: false
          negative_prompt:
            type: string
            description: Things you do not want to see in the video
            default: ''
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          start_image:
            type: string
            description: First frame of the video. You must use a start image with kling-v2.1.
            required: true
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v2.1
- id: replicate.x.kwaivgi-kling-v2-5-turbo-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v2.5-turbo-pro/predictions
  name: kwaivgi/kling-v2.5-turbo-pro
  summary: 'Kling 2.5 Turbo Pro: Unlock pro-level text-to-video and image-to-video creation with smooth motion, cinematic depth, and remarkable prompt adherence.'
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          negative_prompt:
            type: string
            description: Things you do not want to see in the video
            default: ''
            required: false
          start_image:
            type: string
            description: First frame of the video
            required: false
          end_image:
            type: string
            description: Last frame of the video
            required: false
          aspect_ratio:
            description: Aspect ratio of the video. Ignored if start_image is provided.
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          image:
            type: string
            description: 'Deprecated: Use start_image instead.'
            required: false
          guidance_scale:
            type: number
            description: Kept for backwards compatibility. This parameter is not used.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v2.5-turbo-pro
- id: replicate.x.kwaivgi-kling-v2-6
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v2.6/predictions
  name: kwaivgi/kling-v2.6
  summary: 'Kling 2.6 Pro: Top-tier image-to-video with cinematic visuals, fluid motion, and native audio generation'
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          negative_prompt:
            type: string
            description: Things you do not want to see in the video
            default: ''
            required: false
          start_image:
            type: string
            description: First frame of the video
            required: false
          aspect_ratio:
            description: Aspect ratio of the video. Ignored if start_image is provided.
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          generate_audio:
            type: boolean
            description: Generate audio for the video. When enabled, the model will create synchronized audio based on the video content.
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v2.6
- id: replicate.x.kwaivgi-kling-v3-omni-video
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v3-omni-video/predictions
  name: kwaivgi/kling-v3-omni-video
  summary: 'Kling Video 3.0 Omni: Unified multimodal video generation with reference images, video editing, native audio, and multi-shot control'
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation. Supports <<<image_1>>>, <<<video_1>>> template references. Max 2500 characters.
            required: true
          start_image:
            type: string
            description: First frame image. Supports .jpg/.jpeg/.png, max 10MB, min 300px, aspect ratio 1:2.5 to 2.5:1.
            required: false
          end_image:
            type: string
            description: Last frame image. Requires start_image. Supports .jpg/.jpeg/.png, max 10MB, min 300px.
            required: false
          reference_images:
            type: array
            description: Reference images for elements, scenes, or styles. Supports .jpg/.jpeg/.png. Max 7 without video, 4 with video.
            required: false
          reference_video:
            type: string
            description: Reference video (.mp4/.mov). Duration 3-10s, resolution 720-2160px per side, max 200MB.
            required: false
          video_reference_type:
            description: 'How to use reference video: ''feature'' for style/camera reference, ''base'' for video editing.'
            default: feature
            required: false
          keep_original_sound:
            type: boolean
            description: Keep original sound from reference video.
            default: true
            required: false
          generate_audio:
            type: boolean
            description: Generate native audio. Mutually exclusive with reference video.
            default: false
            required: false
          mode:
            description: '''standard'' generates 720p, ''pro'' generates 1080p, ''4k'' generates 4K. 4K does not support reference_video.'
            default: pro
            required: false
          aspect_ratio:
            description: Aspect ratio. Required when not using start frame or video editing.
            default: '16:9'
            required: false
          duration:
            type: integer
            description: Video duration in seconds (3-15). Ignored for video editing (base).
            default: 5
            min: 3
            max: 15
            required: false
          multi_prompt:
            type: string
            description: 'JSON array of shot definitions for multi-shot mode. Each shot: {"prompt": "...", "duration": N}. Max 6 shots, min duration 1s per shot, total must equal duration. Example: [{"prompt":"A cat jumps","duration":3},{"prompt":"It lands","duration":2}]'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v3-omni-video
- id: replicate.x.kwaivgi-kling-v3-video
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/kwaivgi/kling-v3-video/predictions
  name: kwaivgi/kling-v3-video
  summary: 'Kling Video 3.0: Generate cinematic videos up to 15 seconds with multi-shot control, native audio, and improved consistency'
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation. Max 2500 characters.
            required: true
          negative_prompt:
            type: string
            description: Things you do not want to see in the video. Max 2500 characters.
            default: ''
            required: false
          start_image:
            type: string
            description: First frame image. Supports .jpg/.jpeg/.png, max 10MB, min 300px, aspect ratio 1:2.5 to 2.5:1.
            required: false
          end_image:
            type: string
            description: Last frame image. Requires start_image. Supports .jpg/.jpeg/.png, max 10MB, min 300px.
            required: false
          mode:
            description: '''standard'' generates 720p, ''pro'' generates 1080p, ''4k'' generates 4K.'
            default: pro
            required: false
          aspect_ratio:
            description: Aspect ratio. Ignored when start_image is provided.
            default: '16:9'
            required: false
          duration:
            type: integer
            description: Video duration in seconds.
            default: 5
            min: 3
            max: 15
            required: false
          generate_audio:
            type: boolean
            description: Generate native audio for the video.
            default: false
            required: false
          multi_prompt:
            type: string
            description: 'JSON array of shot definitions for multi-shot mode. Each shot: {"prompt": "...", "duration": N}. Max 6 shots, min 1s per shot, total must equal duration.'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/kwaivgi/kling-v3-video
- id: replicate.x.leonardoai-motion-2-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/leonardoai/motion-2.0/predictions
  name: leonardoai/motion-2.0
  summary: Create 5s 480p videos from a text prompt
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the output video. Ignored if image is provided.
            default: '16:9'
            required: false
          vibe_style:
            description: Style for the overall vibe of the video. Ignored if image is provided.
            default: None
            required: false
          lighting_style:
            description: Style for the lighting of the video. Ignored if image is provided.
            default: None
            required: false
          shot_type_style:
            description: Style for the shot type of the video. Ignored if image is provided.
            default: None
            required: false
          color_theme_style:
            description: Style for the color theme of the video. Ignored if image is provided.
            default: None
            required: false
          frame_interpolation:
            type: boolean
            description: Smoothly blend frames for fluid video transitions
            default: true
            required: false
          image:
            type: string
            description: Image to use for the first frame of the video
            required: false
          negative_prompt:
            type: string
            description: The negative prompt used for the video generation
            required: false
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          prompt_enhance:
            type: boolean
            description: Whether to enhance the prompt
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/leonardoai/motion-2.0
- id: replicate.x.luma-modify-video
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/luma/modify-video/predictions
  name: luma/modify-video
  summary: Modify a video with style transfer and prompt-based editing
  input:
    body:
      input:
        type: object
        required: true
        properties:
          mode:
            description: 'How closely the output should follow the source video. Adhere: very close, for subtle enhancements. Flex: allows more stylistic change while keeping recognizable elements. Reimagine: loosely follows the source, for dramatic or transformative changes.'
            default: adhere_1
            required: false
          first_frame:
            type: string
            description: An optional first frame of the video to modify. This should be a modified version of the original first frame, it will be used to guide the video modification.
            required: false
          first_frame_url:
            type: string
            description: 'Deprecated: Use first_frame instead.'
            required: false
          prompt:
            type: string
            description: A prompt to guide the video modification
            required: false
          video:
            type: string
            description: The video to modify. Maximum video size is 100mb. Maximum video duration is 30 seconds.
            required: false
          video_url:
            type: string
            description: 'Deprecated: Use video instead.'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/modify-video
- id: replicate.x.luma-ray-2-540p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/luma/ray-2-540p/predictions
  name: luma/ray-2-540p
  summary: Generate 5s and 9s 540p videos
  input:
    body:
      input:
        type: object
        required: true
        properties:
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video
            default: '16:9'
            required: false
          concepts:
            type: array
            description: 'List of camera concepts to apply to the video generation. Concepts include: truck_left, pan_right, pedestal_down, low_angle, pedestal_up, selfie, pan_left, roll_right, zoom_in, over_the_shoulder, orbit_right, orbit_left, static, tiny_planet, high_angle, bolt_cam, dolly_zoom, overhead, zoom_out, handheld, roll_left, pov, aerial_drone, push_in, crane_down, truck_right, tilt_down, elevator_doors, tilt_up, ground_level, pull_out, aerial, crane_up, eye_level'
            required: false
          end_image:
            type: string
            description: An optional last frame of the video to use as the ending frame.
            required: false
          end_image_url:
            type: string
            description: 'Deprecated: Use end_image instead'
            required: false
          loop:
            type: boolean
            description: Whether the video should loop, with the last frame matching the first frame for smooth, continuous playback.
            default: false
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          start_image:
            type: string
            description: An optional first frame of the video to use as the starting frame.
            required: false
          start_image_url:
            type: string
            description: 'Deprecated: Use start_image instead'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/ray-2-540p
- id: replicate.x.luma-ray-2-720p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/luma/ray-2-720p/predictions
  name: luma/ray-2-720p
  summary: Generate 5s and 9s 720p videos
  input:
    body:
      input:
        type: object
        required: true
        properties:
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video
            default: '16:9'
            required: false
          concepts:
            type: array
            description: 'List of camera concepts to apply to the video generation. Concepts include: truck_left, pan_right, pedestal_down, low_angle, pedestal_up, selfie, pan_left, roll_right, zoom_in, over_the_shoulder, orbit_right, orbit_left, static, tiny_planet, high_angle, bolt_cam, dolly_zoom, overhead, zoom_out, handheld, roll_left, pov, aerial_drone, push_in, crane_down, truck_right, tilt_down, elevator_doors, tilt_up, ground_level, pull_out, aerial, crane_up, eye_level'
            required: false
          end_image:
            type: string
            description: An optional last frame of the video to use as the ending frame.
            required: false
          end_image_url:
            type: string
            description: 'Deprecated: Use end_image instead'
            required: false
          loop:
            type: boolean
            description: Whether the video should loop, with the last frame matching the first frame for smooth, continuous playback.
            default: false
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          start_image:
            type: string
            description: An optional first frame of the video to use as the starting frame.
            required: false
          start_image_url:
            type: string
            description: 'Deprecated: Use start_image instead'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/ray-2-720p
- id: replicate.x.luma-ray-3-2
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/luma/ray-3.2/predictions
  name: luma/ray-3.2
  summary: Luma's reasoning video model. Generates cinematic 5s or 10s video from text or images, with native HDR and EXR export for professional production pipelines.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt describing the video. Be specific about subject, motion, camera movement, lighting, and pacing.
            required: true
          aspect_ratio:
            description: Aspect ratio of the generated video. Ignored when start_image or end_image is set - the model derives the ratio from the anchor frame(s).
            default: '16:9'
            required: false
          resolution:
            description: Output resolution. 540p is not available when hdr is true.
            default: 720p
            required: false
          duration:
            description: Video duration in seconds. 10s is not supported with hdr, start_image, end_image, or loop.
            default: 5
            required: false
          hdr:
            type: boolean
            description: Generate HDR-encoded MP4. Requires 720p or 1080p, 5s duration, and no loop.
            default: false
            required: false
          exr_export:
            type: boolean
            description: Export an EXR file alongside the MP4 for professional colour-grading workflows. Requires hdr=true.
            default: false
            required: false
          loop:
            type: boolean
            description: Generate a seamlessly looping video. Not supported with 10s duration, hdr, or end_image.
            default: false
            required: false
          start_image:
            type: string
            description: Optional first frame of the video. Only valid with 5s duration.
            default: null
            required: false
          end_image:
            type: string
            description: Optional last frame of the video. Only valid with 5s duration; not supported with loop.
            default: null
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/ray-3.2
- id: replicate.x.luma-ray-flash-2-540p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/luma/ray-flash-2-540p/predictions
  name: luma/ray-flash-2-540p
  summary: Generate 5s and 9s 540p videos, faster and cheaper than Ray 2
  input:
    body:
      input:
        type: object
        required: true
        properties:
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video
            default: '16:9'
            required: false
          concepts:
            type: array
            description: 'List of camera concepts to apply to the video generation. Concepts include: truck_left, pan_right, pedestal_down, low_angle, pedestal_up, selfie, pan_left, roll_right, zoom_in, over_the_shoulder, orbit_right, orbit_left, static, tiny_planet, high_angle, bolt_cam, dolly_zoom, overhead, zoom_out, handheld, roll_left, pov, aerial_drone, push_in, crane_down, truck_right, tilt_down, elevator_doors, tilt_up, ground_level, pull_out, aerial, crane_up, eye_level'
            required: false
          end_image:
            type: string
            description: An optional last frame of the video to use as the ending frame.
            required: false
          end_image_url:
            type: string
            description: 'Deprecated: Use end_image instead'
            required: false
          loop:
            type: boolean
            description: Whether the video should loop, with the last frame matching the first frame for smooth, continuous playback.
            default: false
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          start_image:
            type: string
            description: An optional first frame of the video to use as the starting frame.
            required: false
          start_image_url:
            type: string
            description: 'Deprecated: Use start_image instead'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/ray-flash-2-540p
- id: replicate.x.luma-ray-flash-2-720p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/luma/ray-flash-2-720p/predictions
  name: luma/ray-flash-2-720p
  summary: Generate 5s and 9s 720p videos, faster and cheaper than Ray 2
  input:
    body:
      input:
        type: object
        required: true
        properties:
          duration:
            description: Duration of the video in seconds
            default: 5
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video
            default: '16:9'
            required: false
          concepts:
            type: array
            description: 'List of camera concepts to apply to the video generation. Concepts include: truck_left, pan_right, pedestal_down, low_angle, pedestal_up, selfie, pan_left, roll_right, zoom_in, over_the_shoulder, orbit_right, orbit_left, static, tiny_planet, high_angle, bolt_cam, dolly_zoom, overhead, zoom_out, handheld, roll_left, pov, aerial_drone, push_in, crane_down, truck_right, tilt_down, elevator_doors, tilt_up, ground_level, pull_out, aerial, crane_up, eye_level'
            required: false
          end_image:
            type: string
            description: An optional last frame of the video to use as the ending frame.
            required: false
          end_image_url:
            type: string
            description: 'Deprecated: Use end_image instead'
            required: false
          loop:
            type: boolean
            description: Whether the video should loop, with the last frame matching the first frame for smooth, continuous playback.
            default: false
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          start_image:
            type: string
            description: An optional first frame of the video to use as the starting frame.
            required: false
          start_image_url:
            type: string
            description: 'Deprecated: Use start_image instead'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/luma/ray-flash-2-720p
- id: replicate.x.minimax-hailuo-02
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/minimax/hailuo-02/predictions
  name: minimax/hailuo-02
  summary: Hailuo 2 is a text-to-video and image-to-video model that can make 6s or 10s videos at 768p (standard) or 1080p (pro). It excels at real world physics.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          duration:
            description: Duration of the video in seconds. 10 seconds is only available for 768p resolution.
            default: 6
            required: false
          resolution:
            description: Pick between standard 512p, 768p, or pro 1080p resolution. The pro model is not just high resolution, it is also higher quality.
            default: 1080p
            required: false
          first_frame_image:
            type: string
            description: First frame image for video generation. The output video will have the same aspect ratio as this image.
            required: false
          last_frame_image:
            type: string
            description: Last frame image for video generation. The final frame of the output video will match this image.
            required: false
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          prompt_optimizer:
            type: boolean
            description: Use prompt optimizer
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/minimax/hailuo-02
- id: replicate.x.minimax-hailuo-2-3-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/minimax/hailuo-2.3-fast/predictions
  name: minimax/hailuo-2.3-fast
  summary: A lower-latency image-to-video version of Hailuo 2.3 that preserves core motion quality, visual consistency, and stylization performance while enabling faster iteration cycles.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          duration:
            description: Duration of the video in seconds. 10 seconds is only available for 768p resolution.
            default: 6
            required: false
          resolution:
            description: Pick between 768p or 1080p resolution. 1080p supports only 6-second duration.
            default: 768p
            required: false
          first_frame_image:
            type: string
            description: First frame image for video generation. The output video will have the same aspect ratio as this image.
            required: true
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          prompt_optimizer:
            type: boolean
            description: Use prompt optimizer
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/minimax/hailuo-2.3-fast
- id: replicate.x.minimax-hailuo-2-3
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/minimax/hailuo-2.3/predictions
  name: minimax/hailuo-2.3
  summary: A high-fidelity video generation model optimized for realistic human motion, cinematic VFX, expressive characters, and strong prompt and style adherence across both text-to-video and image-to-video workflows
  input:
    body:
      input:
        type: object
        required: true
        properties:
          duration:
            description: Duration of the video in seconds. 10 seconds is only available for 768p resolution.
            default: 6
            required: false
          resolution:
            description: Pick between 768p or 1080p resolution. 1080p supports only 6-second duration.
            default: 768p
            required: false
          first_frame_image:
            type: string
            description: First frame image for video generation. The output video will have the same aspect ratio as this image.
            required: false
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          prompt_optimizer:
            type: boolean
            description: Use prompt optimizer
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/minimax/hailuo-2.3
- id: replicate.x.minimax-video-01-director
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/minimax/video-01-director/predictions
  name: minimax/video-01-director
  summary: Generate videos with specific camera movements
  input:
    body:
      input:
        type: object
        required: true
        properties:
          first_frame_image:
            type: string
            description: First frame image for video generation. The output video will have the same aspect ratio as this image.
            required: false
          prompt:
            type: string
            description: 'Text prompt for video generation. Camera movement instructions can be added using square brackets (e.g. [Pan left] or [Zoom in]). You can use up to 3 combined movements per prompt. Supported movements: Truck left/right, Pan left/right, Push in/Pull out, Pedestal up/down, Tilt up/down, Zoom in/out, Shake, Tracking shot, Static shot. For example: [Truck left, Pan right, Zoom in]'
            required: true
          prompt_optimizer:
            type: boolean
            description: Use prompt optimizer
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/minimax/video-01-director
- id: replicate.x.minimax-video-01-live
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/minimax/video-01-live/predictions
  name: minimax/video-01-live
  summary: An image-to-video (I2V) model specifically trained for Live2D and general animation use cases
  input:
    body:
      input:
        type: object
        required: true
        properties:
          first_frame_image:
            type: string
            description: First frame image for video generation. The output video will have the same aspect ratio as this image.
            required: true
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          prompt_optimizer:
            type: boolean
            description: Use prompt optimizer
            default: true
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/minimax/video-01-live
- id: replicate.x.minimax-video-01
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/minimax/video-01/predictions
  name: minimax/video-01
  summary: Generate 6s videos with prompts or images. (Also known as Hailuo). Use a subject reference to make a video with a character and the S2V-01 model.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          first_frame_image:
            type: string
            description: First frame image for video generation. The output video will have the same aspect ratio as this image.
            required: false
          prompt:
            type: string
            description: Text prompt for generation
            required: true
          prompt_optimizer:
            type: boolean
            description: Use prompt optimizer
            default: true
            required: false
          subject_reference:
            type: string
            description: An optional character reference image to use as the subject in the generated video (this will use the S2V-01 model)
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/minimax/video-01
- id: replicate.x.openai-sora-2-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/openai/sora-2-pro/predictions
  name: openai/sora-2-pro
  summary: OpenAI's Most advanced synced-audio video generation
  input:
    body:
      input:
        type: object
        required: true
        properties:
          openai_api_key:
            type: string
            description: 'Optional: Your OpenAI API key. If you use your own OpenAI API key, you will be charged directly by OpenAI.'
            required: false
          prompt:
            type: string
            description: A text description of the video to generate
            required: true
          input_reference:
            type: string
            description: An optional image to use as the first frame of the video. The image must be the same aspect ratio as the video.
            required: false
          seconds:
            description: Duration of the video in seconds
            default: 4
            required: false
          aspect_ratio:
            description: Aspect ratio of the video
            default: portrait
            required: false
          resolution:
            description: Resolution quality. Standard is 720p (1280x720 landscape, 720x1280 portrait). High is 1024p (1792x1024 landscape, 1024x1792 portrait).
            default: standard
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/openai/sora-2-pro
- id: replicate.x.openai-sora-2
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/openai/sora-2/predictions
  name: openai/sora-2
  summary: OpenAI's Flagship video generation with synced audio
  input:
    body:
      input:
        type: object
        required: true
        properties:
          openai_api_key:
            type: string
            description: 'Optional: Your OpenAI API key. If you use your own OpenAI API key, you will be charged directly by OpenAI.'
            required: false
          prompt:
            type: string
            description: A text description of the video to generate
            required: true
          input_reference:
            type: string
            description: An optional image to use as the first frame of the video. The image must be the same aspect ratio as the video.
            required: false
          seconds:
            description: Duration of the video in seconds
            default: 4
            required: false
          aspect_ratio:
            description: Aspect ratio of the video. Portrait is 720x1280, landscape is 1280x720
            default: portrait
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/openai/sora-2
- id: replicate.x.pixverse-pixverse-v4-5
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/pixverse/pixverse-v4.5/predictions
  name: pixverse/pixverse-v4.5
  summary: Quickly make 5s or 8s videos at 540p, 720p or 1080p. It has enhanced motion, prompt coherence and handles complex actions well.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Image to use for the first frame of the video
            required: false
          last_frame_image:
            type: string
            description: Use to generate a video that transitions from the first image to the last image. Must be used with image.
            required: false
          quality:
            description: Resolution of the video. 360p and 540p cost the same, but 720p and 1080p cost more. See the README for details.
            default: 540p
            required: false
          aspect_ratio:
            description: Aspect ratio of the video
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds. 8 second videos cost twice as much as 5 second videos. (1080p does not support 8 second duration)
            default: 5
            required: false
          motion_mode:
            description: Motion mode for the video. Smooth videos generate more frames, so they cost twice as much. (smooth is only available when using a 5 second duration, 1080p does not support smooth motion)
            default: normal
            required: false
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements in the video
            default: ''
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          style:
            description: Style of the video
            default: None
            required: false
          effect:
            description: Special effect to apply to the video. Does not work with last_frame_image.
            default: None
            required: false
          sound_effect_switch:
            type: boolean
            description: Enable background music or sound effects
            default: false
            required: false
          sound_effect_content:
            type: string
            description: Sound effect prompt. If not given, a random sound effect will be generated.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/pixverse/pixverse-v4.5
- id: replicate.x.pixverse-pixverse-v4
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/pixverse/pixverse-v4/predictions
  name: pixverse/pixverse-v4
  summary: Quickly generate smooth 5s or 8s videos at 540p, 720p or 1080p
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Image to use for the first frame of the video
            required: false
          last_frame_image:
            type: string
            description: Use to generate a video that transitions from the first image to the last image. Must be used with image.
            required: false
          quality:
            description: Resolution of the video. 360p and 540p cost the same, but 720p and 1080p cost more. See the README for details.
            default: 540p
            required: false
          aspect_ratio:
            description: Aspect ratio of the video
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds. 8 second videos cost twice as much as 5 second videos. (1080p does not support 8 second duration)
            default: 5
            required: false
          motion_mode:
            description: Motion mode for the video. Smooth videos generate more frames, so they cost twice as much. (smooth is only available when using a 5 second duration, 1080p does not support smooth motion)
            default: normal
            required: false
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements in the video
            default: ''
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          style:
            description: Style of the video
            default: None
            required: false
          effect:
            description: Special effect to apply to the video. Does not work with last_frame_image.
            default: None
            required: false
          sound_effect_switch:
            type: boolean
            description: Enable background music or sound effects
            default: false
            required: false
          sound_effect_content:
            type: string
            description: Sound effect prompt. If not given, a random sound effect will be generated.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/pixverse/pixverse-v4
- id: replicate.x.pixverse-pixverse-v5-6
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/pixverse/pixverse-v5.6/predictions
  name: pixverse/pixverse-v5.6
  summary: Latest video model from Pixverse with astonishing physics
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Image to use for the first frame of the video
            required: false
          last_frame_image:
            type: string
            description: Use to generate a video that transitions from the first image to the last image. Must be used with image.
            required: false
          quality:
            description: Resolution of the video. Higher resolutions cost more. See pricing for details.
            default: 540p
            required: false
          aspect_ratio:
            description: Aspect ratio of the video
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds. 10 second videos are only available for 360p, 540p, and 720p.
            default: 5
            required: false
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements in the video
            default: ''
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          generate_audio_switch:
            type: boolean
            description: Enable AI-generated audio including BGM, SFX, and character dialogues
            default: false
            required: false
          thinking_type:
            description: Prompt reasoning enhancement. Controls whether the system enhances your prompt with internal reasoning and optimization.
            default: auto
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/pixverse/pixverse-v5.6
- id: replicate.x.pixverse-pixverse-v5
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/pixverse/pixverse-v5/predictions
  name: pixverse/pixverse-v5
  summary: Create 5s-8s videos with enhanced character movement, visual effects, and exclusive 1080p-8s support. Optimized for anime characters and complex actions
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Image to use for the first frame of the video
            required: false
          last_frame_image:
            type: string
            description: Use to generate a video that transitions from the first image to the last image. Must be used with image.
            required: false
          quality:
            description: Resolution of the video. 360p and 540p cost the same, but 720p and 1080p cost more. V5 supports 1080p with 8 second duration.
            default: 540p
            required: false
          aspect_ratio:
            description: Aspect ratio of the video
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds. 8 second videos cost twice as much as 5 second videos. V5 supports 1080p with 8 second duration.
            default: 5
            required: false
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements in the video
            default: ''
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          effect:
            description: Special effect to apply to the video. V5 supports effects. Does not work with last_frame_image.
            default: None
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/pixverse/pixverse-v5
- id: replicate.x.pixverse-pixverse-v6
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/pixverse/pixverse-v6/predictions
  name: pixverse/pixverse-v6
  summary: PixVerse's flagship video generation model. Generate cinematic videos with synchronized audio, multi-shot sequences, and precise camera control.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Image to use for the first frame of the video
            required: false
          last_frame_image:
            type: string
            description: Use to generate a video that transitions from the first image to the last image. Must be used with image.
            required: false
          quality:
            description: Resolution of the video. Higher resolutions cost more. See pricing for details.
            default: 540p
            required: false
          aspect_ratio:
            description: Aspect ratio of the video. Only used for text-to-video. Ignored when image or last_frame_image is provided.
            default: '16:9'
            required: false
          duration:
            description: Duration of the video in seconds. Billing is per second.
            default: 5
            required: false
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements in the video
            default: ''
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          generate_audio_switch:
            type: boolean
            description: Enable AI-generated audio including BGM, SFX, and character dialogues
            default: false
            required: false
          generate_multi_clip_switch:
            type: boolean
            description: Enable multi-shot generation for cinematic sequences with scene transitions. Only available for text-to-video and image-to-video (not transitions).
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/pixverse/pixverse-v6
- id: replicate.x.prunaai-p-video-animate
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/prunaai/p-video-animate/predictions
  name: prunaai/p-video-animate
  summary: p-video-animate animates a reference image with the motion and audio of a source video. Optimized for speed and cost - 5.24s per 1s of video.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          video:
            type: string
            description: 'Source RGB video (.mp4): motion + audio source.'
            required: true
          image:
            type: string
            description: Reference image of the subject to animate.
            required: true
          instruction_prompt:
            type: string
            description: Further instruction on how the reference subject should be animated using the motion of the input video.
            default: ''
            required: false
          resolution:
            description: 'Target ~megapixel budget: 720p ≈ 1 MP, 1080p ≈ 2 MP (aspect ratio preserved).'
            default: 720p
            required: false
          target_fps:
            description: Target FPS for the working video.
            default: original
            required: false
          save_audio:
            type: boolean
            description: Save the video with audio.
            default: true
            required: false
          ignore_audio:
            type: boolean
            description: Ignore source audio during generation. If save_audio is true, the source audio is still saved with the final video.
            default: false
            required: false
          turbo:
            type: boolean
            description: 'Turbo mode: faster generation for slightly lower quality.'
            default: false
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated videos.
            default: false
            required: false
          seed:
            type: integer
            description: Random seed. Leave blank for random.
            required: false
          no_op:
            type: boolean
            description: ''
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/p-video-animate
- id: replicate.x.prunaai-p-video-avatar
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/prunaai/p-video-avatar/predictions
  name: prunaai/p-video-avatar
  summary: p-video-avatar is the fastest and cheapest avatar/lipsync video model on the market.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          image:
            type: string
            description: Input image (first frame). Supports jpg, jpeg, png, webp.
            required: true
          resolution:
            description: Resolution of the video.
            default: 720p
            required: false
          audio:
            type: string
            description: Optional uploaded audio to drive avatar speech. If provided, this is used instead of voice_script and voice settings.
            required: false
          voice:
            description: Voice to use when generating speech from voice_script.
            default: Zephyr (Female)
            required: false
          voice_script:
            type: string
            description: Exact words the avatar should say. Required when no audio file is uploaded.
            default: ''
            required: false
          voice_prompt:
            type: string
            description: Optional style instructions for how to speak voice_script, such as tone, pacing, accent, or emotion. These instructions are not spoken.
            default: Say the following.
            required: false
          voice_language:
            description: Language/accent target for generated speech.
            default: English (US)
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation.
            required: false
          video_prompt:
            type: string
            description: Optional visual prompt describing how the person should appear or behave while speaking.
            default: The person is talking.
            required: false
          negative_prompt:
            type: string
            description: Disabled if empty.Mention what you do NOT want in the video, e.g. "subtitles, text, blurry, low quality, frames, watermark, titles, scene change". We recommend using multiple keywords at once.
            default: ''
            required: false
          strength_negative_prompt:
            type: number
            description: Strength of the Negative Prompt. Optimal value can differ for different video lengths (Experimental Feature)
            default: 0.5
            min: 0
            max: 4
            required: false
          disable_safety_filter:
            type: boolean
            description: Disable safety filter for prompts and input image. When disabled, prompts are not checked for unsafe content before generation.
            default: true
            required: false
          disable_prompt_upsampling:
            type: boolean
            description: When true, skip automatic enhancement of the visual video prompt and use video_prompt directly.
            default: false
            required: false
          no_op:
            type: boolean
            description: Health check mode - returns status without inference.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/p-video-avatar
- id: replicate.x.prunaai-p-video
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/prunaai/p-video/predictions
  name: prunaai/p-video
  summary: Fast video generation with built-in draft mode for rapid creative iteration. Text-to-video, image-to-video, and audio-to-video in a single endpoint.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation.
            required: true
          image:
            type: string
            description: Input image to generate video from (image-to-video). Supports jpg, jpeg, png, webp.
            required: false
          last_frame_image:
            type: string
            description: Reference image for the last frame of the video. Supports jpg, jpeg, png, webp.
            required: false
          audio:
            type: string
            description: Input audio to condition video generation. Supports flac, mp3, wav.
            required: false
          duration:
            type: integer
            description: Duration of the video in seconds (1-20). Ignored when audio is provided.
            default: 5
            min: 1
            max: 20
            required: false
          aspect_ratio:
            description: Aspect ratio of the video. Ignored when an input image is provided.
            default: '16:9'
            required: false
          resolution:
            description: Resolution of the video.
            default: 720p
            required: false
          fps:
            description: Frames per second of the video.
            default: 24
            required: false
          draft:
            type: boolean
            description: Draft mode. Generates a lower-quality preview of the video.
            default: false
            required: false
          prompt_upsampling:
            type: boolean
            description: Use prompt upsampling to enhance the prompt.
            default: true
            required: false
          disable_safety_filter:
            type: boolean
            description: Disable safety filter for prompts (and input image). When disabled, prompts are not checked for unsafe content before generation.
            default: true
            required: false
          save_audio:
            type: boolean
            description: Save the video with audio.
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation.
            required: false
          no_op:
            type: boolean
            description: Health check mode - returns status without inference.
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/prunaai/p-video
- id: replicate.x.runwayml-gen-4-5
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/runwayml/gen-4.5/predictions
  name: runwayml/gen-4.5
  summary: State-of-the-art video motion quality, prompt adherence and visual fidelity
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Optional initial image for video generation (first frame). If not provided, video will be generated from text only.
            required: false
          aspect_ratio:
            description: Video aspect ratio
            default: '16:9'
            required: false
          duration:
            description: Duration of the output video in seconds
            default: 5
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/runwayml/gen-4.5
- id: replicate.x.veed-fabric-1-0
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/veed/fabric-1.0/predictions
  name: veed/fabric-1.0
  summary: VEED Fabric 1.0 is an image-to-video API that turns any image into a talking video
  input:
    body:
      input:
        type: object
        required: true
        properties:
          image:
            type: string
            description: Image to generate video from
            required: true
          audio:
            type: string
            description: Audio to generate video from
            required: true
          resolution:
            description: Resolution of the generated video
            default: 720p
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/veed/fabric-1.0
- id: replicate.x.vidu-q3-pro
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/vidu/q3-pro/predictions
  name: vidu/q3-pro
  summary: High-fidelity video generation with text-to-video, image-to-video, and start-end-to-video modes. Up to 16 seconds at 1080p with synchronized audio.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation. Maximum 5000 characters.
            required: true
          start_image:
            type: string
            description: 'Start frame image for the video. When provided without an end_image, the model runs in image-to-video mode. Supported formats: png, jpeg, jpg, webp.'
            required: false
          end_image:
            type: string
            description: 'End frame image for the video. Must be used together with start_image for start-end-to-video mode. The aspect ratios of start and end images must be similar (ratio between 0.8 and 1.25). Supported formats: png, jpeg, jpg, webp.'
            required: false
          duration:
            type: integer
            description: Duration of the video in seconds.
            default: 5
            min: 1
            max: 16
            required: false
          aspect_ratio:
            description: Aspect ratio of the output video. Only used in text-to-video mode (ignored when images are provided).
            default: '16:9'
            required: false
          resolution:
            description: Resolution of the output video.
            default: 720p
            required: false
          audio:
            type: boolean
            description: Whether to generate audio synchronized with the video (dialogue and sound effects).
            default: true
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation.
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/vidu/q3-pro
- id: replicate.x.wan-video-wan-2-1-1-3b
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.1-1.3b/predictions
  name: wan-video/wan-2.1-1.3b
  summary: Generate 5s 480p videos. Wan is an advanced and powerful visual generation model developed by Tongyi Lab of Alibaba Group
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt describing what you want to generate
            required: true
          aspect_ratio:
            description: Video aspect ratio
            default: '16:9'
            required: false
          frame_num:
            description: Video duration in frames (based on standard 16fps playback)
            default: 81
            required: false
          resolution:
            description: Video resolution
            default: 480p
            required: false
          sample_steps:
            type: integer
            description: Number of sampling steps (higher = better quality but slower)
            default: 30
            min: 10
            max: 50
            required: false
          sample_guide_scale:
            type: number
            description: Classifier free guidance scale (higher values strengthen prompt adherence)
            default: 6
            min: 0
            max: 20
            required: false
          sample_shift:
            type: number
            description: 'Sampling shift factor for flow matching (recommended range: 8-12)'
            default: 8
            min: 0
            max: 20
            required: false
          seed:
            type: integer
            description: Random seed for reproducible results (leave blank for random)
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.1-1.3b
- id: replicate.x.wan-video-wan-2-2-i2v-a14b
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.2-i2v-a14b/predictions
  name: wan-video/wan-2.2-i2v-a14b
  summary: Image-to-video at 720p and 480p with Wan 2.2 A14B
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Prompt for video generation
            required: true
          image:
            type: string
            description: Input image to generate video from.
            required: true
          go_fast:
            type: boolean
            description: Go fast
            default: false
            required: false
          num_frames:
            type: integer
            description: Number of video frames. 81 frames give the best results
            default: 81
            min: 81
            max: 100
            required: false
          resolution:
            description: Resolution of video. 832x480px corresponds to 16:9 aspect ratio, and 480x832px is 9:16
            default: 480p
            required: false
          frames_per_second:
            type: integer
            description: Frames per second. Note that the pricing of this model is based on the video duration at 16 fps
            default: 16
            min: 5
            max: 24
            required: false
          sample_steps:
            type: integer
            description: Number of generation steps. Fewer steps means faster generation, at the expensive of output quality. 30 steps is sufficient for most prompts
            default: 40
            min: 1
            max: 50
            required: false
          sample_shift:
            type: number
            description: Sample shift factor
            default: 5
            min: 1
            max: 20
            required: false
          seed:
            type: integer
            description: Random seed. Leave blank for random
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.2-i2v-a14b
- id: replicate.x.wan-video-wan-2-2-i2v-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.2-i2v-fast/predictions
  name: wan-video/wan-2.2-i2v-fast
  summary: A very fast and cheap PrunaAI optimized version of Wan 2.2 A14B image-to-video
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Prompt for video generation
            required: true
          image:
            type: string
            description: Input image to generate video from.
            required: true
          last_image:
            type: string
            description: Optional last image to condition the video generation. If provided, creates smoother transitions between frames.
            required: false
          num_frames:
            type: integer
            description: Number of video frames. 81 frames give the best results
            default: 81
            min: 81
            max: 121
            required: false
          resolution:
            description: Resolution of video. 16:9 corresponds to 832x480px, and 9:16 is 480x832px
            default: 480p
            required: false
          frames_per_second:
            type: integer
            description: Frames per second. Note that the pricing of this model is based on the video duration at 16 fps
            default: 16
            min: 5
            max: 30
            required: false
          interpolate_output:
            type: boolean
            description: Interpolate the generated video to 30 FPS using ffmpeg
            default: false
            required: false
          go_fast:
            type: boolean
            description: Go fast
            default: true
            required: false
          sample_shift:
            type: number
            description: Sample shift factor
            default: 12
            min: 1
            max: 20
            required: false
          seed:
            type: integer
            description: Random seed. Leave blank for random
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated video.
            default: false
            required: false
          lora_weights_transformer:
            type: string
            description: Load LoRA weights for the HIGH transformer. Supports arbitrary .safetensors URLs from the Internet (for example, 'https://huggingface.co/TheRaf7/instagirl-v2/resolve/main/Instagirlv2.0_hinoise.safetensors')
            required: false
          lora_scale_transformer:
            type: number
            description: Determines how strongly the transformer LoRA should be applied.
            default: 1
            required: false
          lora_weights_transformer_2:
            type: string
            description: Load LoRA weights for the LOW transformer_2. Supports arbitrary .safetensors URLs from the Internet. Can be different from transformer LoRA. (for example, 'https://huggingface.co/TheRaf7/instagirl-v2/resolve/main/Instagirlv2.0_lownoise.safetensors')
            required: false
          lora_scale_transformer_2:
            type: number
            description: Determines how strongly the transformer_2 LoRA should be applied.
            default: 1
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.2-i2v-fast
- id: replicate.x.wan-video-wan-2-2-t2v-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.2-t2v-fast/predictions
  name: wan-video/wan-2.2-t2v-fast
  summary: A very fast and cheap PrunaAI optimized version of Wan 2.2 A14B text-to-video
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Prompt for video generation
            required: true
          optimize_prompt:
            type: boolean
            description: Translate prompt to Chinese before generation
            default: false
            required: false
          num_frames:
            type: integer
            description: Number of video frames. 81 frames give the best results
            default: 81
            min: 81
            max: 121
            required: false
          aspect_ratio:
            description: Aspect ratio of video. 16:9 corresponds to 832x480px, and 9:16 is 480x832px
            default: '16:9'
            required: false
          resolution:
            description: Resolution of video. 16:9 corresponds to 832x480px, and 9:16 is 480x832px
            default: 480p
            required: false
          frames_per_second:
            type: integer
            description: Frames per second. Note that the pricing of this model is based on the video duration at 16 fps
            default: 16
            min: 5
            max: 30
            required: false
          interpolate_output:
            type: boolean
            description: Interpolate the generated video to 30 FPS using ffmpeg
            default: true
            required: false
          go_fast:
            type: boolean
            description: Go fast
            default: true
            required: false
          sample_shift:
            type: number
            description: Sample shift factor
            default: 12
            min: 1
            max: 20
            required: false
          seed:
            type: integer
            description: Random seed. Leave blank for random
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated video.
            default: false
            required: false
          lora_weights_transformer:
            type: string
            description: Load LoRA weights for transformer. Supports arbitrary .safetensors URLs from the Internet (for example, 'https://huggingface.co/Viktor1717/scandinavian-interior-style1/resolve/main/my_first_flux_lora_v1.safetensors')
            required: false
          lora_scale_transformer:
            type: number
            description: Determines how strongly the transformer LoRA should be applied.
            default: 1
            required: false
          lora_weights_transformer_2:
            type: string
            description: Load LoRA weights for transformer_2. Supports arbitrary .safetensors URLs from the Internet. Can be different from transformer LoRA.
            required: false
          lora_scale_transformer_2:
            type: number
            description: Determines how strongly the transformer_2 LoRA should be applied.
            default: 1
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.2-t2v-fast
- id: replicate.x.wan-video-wan-2-5-i2v-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.5-i2v-fast/predictions
  name: wan-video/wan-2.5-i2v-fast
  summary: Wan 2.5 image-to-video, optimized for speed
  input:
    body:
      input:
        type: object
        required: true
        properties:
          resolution:
            description: Video resolution
            default: 720p
            required: false
          duration:
            description: Duration of the generated video in seconds
            default: 5
            required: false
          audio:
            type: string
            description: Audio file (wav/mp3, 3-30s, ≤15MB) for voice/music synchronization
            required: false
          enable_prompt_expansion:
            type: boolean
            description: If set to true, the prompt optimizer will be enabled
            default: true
            required: false
          image:
            type: string
            description: Input image for video generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          seed:
            type: integer
            description: Random seed for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.5-i2v-fast
- id: replicate.x.wan-video-wan-2-5-i2v
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.5-i2v/predictions
  name: wan-video/wan-2.5-i2v
  summary: Alibaba Wan 2.5 Image to video generation with background audio
  input:
    body:
      input:
        type: object
        required: true
        properties:
          image:
            type: string
            description: Input image for video generation
            required: true
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          audio:
            type: string
            description: Audio file (wav/mp3, 3-30s, ≤15MB) for voice/music synchronization
            required: false
          resolution:
            description: Video resolution
            default: 720p
            required: false
          duration:
            description: Duration of the generated video in seconds
            default: 5
            required: false
          enable_prompt_expansion:
            type: boolean
            description: If set to true, the prompt optimizer will be enabled
            default: true
            required: false
          seed:
            type: integer
            description: Random seed for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.5-i2v
- id: replicate.x.wan-video-wan-2-5-t2v-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.5-t2v-fast/predictions
  name: wan-video/wan-2.5-t2v-fast
  summary: Wan 2.5 text-to-video, optimized for speed
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          audio:
            type: string
            description: Audio file (wav/mp3, 3-30s, ≤15MB) for voice/music synchronization
            required: false
          size:
            description: Video resolution and aspect ratio
            default: 1280*720
            required: false
          duration:
            description: Duration of the generated video in seconds
            default: 5
            required: false
          enable_prompt_expansion:
            type: boolean
            description: If set to true, the prompt optimizer will be enabled
            default: true
            required: false
          seed:
            type: integer
            description: Random seed for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.5-t2v-fast
- id: replicate.x.wan-video-wan-2-5-t2v
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.5-t2v/predictions
  name: wan-video/wan-2.5-t2v
  summary: Alibaba Wan 2.5 text to video generation model
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          audio:
            type: string
            description: Audio file (wav/mp3, 3-30s, ≤15MB) for voice/music synchronization
            required: false
          size:
            description: Video resolution and aspect ratio
            default: 1280*720
            required: false
          duration:
            description: Duration of the generated video in seconds
            default: 5
            required: false
          enable_prompt_expansion:
            type: boolean
            description: If set to true, the prompt optimizer will be enabled
            default: true
            required: false
          seed:
            type: integer
            description: Random seed for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.5-t2v
- id: replicate.x.wan-video-wan-2-7-i2v
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.7-i2v/predictions
  name: wan-video/wan-2.7-i2v
  summary: Generate videos from images, with support for first-and-last-frame control, clip continuation, and audio synchronization using Alibaba's Wan 2.7 model
  input:
    body:
      input:
        type: object
        required: true
        properties:
          first_frame:
            type: string
            description: First frame image to animate into video (jpg/png/bmp/webp, ≤20MB)
            required: false
          last_frame:
            type: string
            description: Last frame image for first-and-last-frame video generation (jpg/png/bmp/webp, ≤20MB). Requires first_frame.
            required: false
          first_clip:
            type: string
            description: Video clip to continue from (mp4/mov, 2-10s, ≤100MB). Cannot be combined with first_frame.
            required: false
          audio:
            type: string
            description: Audio file (wav/mp3, 3-30s, ≤15MB) for voice/music synchronization. If not provided, the model auto-generates matching audio.
            required: false
          prompt:
            type: string
            description: Text prompt for video generation
            default: ''
            required: false
          negative_prompt:
            type: string
            description: Negative prompt - describes content that should not appear in the video
            default: ''
            required: false
          resolution:
            description: Output video resolution
            default: 1080p
            required: false
          duration:
            type: integer
            description: Duration of the generated video in seconds
            default: 5
            min: 2
            max: 15
            required: false
          enable_prompt_expansion:
            type: boolean
            description: Automatically expand and optimize the prompt for better results. Improves quality for short prompts but increases latency.
            default: true
            required: false
          seed:
            type: integer
            description: 'Random seed for reproducible generation. Range: 0-2147483647'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.7-i2v
- id: replicate.x.wan-video-wan-2-7-r2v
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wan-video/wan-2.7-r2v/predictions
  name: wan-video/wan-2.7-r2v
  summary: Generate videos from reference images or clips while preserving subject identity using Alibaba's Wan 2.7 reference-to-video model
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          reference_images:
            type: array
            description: Reference images of the character/object to feature in the video (jpg/png/bmp/webp)
            default: []
            required: false
          reference_videos:
            type: array
            description: Reference videos of the character/object to feature in the video (mp4/mov)
            default: []
            required: false
          negative_prompt:
            type: string
            description: Negative prompt - describes content that should not appear in the video
            default: ''
            required: false
          resolution:
            description: Output video resolution
            default: 1080p
            required: false
          aspect_ratio:
            description: Aspect ratio of the generated video
            default: '16:9'
            required: false
          duration:
            type: integer
            description: Duration of the generated video in seconds
            default: 5
            min: 2
            max: 10
            required: false
          shot_type:
            description: Shot structure of the generated video
            default: single
            required: false
          seed:
            type: integer
            description: 'Random seed for reproducible generation. Range: 0-2147483647'
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wan-video/wan-2.7-r2v
- id: replicate.x.wavespeedai-hunyuan-video-fast
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wavespeedai/hunyuan-video-fast/predictions
  name: wavespeedai/hunyuan-video-fast
  summary: Accelerated inference for HunyuanVideo with high resolution (1280x720), a state-of-the-art text-to-video generation model capable of creating high-quality videos with realistic motion from text descriptions
  input:
    body:
      input:
        type: object
        required: true
        properties: {}
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wavespeedai/hunyuan-video-fast
- id: replicate.x.wavespeedai-wan-2-1-i2v-480p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wavespeedai/wan-2.1-i2v-480p/predictions
  name: wavespeedai/wan-2.1-i2v-480p
  summary: Accelerated inference for Wan 2.1 14B image to video, a comprehensive and open suite of video foundation models that pushes the boundaries of video generation.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          aspect_ratio:
            description: Aspect ratio of the output video.
            default: '16:9'
            required: false
          image:
            type: string
            description: Image for use as the initial frame of the video.
            required: true
          fast_mode:
            description: Speed up generation with different levels of acceleration. Faster modes may degrade quality somewhat. The speedup is dependent on the content, so different videos may see different speedups.
            default: Balanced
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          sample_guide_scale:
            type: number
            description: Guidance scale for generation
            default: 5
            min: 1
            max: 10
            required: false
          sample_steps:
            type: integer
            description: Number of inference steps
            default: 30
            min: 1
            max: 40
            required: false
          sample_shift:
            type: integer
            description: Flow shift parameter for video generation
            default: 3
            min: 0
            max: 10
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Supports HuggingFace URLs in the format huggingface.co/<owner>/<model-name>, CivitAI URLs in the format civitai.com/models/<id>[/<model-name>], or arbitrary .safetensors URLs from the Internet.
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied. Sane results between 0 and 1 for base inference. You may still need to experiment to find the best value for your particular lora.
            default: 1
            min: 0
            max: 4
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated videos
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wavespeedai/wan-2.1-i2v-480p
- id: replicate.x.wavespeedai-wan-2-1-i2v-720p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wavespeedai/wan-2.1-i2v-720p/predictions
  name: wavespeedai/wan-2.1-i2v-720p
  summary: Accelerated inference for Wan 2.1 14B image to video with high resolution, a comprehensive and open suite of video foundation models that pushes the boundaries of video generation.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          aspect_ratio:
            description: Aspect ratio of the output video.
            default: '16:9'
            required: false
          image:
            type: string
            description: Image for use as the initial frame of the video.
            required: true
          fast_mode:
            description: Speed up generation with different levels of acceleration. Faster modes may degrade quality somewhat. The speedup is dependent on the content, so different videos may see different speedups.
            default: Balanced
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          sample_guide_scale:
            type: number
            description: Guidance scale for generation
            default: 5
            min: 1
            max: 10
            required: false
          sample_steps:
            type: integer
            description: Number of inference steps
            default: 30
            min: 1
            max: 40
            required: false
          sample_shift:
            type: integer
            description: Flow shift parameter for video generation
            default: 3
            min: 0
            max: 10
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Supports HuggingFace URLs in the format huggingface.co/<owner>/<model-name>, CivitAI URLs in the format civitai.com/models/<id>[/<model-name>], or arbitrary .safetensors URLs from the Internet.
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied. Sane results between 0 and 1 for base inference. You may still need to experiment to find the best value for your particular lora.
            default: 1
            min: 0
            max: 4
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated videos
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wavespeedai/wan-2.1-i2v-720p
- id: replicate.x.wavespeedai-wan-2-1-t2v-480p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wavespeedai/wan-2.1-t2v-480p/predictions
  name: wavespeedai/wan-2.1-t2v-480p
  summary: Accelerated inference for Wan 2.1 14B text to video, a comprehensive and open suite of video foundation models that pushes the boundaries of video generation.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          aspect_ratio:
            description: Aspect ratio of the output video.
            default: '16:9'
            required: false
          fast_mode:
            description: Speed up generation with different levels of acceleration. Faster modes may degrade quality somewhat. The speedup is dependent on the content, so different videos may see different speedups.
            default: Balanced
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated videos
            default: false
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied. Sane results between 0 and 1 for base inference. You may still need to experiment to find the best value for your particular lora.
            default: 1
            min: 0
            max: 4
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Supports HuggingFace URLs in the format huggingface.co/<owner>/<model-name>, CivitAI URLs in the format civitai.com/models/<id>[/<model-name>], or arbitrary .safetensors URLs from the Internet.
            required: false
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          sample_guide_scale:
            type: number
            description: Guidance scale for generation
            default: 5
            min: 1
            max: 10
            required: false
          sample_shift:
            type: integer
            description: Flow shift parameter for video generation
            default: 3
            min: 0
            max: 10
            required: false
          sample_steps:
            type: integer
            description: Number of inference steps
            default: 30
            min: 1
            max: 40
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wavespeedai/wan-2.1-t2v-480p
- id: replicate.x.wavespeedai-wan-2-1-t2v-720p
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/wavespeedai/wan-2.1-t2v-720p/predictions
  name: wavespeedai/wan-2.1-t2v-720p
  summary: Accelerated inference for Wan 2.1 14B text to video with high resolution, a comprehensive and open suite of video foundation models that pushes the boundaries of video generation.
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for image generation
            required: true
          negative_prompt:
            type: string
            description: Negative prompt to avoid certain elements
            default: ''
            required: false
          aspect_ratio:
            description: Aspect ratio of the output video.
            default: '16:9'
            required: false
          fast_mode:
            description: Speed up generation with different levels of acceleration. Faster modes may degrade quality somewhat. The speedup is dependent on the content, so different videos may see different speedups.
            default: Balanced
            required: false
          seed:
            type: integer
            description: Random seed. Set for reproducible generation
            required: false
          sample_guide_scale:
            type: number
            description: Guidance scale for generation
            default: 5
            min: 1
            max: 10
            required: false
          sample_steps:
            type: integer
            description: Number of inference steps
            default: 30
            min: 1
            max: 40
            required: false
          sample_shift:
            type: integer
            description: Flow shift parameter for video generation
            default: 3
            min: 0
            max: 10
            required: false
          lora_weights:
            type: string
            description: Load LoRA weights. Supports HuggingFace URLs in the format huggingface.co/<owner>/<model-name>, CivitAI URLs in the format civitai.com/models/<id>[/<model-name>], or arbitrary .safetensors URLs from the Internet.
            required: false
          lora_scale:
            type: number
            description: Determines how strongly the main LoRA should be applied. Sane results between 0 and 1 for base inference. You may still need to experiment to find the best value for your particular lora.
            default: 1
            min: 0
            max: 4
            required: false
          disable_safety_checker:
            type: boolean
            description: Disable safety checker for generated videos
            default: false
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/wavespeedai/wan-2.1-t2v-720p
- id: replicate.x.xai-grok-imagine-video-1-5
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/xai/grok-imagine-video-1.5/predictions
  name: xai/grok-imagine-video-1.5
  summary: Image-to-video with synchronized audio using xAI's Grok Imagine Video 1.5 preview model
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt describing the motion or scene for the generated video.
            required: true
          image:
            type: string
            description: Input image to animate (image-to-video). Supports jpg, jpeg, png, webp. The output aspect ratio defaults to the image's native aspect ratio.
            required: true
          duration:
            type: integer
            description: Duration of the video in seconds.
            default: 5
            min: 1
            max: 15
            required: false
          aspect_ratio:
            description: Aspect ratio of the output video. Defaults to the input image's native aspect ratio when 'auto' is selected.
            default: auto
            required: false
          resolution:
            description: Resolution of the video.
            default: 720p
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/xai/grok-imagine-video-1.5
- id: replicate.x.xai-grok-imagine-video
  tier: extended
  platform: video-gen
  domain: models
  method: POST
  path: /models/xai/grok-imagine-video/predictions
  name: xai/grok-imagine-video
  summary: Generate videos using xAI's Grok Imagine Video model
  input:
    body:
      input:
        type: object
        required: true
        properties:
          prompt:
            type: string
            description: Text prompt for video generation
            required: true
          image:
            type: string
            description: Input image to generate video from (image-to-video). Supports jpg, jpeg, png, webp.
            default: null
            required: false
          video:
            type: string
            description: Input video to edit (video editing mode). Must be a direct link, max 8.7 seconds. Supports mp4, mov, webm.
            default: null
            required: false
          duration:
            type: integer
            description: Duration of the video in seconds (1-15). Ignored when editing a video.
            default: 5
            min: 1
            max: 15
            required: false
          aspect_ratio:
            description: Aspect ratio of the video. For text-to-video, defaults to 16:9. For image-to-video, defaults to the input image's native aspect ratio. Ignored when editing a video.
            default: auto
            required: false
          resolution:
            description: Resolution of the video. Ignored when editing a video.
            default: 720p
            required: false
    bodyType: json
  cost:
    type: per_success
    value: null
    confidence: unknown
    note: Replicate does not expose this model's price in the collection API; the generated row is BYOK-only.
  docs_url: https://replicate.com/xai/grok-imagine-video
